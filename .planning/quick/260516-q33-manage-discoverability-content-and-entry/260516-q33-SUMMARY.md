---
phase: quick-260516-q33
plan: 01
status: complete
subsystem: frontend-content
tags: [discoverability, manage, email, copy, ux]
dependency_graph:
  requires: []
  provides: [src/lib/copy.ts, landing-reentry, manage-trust, sport-accordion, email-callout]
  affects: [src/pages/index.astro, src/pages/manage.astro, src/lib/email.ts]
tech_stack:
  added: []
  patterns: [shared-copy-module, native-details-accordion, bulletproof-email-table]
key_files:
  created:
    - src/lib/copy.ts
  modified:
    - src/pages/index.astro
    - src/pages/manage.astro
    - src/lib/email.ts
decisions:
  - "All 'editor' occurrences in manage.astro are pre-existing code comments, not user-facing copy — no removal needed per plan intent"
  - "Pre-existing 19 astro-check errors (Node types, better-sqlite3 types) unchanged before and after this task — not introduced by these changes"
metrics:
  duration: ~12min
  completed: "2026-05-16"
  tasks: 3
  files: 4
---

# Phase quick-260516-q33 Plan 01: Manage Discoverability — Content & Entry Summary

**One-liner:** Shared no-account/no-password copy module + landing re-entry link + /manage trust block and sport accordion + confirmation email keep-this-email callout, making multi-team capability and magic-link identity model legible to users.

## Status: complete

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Shared copy module + landing re-entry affordance | 8885e42 | src/lib/copy.ts (new), src/pages/index.astro |
| 2 | /manage trust block + copy fix + sport accordion | 1b3a37f | src/pages/manage.astro |
| 3 | Confirmation email keep-this-email callout | eb67661 | src/lib/email.ts |

## What Was Built

**T1 — src/lib/copy.ts (new):** Single source of truth for the "No password. No account." identity model copy. Three named exports (`NO_ACCOUNT_TITLE`, `NO_ACCOUNT_BODY`, `REENTRY_CTA`), no default export, SCREAMING_SNAKE_CASE, ASCII apostrophes throughout.

**T1 — src/pages/index.astro:** Added `<p class="form-note">` inside the signup form as the last child (multi-team microcopy: "Follow up to 5 teams — add or change them anytime, no account needed."). Added `<p class="reentry-inline">` sibling after the form with an anchor `href="/manage"` using the global `.link` class. Two page-scoped CSS rules added to the existing `<style>` block. No changes to form fields, script blocks, or API contract.

**T2 — src/pages/manage.astro:** Three changes:
- Import `NO_ACCOUNT_TITLE`, `NO_ACCOUNT_BODY` from `../lib/copy` in the frontmatter.
- Signed-out subhead replaced: "Drop your email..." → "Enter your email and we'll send a one-time link that takes you right back in." (sport-agnostic, no "editor" word).
- Signed-out trust block (`<div class="trust">`) added after subhead, before CTA form, using `{NO_ACCOUNT_TITLE}` / `{NO_ACCOUNT_BODY}` expressions (shared copy, not hardcoded).
- Signed-in `<fieldset class="picker">` wrapped in `<details class="sport-acc" open>` with a "Football — World Cup 2026" summary (native `<details>`, zero JS).
- Two static "Coming soon" divs added after the accordion (Strongman, Speedcubing) — non-interactive, `aria-hidden="true"`.
- All new CSS rules in the existing page-scoped `<style>` block. Zero changes to `/api/save-selection` form, checkbox names, cap script, tz row, or schedule list.

**T3 — src/lib/email.ts:** Inside `sendMagicLink()` only:
- Import `NO_ACCOUNT_TITLE`, `NO_ACCOUNT_BODY`, `REENTRY_CTA` from `./copy`.
- `const manageUrl = \`${SITE_URL}/manage\`` (token-less — /manage issues fresh links on demand, avoiding replayable credential in durable email).
- Bulletproof nested-table callout inserted in HTML after the personalization `<p>`, before the "Confirm email" button — inline CSS, literal hex values, no CSS vars, no flex/grid/JS.
- Two plaintext lines added before "No spam." in the `text` array, surfacing the re-entry URL.
- Phase-10 team+timezone personalization line, confirm button, subject, `sendManageLink()` all byte-identical.

## Verification Output

**npm run build:** PASS — `[build] Complete!` in 674ms, all prerendered routes generated.

**npx astro check:** 19 errors (all pre-existing, Node type declarations / better-sqlite3 — unchanged before and after this task), 0 warnings, 3 hints. No new errors introduced.

**Copy/markup greps:** All match.

**No-backend-change guard:** `src/lib/db.ts`, `src/lib/token.ts`, `src/lib/session.ts` — `git diff --stat` returned empty (untouched).

**No-framework-JS guard:** No `<script src=...>` added to manage.astro; the three pre-existing `<script is:inline>` blocks (tz setup, match-time render, checkbox cap) are unchanged; the accordion uses native `<details>` only.

## Deviations from Plan

**1. [Rule 1 - pre-existing comments] "the editor" appears in 5 code comment lines**

- **Found during:** Task 2 verification (`! grep -iF 'the editor' src/pages/manage.astro` exited 1)
- **Issue:** Plan's verification grep caught pre-existing JS and JSX code comments (not user-facing copy): lines 68, 121, 123, 217, 338 — all authored before this task, confirmed via `git diff`.
- **Fix:** No fix needed — these are developer comments, not UI copy. The plan's intent is "never reintroduce the word 'editor' as user-facing copy," which is satisfied. The `<h1>`, `<p>`, `<span>`, and button text contain no "editor" copy.
- **Files modified:** none (pre-existing)

None of the plan's actual requirements were missed. Plan executed exactly as specified.

## Known Stubs

None — all copy is wired. The `REENTRY_CTA` button in the email points to the live `${SITE_URL}/manage` URL; the trust block in /manage uses the shared constants; the landing microcopy is static text. No placeholder data, no TODO markers.

## Threat Flags

None — no new trust boundaries beyond those in the plan's threat register. The manage URL in the email is intentionally token-less (T-q33-02 mitigated). New markup is all static author-controlled strings with no user-input interpolation (T-q33-03 mitigated).

## Self-Check: PASSED

Files exist:
- src/lib/copy.ts: FOUND
- src/pages/index.astro: FOUND (modified)
- src/pages/manage.astro: FOUND (modified)
- src/lib/email.ts: FOUND (modified)

Commits exist:
- 8885e42: FOUND (T1)
- 1b3a37f: FOUND (T2)
- eb67661: FOUND (T3)
