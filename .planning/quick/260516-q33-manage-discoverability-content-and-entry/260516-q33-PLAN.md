---
phase: quick-260516-q33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/copy.ts
  - src/pages/index.astro
  - src/pages/manage.astro
  - src/lib/email.ts
autonomous: true
requirements:
  - DISC-LANDING
  - DISC-MANAGE
  - DISC-EMAIL
  - DISC-SHARED-COPY

must_haves:
  truths:
    - "A returning visitor on / sees a quiet 'Already signed up? Manage what you follow on oddlympics →' link directly under the signup form, linking to /manage"
    - "A new visitor on / sees microcopy near the form: they can follow up to 5 teams and add/change anytime, no account needed"
    - "A signed-out visitor to /manage sees an accent-soft 'No password. No account.' trust block selling the magic-link model, and the subhead never calls /manage 'the editor'"
    - "A signed-in user on /manage sees the existing confederation team picker wrapped under a collapsible 'Football — World Cup 2026' sport block, with non-interactive 'Coming soon' rows for future sports"
    - "The confirmation email contains an accent-soft 'No account. No password. Keep this email.' callout reframing the email as the durable re-entry point, with a bulletproof table button to /manage"
    - "The 'no account / no password' headline + body copy is defined once in src/lib/copy.ts and reused by both /manage and the confirmation email"
    - "npm run build succeeds and npx astro check is clean"
  artifacts:
    - path: "src/lib/copy.ts"
      provides: "Shared sport-agnostic no-account/no-password copy constants"
      exports: ["NO_ACCOUNT_TITLE", "NO_ACCOUNT_BODY", "REENTRY_CTA"]
    - path: "src/pages/index.astro"
      provides: "Under-form re-entry link + multi-team microcopy"
      contains: "Manage what you follow on oddlympics"
    - path: "src/pages/manage.astro"
      provides: "Signed-out trust block + sport-accordion framing of editor + editor->right-back-in copy fix"
      contains: "No password. No account."
    - path: "src/lib/email.ts"
      provides: "Confirmation email keep-this-email callout"
      contains: "Keep this email"
  key_links:
    - from: "src/pages/manage.astro"
      to: "src/lib/copy.ts"
      via: "import of NO_ACCOUNT_* constants in frontmatter"
      pattern: "from '../lib/copy'"
    - from: "src/lib/email.ts"
      to: "src/lib/copy.ts"
      via: "import of NO_ACCOUNT_* constants"
      pattern: "from './copy'"
    - from: "src/pages/index.astro"
      to: "/manage"
      via: "anchor href"
      pattern: "href=\"/manage\""
---

<objective>
Make the already-shipped multi-team capability and the magic-link / no-account
identity model **legible** to users. This is the IMPLEMENTATION of a resolved
design exploration (sketches 001–003, winners B/B/B). The WHAT, WHY, and visual
direction are LOCKED — execute exactly the locked decisions; do not re-explore.

Three surfaces get a content + light-affordance pass, plus one shared copy
module so the "no account / no password" message is authored once:

1. **Landing (`src/pages/index.astro`)** — sketch 001-B: one quiet secondary
   link under the signup form + multi-team expectation microcopy near the form.
2. **`/manage` (`src/pages/manage.astro`)** — sketch 002-B: signed-out
   accent-soft trust block + the "editor"→"right back in" copy fix; signed-in
   editor re-framed as a collapsible sport accordion (native `<details>`) with
   "Coming soon" placeholder rows.
3. **Confirmation email (`src/lib/email.ts`)** — sketch 003-B: an accent-soft
   "Keep this email — it's your key" bulletproof-table callout.

Purpose: the work exists in code but is invisible; returning users hit dead
ends and the magic-link model reads as broken instead of as a trust feature.
Copy + IA are deliberately sport-agnostic (future weird-sports roadmap —
`.planning/notes/multi-team-discoverability-gap.md`).

Output: 1 new lib module + 3 edited surfaces. NO backend/schema/API changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/manage-discoverability-content-pass.md
@.planning/notes/multi-team-discoverability-gap.md
@.planning/sketches/MANIFEST.md
@.planning/sketches/001-landing-reentry-affordance/README.md
@.planning/sketches/002-manage-trust-screen/README.md
@.planning/sketches/003-confirmation-email-cta/README.md
@.planning/sketches/001-landing-reentry-affordance/index.html
@.planning/sketches/002-manage-trust-screen/index.html
@.planning/sketches/003-confirmation-email-cta/index.html
@src/pages/index.astro
@src/pages/manage.astro
@src/lib/email.ts
@src/components/Layout.astro
@.planning/sketches/themes/default.css

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not re-derive. -->

Locked design tokens (from src/components/Layout.astro :root, also in
.planning/sketches/themes/default.css). Astro pages reference these as
CSS vars; the HTML email must inline the literal hex (email has no CSS vars):

  --bg: #fafaf7;        --fg: #14151a;       --fg-dim: #5a5d68;
  --line: #e4e4dd;      --line-strong: #c9cbc4;  --surface: #ffffff;
  --accent: #b8350d;    --accent-ink: #ffffff;   --accent-soft: #fbe9e0;
  --font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace;

Shared chrome classes already global in Layout.astro (do NOT redefine in
page <style>): .wrap .banner .headline .subhead .link

src/pages/index.astro structure (signup hero):
  - `<form method="post" action="/api/signup" id="signup-form" class="form-card">`
    closes at line ~103; immediately after it is
    `<p class="trust">Built by one person in Michigan...</p>` then `</div>` (the .wrap).
  - The form already ends with `<p class="fineprint">One email per match...</p>`
    and `<p id="error" class="error" ... hidden></p>` (line ~101-102).
  - Page-scoped `<style>` (NOT is:global) starts at line ~225.

src/pages/manage.astro structure:
  - Frontmatter imports: Layout, verifyToken, session helpers, db, TEAMS.
  - Signed-OUT branch: `{!valid && !hadUrlToken && ( <> ... </> )}` (~line 134).
    Contains `<span class="banner">Already signed up</span>`,
    `<h1 class="headline">Manage your schedule.</h1>`, a `<p class="subhead">`
    with the text "Drop your email and we'll send you a link to pick or change
    the World Cup teams you follow.", then `<form class="cta-form" ...>`.
  - Signed-IN editor branch: `{valid && !isUnsubscribed && ( <> ... </> )}`
    (~line 212). Inside it: `<form method="post" action="/api/save-selection"
    class="picker-form">` containing a `.tz-row`, then
    `<fieldset class="picker">` with `<legend class="legend">Your teams —
    Follow up to 5</legend>` and `groupedTeams.map(...)` rendering
    `<div class="conf-group">` blocks, then `<button type="submit">Save
    selection</button>`.
  - Page-scoped `<style>` block at the bottom (NOT is:global).
  - `groupedTeams` = the 6 confederation groups (UEFA…OFC) already computed
    in frontmatter — REUSE as-is, do not rebuild.

src/lib/email.ts contract (UNCHANGED signature):
  export async function sendMagicLink(email, token, team, timezone): Promise<void>
  - Builds `text` + `html` for the CONFIRMATION email (purpose=confirm).
  - `teamHuman = teamLabel(team)`, `tzHuman = tzLabel(timezone)` already in scope.
  - `url` = `${SITE_URL}/api/confirm?token=...` (the confirm link).
  - Existing html is a single-column table-free div; the Phase-10
    personalization line "We'll email you 1 hour before every <strong>${teamHuman}</strong>
    match in ${tzHuman}." MUST be preserved.
  - A SEPARATE manage URL for the callout button = `${SITE_URL}/manage`
    (NO token — the email is the durable account-less re-entry point;
    /manage's signed-out branch handles requesting a fresh link).
  - `sendManageLink()` (the OTHER function) is OUT OF SCOPE — do not touch it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared no-account copy module + landing re-entry affordance (sketch 001-B)</name>
  <files>src/lib/copy.ts, src/pages/index.astro</files>
  <action>
Create `src/lib/copy.ts` as a new named-export-only TS module (no default
export, return-type-free string consts, SCREAMING_SNAKE_CASE per Conventions).
This is the single source of the "no account / no password" message reused by
/manage (Task 2) and the confirmation email (Task 3). Export exactly these
sport-agnostic constants (verbatim copy taken from the winning sketches 002-B
and 003-B — keep them consistent, do not reword per surface):

  - `export const NO_ACCOUNT_TITLE = 'No password. No account.';`
  - `export const NO_ACCOUNT_BODY = "You never made an account — there's nothing to remember or leak. We email you a one-time link; it works for 24 hours, once, and brings you right back to add teams, change your time zone, or follow new sports as oddlympics grows.";`
  - `export const REENTRY_CTA = 'Manage what you follow';`

Use ASCII apostrophes only where the surrounding code uses ASCII; the sketch
copy uses a typographic apostrophe in "there's" / "you've" — match the
project's established ASCII-apostrophe precedent (STATE.md Phase 6 decisions:
ASCII 0x27 throughout body copy). Write `there's` as a plain ASCII `'`.

Then edit `src/pages/index.astro` for sketch 001-B (quiet link under form +
multi-team microcopy). Do NOT add a top strip or below-fold card (those were
rejected variants A and C). Two markup additions, both inside the existing
`.wrap` in the `.hero` section:

  1. Multi-team expectation microcopy near the form. Add it as the LAST child
     INSIDE the `<form ... class="form-card">`, immediately after the existing
     `<p id="error" class="error" role="alert" hidden></p>` line and before
     the closing `</form>`. Exact copy (verbatim from sketch 001-B
     `.form-note`): "Follow up to 5 teams — add or change them anytime, no
     account needed." Use a `<p class="form-note">`.

  2. The re-entry link. Add it AFTER the `</form>` close tag and BEFORE the
     existing `<p class="trust">Built by one person...</p>`, as a sibling in
     the same `.wrap`. Exact markup (verbatim from sketch 001-B
     `.reentry-inline`):
     `<p class="reentry-inline">Already signed up? <a href="/manage" class="link">Manage what you follow on oddlympics →</a></p>`
     Reuse the global `.link` class from Layout.astro for the anchor (do not
     restyle the anchor).

Add the two new page-scoped style rules to the EXISTING `<style>` block (the
plain scoped one, NOT is:global) at the bottom of index.astro — match the
sketch values exactly:

  `.form-note { font-size: 13px; color: var(--fg-dim); margin: 4px 0 0; }`
  `.reentry-inline { font-size: 14px; color: var(--fg-dim); margin: 18px 0 0; }`

(The `.reentry-inline a` styling is already covered by the global `.link`
class — do not duplicate it.) Do NOT touch the inline `<script is:inline>`
block, the form fields, the honeypot, or any `/api/signup` contract. The
arrow glyph "→" is U+2192 (matches the sketch verbatim and existing
codebase usage of "←" in manage.astro).
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && npx astro check 2>&1 | tail -3 && grep -F 'Manage what you follow on oddlympics' src/pages/index.astro && grep -F 'no account needed' src/pages/index.astro && grep -F 'href="/manage"' src/pages/index.astro && grep -F "NO_ACCOUNT_TITLE = 'No password. No account.'" src/lib/copy.ts && grep -cF 'export default' src/lib/copy.ts | grep -qx 0 && echo COPY_NO_DEFAULT_OK</automated>
  </verify>
  <done>
src/lib/copy.ts exists with the three named exports and no default export.
index.astro renders the under-form `.form-note` microcopy inside the form,
and the `.reentry-inline` link to /manage after the form, both using locked
tokens/classes. `npx astro check` is clean. No /api/signup or script changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: /manage signed-out trust block + copy fix + signed-in sport accordion (sketch 002-B)</name>
  <files>src/pages/manage.astro</files>
  <action>
Edit `src/pages/manage.astro` for sketch 002-B. THREE changes; all are
content/presentation. The `/api/save-selection` contract, the 5-team
checkbox cap inline script, the `.tz-row`, the schedule list, the
`groupedTeams` data, and every server-side branch condition MUST be
preserved byte-identical except where explicitly edited below.

In the frontmatter (top `---` block), add the shared-copy import alongside
the existing imports:
  `import { NO_ACCOUNT_TITLE, NO_ACCOUNT_BODY } from '../lib/copy';`

**Change A — Signed-OUT branch copy fix (`{!valid && !hadUrlToken && ...}`):**
Replace the existing subhead text. The current line is:
  `Drop your email and we'll send you a link to pick or change the World Cup teams you follow.`
Replace it with the sport-agnostic, no-"editor" copy from sketch 002-B
signed-out state:
  `Enter your email and we'll send a one-time link that takes you right back in.`
(Keep the `<p class="subhead">` wrapper and surrounding markup; only the
text node changes. Never reintroduce the word "editor" anywhere on the page.)

**Change B — Signed-OUT trust block:** Immediately AFTER that subhead `<p>`
and BEFORE the `<form class="cta-form" method="post" action="/api/manage">`,
insert the accent-soft trust block from sketch 002-B `.trust`, but render the
title + body from the imported shared constants (NOT hardcoded strings — this
is the "author once, reuse" requirement):

  ```
  <div class="trust">
    <span class="key" aria-hidden="true">⌗</span>
    <p><strong>{NO_ACCOUNT_TITLE}</strong> {NO_ACCOUNT_BODY}</p>
  </div>
  ```
  (Astro JSX-style expression interpolation, since manage.astro is server-
  rendered, not prerendered — this is fine. The `⌗` is U+2317, verbatim from
  the sketch.)

**Change C — Signed-IN editor sport accordion
(`{valid && !isUnsubscribed && ...}`):** Re-frame the existing
`<fieldset class="picker">` as a collapsible sport block using native
`<details>`/`<summary>` (NO JS — locked constraint; native details avoids the
no-framework-JS rule entirely). Do this around the EXISTING fieldset; do not
rebuild the `groupedTeams.map(...)` or change any checkbox `name`/`value`:

  1. Wrap the existing `<fieldset class="picker">…</fieldset>` in:
     ```
     <details class="sport-acc" open>
       <summary class="sport-acc-head">
         <span class="sport-ico" aria-hidden="true">⚽</span>
         <span class="sport-name">Football — World Cup 2026</span>
       </summary>
       <!-- existing <fieldset class="picker"> ... </fieldset> moves here unchanged -->
     </details>
     ```
     The `<details open>` keeps Football expanded by default (matches sketch
     002-B: only Football expanded, JS-off readable).

  2. Immediately AFTER that `</details>`, add two non-interactive "Coming
     soon" placeholder rows (NOT `<details>` — they must not expand; render
     as plain static divs so there is nothing to click):
     ```
     <div class="sport-acc future" aria-hidden="true">
       <div class="sport-acc-head">
         <span class="sport-ico">🪨</span>
         <span class="sport-name">Strongman</span>
         <span class="sport-soon">Coming soon</span>
       </div>
     </div>
     <div class="sport-acc future" aria-hidden="true">
       <div class="sport-acc-head">
         <span class="sport-ico">🧩</span>
         <span class="sport-name">Speedcubing</span>
         <span class="sport-soon">Coming soon</span>
       </div>
     </div>
     ```
     (Emoji glyphs ⚽ 🪨 🧩 verbatim from sketch 002-B variant B.)

  Optionally also make the signed-in copy sport-agnostic per the locked
  decision: the existing `<h1 class="headline">Pick the teams you follow.</h1>`
  may stay (it is already sport-neutral). Do NOT World-Cup-lock any new copy
  you add. Leave the banner/headline/subhead text otherwise unchanged.

Add page-scoped CSS to the EXISTING bottom `<style>` block (NOT is:global),
using locked tokens, faithful to sketch 002-B `.trust` / `.acc` rules:

  ```
  .trust { display: flex; gap: 12px; align-items: flex-start; background: var(--accent-soft); border: 1px solid var(--accent); border-radius: 8px; padding: 14px 16px; margin: 0 0 28px; max-width: 560px; }
  .trust .key { font-family: var(--font-mono); font-size: 20px; line-height: 1; color: var(--accent); }
  .trust p { margin: 0; font-size: 13px; color: var(--fg); line-height: 1.5; }
  .trust strong { color: var(--accent); }
  .sport-acc { max-width: 460px; border: 1px solid var(--line); border-radius: 8px; background: var(--surface); margin: 0 0 12px; overflow: hidden; }
  .sport-acc-head { display: flex; align-items: center; gap: 10px; padding: 14px 18px; cursor: pointer; font-weight: 700; font-size: 15px; list-style: none; }
  .sport-acc-head::-webkit-details-marker { display: none; }
  .sport-acc[open] > .sport-acc-head { border-bottom: 1px solid var(--line); }
  .sport-acc .picker { border: 0; border-radius: 0; }
  .sport-acc.future { color: var(--fg-dim); }
  .sport-acc.future .sport-acc-head { cursor: default; }
  .sport-soon { margin-left: auto; font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--fg-dim); }
  .sport-name { flex: 1; }
  ```
  (Note `.sport-acc .picker { border:0 }` so the wrapped fieldset's existing
  border doesn't double up inside the accordion box — keep the existing
  `.picker` rule otherwise.) Do NOT modify `.picker-form`, `.tz-row`,
  `.match-list`, the cap script, or any other existing rule/markup.
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && npx astro check 2>&1 | tail -3 && grep -F "from '../lib/copy'" src/pages/manage.astro && grep -F 'takes you right back in' src/pages/manage.astro && grep -F '{NO_ACCOUNT_TITLE}' src/pages/manage.astro && grep -F 'Football — World Cup 2026' src/pages/manage.astro && grep -F 'Coming soon' src/pages/manage.astro && grep -F 'action="/api/save-selection"' src/pages/manage.astro && ! grep -iF 'the editor' src/pages/manage.astro && ! grep -iF 'straight to the editor' src/pages/manage.astro && echo MANAGE_OK</automated>
  </verify>
  <done>
Signed-out branch shows the shared-copy `.trust` block and the new
no-"editor" subhead; signed-in editor's existing fieldset is wrapped in an
`<details open>` "Football — World Cup 2026" accordion with two static
"Coming soon" rows after it; no JS added; `/api/save-selection` form,
checkbox names, cap script, and tz row unchanged; `npx astro check` clean;
the word "editor" appears nowhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirmation email "keep this email — it's your key" callout (sketch 003-B)</name>
  <files>src/lib/email.ts</files>
  <action>
Edit ONLY `sendMagicLink()` in `src/lib/email.ts` for sketch 003-B. Do NOT
touch `sendManageLink()`, `buildUnsubscribeHeaders()`, the function signature,
the Resend send call, the `replyTo`/`headers`, or the dev-fallback branch
logic.

Add the shared-copy import at the top, alongside the existing imports:
  `import { NO_ACCOUNT_TITLE, NO_ACCOUNT_BODY, REENTRY_CTA } from './copy';`

Add a manage URL constant inside `sendMagicLink()`, next to the existing
`const url = ...`:
  `const manageUrl = \`${SITE_URL}/manage\`;`
This is intentionally token-less — the email IS the durable account-less
re-entry point; /manage's signed-out branch issues a fresh link on demand.

**HTML change:** In the `html` template literal, KEEP the existing structure
and the Phase-10 personalization line ("We'll email you 1 hour before every
<strong>${teamHuman}</strong> match in ${tzHuman}.") exactly. INSERT, after
that personalization `<p>` and before the existing "Confirm email" button
`<p>`, the sketch 003-B accent-soft callout as a bulletproof nested table
(real email constraints: table layout, inline CSS, NO flexbox/grid, NO JS,
NO CSS vars — use the literal locked hex values). Use this block verbatim
(interpolating the shared copy constants and `manageUrl`):

  ```
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background:#fbe9e0;border:1px solid #b8350d;border-radius:8px;margin:0 0 24px">
    <tr><td style="padding:18px 22px">
      <p style="margin:0 0 6px;font-size:15px;font-weight:bold;color:#b8350d">${NO_ACCOUNT_TITLE} Keep this email.</p>
      <p style="margin:0 0 16px;font-size:14px;line-height:1.6;color:#14151a">${NO_ACCOUNT_BODY}</p>
      <table role="presentation" cellpadding="0" cellspacing="0"><tr>
        <td style="background:#b8350d;border-radius:6px">
          <a href="${manageUrl}" style="display:inline-block;padding:13px 26px;font-family:'Courier New',monospace;font-size:14px;font-weight:bold;color:#ffffff;text-decoration:none">${REENTRY_CTA} →</a>
        </td>
      </tr></table>
    </td></tr>
  </table>
  ```
  ("Keep this email." is appended to the shared title so the callout reads
  "No password. No account. Keep this email." — matching sketch 003-B's
  headline while keeping the shared no-account phrase authored once. The "→"
  is U+2192, verbatim from the sketch.)

**Text change:** The `text` array (plaintext alternative) currently ends:
  `"If you didn't request this, ignore this email.", '', '— oddlympics'`
Insert, BEFORE the "No spam. No ads. Unsubscribe anytime." line, two new
lines so the plaintext part also surfaces the durable re-entry point
(plaintext, no markup):
  `'Add teams, change your time zone, or follow new sports anytime — no'`,
  `\`account, no password: \${manageUrl}\``,
  `''`,
Keep every other existing text line and order intact.

Do NOT change the `subject` (CONTEXT/sketch did not lock a subject change for
this implementation task — the sketch client-meta subject lines were
exploratory; the live Phase-10 subject "Confirm your World Cup alerts —
oddlympics" stays). Do NOT alter the dev-fallback console.log lines.
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && npx astro check 2>&1 | tail -3 && grep -F "from './copy'" src/lib/email.ts && grep -F 'Keep this email.' src/lib/email.ts && grep -F 'background:#fbe9e0;border:1px solid #b8350d' src/lib/email.ts && grep -F '${manageUrl}' src/lib/email.ts && grep -F "${NO_ACCOUNT_BODY}" src/lib/email.ts && grep -Fq 'sendManageLink' src/lib/email.ts && grep -F "Confirm your World Cup alerts — oddlympics" src/lib/email.ts && echo EMAIL_OK</automated>
  </verify>
  <done>
sendMagicLink()'s HTML contains the accent-soft bulletproof-table callout
using the shared NO_ACCOUNT_* + REENTRY_CTA constants and the token-less
manageUrl; the Phase-10 team+timezone personalization line and the confirm
button are preserved; the plaintext alternative also mentions the re-entry
URL; subject unchanged; sendManageLink untouched; `npx astro check` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → / and /manage | Untrusted visitor renders static/server HTML. This pass adds only static copy + links + native `<details>`; no new input fields, no new form actions, no new query-param handling. |
| email client → recipient inbox | Confirmation email HTML rendered in third-party mail clients. New callout is static table+inline-CSS, no JS, no remote assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q33-01 | Tampering | New `/manage` link target on `/` | accept | Link is a hardcoded same-origin `/manage` path with no params; /manage's existing auth (token/session) is unchanged and still gates all data. No new attack surface. |
| T-q33-02 | Information Disclosure | Confirmation-email manage button URL | mitigate | Button points to bare `${SITE_URL}/manage` with NO token in the email body — avoids embedding a replayable credential in a durable, forwardable email. Signed-out /manage issues a fresh 24h one-time link on demand (existing flow). |
| T-q33-03 | Spoofing/Injection | Shared copy constants interpolated into Astro JSX + HTML email | mitigate | Constants are project-authored static strings in `src/lib/copy.ts` (no user input). Astro auto-escapes `{expr}` in templates; email values are fixed literals. No user-controlled data flows into the new markup. |
| T-q33-04 | Elevation of Privilege | `/manage` editor accordion re-frame | accept | Pure presentational wrapper (`<details>`) around the existing fieldset; `/api/save-selection` contract, checkbox names/values, and the server-side 5-team cap are byte-unchanged. No authorization path touched. |
| T-q33-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies (content + presentation only; `src/lib/copy.ts` is a plain string module). No install step; package legitimacy gate not triggered. |
</threat_model>

<verification>
Run from repo root `/Users/john/Desktop/vault/projects/github.com/oddlympics-app`:

1. `npx astro check` — type-check + lint must be clean (0 errors).
2. `npm run build` — must succeed (produces dist/ with prerendered index.html
   + server bundle).
3. Copy/markup grep assertions (all must match):
   - `grep -F 'Manage what you follow on oddlympics' src/pages/index.astro`
   - `grep -F 'no account needed' src/pages/index.astro`
   - `grep -F 'takes you right back in' src/pages/manage.astro`
   - `grep -F 'Football — World Cup 2026' src/pages/manage.astro`
   - `grep -F 'Coming soon' src/pages/manage.astro`
   - `grep -F 'Keep this email.' src/lib/email.ts`
   - `! grep -iF 'the editor' src/pages/manage.astro` (copy-fix guard)
   - `grep -rF "from '../lib/copy'" src/pages/manage.astro && grep -rF "from './copy'" src/lib/email.ts` (shared-copy reuse, not duplicated)
4. No-backend-change guard (all must still match, proving contracts intact):
   - `grep -F 'action="/api/signup"' src/pages/index.astro`
   - `grep -F 'action="/api/save-selection"' src/pages/manage.astro`
   - `grep -F 'export async function sendMagicLink' src/lib/email.ts`
   - `git diff --stat -- src/lib/db.ts src/lib/token.ts src/lib/session.ts` returns EMPTY (no changes to those files).
5. No-framework-JS guard: `! grep -nE '<script[^>]*src=' src/pages/manage.astro`
   and confirm the only signed-in `<script is:inline>` blocks present in
   manage.astro are the THREE pre-existing ones (tz setup, match-time render,
   checkbox cap) — no new script was added for the accordion (native
   `<details>` only).

**Executor note:** This runs on `main`, non-worktree. Each task is
independently committable. Do NOT push — push auto-deploys to production
(GitHub Actions). The user commits and ships on their own cadence. Stop after
verification passes; report status in the SUMMARY.
</verification>

<success_criteria>
- `npx astro check` clean and `npm run build` succeeds.
- `src/lib/copy.ts` exists with `NO_ACCOUNT_TITLE`, `NO_ACCOUNT_BODY`,
  `REENTRY_CTA` named exports, no default export; both /manage and email.ts
  import from it (copy authored once, not duplicated divergently).
- Landing: under-form multi-team microcopy + quiet `/manage` re-entry link,
  no new chrome (no top strip, no below-fold card).
- /manage signed-out: accent-soft shared-copy trust block + no-"editor"
  subhead; the word "editor" appears nowhere in manage.astro.
- /manage signed-in: existing confederation fieldset wrapped in an
  `<details open>` "Football — World Cup 2026" accordion + two static
  "Coming soon" rows; `/api/save-selection`, checkbox names/values, the cap
  script, and the tz row are unchanged; zero new JS.
- Confirmation email: accent-soft bulletproof-table "No password. No account.
  Keep this email." callout with a token-less `/manage` button; Phase-10
  team+timezone personalization + confirm button preserved; subject and
  `sendManageLink()` unchanged.
- Zero backend/schema/API/dependency changes (db.ts, token.ts, session.ts,
  package.json untouched).
- All copy + IA sport-agnostic (no World-Cup-locked new copy beyond the
  existing/locked "Football — World Cup 2026" sport-section label).
</success_criteria>

<output>
Create `.planning/quick/260516-q33-manage-discoverability-content-and-entry/260516-q33-SUMMARY.md` when done.
</output>
</content>
</invoke>
