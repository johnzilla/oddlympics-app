---
phase: 06-landing-page-form-meta-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-04
  - FORM-01
  - FORM-02
  - FORM-03
  - META-01
requirements_addressed:
  - LAND-01
  - LAND-02
  - LAND-04
  - FORM-01
  - FORM-02
  - FORM-03
  - META-01

must_haves:
  truths:
    - "A first-time visitor on /  sees the headline 'Your team's matches. In your time zone. One ping before kickoff.'"
    - "The banner pill above the headline reads `WORLD CUP 2026 · JUNE 11 – JULY 19` (en-dash, middle-dot, mono caps)."
    - "Four below-fold sections render in this exact order: How it works (3 numbered steps), Why this exists (2 paragraphs), After the World Cup (2 paragraphs), Common questions (5 native <details> accordions, closed by default)."
    - "The consumer footer renders four links (Manage subscription -> /manage, Privacy -> /privacy, Terms -> /terms, Contact -> mailto:hello@oddlympics.app) plus the copyright line `© 2026 Oddlympics · Independent project · Not affiliated with FIFA`."
    - "The signup form posts five fields to /api/signup: visible `team` (required <select>) + visible `email` (required type=email), hidden `timezone`, hidden `requested_sport=world_cup`, hidden honeypot `website` with class `.hp`. Method is POST, action is /api/signup."
    - "The team <select> contains exactly six <optgroup> blocks in confederation order UEFA -> CONMEBOL -> CONCACAF -> CAF -> AFC -> OFC, totalling 48 <option> rows (plus one placeholder `<option value=\"\" disabled selected>Pick your team</option>`)."
    - "Zero occurrences (case-insensitive) of `bitcoin`, `lightning`, `crypto`, `world domination`, `personal olympics` anywhere in dist/client/index.html."
    - "The <head> carries the locked title, description, OG (og:title, og:description, og:type=website, og:url=https://oddlympics.app, og:site_name=Oddlympics, og:image=https://oddlympics.app/og-image.png + width/height/alt), and Twitter card (twitter:card=summary_large_image, twitter:title, twitter:description, twitter:image) tags verbatim per UI-SPEC §Meta tags."
    - "Page renders at 390 / 768 / 1280 px without horizontal scroll or text overlap (verified manually before merge)."
    - "CONTEXT D-02: `references/oddlympics_landing_copy.md` is the canonical source for headline, sub-headline, all 4 below-fold bodies, all 5 FAQ items, footer micro-copy, and exact meta tag strings; `references/index.html` is a structural reference only — NOT a literal port. CSS-variable convention (`--bg`, `--fg`, `--fg-dim`, `--line`, `--surface`, `--accent`, `--accent-ink`, `--mono`) and `<style is:global>` pattern are reused with values tuned for the light, sans-serif, consumer-friendly target."
    - "CONTEXT D-06: Phase 6 ships final OG/Twitter image meta tags pointing at `https://oddlympics.app/og-image.png` (1200×630) even though the PNG does not exist yet — Phase 8 commits `public/og-image.png` from `references/og-image.svg`. No tag rewiring later."
    - "CONTEXT D-08: All `https://oddlympics.app/*` URLs in the meta block are hardcoded verbatim to match the copy doc — NOT derived from `Astro.site` (canonical-copy precedent outranks DRY for this block)."
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Full consumer landing — rewritten frontmatter, head, body, form, four below-fold sections, footer, retuned <style is:global> (sans-serif primary, mono accents, light palette)."
      contains: "Your team's matches"
    - path: "src/pages/index.astro"
      provides: "48-team confederation-grouped <select>"
      contains: "<optgroup label=\"UEFA — Europe\""
  key_links:
    - from: "src/pages/index.astro frontmatter"
      to: "references/teams.json"
      via: "build-time JSON import `import teams from '../../references/teams.json' with { type: 'json' };`"
      pattern: "import teams from '\\.\\./\\.\\./references/teams\\.json' with"
    - from: "src/pages/index.astro form"
      to: "src/pages/api/signup.ts"
      via: "POST /api/signup with team+email+timezone+requested_sport+website fields"
      pattern: "<form method=\"post\" action=\"/api/signup\""
    - from: "src/pages/index.astro <head>"
      to: "https://oddlympics.app/og-image.png"
      via: "hardcoded og:image + twitter:image meta tag URL (Phase 8 ships the PNG; URL returns 404 between Phase 6 and Phase 8 ship — accepted per CONTEXT D-07)"
      pattern: "<meta property=\"og:image\" content=\"https://oddlympics\\.app/og-image\\.png\""
---

<objective>
Rewrite `src/pages/index.astro` end-to-end with the v2.0 consumer World Cup 2026 landing page: new frontmatter (build-time teams.json import + locked copy constants), new <head> (META-01 OG + Twitter tags verbatim, prohibited terms removed), new <body> (banner pill + headline + sub-headline with `<span id="tz-label">your local time</span>` SSR fallback + new form with 48-team confederation-grouped <select> + retained honeypot + retained `requested_sport=world_cup` + hidden `timezone` + new submit copy + the four below-fold sections + consumer footer), retuned `<style is:global>` (sans-serif primary, mono accents, warm-off-white/rust palette per UI-SPEC). This plan delivers all of LAND-01, LAND-02, LAND-04, FORM-01, FORM-02, FORM-03, META-01.

Purpose: This is the conversion surface. Every byte must match UI-SPEC §Copywriting Contract verbatim (the gsd-ui-checker signed off on these strings 6/6 PASS); every prohibited term must be scrubbed; the form contract must keep working against the Phase 5 `/api/signup` endpoint without any backend change.

Output: A rewritten `src/pages/index.astro` that builds cleanly, contains zero prohibited terms in the rendered HTML, and renders the locked UI-SPEC contract. Inline JS (tz-label swap, ?error= swap, Plausible Signup Submit listener) lands in Plan 02 — this plan ships the SSR-default span text `your local time` and the empty hidden `<input id="timezone">` so the page is correct JS-off and ready for the listener block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-landing-page-form-meta-analytics/06-CONTEXT.md
@.planning/phases/06-landing-page-form-meta-analytics/06-UI-SPEC.md
@.planning/phases/06-landing-page-form-meta-analytics/06-RESEARCH.md
@.planning/phases/06-landing-page-form-meta-analytics/06-PATTERNS.md

@references/oddlympics_landing_copy.md
@references/index.html
@references/landing_preview.png
@references/teams.json

@src/pages/index.astro
@src/pages/api/signup.ts
@src/pages/pending.astro
@src/lib/teams.ts
@src/lib/timezones.ts

<interfaces>
<!-- Key types and shapes the executor needs. Extracted from existing code. -->

From references/teams.json (48 entries, hand-ordered, snake_case slugs):
```json
[
  { "slug": "england", "label": "England", "confederation": "UEFA" },
  { "slug": "france", "label": "France", "confederation": "UEFA" },
  ...
  { "slug": "new_zealand", "label": "New Zealand", "confederation": "OFC" }
]
```

Confederation order (locked per CONTEXT D-02 + UI-SPEC §Copywriting Contract): UEFA, CONMEBOL, CONCACAF, CAF, AFC, OFC.

Confederation <optgroup> labels (UI-SPEC §Copywriting Contract, verbatim, em-dash U+2014):
- `UEFA — Europe`
- `CONMEBOL — South America`
- `CONCACAF — North & Central America`
- `CAF — Africa`
- `AFC — Asia`
- `OFC — Oceania`

From src/pages/api/signup.ts (Phase 5; do NOT modify):
- Reads via `request.formData()` — accepts URL-encoded.
- Expects field names: `email` (required), `team` (validates against `VALID_TEAMS` from `src/lib/teams.ts`), `timezone` (optional; invalid/empty falls back to `America/New_York` via `FALLBACK_TZ`), `requested_sport` (defaults `world_cup`), `website` (honeypot; non-empty → silent 303 to /pending).
- Origin check + rate-limit + email regex + `?error=` 303-redirect contract unchanged.

From src/lib/teams.ts:1 (canonical JSON-import pattern, proven working):
```ts
import teams from '../../references/teams.json' with { type: 'json' };
```

UI-SPEC §Meta tags — locked exact strings (PASTE VERBATIM, no paraphrase):
- `<title>` → `Oddlympics — World Cup 2026 alerts in your time zone`
- `<meta name="description">` → `Pick your team. Get one email one hour before every 2026 World Cup match, in your local time zone. Free. No ads. No betting odds.`
- `<meta property="og:title">` → `World Cup 2026 alerts in your time zone`
- `<meta property="og:description">` → `Pick your team. One ping, one hour before kickoff, in your local time. Free for the whole tournament.`
- `<meta property="og:type">` → `website`
- `<meta property="og:url">` → `https://oddlympics.app`
- `<meta property="og:site_name">` → `Oddlympics`
- `<meta property="og:image">` → `https://oddlympics.app/og-image.png`
- `<meta property="og:image:width">` → `1200`
- `<meta property="og:image:height">` → `630`
- `<meta property="og:image:alt">` → `Oddlympics — World Cup 2026 alerts in your time zone`
- `<meta name="twitter:card">` → `summary_large_image`
- `<meta name="twitter:title">` → `World Cup 2026 alerts in your time zone`
- `<meta name="twitter:description">` → `Pick your team. One ping, one hour before kickoff. Free.`
- `<meta name="twitter:image">` → `https://oddlympics.app/og-image.png`

UI-SPEC §Copywriting Contract — locked above-the-fold copy (PASTE VERBATIM):
- Banner pill: `WORLD CUP 2026 · JUNE 11 – JULY 19` (middle-dot U+00B7, en-dash U+2013)
- Headline: `Your team's matches. In your time zone. One ping before kickoff.` (use the curly apostrophe U+2019 in `team's`)
- Sub-headline SSR: `Pick your team. We'll email you one hour before every match in <span id="tz-label">your local time</span>. Nothing else. Free, no ads, no betting odds.`
- Team field label: `Your team`
- Placeholder option: `Pick your team` (`value=""` `disabled` `selected`)
- Email field label: `Email`, placeholder `you@example.com`
- Submit button: `Get match alerts`
- Fineprint (inside form): `One email per match for the team you pick. Unsubscribe in one click.`
- Trust line (outside form): `Built by one person in Michigan. No app to install. We'll never sell your email.`

UI-SPEC §Copywriting Contract — below-fold sections (PASTE VERBATIM from the table; the full text is in UI-SPEC.md §"Below-fold sections" — read that section before writing):
- Section 1 "How it works" — 3 numbered steps.
- Section 2 "Why this exists" — 2 paragraphs (P1 primary, P2 `.muted`).
- Section 3 "After the World Cup" — 2 paragraphs (P1 primary, P2 `.muted`).
- Section 4 "Common questions" — 5 <details><summary> accordions, closed by default.

UI-SPEC §Color — CSS tokens (locked, retune values from v1):
- `--bg: #fafaf7`
- `--fg: #14151a`
- `--fg-dim: #5a5d68`
- `--line: #e4e4dd`
- `--line-strong: #c9cbc4`
- `--surface: #ffffff`
- `--accent: #d94a1f`
- `--accent-ink: #ffffff`
- `--accent-soft: #fbe9e0`
- `--font-sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`
- `--font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace`
- `--max: 720px`
- `--pad-x: clamp(20px, 5vw, 48px)`

UI-SPEC §Typography — exactly 4 sizes, exactly 2 weights:
- Display (`h1.headline`): `clamp(28px, 5vw, 40px)`, sans, 700, line-height 1.15, letter-spacing -0.015em
- Subhead: 17px, sans, 400, line-height 1.55
- Body: 15px, sans, 400 (700 for `<strong>`), line-height 1.55
- Label/Caption: 13px, mono for banner pill / step numerals / footer copyright; sans for everything else. 700 for banner pill / form labels / step numerals / `section h2` eyebrows; 400 for fineprint / trust / error / footer links. Letter-spacing 0.16em on banner pill, 0.12em on section eyebrows, 0.02em on form labels.

Plausible script block (KEEP VERBATIM from current `src/pages/index.astro:19-24`):
```html
<script async src="https://plausible.io/js/pa-wRAab3seDWDDBnGbRbe0K.js"></script>
<script is:inline>
  window.plausible=window.plausible||function(){(plausible.q=plausible.q||[]).push(arguments)},plausible.init=plausible.init||function(i){plausible.o=i||{}};
  plausible.init()
</script>
```

Honeypot CSS (KEEP VERBATIM from current `src/pages/index.astro:233-240` — do NOT switch to `display: none`):
```css
.hp {
  position: absolute;
  left: -10000px;
  width: 1px;
  height: 1px;
  opacity: 0;
  pointer-events: none;
}
```

Prerendered-page URL-param swap precedent (extend, don't replace):
- `src/pages/index.astro:65-82` is the canonical `?error=` swap block — Plan 02 keeps it verbatim. Plan 01 ships the empty `<p id="error" class="error" role="alert" hidden></p>` host element inside the form so Plan 02's listener has a target.

Multi-key URL-param swap precedent: `src/pages/pending.astro:28-54` (multi-key COPY map driven by `?action=` + `?email=`) — useful reference for how multiple inline scripts coexist on a prerendered page.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/signup | User-controlled form fields cross from untrusted browser to trusted Node SSR endpoint (Phase 5 already validates) |
| build-time → served HTML | references/teams.json content is inlined at build; supply-chain trust on the JSON file (it's in our repo) |
| Plausible → plausible.io | External analytics script (covered by CSP allow-list at deploy/Caddyfile:30) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | Honeypot `<input name="website" class="hp">` | accept (existing) | Keep markup + `.hp` CSS verbatim (position:absolute left:-10000px). `/api/signup:57-61` short-circuits if non-empty. Anti-pattern alert: do NOT switch to `display: none` — bots that parse CSS skip the field. |
| T-06-02 | Tampering | Form `team` field | mitigate (existing) | Server-side allow-list at `src/pages/api/signup.ts` against `VALID_TEAMS` (`src/lib/teams.ts`). Client-side HTML5 `required` is convenience, not defense. Bad team → 303 `/?error=bad-form`, no row. |
| T-06-03 | Tampering | Form `timezone` field | mitigate (existing) | Server-side fallback at `src/lib/timezones.ts` `FALLBACK_TZ='America/New_York'` (Phase 5 D-03) — invalid/empty does not reject, just falls back. |
| T-06-04 | Tampering | Inline `?error=` swap script | mitigate (existing) | Plan 02 ships this; Plan 01 only renders the `<p id="error">` host. Lookup-table COPY map + `el.textContent = COPY[code] || 'Something went wrong.'` — `textContent` does NOT parse HTML, so injected `?error=<script>` does not XSS. Defense in depth via lookup table. |
| T-06-05 | Information Disclosure | Inline `<style>` / `<script>` text | mitigate | LAND-02 grep-on-build gate. The current `index.astro:3-4, 30` contains three prohibited terms (`World domination`, `Personal Olympics`, `Bitcoin/Lightning`) — Plan 01's rewrite scrubs them. Verify: `! grep -qiE 'bitcoin\|lightning\|crypto\|world domination\|personal olympics' dist/client/index.html`. |
| T-06-06 | Information Disclosure | references/teams.json supply chain | accept | File is in-repo, reviewed in Phase 5. No runtime fetch — build-time import only. |
| T-06-07 | Repudiation / Availability | og:image 404 between Phase 6 ship and Phase 8 ship | accept (per CONTEXT D-07) | Social shares between Phase 6 ship and Phase 8 ship lack image preview but the link still works. Phase 8 closes the window; aim to land Phases 6 and 8 close together. |
| T-06-08 | Tampering | CSP regression | mitigate | Plan 01 does NOT modify deploy/Caddyfile. CSP already allows `'unsafe-inline'` on script-src+style-src and `https://plausible.io` on script-src+connect-src (verified at deploy/Caddyfile:30). Inline `<script>` and `<style is:global>` blocks remain compatible. |
| T-06-09 | Tampering | Form `<form action>` open-redirect | accept | Action is the hardcoded same-origin string `/api/signup`. Not user-controlled. CSP `form-action 'self'` is the backstop. |

**Operational risk (non-STRIDE, tracked here per CONTEXT):**

| Risk | Mitigation Plan |
|------|-----------------|
| Plausible `Signup Submit` events drop silently if the goal is not configured in the dashboard (R-4) | Tracked in Plan 03's pre-deploy operator-action task; Plan 01 itself does not trigger Plausible events (Plan 02 does). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite src/pages/index.astro frontmatter + <head> + everything between <body> and </body></name>
  <files>src/pages/index.astro</files>
  <read_first>
    @src/pages/index.astro (current full 254-line file — Plan 01 replaces every section except the Plausible script block at lines 19-24 and the honeypot CSS rule at lines 233-240)
    @.planning/phases/06-landing-page-form-meta-analytics/06-UI-SPEC.md (§"Copywriting Contract" — verbatim copy strings for headline, sub-headline, banner pill, 4 below-fold sections, 5 FAQ items, footer, meta tags; §"Color" — token values; §"Typography" — sizes/weights/families; §"Spacing Scale" — tokens; §"Visual Hierarchy"; §"Interaction Contracts → Form behavior" / §"Responsive behavior"; §"Negative Space" — prohibited-terms list)
    @.planning/phases/06-landing-page-form-meta-analytics/06-CONTEXT.md (D-01 through D-08 — CSS-variable convention, hardcoded URL strategy, sub-headline span pattern, tz-label fallback)
    @.planning/phases/06-landing-page-form-meta-analytics/06-RESEARCH.md §"Code Examples" (frontmatter pattern, head meta block, form markup — these are non-binding scaffolds; UI-SPEC strings are authoritative)
    @.planning/phases/06-landing-page-form-meta-analytics/06-PATTERNS.md §"src/pages/index.astro" Patterns 1-6 (must-preserve / must-change / may-change lists)
    @references/oddlympics_landing_copy.md (canonical source for all copy strings including the four below-fold section bodies and 5 FAQ items — UI-SPEC mirrors this; treat both as authoritative)
    @references/landing_preview.png (visual diff target; informs section ordering, spacing rhythm, and the white form-card focal point)
    @references/index.html (structural reference for the 4 sections + FAQ accordions — NOT a literal port per CONTEXT D-02; use it to inform CSS classes and DOM structure, not to copy verbatim)
    @references/teams.json (48 entries the frontmatter must import + group by confederation)
    @src/lib/teams.ts (line 1 — the proven `import teams from '../../references/teams.json' with { type: 'json' };` pattern)
    @src/pages/pending.astro (lines 1-82 — secondary reference for the `<style is:global>` per-page convention and inline-script positioning)
  </read_first>
  <action>
    Replace the entire contents of `src/pages/index.astro` with the v2.0 consumer landing page. Concrete structural changes:

    **Frontmatter (`---` block):**
    - Keep `export const prerender = true;` (LAND-03 cacheability).
    - Replace the v1 `title` / `description` consts with named consts hoisted from UI-SPEC §Meta tags: `TITLE`, `DESCRIPTION`, `OG_TITLE`, `OG_DESCRIPTION`, `TWITTER_DESCRIPTION`, `SITE_URL = 'https://oddlympics.app'`, `OG_IMAGE = 'https://oddlympics.app/og-image.png'`. Use the verbatim strings from UI-SPEC §Meta tags / Copywriting Contract — no paraphrase.
    - Add `import teams from '../../references/teams.json' with { type: 'json' };` (Node 22+, already proven at src/lib/teams.ts:1). Alternative `import { TEAMS } from '../lib/teams';` is acceptable per CONTEXT discretion; either is fine.
    - Add `const CONFEDERATION_ORDER = ['UEFA', 'CONMEBOL', 'CONCACAF', 'CAF', 'AFC', 'OFC'] as const;` and a `CONFEDERATION_LABEL` record with the six labels listed in the `<interfaces>` block above (em-dash U+2014, e.g. `'UEFA — Europe'`).
    - Compute `const groupedTeams = CONFEDERATION_ORDER.map((conf) => ({ conf, label: CONFEDERATION_LABEL[conf], teams: teams.filter(t => t.confederation === conf) }));` — respect insertion order inside each group (do NOT re-sort alphabetically).

    **<head> block (replace lines 9-25 of v1):**
    - Keep `<meta charset>`, `<meta name="viewport">`, `<link rel="icon" type="image/svg+xml" href="/favicon.svg">`, the `<script async src="https://plausible.io/js/pa-wRAab3seDWDDBnGbRbe0K.js"></script>` line, and the 2-line Plausible global-shim `<script is:inline>` block — VERBATIM from current `src/pages/index.astro:19-24`.
    - Replace `<title>{title}</title>` with `<title>{TITLE}</title>` (note: TITLE is the new const).
    - Replace `<meta name="description">` with the new DESCRIPTION value.
    - Add the full OG meta block: `og:title`, `og:description`, `og:type=website`, `og:url=https://oddlympics.app`, `og:site_name=Oddlympics`, `og:image=https://oddlympics.app/og-image.png`, `og:image:width=1200`, `og:image:height=630`, `og:image:alt`.
    - Add Twitter card block: `twitter:card=summary_large_image`, `twitter:title`, `twitter:description`, `twitter:image`.
    - Use Astro template-literal `content={CONST}` for the const-backed tags; hardcode string literals for `og:type`, `og:url`, `og:site_name`, `og:image:width`, `og:image:height`, `twitter:card`. Per CONTEXT D-08: do NOT derive any URL from `Astro.site` — hardcode `https://oddlympics.app/*` verbatim.

    **<body> structure (replace lines 26-86 of v1):**
    The v1 body is a single `<main class="hero">` with one `<div class="hero-content">`. The new body is structured as: `<main>` containing `<section class="hero">` (banner + headline + sub-headline + form-card + fineprint + trust), then four sibling `<section>` blocks below the fold, then `<footer>`. All section blocks use a shared `.wrap` class with `max-width: var(--max)`. Use semantic HTML (`<section>`, `<footer>`, `<details>`, `<summary>`). Required elements:

    1. **Banner pill** at top of hero: `<p class="banner">WORLD CUP 2026 · JUNE 11 – JULY 19</p>` (middle-dot U+00B7, en-dash U+2013 — NOT hyphen). CSS: `font-family: var(--font-mono); font-weight: 700; font-size: 13px; letter-spacing: 0.16em; text-transform: uppercase; color: var(--accent); background: var(--accent-soft); border: 1px solid var(--accent); border-radius: 999px; padding: 6px 12px; display: inline-block;`.

    2. **Headline** `<h1 class="headline">Your team's matches. In your time zone. One ping before kickoff.</h1>` — note curly apostrophe U+2019.

    3. **Sub-headline** `<p class="subhead">Pick your team. We'll email you one hour before every match in <span id="tz-label">your local time</span>. Nothing else. Free, no ads, no betting odds.</p>` — exact text. The `<span id="tz-label">` is the swap target for Plan 02; Plan 01 ships it with the verbatim SSR fallback text. JS-off users see `your local time` and the sentence reads correctly.

    4. **Form card** (UI-SPEC §"Visual Hierarchy" primary focal point — white `--surface` background, 1px `--line` hairline border, light drop-shadow, rounded corners):
       ```
       <form method="post" action="/api/signup" id="signup-form" class="form-card">
         <div class="field">
           <label for="team">Your team</label>
           <select name="team" id="team" required>
             <option value="" disabled selected>Pick your team</option>
             {groupedTeams.map(({ label, teams }) => (
               <optgroup label={label}>
                 {teams.map((t) => <option value={t.slug}>{t.label}</option>)}
               </optgroup>
             ))}
           </select>
         </div>
         <div class="field">
           <label for="email">Email</label>
           <input type="email" id="email" name="email" required autocomplete="email" placeholder="you@example.com" />
         </div>
         <input type="hidden" name="timezone" id="timezone" value="" />
         <input type="hidden" name="requested_sport" value="world_cup" />
         <input type="text" name="website" tabindex="-1" autocomplete="off" class="hp" aria-hidden="true" />
         <div class="cta">
           <button type="submit">Get match alerts</button>
         </div>
         <p class="fineprint">One email per match for the team you pick. Unsubscribe in one click.</p>
         <p id="error" class="error" role="alert" hidden></p>
       </form>
       ```
       Note: `id="signup-form"` is REQUIRED (Plan 02's Plausible listener uses `getElementById('signup-form')`). The `<p id="error">` is INSIDE the form (UI-SPEC §"Error message rendering"); current v1 has it outside — move it inside. The honeypot `<input name="website">` MUST keep the exact attribute set (`type="text" tabindex="-1" autocomplete="off" class="hp" aria-hidden="true"`).

    5. **Trust line** OUTSIDE the form card: `<p class="trust">Built by one person in Michigan. No app to install. We'll never sell your email.</p>`.

    6. **Section 1 "How it works"** — eyebrow + 3 numbered steps. Paste the verbatim step bodies from UI-SPEC §"Below-fold sections" row 1 / `references/oddlympics_landing_copy.md`. Step structure: `<ol class="steps"><li><span class="num">1</span><div class="body"><strong>Pick your team.</strong> ...</div></li>...</ol>`. The `.num` span uses `font-family: var(--font-mono); color: var(--accent); background: var(--accent-soft);` per UI-SPEC §Color "Accent reserved for".

    7. **Section 2 "Why this exists"** — eyebrow + 2 paragraphs. P1 is primary text (`color: var(--fg)`); P2 has `class="muted"` (`color: var(--fg-dim)`). Paste verbatim from UI-SPEC §"Below-fold sections" row 2.

    8. **Section 3 "After the World Cup"** — same structure as section 2. Paste verbatim from UI-SPEC §"Below-fold sections" row 3.

    9. **Section 4 "Common questions"** — eyebrow + 5 native `<details><summary>` accordions, closed by default. Q/A pairs verbatim from UI-SPEC §"FAQ (5 items)" table. Accordion `<summary>` uses sans-serif 15px, 700; `<summary>::after` shows a `+` glyph (mono, color `var(--fg-dim)`); `details[open] summary::after` shows `−`. Transition `0.15s ease` on the glyph, overridden under `prefers-reduced-motion: reduce` to `none`.

    10. **Footer** at end of body: `<footer class="site-footer"><div class="wrap">` containing a row of four links (`Manage subscription` → `/manage`, `Privacy` → `/privacy`, `Terms` → `/terms`, `Contact` → `mailto:hello@oddlympics.app`) and a copyright line `<p class="copy">© 2026 Oddlympics · Independent project · Not affiliated with FIFA</p>` (`font-family: var(--font-mono); font-size: 13px; color: var(--fg-dim);`). Top border `1px solid var(--line)`.

    11. **DELETE** the v1-only elements: `.vip-cue` ("Watching something stranger?...") and `.manage-cue` ("Already signed up?..."). The footer "Manage subscription" link replaces `.manage-cue`.

    **<style is:global> block (replace lines 88-254 of v1):**
    - Keep `<style is:global>` mechanism (CLAUDE.md §Conventions; Layout.astro deferred to v1.1).
    - Replace `:root` variable VALUES per the UI-SPEC §Color and §Typography tokens listed in `<interfaces>` above. Token NAMES stay (`--bg`, `--fg`, `--fg-dim`, `--line`, `--surface`, `--accent`, `--accent-ink`); add the new tokens (`--accent-soft`, `--line-strong`, `--font-sans`, `--font-mono`, `--max`, `--pad-x`). DELETE the v1-only `--mono` token assignment (rename to `--font-mono`).
    - Keep `* { box-sizing: border-box; }` reset.
    - Body baseline: `font-family: var(--font-sans); background: var(--bg); color: var(--fg); font-size: 15px; line-height: 1.55;`.
    - Container: `.wrap { max-width: var(--max); margin: 0 auto; padding-left: var(--pad-x); padding-right: var(--pad-x); }`.
    - Sections: `section { padding: 32px 0; border-top: 1px solid var(--line); }`. First section after hero has the top border; hero itself does not.
    - Hero top padding: `64px` desktop, `36px` under `@media (max-width: 520px)`.
    - Form card: white surface, 1px `--line` border, 6-8px radius, light drop-shadow `0 1px 3px rgba(0,0,0,0.04)`, internal padding 16px (`--md` token), field vertical gap 12px (UI-SPEC §Spacing — documented exception, multiple of 4). Submit button takes full row width inside `.cta` (`button[type="submit"] { background: var(--accent); color: var(--accent-ink); width: 100%; padding: 12px 16px; border: none; border-radius: 6px; font-family: var(--font-sans); font-weight: 700; font-size: 15px; cursor: pointer; } button[type="submit"]:hover { filter: brightness(1.05); } button[type="submit"]:active { transform: translateY(1px); }`.
    - Focus rings on inputs/select/links/button: 1px `var(--accent)` border + 3px `rgba(217, 74, 31, 0.15)` outer ring on `:focus-visible`. Never `outline: none` without a replacement focus indicator (LAND-03 Accessibility).
    - Native `<select>` chevron: replace default with inline SVG `background-image: url("data:image/svg+xml,...")` stroked in `var(--fg-dim)`; `appearance: none; -webkit-appearance: none;`.
    - Error block (`.error`): `background: var(--accent-soft); color: var(--accent); border-radius: 6px; padding: 8px;`.
    - Honeypot CSS rule `.hp { position: absolute; left: -10000px; width: 1px; height: 1px; opacity: 0; pointer-events: none; }` — VERBATIM from v1 (do NOT switch to `display: none`; bots parse CSS and skip those).
    - FAQ `<details>` styles: `details { border-top: 1px solid var(--line); padding: 12px 0; } details summary { list-style: none; cursor: pointer; position: relative; padding-right: 24px; font-weight: 700; } details summary::-webkit-details-marker { display: none; } details summary::after { content: '+'; position: absolute; right: 0; font-family: var(--font-mono); color: var(--fg-dim); transition: transform 0.15s ease; } details[open] summary::after { content: '−'; }`.
    - Footer styles: `.site-footer { border-top: 1px solid var(--line); padding: 32px 0 64px; } .site-footer a { color: var(--fg-dim); margin-right: 16px; } .site-footer .copy { font-family: var(--font-mono); font-size: 13px; color: var(--fg-dim); margin-top: 16px; }`.
    - Mobile breakpoint `@media (max-width: 520px) { .hero { padding-top: 36px; } /* submit row stacks (no-op single button) */ }` — keep the 520px breakpoint exactly per UI-SPEC §Spacing.
    - Reduced motion block: `@media (prefers-reduced-motion: reduce) { details summary::after { transition: none; } button[type="submit"]:active { transform: none; } }`.

    **MUST NOT APPEAR (LAND-02 — case-insensitive):** `bitcoin`, `lightning`, `crypto`, `world domination`, `personal olympics` — anywhere in the file (frontmatter, head, body, inline scripts, inline styles, CSS comments, CSS variable names, asset URLs).

    **Inline JS not part of Plan 01:** The current `?error=` inline `<script is:inline>` block at v1 lines 65-82 is REMOVED here. Plan 02 ships the combined inline script (tz-label swap + `?error=` swap + Plausible listener). Plan 01's HTML must include the empty `<p id="error" ... hidden></p>` host element and the empty `<input id="timezone" ...>` host element so Plan 02 has DOM targets to write into.

    Do NOT modify: `src/pages/api/signup.ts`, `src/lib/teams.ts`, `src/lib/timezones.ts`, `deploy/Caddyfile`, any other page, any package.json dependency, any reference under `references/` (those moves happened in the pre-plan docs sweep).
  </action>
  <acceptance_criteria>
    - Source: `grep -F "Your team's matches. In your time zone. One ping before kickoff." src/pages/index.astro` exits 0 (LAND-01 headline present, verbatim with curly apostrophe).
    - Source: `grep -F "WORLD CUP 2026 · JUNE 11 – JULY 19" src/pages/index.astro` exits 0 (LAND-01 banner pill, middle-dot + en-dash).
    - Source: `grep -F "Built by one person in Michigan. No app to install. We'll never sell your email." src/pages/index.astro` exits 0 (trust line).
    - Source: `grep -F "Independent project · Not affiliated with FIFA" src/pages/index.astro` exits 0 (LAND-04 / footer disclaimer).
    - Source: `grep -F "hello@oddlympics.app" src/pages/index.astro` exits 0 (footer contact link).
    - Source: `grep -cE '<optgroup label="(UEFA — Europe\|CONMEBOL — South America\|CONCACAF — North & Central America\|CAF — Africa\|AFC — Asia\|OFC — Oceania)"' src/pages/index.astro` returns `6` (FORM-02).
    - Source: `grep -E '<input[^>]+type="hidden"[^>]+name="timezone"' src/pages/index.astro` exits 0 (FORM-01 hidden timezone field with id for Plan 02 to populate).
    - Source: `grep -E '<input[^>]+type="hidden"[^>]+name="requested_sport"[^>]+value="world_cup"' src/pages/index.astro` exits 0 (FORM-01 retained).
    - Source: `grep -E '<input[^>]+name="website"[^>]+class="hp"' src/pages/index.astro` exits 0 (FORM-01 honeypot retained).
    - Source: `grep -E '<form[^>]+method="post"[^>]+action="/api/signup"[^>]+id="signup-form"' src/pages/index.astro` exits 0 (FORM-03 retained + new id for Plan 02 listener).
    - Source: `grep -F '<span id="tz-label">your local time</span>' src/pages/index.astro` exits 0 (CONTEXT D-04 SSR fallback in place).
    - Source: `grep -F '<p id="error" class="error" role="alert" hidden></p>' src/pages/index.astro` exits 0 (FORM-03 error host element inside form for Plan 02).
    - Source: `grep -F "<title>Oddlympics — World Cup 2026 alerts in your time zone</title>" src/pages/index.astro` exits 0 — OR equivalent template form `<title>{TITLE}</title>` plus `TITLE = 'Oddlympics — World Cup 2026 alerts in your time zone'` const in frontmatter (META-01).
    - Source: `grep -E 'og:title\|og:description\|og:type\|og:url\|og:site_name\|og:image\|og:image:width\|og:image:height\|og:image:alt\|twitter:card\|twitter:title\|twitter:description\|twitter:image' src/pages/index.astro | wc -l | awk '{ exit !($1 >= 13) }'` exits 0 (META-01 — all 13 OG/Twitter meta tags present in source).
    - Source: `grep -F 'https://oddlympics.app/og-image.png' src/pages/index.astro` exits 0 (CONTEXT D-06/D-08 hardcoded image URL).
    - Source: `! grep -iE 'bitcoin\|lightning\|crypto\|world domination\|personal olympics' src/pages/index.astro` exits 0 (LAND-02 source-level scrub).
    - Build: `npm run build` exits 0.
    - Type: `npx astro check` exits 0 (zero errors and zero warnings on the rewritten file).
    - Build artifact: `! grep -iE 'bitcoin\|lightning\|crypto\|world domination\|personal olympics' dist/client/index.html` exits 0 (LAND-02 rendered-HTML gate — the canonical gate; Phase 11 AC7 runs the same one on prod).
    - Build artifact: `grep -c '<optgroup ' dist/client/index.html` returns `6` (FORM-02 rendered).
    - Build artifact: `grep -cE '<option value="[^"]+"' dist/client/index.html` returns a number >= 48 (48 teams + placeholder + any other unrelated <option> tags; the smoke script in Plan 03 will tighten this to count under the signup-form select specifically).
    - Build artifact: `grep -F 'Your team' dist/client/index.html` exits 0 (form team label rendered).
    - Build artifact: `grep -F '<span id="tz-label">your local time</span>' dist/client/index.html` exits 0 (LAND-01 SSR fallback survives the build).
    - Manual (recorded in commit body): open `npm run dev`, browse `http://localhost:4321/` in Chrome DevTools responsive mode at 390 / 768 / 1280 px; LAND-04 — confirm no horizontal scroll, no text overlap, the form card is the focal point at all three viewports.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npx astro check &amp;&amp; ! grep -iE 'bitcoin|lightning|crypto|world domination|personal olympics' dist/client/index.html &amp;&amp; [ "$(grep -c '&lt;optgroup ' dist/client/index.html)" = "6" ] &amp;&amp; grep -F "Your team's matches. In your time zone. One ping before kickoff." dist/client/index.html &amp;&amp; grep -F 'WORLD CUP 2026 · JUNE 11 – JULY 19' dist/client/index.html &amp;&amp; grep -F '&lt;span id="tz-label"&gt;your local time&lt;/span&gt;' dist/client/index.html &amp;&amp; grep -F 'https://oddlympics.app/og-image.png' dist/client/index.html &amp;&amp; grep -F 'twitter:card' dist/client/index.html &amp;&amp; grep -F 'og:site_name' dist/client/index.html</automated>
  </verify>
  <done>
    `src/pages/index.astro` has been rewritten end-to-end. `npm run build` and `npx astro check` both exit 0. The built `dist/client/index.html` contains the locked headline, banner pill, four below-fold sections, footer, 48-team confederation-grouped <select>, all 13 OG/Twitter meta tags, and the SSR-fallback tz-label span — and contains zero prohibited terms. The 390 / 768 / 1280 px manual viewport check has been recorded as a one-line note in the commit body. Plan 02 can now attach the inline JS to the `#signup-form` / `#tz-label` / `#timezone` / `#error` DOM targets.
  </done>
</task>

</tasks>

<verification>
1. Build verification: `npm run build && npx astro check` — must exit 0.
2. LAND-02 gate (the canonical one): `! grep -iE 'bitcoin|lightning|crypto|world domination|personal olympics' dist/client/index.html`.
3. LAND-01 markers: headline + banner pill + four-section eyebrows + footer + trust line all present in dist HTML.
4. FORM-01/02/03 markers: form action + method, six `<optgroup>`s, 48+ `<option>`s, hidden timezone field, hidden requested_sport, honeypot, submit button label.
5. META-01 markers: all 13 OG + Twitter meta tags + new <title> + new description, plus hardcoded `https://oddlympics.app/og-image.png`.
6. LAND-04 manual: 390 / 768 / 1280 px DevTools responsive check — no horizontal scroll, no text overlap, form card focal point intact.
7. No file outside `src/pages/index.astro` is modified (verify with `git status --short` after commit prep — only `src/pages/index.astro` and `.planning/STATE.md` should show as changed).
</verification>

<success_criteria>
- `src/pages/index.astro` rewritten; 254 → ~similar order-of-magnitude line count (a few sections shorter, a few longer; not a hard target).
- All acceptance criteria in Task 1 exit 0.
- The Plausible script block at v1 lines 19-24 is preserved verbatim in the rewritten file's <head>.
- Honeypot HTML + CSS rule preserved verbatim.
- Plan 02 has the DOM targets it needs: `#signup-form`, `#tz-label`, `#timezone`, `#error`.
- Git commit message format: `feat(06-01): rewrite landing page — consumer copy, 48-team select, OG/Twitter meta tags (LAND-01/02/04, FORM-01/02/03, META-01)`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page-form-meta-analytics/06-01-SUMMARY.md` per the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Final file size + structure outline (frontmatter consts, head meta tag count, body section count, style block token list).
- Which UI-SPEC sections were preserved verbatim vs. tuned (with rationale for any tune).
- Manual viewport check result (recorded screenshots or "no horizontal scroll observed at 390/768/1280").
- Any deviations from this plan's <action> block (with rationale).
</output>
