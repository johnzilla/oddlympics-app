---
phase: 09-manage-editor-unsubscribe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/lib/email.ts
autonomous: true
requirements:
  - MANAGE-02
user_setup: []
tags:
  - db
  - email
  - re-subscribe
  - manage-02
must_haves:
  truths:
    - "After markConfirmed.get(email) on a previously-unsubscribed confirmed row, that row's unsubscribed_at is NULL and confirmed_at is the current epoch second"
    - "After markConfirmed.get(email) on a fresh unconfirmed row, that row's confirmed_at is set and unsubscribed_at remains NULL"
    - "markConfirmed.get(email) on an already-confirmed, never-unsubscribed row returns undefined (0 rows affected — idempotent)"
    - "markUnsubscribed.get(email) followed by a second markUnsubscribed.get(email) returns a row the first time and undefined the second time (DB-layer idempotent single-use, formalizing D-06)"
    - "sendManageLink builds a URL of shape ${SITE_URL}/manage?token=... (not /schedule?token=...)"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "markConfirmed prepared statement widened to also match unsubscribed_at IS NOT NULL and clear unsubscribed_at on confirm"
      contains: "unsubscribed_at = NULL"
    - path: "src/lib/email.ts"
      provides: "sendManageLink builds /manage?token=... URL"
      contains: "/manage?token="
  key_links:
    - from: "src/lib/db.ts markConfirmed"
      to: "vip_signups WHERE clause"
      via: "WHERE email = ? AND (confirmed_at IS NULL OR unsubscribed_at IS NOT NULL)"
      pattern: "confirmed_at IS NULL OR unsubscribed_at IS NOT NULL"
    - from: "src/lib/email.ts sendManageLink"
      to: "rendered email URL"
      via: "${SITE_URL}/manage?token=${encodeURIComponent(token)}"
      pattern: "/manage\\?token="
---

<objective>
Two surgical library edits that complete the back-end half of Phase 9
before Wave 2 touches any UI surface:

1. Widen `markConfirmed` in `src/lib/db.ts` so it ALSO matches rows where
   `unsubscribed_at IS NOT NULL`, and SETs `unsubscribed_at = NULL` on
   match. This implements CONTEXT D-07 and unlocks SC4 (re-subscribe path).
   The existing `markUnsubscribed` already provides D-06's DB-layer
   idempotency contract via `WHERE unsubscribed_at IS NULL` — no code
   change there, just verify via grep that the contract still holds.

2. Change `sendManageLink()` in `src/lib/email.ts:68` from
   `${SITE_URL}/schedule?token=...` to `${SITE_URL}/manage?token=...` per
   CONTEXT D-01. After this lands, newly-minted manage-link emails point
   at `/manage`; old in-flight emails (max 24h TTL on manage-purpose
   tokens) keep working via the Wave 2 `/schedule` 301 handler.

Purpose: Lands before Wave 2 so the page rewrite at `/manage` operates on
the correct semantics from the moment it ships. Same-wave with Plan 09-01
because the two files share no overlap with token.ts.

Output: Two modified library files. No new exports. No new files. No DB
migration (the WHERE-clause change is statement-only; SQLite 3.35+ already
verified by the Phase 5 version assert at db.ts:44-52).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md
@.planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md
@.planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md

@src/lib/db.ts
@src/lib/email.ts
@src/pages/api/confirm.ts

<interfaces>
src/lib/db.ts (current, lines 95-100):
```
export const markConfirmed = db.prepare<[string]>(`
  UPDATE vip_signups
  SET confirmed_at = strftime('%s','now')
  WHERE email = ? AND confirmed_at IS NULL
  RETURNING *
`);
```
- Caller: src/pages/api/confirm.ts → `markConfirmed.get(result.email)` then falls back to `getByEmail.get(...)` on undefined return (treats undefined as "already confirmed"). Behaviour after widening (per RESEARCH Q4 verified): re-confirm of an unsubscribed user returns the row (status=ok), already-confirmed-and-active still returns undefined (status=already via fallback path).

src/lib/db.ts (markUnsubscribed at lines 106-110) — unchanged. Existing shape:
```
WHERE email = ? AND unsubscribed_at IS NULL
```
This is the D-06 idempotency contract. Verify only; do NOT edit.

src/lib/email.ts (current, line 68): `const url = `${SITE_URL}/schedule?token=${encodeURIComponent(token)}`;`
- Caller: `src/pages/api/manage.ts` (magic-link request handler). Existing HTML/text bodies reference `${url}` — they remain correct after the path change because the URL is interpolated, not hardcoded twice.
- `buildUnsubscribeHeaders()` at lines 55-65 is unaffected (it builds an `/api/unsubscribe?token=` URL, not `/schedule` or `/manage`).
- `sendMagicLink()` at lines 15-53 is unaffected (it builds `/api/confirm?token=` — a different flow).

Verified by RESEARCH Q2: changing this URL plus deploying the schedule.astro thin redirect (Plan 09-03) is the canonical pair; partial deploys are not safe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Widen markConfirmed WHERE clause and clear unsubscribed_at on confirm (D-07)</name>
  <files>src/lib/db.ts</files>
  <read_first>
    - src/lib/db.ts (the file being modified; markConfirmed at lines 95-100, markUnsubscribed reference shape at 106-110)
    - src/pages/api/confirm.ts (caller — must not require changes; returns getByEmail fallback when markConfirmed.get returns undefined)
    - .planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md (D-07 exact SQL shape)
    - .planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md (Q4: behavioural verification table for each scenario)
    - .planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md ("src/lib/db.ts (lib — markConfirmed WHERE widening)" section)
  </read_first>
  <action>
    Edit `src/lib/db.ts` only. Replace the `markConfirmed` prepared statement body (currently lines 95-100) so that:
    - The SET list adds `unsubscribed_at = NULL` alongside the existing `confirmed_at = strftime('%s','now')`.
    - The WHERE clause becomes `email = ? AND (confirmed_at IS NULL OR unsubscribed_at IS NOT NULL)`.
    - `RETURNING *` is preserved.
    - The prepared-statement generic `<[string]>` is preserved.

    Replace the inline comment above the statement with a why-comment naming D-07 and the re-subscribe SC4 path: re-confirm after unsubscribe matches via `unsubscribed_at IS NOT NULL`; the SET clears `unsubscribed_at` to restore the row to fully active; already-confirmed-and-not-unsubscribed rows still produce 0 rows (idempotent). Do NOT touch markUnsubscribed (lines 106-110) — its `WHERE email = ? AND unsubscribed_at IS NULL` is already the D-06 contract.

    Do NOT add/remove any other prepared statements. Do NOT touch the migration block (lines 33-61) or the schema CREATE TABLE blocks. Do NOT change the `VipSignup` type (lines 63-75). The src/pages/api/confirm.ts caller MUST work unchanged — its current `if (!updated) → getByEmail.get → 'already'` fallback is correct because RESEARCH Q4 confirms semantically that re-confirming an unsubscribed user returns the row (status=ok), which is the intended SC4 behaviour.
  </action>
  <verify>
    <automated>grep -E "WHERE email = \? AND \(confirmed_at IS NULL OR unsubscribed_at IS NOT NULL\)" src/lib/db.ts</automated>
    Plus:
    - `grep -c "unsubscribed_at = NULL" src/lib/db.ts` returns 1 (the new SET addition; only the markConfirmed statement should contain this)
    - `grep -E "markUnsubscribed.*WHERE email = \? AND unsubscribed_at IS NULL" src/lib/db.ts -A 2` confirms markUnsubscribed unchanged (existing single-use contract preserved)
    - `npx astro check` exits 0
    - `npm run build` exits 0 (the build boots db.ts at module load via better-sqlite3 prepare(); a syntactically broken prepared statement throws here)
  </verify>
  <done>
    markConfirmed in src/lib/db.ts has the widened WHERE clause and SETs unsubscribed_at = NULL; markUnsubscribed is unchanged; npm run build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Change sendManageLink URL from /schedule?token= to /manage?token= (D-01)</name>
  <files>src/lib/email.ts</files>
  <read_first>
    - src/lib/email.ts (the file being modified; sendManageLink at lines 67-100, line 68 specifically)
    - src/pages/api/manage.ts (caller — uses sendManageLink with the new URL; verify nothing else hardcodes /schedule)
    - .planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md (D-01 URL change spec)
    - .planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md (Q2 in-flight magic-link compatibility — 24h TTL window closes within a day of deploy)
    - .planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md ("src/lib/email.ts (lib — sendManageLink URL change)" section)
  </read_first>
  <action>
    Edit `src/lib/email.ts` only. Change line 68 from `${SITE_URL}/schedule?token=${encodeURIComponent(token)}` to `${SITE_URL}/manage?token=${encodeURIComponent(token)}`. The variable name `url` and the surrounding text/html bodies that reference `${url}` stay identical (the URL is interpolated). The dev-fallback console.log and the Resend.emails.send call are unchanged.

    Do NOT touch:
    - `sendMagicLink()` (lines 15-53) — its URL is `/api/confirm?token=`, a different flow.
    - `buildUnsubscribeHeaders()` (lines 55-65) — its URL is `/api/unsubscribe?token=`, unaffected.
    - The HTML body of `sendManageLink` (lines 80-90) — `${url}` interpolation already routes the new path.

    One single-character edit conceptually (`/schedule` → `/manage`); no other diff.
  </action>
  <verify>
    <automated>grep -c "/manage?token=" src/lib/email.ts</automated>
    Expected: at least 1 (the new URL string). Plus:
    - `! grep -q "/schedule?token=" src/lib/email.ts` (the old URL is fully gone)
    - `grep -n "sendManageLink" src/lib/email.ts | head -1` still finds the function (no accidental rename)
    - `npx astro check` exits 0
  </verify>
  <done>
    src/lib/email.ts sendManageLink builds a /manage?token= URL; no /schedule?token= remains anywhere in email.ts; npx astro check passes.
  </done>
</task>

</tasks>

<verification>
- `grep -E "WHERE email = \? AND \(confirmed_at IS NULL OR unsubscribed_at IS NOT NULL\)" src/lib/db.ts` matches one line
- `grep -c "unsubscribed_at = NULL" src/lib/db.ts` returns 1
- `grep -E "markUnsubscribed.*\n.*UPDATE vip_signups.*\n.*SET unsubscribed_at = strftime" src/lib/db.ts` shows markUnsubscribed body unchanged (or visual inspect lines 106-110)
- `grep -c "/manage?token=" src/lib/email.ts` returns ≥ 1
- `grep -c "/schedule?token=" src/lib/email.ts` returns 0
- `npx astro check` exits 0
- `npm run build` exits 0 (db.ts prepared statements compile at module load)
- Manual DB sanity (optional, for SUMMARY evidence): in a node REPL against a dev DB, call markConfirmed.get on a synthetic row with `unsubscribed_at` set; row returns with `unsubscribed_at = null` and confirmed_at set. Full verification lives in Plan 09-05 M9.
</verification>

<success_criteria>
- markConfirmed WHERE clause widened per D-07
- markConfirmed SET clears unsubscribed_at on match
- markUnsubscribed unchanged (D-06 contract preserved)
- sendManageLink URL points at /manage (D-01)
- No other library edits
- Build + type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-manage-editor-unsubscribe/09-02-SUMMARY.md` capturing:
- Diff for markConfirmed (before/after SQL bodies)
- Diff for sendManageLink URL line (before/after)
- Confirmation that markUnsubscribed at db.ts:106-110 was NOT touched (the D-06 contract is the existing partial WHERE)
- Note that any /schedule?token= magic links already in user inboxes will route via Plan 09-03's 301 handler within their 24h TTL window
</output>
