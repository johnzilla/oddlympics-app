---
phase: 09-manage-editor-unsubscribe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/token.ts
autonomous: true
requirements:
  - MANAGE-02
user_setup: []
tags:
  - token
  - ttl
  - hmac
  - manage-02
must_haves:
  truths:
    - "mintToken({ purpose: 'unsubscribe' }) produces a token whose exp is ~1 year (60*60*24*365 seconds) ahead of now"
    - "mintToken({ purpose: 'confirm' }) and mintToken({ purpose: 'manage' }) continue to produce 24h tokens (60*60*24 seconds)"
    - "mintToken called by buildSessionCookie (which passes ttlSeconds explicitly) still produces a 30-day session token (no regression)"
    - "Existing call sites that pass opts.ttlSeconds continue to take precedence over TTL_BY_PURPOSE (override semantics preserved)"
  artifacts:
    - path: "src/lib/token.ts"
      provides: "TTL_BY_PURPOSE constant table + purpose-aware TTL resolution in mintToken"
      contains: "TTL_BY_PURPOSE"
  key_links:
    - from: "src/lib/token.ts (mintToken)"
      to: "TTL_BY_PURPOSE table"
      via: "opts?.ttlSeconds ?? TTL_BY_PURPOSE[opts?.purpose ?? 'confirm']"
      pattern: "TTL_BY_PURPOSE\\[opts\\?\\.purpose"
    - from: "src/lib/email.ts (buildUnsubscribeHeaders → mintToken({ purpose: 'unsubscribe' }))"
      to: "TTL_BY_PURPOSE.unsubscribe (1 year)"
      via: "no call-site change required — mintToken resolves automatically"
      pattern: "purpose: 'unsubscribe'"
---

<objective>
Replace the single `TTL_SECONDS = 60 * 60 * 24` scalar in `src/lib/token.ts`
with a `TTL_BY_PURPOSE` constant table (per CONTEXT D-05), and update
`mintToken()` so its TTL resolution falls back to `TTL_BY_PURPOSE[purpose]`
when no explicit `opts.ttlSeconds` is passed. This satisfies MANAGE-02's
"HMAC-signed, 1-year expiry" requirement for unsubscribe tokens with zero
changes at any call site — `buildUnsubscribeHeaders` automatically yields
1-year tokens after this edit.

Purpose: Pure library change; no UI surface, no HTTP behaviour change. Wave
1 lands first because Wave 2 plans assume the new TTL semantics.

Output: `src/lib/token.ts` with `TTL_BY_PURPOSE` table and a purpose-aware
TTL lookup in `mintToken()`. No new exports. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md
@.planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md
@.planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md

@src/lib/token.ts
@src/lib/session.ts
@src/lib/email.ts

<interfaces>
Current token.ts exports (lines 31-47):
- `mintToken(email: string, opts?: { purpose?: 'confirm' | 'unsubscribe' | 'manage' | 'session'; ttlSeconds?: number }): string`
- `verifyToken(token: string, expectedPurpose?: 'confirm' | 'unsubscribe' | 'manage' | 'session'): { email: string } | null`

Internal type `Payload = { email: string; exp: number; purpose?: 'confirm' | 'unsubscribe' | 'manage' | 'session' }` at lines 12-16. Do NOT widen the union; only the constant table is added.

Current line 4: `const TTL_SECONDS = 60 * 60 * 24; // 24 hours`
Current line 38: `const ttl = opts?.ttlSeconds ?? TTL_SECONDS;`

Existing call sites that depend on this:
- `src/lib/session.ts:9-11` — passes `ttlSeconds: SESSION_TTL_SECONDS` explicitly (60*60*24*30). MUST remain 30d.
- `src/lib/email.ts:59` — calls `mintToken(email, { purpose: 'unsubscribe' })` with no ttlSeconds. AFTER this edit: 1 year.
- `src/pages/api/signup.ts` — calls `mintToken(email, { purpose: 'confirm' })`. AFTER this edit: still 24h.
- `src/pages/api/manage.ts` — calls `mintToken(email, { purpose: 'manage' })`. AFTER this edit: still 24h.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace TTL_SECONDS scalar with TTL_BY_PURPOSE table and update mintToken TTL resolution</name>
  <files>src/lib/token.ts</files>
  <read_first>
    - src/lib/token.ts (the file being modified; current scalar at line 4, mintToken lookup at line 38)
    - src/lib/session.ts (call site that passes ttlSeconds explicitly — must remain unaffected)
    - src/lib/email.ts (lines 55-65, the buildUnsubscribeHeaders call site that inherits 1y automatically)
    - .planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md (Q3: TTL_BY_PURPOSE mechanics; legacy token handling)
    - .planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md (Change 1 + Change 2 under "src/lib/token.ts")
    - .planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md (D-05 locks the exact values and structure)
  </read_first>
  <action>
    Edit src/lib/token.ts. Two surgical changes per D-05:

    (1) Replace the single-line `TTL_SECONDS` constant (line 4) with a `TTL_BY_PURPOSE` object literal keyed by the four purpose strings (`confirm`, `manage`, `unsubscribe`, `session`). Values per D-05: `confirm`=24h (60*60*24), `manage`=24h (60*60*24), `unsubscribe`=1y (60*60*24*365), `session`=30d (60*60*24*30). Use `as const` for narrow inference. Add a brief why-comment naming MANAGE-02 as the reason unsubscribe is 1 year and noting that the session entry mirrors src/lib/session.ts SESSION_TTL_SECONDS.

    (2) Inside `mintToken()` (currently line 38), change `opts?.ttlSeconds ?? TTL_SECONDS` to look up via the purpose: `opts?.ttlSeconds ?? TTL_BY_PURPOSE[opts?.purpose ?? 'confirm']`. The `'confirm'` fallback handles the no-opts call shape; opts.ttlSeconds remains the highest-precedence override (preserving buildSessionCookie's explicit ttlSeconds path).

    Do NOT touch:
    - The `Payload` type at lines 12-16 (no new purposes).
    - `verifyToken()` (its expiry check uses payload.exp, which is already populated by mintToken; legacy purpose-less tokens still treated as 'confirm' per the existing fallback at line 70).
    - The `SECRET` / `effectiveSecret` / sign helpers.
    - Any other line in the file.

    Preserve existing exports, comments style (why-only), and `node:crypto` import pattern.
  </action>
  <verify>
    <automated>grep -c "TTL_BY_PURPOSE" src/lib/token.ts</automated>
    Expected: at least 2 (declaration + use inside mintToken). Run additionally:
    - `! grep -q "TTL_SECONDS" src/lib/token.ts` (legacy scalar fully removed)
    - `grep -E "unsubscribe:\s*60\s*\*\s*60\s*\*\s*24\s*\*\s*365" src/lib/token.ts` (1-year value present)
    - `grep -E "TTL_BY_PURPOSE\[opts\?\.purpose \?\? 'confirm'\]" src/lib/token.ts` (mintToken lookup form correct)
    - `npx astro check` exits 0 (no type errors)
  </verify>
  <done>
    src/lib/token.ts contains a `TTL_BY_PURPOSE` constant table with the four entries (confirm/manage/unsubscribe/session) at the values specified in D-05, the legacy `TTL_SECONDS` scalar is gone, `mintToken()` resolves TTL via `opts?.ttlSeconds ?? TTL_BY_PURPOSE[opts?.purpose ?? 'confirm']`, and `npx astro check` exits 0.
  </done>
</task>

</tasks>

<verification>
- `grep -c "TTL_BY_PURPOSE" src/lib/token.ts` ≥ 2
- `grep -v '^#' src/lib/token.ts | grep -c "TTL_SECONDS"` returns 0 (legacy scalar removed; filter excludes nothing since file is .ts but pattern still safe)
- `grep -E "unsubscribe:\s*60\s*\*\s*60\s*\*\s*24\s*\*\s*365" src/lib/token.ts` matches one line
- `grep -E "confirm:\s*60\s*\*\s*60\s*\*\s*24" src/lib/token.ts` matches one line (confirm=24h)
- `grep -E "session:\s*60\s*\*\s*60\s*\*\s*24\s*\*\s*30" src/lib/token.ts` matches one line
- `grep -E "manage:\s*60\s*\*\s*60\s*\*\s*24" src/lib/token.ts` matches one line
- `npx astro check` exits 0
- Sanity smoke (optional, inline): `node --input-type=module -e "const {mintToken,verifyToken}=await import('./src/lib/token.ts'); /* expect 1y exp via JSON parse */"` — final smoke lands in plan 09-05.
</verification>

<success_criteria>
- TTL_BY_PURPOSE table present in src/lib/token.ts with all four purpose entries at the D-05 values
- Legacy TTL_SECONDS scalar removed
- mintToken TTL resolution uses opts?.ttlSeconds ?? TTL_BY_PURPOSE[opts?.purpose ?? 'confirm']
- No other line in token.ts modified
- No new exports
- npx astro check passes
- All existing call sites compile (no edits required at any call site)
</success_criteria>

<output>
After completion, create `.planning/phases/09-manage-editor-unsubscribe/09-01-SUMMARY.md` capturing:
- Exact diff applied to token.ts (line numbers before/after)
- Confirmation that grep gates pass
- Note that buildUnsubscribeHeaders (src/lib/email.ts:55-65) now produces 1-year unsubscribe tokens with no call-site change
- Note that buildSessionCookie (src/lib/session.ts:9-11) is unaffected because it passes ttlSeconds explicitly
</output>
