---
phase: 09-manage-editor-unsubscribe
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/pages/manage.astro
  - src/pages/api/save-selection.ts
autonomous: true
requirements:
  - MANAGE-01
  - COMPAT-01
user_setup: []
tags:
  - manage
  - editor
  - banner
  - save-selection
  - compat-01
  - manage-01
must_haves:
  truths:
    - "GET /manage with no session and no token returns 200 with the existing magic-link request form (signed-out branch preserved)"
    - "GET /manage?token=<valid-manage-token> returns 200 with the editor UI and a Set-Cookie header minting a 30-day session"
    - "GET /manage with a valid session cookie returns 200 with the editor UI and renders user.team's slug as the selected <option>"
    - "GET /manage with a valid session cookie for a row where team IS NULL renders the banner with copy 'Pick a team'"
    - "GET /manage with a valid session cookie for a row where team is set renders the banner with copy 'Your schedule'"
    - "POST /api/save-selection with form field team=<valid-slug> and a session cookie returns 303 to /manage?status=saved and persists the slug + timezone via setSelection"
    - "POST /api/save-selection with form field team=fake_slug returns 303 to /manage?status=bad-team and persists nothing"
    - "POST /api/save-selection with team_ids[]=<integer-id> (and no team= field) still works during the transition window (resolves slug via teams.id lookup) and redirects to /manage?status=saved"
    - "POST /api/save-selection with no auth (no token, no session) returns 303 to /manage?error=bad-token"
    - "manage.astro frontmatter contains no Astro.redirect('/schedule') call — the legacy line 7 redirect is removed BEFORE the editor branch is added"
    - "STATUS_COPY map in manage.astro contains 'bad-team' and does NOT contain 'too-many'"
    - "Rendered /manage HTML contains zero occurrences (case-insensitive) of: bitcoin, lightning, crypto, world domination, personal olympics (LAND-02 compliance)"
  artifacts:
    - path: "src/pages/manage.astro"
      provides: "Dual-mode page: signed-out magic-link form (unchanged) + signed-in editor branch with banner, team <select>, tz row, save form, conditional matches list, logout"
      contains: "form method=\"post\" action=\"/api/save-selection\""
    - path: "src/pages/api/save-selection.ts"
      provides: "Primary parse of team=<slug> via VALID_TEAMS; team_ids[] fallback for transition window; redirect target /manage?status=...; 'too-many' replaced with 'bad-team'"
      contains: "VALID_TEAMS"
  key_links:
    - from: "manage.astro signed-in editor branch"
      to: "/api/save-selection"
      via: "<form method=\"post\" action=\"/api/save-selection\">"
      pattern: "action=\"/api/save-selection\""
    - from: "manage.astro frontmatter"
      to: "TEAMS (src/lib/teams.ts) + groupedTeams (CONFEDERATION_ORDER per index.astro:17-39)"
      via: "import { TEAMS } from '../lib/teams' + CONFEDERATION_ORDER/CONFEDERATION_LABEL constants + groupedTeams build"
      pattern: "groupedTeams"
    - from: "save-selection.ts redirectTo()"
      to: "/manage?status=..."
      via: "Location header: /manage?<URLSearchParams>"
      pattern: "Location.*\\/manage\\?"
    - from: "save-selection.ts team-input parse"
      to: "VALID_TEAMS.has(slug)"
      via: "form.get('team') trimmed + lowercased + allow-list check"
      pattern: "VALID_TEAMS\\.has"
---

<objective>
The keystone plan of Phase 9. Three concrete deliverables, ordered to
prevent the redirect-loop risk identified in RESEARCH Risk 1:

1. Remove the legacy `if (session) return Astro.redirect('/schedule');`
   block at `src/pages/manage.astro:5-7` FIRST. After Plan 09-03 ships
   the schedule.astro 301, leaving this line in place would create an
   infinite loop (manage→schedule→manage→schedule…). Removal is the
   first edit in this plan's file-level diff.

2. Rewrite `src/pages/manage.astro` as a dual-mode page (CONTEXT D-01):
   - Signed-out branch (no session, no URL token): keep the existing
     magic-link request form verbatim (current lines 9-82). One small
     re-use: the existing `manage.astro` <style is:global> block is the
     leaner of the two source blocks and lacks `--ok`/`--err`/.flash etc.
     The new style block must be the richer one ported from
     `src/pages/schedule.astro:273-351` (drops .feature-request rules
     and .grid/.team rules — see UI-SPEC Component Inventory).
   - Signed-in branch (valid session cookie OR valid URL token w/
     purpose=manage): render the editor — banner (conditional copy),
     headline + subhead (conditional subhead when team IS NULL per
     UI-SPEC §"Copywriting Contract" §"Banner"), STATUS_COPY flash, form
     posting to /api/save-selection with team <select> + tz row, then
     conditionally the matches list, then a logout form + footer
     back-link. All UI primitives ported verbatim from schedule.astro
     except the 48-team checkbox grid which is replaced by a single
     <select name="team"> mirroring index.astro:83-93's optgroup
     structure. The feature-request textarea (schedule.astro:152-163)
     is DROPPED per RESEARCH Open Q1.

3. Edit `src/pages/api/save-selection.ts` (CONTEXT D-02 + D-03):
   - Change `redirectTo()`'s Location template from `/schedule?${params}`
     to `/manage?${params}`.
   - Add a new primary parse path that reads `form.get('team')` as a
     slug, trims + lowercases, validates against VALID_TEAMS, and stores
     it directly. Keep the existing `team_ids[]` → teams.id → slug
     resolution as a fallback for the deploy-window transition (stale
     /manage tabs that still POST the old checkbox form). Replace the
     `redirectTo(formToken, 'too-many')` call with
     `redirectTo(formToken, 'bad-team')` per RESEARCH Q10 (the
     `too-many` slug is dead code in the single-team model).

The plan implements MANAGE-01 (editor + endpoint pinning) and COMPAT-01
(banner for pre-milestone team=NULL rows). Re-subscribe (SC4) is already
wired by Plan 09-02. Unsubscribe (MANAGE-02 / D-06) is already wired by
Plan 09-01 (TTL) + existing markUnsubscribed (D-06 idempotent contract
formalized, no code change there). Verification of all four SCs lives in
Plan 09-05's smoke.

Wave 2 alongside Plan 09-03. Depends on 09-01 (TTL_BY_PURPOSE underpins
session minting via buildSessionCookie's explicit ttlSeconds — no
functional dependency, but the libraries should be coherent at deploy
time) and 09-02 (sendManageLink URL change is the producer-side counterpart
to this consumer-side change).

Output: Two modified files. No new files. No new exports.

**Defensive branch for unsubscribed users (RESEARCH Risk 3):** A
session-valid user whose `unsubscribed_at IS NOT NULL` would otherwise
see the editor and get `?status=unknown` on save (because setSelection's
WHERE clause has `AND unsubscribed_at IS NULL`). Show a minimal
"You've unsubscribed — sign up again at /" message instead of the editor
form. Implemented as a conditional render check in the frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md
@.planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md
@.planning/phases/09-manage-editor-unsubscribe/09-UI-SPEC.md
@.planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md

@src/pages/manage.astro
@src/pages/schedule.astro
@src/pages/index.astro
@src/pages/api/save-selection.ts
@src/pages/api/manage.ts
@src/lib/teams.ts
@src/lib/timezones.ts
@src/lib/session.ts
@src/lib/db.ts

<interfaces>
Current src/pages/manage.astro (114 lines):
- Lines 1-9 frontmatter: imports readSessionFromCookie, redirects to /schedule if session valid (THIS REDIRECT MUST GO FIRST).
- Lines 11-82 body: magic-link request form posting to /api/manage. KEEP this branch.
- Lines 84-114 style block: lean — lacks --ok/--err/.flash/.picker/.tz-row/.match-list. REPLACE with the richer block from schedule.astro:273-351 minus the feature-request and grid/team rules.

Current src/pages/schedule.astro (351 lines BEFORE Plan 09-03 lands; after 09-03 lands, schedule.astro is ~7 lines):
The editor body, JS, and richer style block are the SOURCE OF TRUTH for the new signed-in editor branch. Port verbatim per PATTERNS.md §"src/pages/manage.astro" with the substitutions listed.

Key imports the new manage.astro needs (per PATTERNS.md):
```
import { verifyToken } from '../lib/token';
import { buildSessionCookie, readSessionFromCookie } from '../lib/session';
import { db, getByEmail, type VipSignup, type Match } from '../lib/db';
import { TEAMS } from '../lib/teams';
```
Plus the `CONFEDERATION_ORDER`, `CONFEDERATION_LABEL`, and `groupedTeams` constants ported verbatim from index.astro:17-39.

Current src/pages/api/save-selection.ts (99 lines):
- redirectTo() at line 26: `{ Location: \`/schedule?${params}\` }` — CHANGE to `/manage`.
- Line 53-68: team_ids[] integer parse → teams.id → slug, redirects 'too-many' on miss. ADD primary `form.get('team')` slug parse via VALID_TEAMS first; keep the integer fallback; change 'too-many' to 'bad-team'.
- The session-or-token dual auth at lines 40-48 stays unchanged (already correct shape).
- The feature_request insert at lines 82-90 stays unchanged (RESEARCH Open Q1 drops the textarea from /manage UI but the handler keeps the insert for forward-compat — failing insert never gates save).
- setSelection (db.ts:231-236) is called with (teamSlug, tz, email); its WHERE clause excludes unsubscribed users (lines 234 `AND unsubscribed_at IS NULL`) — see RESEARCH Risk 3, handled defensively in manage.astro itself.

UI-SPEC verbatim strings (Copywriting Contract — non-negotiable):
- Banner (team set): `Your schedule`
- Banner (team NULL): `Pick a team`
- Headline: `Pick the teams you follow.`
- Standard subhead (team set): `We'll email you about an hour before each match they play, in your local time. Change anytime — your sign-in lasts 30 days on this device.`
- Banner subhead (team NULL): `You're signed up. Choose a team below to start getting kickoff alerts.`
- TZ prefix: `Notifications in:`
- TZ change button: `change`
- Submit: `Save selection`
- Logout: `Log out`
- Footer back: `← oddlympics.app`
- STATUS_COPY entries verbatim per UI-SPEC §"Status / error messages" — note `bad-team` replaces `too-many`.
- For an unsubscribed user defensive branch: copy is `You've unsubscribed.` headline + `<a href="/">Sign up again</a> to start getting alerts.` subhead.

Existing inline scripts to port verbatim from schedule.astro:207-268:
- setupTz() TZ auto-detect + manual override
- match-time local-render via Intl.DateTimeFormat
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove legacy session→/schedule redirect at manage.astro:5-7 (precondition for the editor branch)</name>
  <files>src/pages/manage.astro</files>
  <read_first>
    - src/pages/manage.astro (lines 1-9 frontmatter; the legacy redirect is at lines 5-7)
    - .planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md (Risk 1: removing this is a precondition for everything; failing creates an infinite 301 loop after Plan 09-03 lands)
    - .planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md (Critical removal section — exact lines to remove)
  </read_first>
  <action>
    Open src/pages/manage.astro. In the frontmatter block (between the `---` fences), DELETE the three lines:

    ```
    // If the user already has a valid session cookie, skip the email step entirely.
    const session = readSessionFromCookie(Astro.request.headers.get('cookie'));
    if (session) return Astro.redirect('/schedule');
    ```

    (Currently lines 5-7, plus the leading comment. The `readSessionFromCookie` import on line 3 will be re-added in Task 2 as part of the new frontmatter, so leave imports alone for now — Task 2 rewrites the full frontmatter anyway.)

    This is a transitional state — after Task 1 the page has no session-handling at all (it serves the signed-out form to everyone). Task 2 immediately follows in the same commit/wave and adds the proper dual-mode logic. Do NOT ship Task 1 alone; commit Tasks 1+2 together (one git commit) so manage.astro is never in a degraded state on main.
  </action>
  <verify>
    <automated>! grep -q "Astro.redirect('/schedule')" src/pages/manage.astro</automated>
    Plus:
    - `! grep -q "Astro\.redirect..'/schedule'" src/pages/manage.astro` (defensive variant)
    - `! grep -q "if (session) return" src/pages/manage.astro` (no legacy session-bounce remains)
  </verify>
  <done>
    The legacy session→/schedule redirect is removed from manage.astro. File continues to parse (the body still renders the signed-out form). Task 2 will follow in the same commit to add the editor branch.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite manage.astro as dual-mode page (signed-out form retained; signed-in editor branch added; style block upgraded to richer schedule.astro tokens)</name>
  <files>src/pages/manage.astro</files>
  <read_first>
    - src/pages/manage.astro (current file, post-Task-1 state — signed-out form survives)
    - src/pages/schedule.astro (SOURCE for the editor branch: lines 1-69 frontmatter shape, lines 102-203 editor body, lines 207-268 inline JS, lines 273-351 style block). NOTE: after Plan 09-03 lands, this file is the thin redirect; READ THE PRE-09-03 ORIGINAL via git show or this plan's PATTERNS.md excerpts. PATTERNS.md §"src/pages/manage.astro" has every needed snippet verbatim.
    - src/pages/index.astro (lines 17-39 for CONFEDERATION_ORDER/CONFEDERATION_LABEL/groupedTeams; lines 83-93 for the team <select> + optgroup structure)
    - src/pages/api/save-selection.ts (the endpoint this form posts to; understand the dual auth at lines 40-48 to write the hidden token input correctly)
    - src/lib/teams.ts (TEAMS export shape: `{ slug, label, confederation }`; used to render <option>s)
    - src/lib/db.ts (VipSignup type at lines 63-75; getByEmail at line 102-104; matches join query pattern at schedule.astro:42-57 — port verbatim)
    - src/lib/session.ts (buildSessionCookie + readSessionFromCookie signatures)
    - src/lib/token.ts (verifyToken signature; pass purpose='manage' for URL-token-first-arrival)
    - .planning/phases/09-manage-editor-unsubscribe/09-UI-SPEC.md (Copywriting Contract + Layout & Wireframe + State Matrix + Component Inventory — non-negotiable verbatim copy)
    - .planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md (the entire "src/pages/manage.astro" section — has frontmatter imports, dual-auth block, STATUS_COPY, banner conditional, <select> with optgroup + pre-selected, tz row, matches list, scripts, footer + logout, CSS block guidance)
    - .planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md (Risk 3: unsubscribed user defensive branch; Q9: session-or-token dual auth pattern; Q10: STATUS_COPY delta — too-many → bad-team)
    - .planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md (D-01 URL consolidation; D-04 banner UX — implicit dismissal)
  </read_first>
  <action>
    Rewrite `src/pages/manage.astro` end-to-end. Structure (top to bottom):

    **Frontmatter (between `---` fences):**
    - `export const prerender = false;`
    - Imports: `verifyToken` from `../lib/token`; `buildSessionCookie`, `readSessionFromCookie` from `../lib/session`; `db`, `getByEmail`, types `VipSignup` and `Match` from `../lib/db`; `TEAMS` from `../lib/teams`.
    - Constants ported verbatim from `src/pages/index.astro:17-39`: `CONFEDERATION_ORDER` tuple, `CONFEDERATION_LABEL` record, and the `groupedTeams` array (filter TEAMS by confederation).
    - Read `Astro.url`, then `urlToken = url.searchParams.get('token') ?? ''`, then `status = url.searchParams.get('status') ?? ''`.
    - Dual auth (port verbatim from schedule.astro:14-20 per PATTERNS.md): try `urlToken ? verifyToken(urlToken, 'manage') : null` first; if it succeeds, set the `Set-Cookie` header to `buildSessionCookie(result.email)`. Otherwise fall back to `readSessionFromCookie(Astro.request.headers.get('cookie'))`.
    - Set local `valid = result !== null` and `hadUrlToken = urlToken.length > 0`.
    - If valid, fetch `user = getByEmail.get(result.email) as VipSignup | undefined`. If `user.team` is set, resolve the matching `teams.id` (single-row SELECT slug→id) and build `selectedIds = [row.id]` or `[]`. If `selectedIds.length > 0`, run the schedule.astro matches JOIN query verbatim (placeholders, ORDER BY utc_date ASC).
    - Build STATUS_COPY record per UI-SPEC §"Status / error messages" — note `bad-team` replaces `too-many`. Six entries: saved (ok), bad-token (err), bad-tz (err), bad-team (err), unknown (err), server (err). Copy strings VERBATIM from UI-SPEC.
    - Compute `statusMsg = status ? STATUS_COPY[status] : null`.
    - Defensive branch (RESEARCH Risk 3): compute `isUnsubscribed = valid && user?.unsubscribed_at != null`. This conditional renders later.

    **HTML body:**
    `<html lang="en">` → `<head>` with charset, viewport, `<meta name="robots" content="noindex">`, favicon link, the existing Plausible script + init from current manage.astro:20-24 (port verbatim), and `<title>` set conditionally — `"Manage your schedule — oddlympics"` for signed-out branch, `"Your schedule — oddlympics"` for signed-in.

    `<body>` → `<main class="hero">` → `<div class="hero-content">`:

    Conditional render tree:

    A. **Signed-out branch** (`!valid && !hadUrlToken`): port the current manage.astro:27-79 body verbatim — banner "Already signed up", headline "Manage your schedule.", subhead, form to /api/manage with honeypot, error scripts. Don't change a byte.

    B. **Expired/invalid URL token** (`!valid && hadUrlToken`): render `<span class="banner">Link expired</span>` + `<h1 class="headline">That link didn't work.</h1>` + subhead `Magic links expire after 24 hours.` followed by an `<a href="/manage" class="link">Request a new one</a>`. Port verbatim from schedule.astro:86-90 but the href stays `/manage` (the request-form is at /manage now, not /schedule).

    C. **Unsubscribed defensive branch** (`valid && isUnsubscribed`): render `<span class="banner">Unsubscribed</span>` + `<h1 class="headline">You've unsubscribed.</h1>` + `<p class="subhead"><a href="/" class="link">Sign up again</a> to start getting alerts.</p>`. NO form. NO matches list. NO logout button (keep the footer back-link only — session-clear can wait for an actual signup).

    D. **Signed-in editor branch** (`valid && !isUnsubscribed`): the bulk of the page.
       - Banner conditional copy: `<span class="banner">{!user?.team ? 'Pick a team' : 'Your schedule'}</span>`.
       - Headline (always): `<h1 class="headline">Pick the teams you follow.</h1>`.
       - Subhead conditional: when `user?.team` is set, use the standard subhead (verbatim); when team IS NULL, use the banner subhead `You're signed up. Choose a team below to start getting kickoff alerts.`.
       - Flash (when `statusMsg`): `<p class={`flash ${statusMsg.kind}`} role="status">{statusMsg.text}</p>`.
       - Form: `<form method="post" action="/api/save-selection" class="picker-form">`.
         - Hidden token input when `hadUrlToken`: `<input type="hidden" name="token" value={urlToken} />`.
         - TZ row: port verbatim from schedule.astro:117-132 (the `<div class="tz-row">` block with tz-display strong, change button, hidden select with data-saved-tz, noscript fallback).
         - Picker fieldset (REPLACES the 48-team checkbox grid): `<fieldset class="picker">` with `<legend class="legend">YOUR TEAM</legend>`. Inside: `<select name="team" aria-label="Your team" required>` followed by a disabled placeholder option `<option value="" disabled selected={!user?.team}>Pick your team</option>`, then `{groupedTeams.map(({ label, teams }) => <optgroup label={label}>{teams.map((t) => <option value={t.slug} selected={t.slug === user?.team}>{t.label}</option>)}</optgroup>)}`.
         - Submit: `<button type="submit">Save selection</button>`.
         - DO NOT include the feature-request fieldset (dropped per RESEARCH Open Q1).
       - Matches list (only when `selectedIds.length > 0`): port verbatim from schedule.astro:168-188 (`<section class="schedule">` with `<h2 class="section-h">Your matches ({matches.length})</h2>`, empty-state `No matches yet…` paragraph, or `<ol class="match-list">` with one `<li class="match" data-utc={m.utc_date}>` per match including match-time, match-stage, match-teams spans).
       - Footer (always, after the editor): `<p class="subhead" style="margin-top:32px"><a href="/" class="link">← oddlympics.app</a>` plus the inline logout form when `valid` (port verbatim from schedule.astro:193-202).

    **Inline scripts** (port verbatim from schedule.astro:207-268, only on the signed-in editor branch — or simply always present, since they no-op when their target elements don't exist):
    - `<script is:inline>` for `setupTz()` — the IIFE wrapping the TZ auto-detect, populate, and change-listener logic.
    - `<script is:inline>` for the match-time local-render loop using `Intl.DateTimeFormat`.

    **Style block** `<style is:global>` (replaces the current leaner block on manage.astro:84-114):
    - Port verbatim from schedule.astro:273-351 (the richer block).
    - DROP `.feature-request` rules (currently schedule.astro:339-343) — no textarea on /manage.
    - DROP `.grid`, `.team`, `.team .tla`, `.team .name`, `.team input`, `.team:hover` rules (currently schedule.astro:308-314) — no checkbox grid.
    - KEEP everything else: `:root` tokens (--bg, --fg, --fg-dim, --line, --surface, --accent, --accent-ink, --ok, --err, --pad, --mono), `*`/html/body reset, .hero/.hero-content, .banner, .headline, .subhead, .link, .flash family, .picker-form, .tz-row family, .picker, .legend, button[type=submit], .schedule, .section-h, .match-list, .match family, .link-button, the 640px mobile breakpoint (the `.grid` rule inside the breakpoint can also be dropped since .grid no longer exists).
    - ADD the honeypot `.hp` rule (currently on manage.astro:112 leaner block) so the signed-out form's honeypot still hides correctly. Place it adjacent to `.match-teams .dim` or before the mobile media query.
    - ADD a `.cta-form` rule set (currently on manage.astro:105-110 leaner block) so the signed-out magic-link form's flex layout + input + button styling continues to render correctly. Drop the .cta-form's mobile breakpoint duplication if the global 640px breakpoint already covers it; otherwise port the 520px → 640px mobile rules.
    - ADD an `.error` rule (currently on manage.astro:111) for the inline `<p id="error" class="error" role="alert" hidden>` element on the signed-out branch.

    Net result: one style block that supports BOTH the signed-out magic-link form AND the signed-in editor. Total CSS volume is roughly the union of the two existing blocks minus the feature-request and grid/team rules.

    **Critical rules:**
    - Zero occurrences of `bitcoin`, `lightning`, `crypto`, `world domination`, `personal olympics` anywhere in the new file (LAND-02). Copy is consumer-safe by construction since every string comes from UI-SPEC.
    - Verbatim copy from UI-SPEC §"Copywriting Contract" — do not paraphrase, do not "improve" wording.
    - The honeypot input on the signed-out form (currently manage.astro:44-51) is preserved exactly.
    - The Plausible script (currently manage.astro:20-24) is preserved exactly and placed in `<head>` (so it loads on both branches).
    - Do NOT add an `/api/save-selection`-bound form to the signed-out branch — only the signed-in editor posts there.
  </action>
  <verify>
    <automated>npx astro check</automated>
    Static greps + runtime curl gates (the latter require `npm run dev`):

    Static:
    - `! grep -q "Astro.redirect..'/schedule'" src/pages/manage.astro` (legacy line gone)
    - `grep -c "action=\"/api/save-selection\"" src/pages/manage.astro` returns 1 (editor form)
    - `grep -c "action=\"/api/manage\"" src/pages/manage.astro` returns 1 (signed-out magic-link form)
    - `grep -c "name=\"team\"" src/pages/manage.astro` returns 1 (the <select> on the editor)
    - `grep -c "groupedTeams" src/pages/manage.astro` returns 2 (declaration + render call)
    - `grep -c "'Pick a team'" src/pages/manage.astro` returns 1
    - `grep -c "'Your schedule'" src/pages/manage.astro` returns 1
    - `grep -c "bad-team" src/pages/manage.astro` returns ≥ 1 (STATUS_COPY entry)
    - `! grep -q "too-many" src/pages/manage.astro` (dead code removed)
    - `! grep -q "feature-request" src/pages/manage.astro` (dropped per RESEARCH Open Q1)
    - `! grep -q "<input.*name=\"team_ids\"" src/pages/manage.astro` (checkbox grid replaced)
    - `! grep -iE "bitcoin|lightning|crypto|world domination|personal olympics" src/pages/manage.astro` (LAND-02 source-side)
    - `npx astro check` exits 0
    - `npm run build` exits 0

    Live (dev server):
    - `curl -sI http://localhost:4321/manage | head -1` returns 200
    - `curl -s http://localhost:4321/manage | grep -c 'action="/api/manage"'` returns ≥ 1 (signed-out form present without session)
    - `curl -sI http://localhost:4321/manage | grep -i set-cookie` returns nothing (no Set-Cookie without auth)
    - `! curl -s http://localhost:4321/manage | grep -iE 'bitcoin|lightning|crypto|world domination|personal olympics'` (LAND-02 rendered-side)

    Full editor-with-session + banner-when-team-NULL behaviour is verified end-to-end in Plan 09-05 smoke.
  </verify>
  <done>
    src/pages/manage.astro renders correctly in both signed-out (200 + magic-link form) and signed-in (editor body present) modes, no legacy schedule redirect remains, the team <select> uses groupedTeams + optgroups + pre-selected option from user.team, STATUS_COPY contains 'bad-team' and not 'too-many', the feature-request textarea is gone, no LAND-02 prohibited terms appear, npx astro check and npm run build both exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update /api/save-selection to parse team=<slug> first (with team_ids[] fallback), redirect to /manage instead of /schedule, and use 'bad-team' in place of 'too-many'</name>
  <files>src/pages/api/save-selection.ts</files>
  <read_first>
    - src/pages/api/save-selection.ts (the file being modified; redirectTo at 23-29, dual auth at 40-48, team_ids parse at 50-68, tz validation at 70-71, setSelection call at 74-75, feature_request insert at 82-90, redirect on success at 97)
    - src/lib/teams.ts (the VALID_TEAMS export)
    - src/lib/timezones.ts (for context — already imported via the existing TZ_RE + Intl-based validator at lines 8-21; no change needed)
    - src/lib/db.ts (setSelection signature at lines 231-236 — still takes (team, tz, email))
    - .planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md (D-02 endpoint reuse; D-03 input is slug, not team_ids[])
    - .planning/phases/09-manage-editor-unsubscribe/09-RESEARCH.md (Q5: dual-parse transition pattern; Q10: STATUS_COPY slug rename)
    - .planning/phases/09-manage-editor-unsubscribe/09-PATTERNS.md ("src/pages/api/save-selection.ts (API route — targeted edits)" — exact diffs for redirectTo, VALID_TEAMS import, dual-parse block)
  </read_first>
  <action>
    Edit `src/pages/api/save-selection.ts`. Three surgical edits per D-02 + D-03 + RESEARCH Q5:

    (1) **Add import:** at the top, after the existing `verifyToken` import, add `import { VALID_TEAMS } from '../../lib/teams';`.

    (2) **Change redirectTo() Location target** (currently line 26): change `Location: `/schedule?${params}`` to `Location: `/manage?${params}``. No other change to that helper — the params, token preservation, and Set-Cookie passthrough all stay.

    (3) **Replace the team-input parse block** (currently lines 50-68). The replacement:
      - First, try `slugInput = ((form.get('team') as string) ?? '').trim().toLowerCase();`. If `slugInput` is non-empty AND `VALID_TEAMS.has(slugInput)` is true, set `teamSlug = slugInput`.
      - Otherwise, fall back to the existing `team_ids[]` integer parse path (lines 53-65 verbatim — keep the TEAM_ID_RE check, the integer conversion, the `SELECT slug FROM teams WHERE id = ?` lookup, and the break on first match).
      - If `teamSlug` is still null after both paths, return `redirectTo(formToken, 'bad-team')` (was `'too-many'`).
      - Keep a why-comment naming Plan 09-04 D-03 and noting that the team_ids[] fallback handles stale /manage tabs submitting the legacy checkbox form during the deploy window; the fallback should be removed after 1 week of stable deploy (record as a TODO comment with the date 2026-05-26 — one week from the Phase 9 expected ship of 2026-05-19).

    Do NOT touch:
    - The dual auth block (lines 40-48) — already correct.
    - The TZ validation (lines 70-71) — already correct.
    - The setSelection call (lines 73-75) — signature unchanged.
    - The feature_request insert (lines 82-90) — keep as-is per RESEARCH Open Q1; the textarea is dropped from manage.astro UI, so this code path is now defensive only (no inbound submissions until v1.1).
    - The final 303 redirect on success (line 97) — uses the updated redirectTo, so it automatically points at /manage?status=saved.
    - The TEAM_ID_RE regex (line 8) — still used by the fallback.

    Total diff: 1 added import, 1 changed Location string, ~15 changed lines in the team-input parse block.
  </action>
  <verify>
    <automated>grep -c "VALID_TEAMS" src/pages/api/save-selection.ts</automated>
    Plus:
    - `grep -c "VALID_TEAMS.has" src/pages/api/save-selection.ts` returns 1
    - `grep -c "'/manage?'" src/pages/api/save-selection.ts` returns 1 (the redirectTo Location target); equivalently `grep -E "Location:.*\\\`/manage\\?" src/pages/api/save-selection.ts`
    - `! grep -q "'/schedule?'" src/pages/api/save-selection.ts` (no legacy /schedule path)
    - `grep -c "'bad-team'" src/pages/api/save-selection.ts` returns 1 (the new redirectTo arg)
    - `! grep -q "'too-many'" src/pages/api/save-selection.ts` (dead code removed)
    - `grep -c "form.get('team')" src/pages/api/save-selection.ts` returns 1 (primary parse path)
    - `grep -c "form.getAll('team_ids')" src/pages/api/save-selection.ts` returns 1 (fallback retained)
    - `npx astro check` exits 0
    - `npm run build` exits 0

    Live (dev server, with a confirmed test row in the DB):
    - POST with `team=england&timezone=Europe/London` + valid session cookie → 303 with Location `/manage?status=saved` and the row's team becomes `england` (Plan 09-05 smoke M3).
    - POST with `team=fake_slug&timezone=Europe/London` + valid session → 303 with Location `/manage?status=bad-team` (Plan 09-05 smoke M4).
    - POST with `team_ids=<integer-id>&timezone=Europe/London` + valid session (no `team=` field) → 303 with Location `/manage?status=saved` and the row updates to the slug resolved from teams.id (transition-window fallback path; Plan 09-05 may add this as an extra case).
  </verify>
  <done>
    save-selection.ts imports VALID_TEAMS, redirectTo() points at /manage, primary parse is form.get('team') validated via VALID_TEAMS.has, team_ids[] fallback retained, 'bad-team' replaces 'too-many', npx astro check + npm run build pass.
  </done>
</task>

</tasks>

<verification>
- Static:
  - `! grep -q "Astro.redirect..'/schedule'" src/pages/manage.astro`
  - `grep -c "groupedTeams" src/pages/manage.astro` ≥ 2
  - `grep -c "bad-team" src/pages/manage.astro` ≥ 1
  - `! grep -q "too-many" src/pages/manage.astro`
  - `! grep -q "feature-request" src/pages/manage.astro`
  - `! grep -iE "bitcoin|lightning|crypto|world domination|personal olympics" src/pages/manage.astro`
  - `grep -c "VALID_TEAMS" src/pages/api/save-selection.ts` = 2 (import + has check)
  - `! grep -q "'too-many'" src/pages/api/save-selection.ts`
  - `! grep -q "'/schedule?'" src/pages/api/save-selection.ts`
- Build:
  - `npx astro check` exits 0
  - `npm run build` exits 0
- Live (dev server):
  - `curl -sI http://localhost:4321/manage | head -1` contains `200`
  - `curl -s http://localhost:4321/manage | grep -c "action=\"/api/manage\""` ≥ 1
  - `! curl -s http://localhost:4321/manage | grep -iE "bitcoin|lightning|crypto|world domination|personal olympics"`
- E2E (smoke): full verification in Plan 09-05 (M1, M2, M3, M4, M5, M6 cover this plan's surface).
</verification>

<success_criteria>
- Legacy session→/schedule redirect at manage.astro:5-7 removed FIRST in the file diff (Task 1 ordering)
- manage.astro is dual-mode: signed-out form preserved verbatim; signed-in editor branch added with banner conditional copy, team <select> using groupedTeams, tz row, save form, matches list (conditional), logout
- Unsubscribed defensive branch renders the "You've unsubscribed" message instead of the editor when user.unsubscribed_at IS NOT NULL
- /api/save-selection parses team=<slug> primary path via VALID_TEAMS; keeps team_ids[] fallback for transition; redirects to /manage; uses 'bad-team' not 'too-many'
- STATUS_COPY map matches UI-SPEC verbatim (6 entries)
- LAND-02 compliance: zero prohibited terms in source or rendered HTML
- npx astro check + npm run build both succeed
- requirements MANAGE-01 and COMPAT-01 covered by this plan's tasks
</success_criteria>

<output>
After completion, create `.planning/phases/09-manage-editor-unsubscribe/09-04-SUMMARY.md` capturing:
- Diff sketches for the three files modified (manage.astro restructure, save-selection.ts 3 surgical edits)
- Confirmation that Task 1 (legacy redirect removal) was the first edit in the commit, before the editor branch was added — proves the redirect-loop risk is mitigated
- Note that Plan 09-03 (schedule.astro thin redirect) is the wave-2 sibling that depends on this plan's manage.astro line-7 removal; they should land together
- Note that the team_ids[] fallback should be removed in a follow-up edit after ~1 week of stable deploy (capture the suggested removal date 2026-05-26 if Phase 9 ships on 2026-05-19)
- LAND-02 grep evidence on the built dist/server output
</output>
