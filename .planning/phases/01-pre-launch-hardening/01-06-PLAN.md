---
phase: 01-pre-launch-hardening
plan: 06
type: execute
wave: 2
depends_on:
  - "01-05"
files_modified:
  - deploy/Caddyfile
autonomous: false
requirements:
  - HARDEN-04
tags:
  - csp
  - caddy
  - security-headers
  - enforce
must_haves:
  truths:
    - "Caddy emits Content-Security-Policy (NOT Content-Security-Policy-Report-Only) header on every response from oddlympics.app and www.oddlympics.app"
    - "The CSP value is unchanged from plan 05 (D-17 verbatim) — only the header NAME flips from report-only to enforce"
    - "All four pages (/, /pending, /confirmed, /unsubscribed) load without browser-console violations and remain functionally identical (Plausible loads, forms post, inline scripts/styles render)"
    - "securityheaders.com scan of https://oddlympics.app returns A grade or higher (D-19 acceptance)"
    - "The other security headers (HSTS, nosniff, frame-deny, Referrer-Policy, -Server) remain present and unchanged"
  artifacts:
    - path: "deploy/Caddyfile"
      provides: "Caddy reverse-proxy config with enforcing CSP header"
      contains: "Content-Security-Policy"
  key_links:
    - from: "deploy/Caddyfile header block"
      to: "oddlympics.app and www.oddlympics.app responses"
      via: "Caddy header directive"
      pattern: "Content-Security-Policy"
---

## Phase Goal

**As a** site operator who has observed clean CSP report-only traffic for 1-2 days, **I want to** flip the policy from report-only to enforcing, **so that** the browser actually blocks malicious or unexpected resource loads going forward and the site achieves an A grade on securityheaders.com.

<objective>
Rename the `Content-Security-Policy-Report-Only` header in `deploy/Caddyfile` to `Content-Security-Policy`. The policy VALUE is unchanged from plan 05 (D-17 verbatim). Deploy via the same manual `scp + systemctl reload caddy` flow used in plan 05. Verify the live response carries the enforcing header (not the report-only one) and run a securityheaders.com scan to confirm an A grade.

Per D-16 (CONTEXT.md): step 2 of the two-step rollout. This plan only runs AFTER plan 05's 1-2 day observation window confirms zero violations under real traffic.

Per D-19 (CONTEXT.md): the verification target is **A grade on securityheaders.com**. Run the scan after the deploy and capture the result.

Output: one line in `deploy/Caddyfile` renamed (header name changed; value identical), deployed manually. The plan blocks on plan 05 (depends_on: ["01-05"]) — if plan 05's checkpoint reported violations, this plan does NOT run until those are resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-pre-launch-hardening/01-CONTEXT.md
@.planning/phases/01-pre-launch-hardening/01-PATTERNS.md
@DEPLOY.md
@deploy/Caddyfile
@.planning/phases/01-pre-launch-hardening/01-05-SUMMARY.md

<interfaces>
After plan 05 lands, `deploy/Caddyfile` contains a line like:

```
Content-Security-Policy-Report-Only "default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io; img-src 'self' data:; style-src 'self' 'unsafe-inline'; connect-src 'self' https://plausible.io; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'"
```

This plan changes ONLY the header name on that line:

```
Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io; img-src 'self' data:; style-src 'self' 'unsafe-inline'; connect-src 'self' https://plausible.io; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'"
```

The policy value is byte-identical. The only diff is the suffix `-Report-Only` removed from the header name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename Content-Security-Policy-Report-Only to Content-Security-Policy in deploy/Caddyfile</name>

  <files>deploy/Caddyfile</files>

  <read_first>
    - deploy/Caddyfile (full file as it stands AFTER plan 05 lands)
    - .planning/phases/01-pre-launch-hardening/01-05-SUMMARY.md (must exist; confirms plan 05 completed and the observation window approved)
    - .planning/phases/01-pre-launch-hardening/01-CONTEXT.md (D-16, D-17, D-19)
    - .planning/phases/01-pre-launch-hardening/01-PATTERNS.md (the "deploy/Caddyfile — modified" section, "Extension D-16 step 2 — enforce")
  </read_first>

  <behavior>
    - Test 1: After `systemctl reload caddy`, `curl -sI https://oddlympics.app/ | grep -i 'content-security-policy:'` returns the full policy string
    - Test 2: After reload, `curl -sI https://oddlympics.app/ | grep -i 'content-security-policy-report-only'` returns NOTHING (the report-only variant is gone)
    - Test 3: All four pages still load without browser-console violations
    - Test 4: Plausible analytics still loads on all four pages (no blocked script-src)
    - Test 5: Form post on / still succeeds (no blocked form-action)
    - Test 6: securityheaders.com scan of https://oddlympics.app returns A grade or higher
  </behavior>

  <action>
Modify `deploy/Caddyfile` to flip the header name. Use the Edit tool with this exact substitution:

OLD (the line added in plan 05):
```
		Content-Security-Policy-Report-Only "default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io; img-src 'self' data:; style-src 'self' 'unsafe-inline'; connect-src 'self' https://plausible.io; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'"
```

NEW:
```
		Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io; img-src 'self' data:; style-src 'self' 'unsafe-inline'; connect-src 'self' https://plausible.io; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'"
```

The ONLY change is the header name (`-Report-Only` removed). The quoted value, the indentation (tabs), and surrounding lines are unchanged.

Do NOT modify any other line. Do NOT change the policy value. Do NOT add `report-uri` (per Claude's Discretion in CONTEXT.md, no collector for v1).

After saving, validate:
```bash
caddy validate --config deploy/Caddyfile
```
(if `caddy` is installed locally; otherwise rely on droplet-side validation in Task 2).
  </action>

  <verify>
    <automated>grep -c '^[[:space:]]*Content-Security-Policy ' deploy/Caddyfile</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^[[:space:]]*Content-Security-Policy ' deploy/Caddyfile` returns 1 (enforcing header present)
    - `grep -cE '^[[:space:]]*Content-Security-Policy-Report-Only' deploy/Caddyfile` returns 0 (report-only variant removed)
    - The policy VALUE is byte-identical to plan 05: `grep -c "default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io" deploy/Caddyfile` returns 1
    - All other security headers preserved: `grep -c "Strict-Transport-Security" deploy/Caddyfile` returns 1; `grep -c "X-Content-Type-Options" deploy/Caddyfile` returns 1; `grep -c "X-Frame-Options" deploy/Caddyfile` returns 1; `grep -c "Referrer-Policy" deploy/Caddyfile` returns 1
    - File line count unchanged from plan 05's 38 lines: `awk 'END{print NR}' deploy/Caddyfile` returns 38
    - The Caddyfile diff vs. plan 05 is EXACTLY one line modified (zero added, zero removed): `git diff deploy/Caddyfile` shows a single `-` line and a single `+` line
  </acceptance_criteria>

  <done>
The `deploy/Caddyfile` contains an enforcing `Content-Security-Policy` header line with the exact D-17 policy. The report-only variant is removed. All other directives are byte-identical to plan 05. The file is syntactically valid Caddy configuration.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy enforcing CSP, verify live header, run securityheaders.com scan, confirm A grade</name>

    <files>(none — checkpoint task; see how-to-verify for the human steps)</files>

  <action>See <how-to-verify> below — this is a checkpoint:human-action task. The structural fields (action/verify/done/files) are present only to satisfy the structural validator; the real instructions are in what-built / how-to-verify / resume-signal.</action>

  <verify>
    <automated>true</automated>
  </verify>

  <done>The checkpoint resume-signal is received from the operator (see resume-signal element below).</done>

<what-built>
    The modified `deploy/Caddyfile` (Task 1) with `Content-Security-Policy` (enforcing) replacing `Content-Security-Policy-Report-Only`. It needs to be deployed to the droplet and verified.

    Why this is human-action: same reason as plan 05's Task 2 — the GitHub Actions workflow does not rsync the Caddyfile. Manual scp + reload is the documented procedure (DEPLOY.md:109). Plus the securityheaders.com scan needs human eyes.
  </what-built>

  <how-to-verify>
    Step 1 — Push the Caddyfile change:

    ```bash
    # From your local repo
    scp deploy/Caddyfile root@oddlympics.app:/etc/caddy/Caddyfile
    ssh root@oddlympics.app 'caddy validate --config /etc/caddy/Caddyfile && systemctl reload caddy && systemctl status caddy --no-pager | head -10'
    ```

    Expected: `caddy validate` exits 0 with `Valid configuration`, `systemctl reload caddy` exits 0, `systemctl status caddy` shows `active (running)`.

    Step 2 — Confirm the enforcing header is live (from your laptop):

    ```bash
    for path in / /pending /confirmed /unsubscribed; do
      echo "=== https://oddlympics.app${path} ==="
      curl -sI "https://oddlympics.app${path}" | grep -iE 'content-security-policy'
    done
    ```

    Expected for each path:
    - `Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io; img-src 'self' data:; style-src 'self' 'unsafe-inline'; connect-src 'self' https://plausible.io; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'`
    - NO line containing `Content-Security-Policy-Report-Only`

    Step 3 — Browser smoke (Chrome + Safari, all four pages):
    - `https://oddlympics.app/` — page renders, Plausible loads, form is interactive
    - `https://oddlympics.app/pending` — page renders
    - `https://oddlympics.app/confirmed?status=ok` — page renders
    - `https://oddlympics.app/unsubscribed?status=ok` — page renders
    - DevTools console shows zero red CSP violation messages (different from plan 05: under enforce, violations would BLOCK loads, not just report)
    - Submit a real test email through the form on / → reaches `/pending?email=...` (form-action 'self' allows same-origin POST)

    Step 4 — Run securityheaders.com scan (D-19 acceptance):

    Visit `https://securityheaders.com/?q=https%3A%2F%2Foddlympics.app&followRedirects=on` (or use the form field).

    Expected: **A grade or higher**. The grade card should show:
    - Strict-Transport-Security: present
    - Content-Security-Policy: present (with the D-17 value)
    - X-Frame-Options: DENY
    - X-Content-Type-Options: nosniff
    - Referrer-Policy: strict-origin-when-cross-origin
    - Server header: missing (good — `-Server` strip is working)

    Common reasons the grade might be below A:
    - Permissions-Policy missing (the scanner used to ding for this; it's now informational only — A grade does not require it)
    - Cross-Origin-Resource-Policy / COOP / COEP missing (informational; not required for A)

    If the grade is below A, capture the scan output, identify the missing/weak header, and either: (a) add it to the Caddyfile if it's a low-risk addition (e.g. `Permissions-Policy "interest-cohort=()"`); OR (b) document why the gap is acceptable for v1 in the SUMMARY.

    Step 5 — Capture the result for HARDEN-04 acceptance:
    - Take a screenshot or copy-paste the securityheaders.com grade card
    - Record the curl header transcript from Step 2
    - Save both to the SUMMARY
  </how-to-verify>

  <resume-signal>Type "approved — A grade confirmed" once the securityheaders.com scan returns A or higher and all four pages render without browser-console violations. If A grade is not achieved, type "needs follow-up: <details>" and a follow-up task will be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → page render | Under enforce mode, the browser actively blocks resource loads that violate the policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Elevation | XSS via attacker-controlled inline script | mitigate (limited) | Same as T-05-01: `'unsafe-inline'` is permitted (D-17), so injected inline IS allowed. The mitigation is partial — connect-src/form-action/base-uri/object-src/frame-ancestors restrictions still apply, defeating exfil and clickjacking even if a script runs. |
| T-06-02 | Information Disclosure | exfiltration to attacker origin | mitigate | `connect-src 'self' https://plausible.io` enforced. Browser blocks fetch/XHR to other origins. |
| T-06-03 | Denial of Service | enforcing breaks the site for users | mitigate | Plan 05's 1-2 day observation window (with zero violations approved) is the safety check. This plan's `depends_on: ["01-05"]` makes that explicit. Task 2's browser smoke catches any regression before declaring done. |
| T-06-04 | (Operational) header rollback | n/a | accept | If a CSP regression is detected post-deploy, rollback is one-line: `ssh root@oddlympics.app 'sed -i "s/^\(\s*\)Content-Security-Policy /\1Content-Security-Policy-Report-Only /" /etc/caddy/Caddyfile && systemctl reload caddy'`. Document this rollback path in the SUMMARY. |
</threat_model>

<verification>
1. `grep -cE '^[[:space:]]*Content-Security-Policy ' deploy/Caddyfile` returns 1
2. `grep -cE '^[[:space:]]*Content-Security-Policy-Report-Only' deploy/Caddyfile` returns 0
3. After deploy: `curl -sI https://oddlympics.app/ | grep -i 'content-security-policy:'` returns the enforcing header
4. After deploy: `curl -sI https://oddlympics.app/ | grep -i 'content-security-policy-report-only'` returns nothing
5. Browser smoke (Chrome + Safari, all four pages): zero CSP violations, all functionality works
6. securityheaders.com scan returns **A grade or higher**
</verification>

<success_criteria>
The enforcing `Content-Security-Policy` header is live on `oddlympics.app` and `www.oddlympics.app` with the D-17 policy verbatim. The report-only variant is gone. All four pages render and function identically to before (Plausible loads, forms post, inline scripts/styles work). securityheaders.com scan returns A grade or higher (D-19). HARDEN-04 phase goal "Caddy serves a Content-Security-Policy header on all pages that passes an automated security header scanner" is met.
</success_criteria>

<output>
After completion (Task 2 approved), create `.planning/phases/01-pre-launch-hardening/01-06-SUMMARY.md` documenting:
- Exact Caddyfile diff applied (one-line rename)
- Curl transcript proving enforcing header live, report-only header gone
- Browser smoke notes (Chrome + Safari, all four pages, zero violations)
- securityheaders.com scan result (grade + screenshot or copy-paste)
- HARDEN-04 marked complete in REQUIREMENTS.md traceability
- Rollback procedure recorded for ops continuity
</output>
