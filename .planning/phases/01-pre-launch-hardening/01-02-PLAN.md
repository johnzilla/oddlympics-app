---
phase: 01-pre-launch-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/api/signup.ts
autonomous: true
requirements:
  - HARDEN-03
tags:
  - api
  - csrf
  - security
  - origin-policy
must_haves:
  truths:
    - "A POST to /api/signup with no Origin header is rejected with the bad-origin error response (303 to /?error=bad-origin)"
    - "A POST to /api/signup with an Origin matching the configured site URL is accepted and processed normally"
    - "A POST to /api/signup with an Origin from localhost or 127.0.0.1 is accepted (preserved dev allowlist)"
    - "A POST to /api/signup with an Origin from an unrelated host is rejected"
  artifacts:
    - path: "src/pages/api/signup.ts"
      provides: "POST /api/signup with default-deny Origin policy"
      contains_pattern: "if \\(!origin\\) return false"
  key_links:
    - from: "src/pages/api/signup.ts originOk()"
      to: "back('bad-origin') redirect"
      via: "early return false on absent Origin header"
      pattern: "originOk.*return false"
---

## Phase Goal

**As a** site operator concerned about cross-origin form abuse, **I want to** reject POSTs to /api/signup that arrive without an Origin header, **so that** crafted cross-origin form submissions cannot enroll emails into the VIP list.

<objective>
Flip the "fail-open on missing Origin" line in `src/pages/api/signup.ts:22` from `return true` to `return false`, defaulting to deny when the `Origin` header is absent on a POST. Keeps the existing localhost/127.0.0.1 allowlist (lines 24-25) and the same-host comparison (lines 26-29) untouched.

Purpose: HARDEN-03 — closes the HIGH-severity gap from CONCERNS.md where a crafted cross-origin POST could omit the Origin header and bypass the cross-site check entirely. Modern browsers attach Origin to cross-origin form submits; treating absence as suspicious is the correct default.

Output: a single-line change (plus the comment update) in `src/pages/api/signup.ts:22`. The existing `back('bad-origin')` branch at line 45 already handles the rejection redirect; the `index.astro` COPY map (line 66) already has the user-facing message "Submission blocked. Please use the form on this page." No other changes needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-pre-launch-hardening/01-CONTEXT.md
@.planning/phases/01-pre-launch-hardening/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@src/pages/api/signup.ts

<interfaces>
<!-- The current originOk function, src/pages/api/signup.ts:18-34 -->

```ts
function originOk(request: Request, siteUrl: string | undefined): boolean {
  // Block obvious cross-site POSTs. Same-origin browser submits include the
  // matching Origin header.
  const origin = request.headers.get('origin');
  if (!origin) return true; // some same-origin form posts omit Origin; fall back to allow
  try {
    const o = new URL(origin);
    if (o.hostname === 'localhost' || o.hostname === '127.0.0.1') return true;
    if (siteUrl) {
      const s = new URL(siteUrl);
      return o.host === s.host;
    }
    return false;
  } catch {
    return false;
  }
}
```

The `back('bad-origin')` branch at signup.ts:44-45 already returns 303 to `/?error=bad-origin`. The COPY map at index.astro:66 already maps that code to "Submission blocked. Please use the form on this page." No downstream changes needed.

Per D-20 and PATTERNS.md "src/pages/api/signup.ts — modified" section: flip line 22 only. Localhost allowlist stays. siteUrl host comparison stays. No new logic required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flip Origin missing default to deny</name>

  <files>src/pages/api/signup.ts</files>

  <read_first>
    - src/pages/api/signup.ts (full current file, lines 1-99 — focus on lines 18-34 originOk)
    - .planning/phases/01-pre-launch-hardening/01-CONTEXT.md (D-20)
    - .planning/phases/01-pre-launch-hardening/01-PATTERNS.md (the "src/pages/api/signup.ts — modified" section)
    - src/pages/index.astro (line 66 — confirms 'bad-origin' COPY entry exists)
  </read_first>

  <behavior>
    - Test 1: POST /api/signup with NO Origin header returns 303 with Location: /?error=bad-origin
    - Test 2: POST /api/signup with Origin: https://oddlympics.app and matching site config returns 303 with Location: /pending?email=... (normal happy path, given valid form body)
    - Test 3: POST /api/signup with Origin: http://localhost:4321 returns 303 with Location: /pending?email=... (localhost allowlist preserved)
    - Test 4: POST /api/signup with Origin: https://evil.example.com returns 303 with Location: /?error=bad-origin
  </behavior>

  <action>
In `src/pages/api/signup.ts`, change line 22 from:

```ts
  if (!origin) return true; // some same-origin form posts omit Origin; fall back to allow
```

to:

```ts
  if (!origin) return false; // modern browsers attach Origin to cross-origin form POSTs; treat absence as suspicious
```

Do NOT change any other line in the file. Do NOT remove the localhost allowlist (lines 24-25). Do NOT touch the same-host comparison (lines 26-29). Do NOT touch the catch (line 31-33). The `back('bad-origin')` branch at line 44-45 stays as-is.

Use the Edit tool with `old_string` matching the entire current line including trailing comment, and `new_string` containing the new line including the new comment. This is a one-line surgical change — do not refactor anything else in the file.
  </action>

  <verify>
    <automated>grep -c "if (!origin) return false" src/pages/api/signup.ts &amp;&amp; ! grep -q "if (!origin) return true" src/pages/api/signup.ts &amp;&amp; npx astro check</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "if (!origin) return false" src/pages/api/signup.ts` returns 1
    - `grep -c "if (!origin) return true" src/pages/api/signup.ts` returns 0
    - `grep -c "treat absence as suspicious" src/pages/api/signup.ts` returns 1 (new comment present)
    - `grep -c "fall back to allow" src/pages/api/signup.ts` returns 0 (old comment removed)
    - `grep -c "o.hostname === 'localhost'" src/pages/api/signup.ts` returns 1 (localhost allowlist preserved)
    - `grep -c "o.hostname === '127.0.0.1'" src/pages/api/signup.ts` returns 1 (127.0.0.1 allowlist preserved)
    - `grep -c "function originOk" src/pages/api/signup.ts` returns 1 (function still defined)
    - `grep -c "back('bad-origin')" src/pages/api/signup.ts` returns 1 (rejection redirect untouched)
    - File line count unchanged at 99 lines (`wc -l src/pages/api/signup.ts` returns "99 src/pages/api/signup.ts" or `awk 'END{print NR}' src/pages/api/signup.ts` returns 99)
    - `npx astro check` exits 0 (no type errors)
    - `npm run build` exits 0
    - Manual smoke test (executor records output in summary): `curl -i -X POST http://localhost:4321/api/signup --data 'email=test@example.com&requested_sport=world_cup' -H 'Content-Type: application/x-www-form-urlencoded'` (no Origin header) returns 303 with Location header containing `error=bad-origin`
    - Manual smoke test: `curl -i -X POST http://localhost:4321/api/signup -H 'Origin: http://localhost:4321' -H 'Content-Type: application/x-www-form-urlencoded' --data 'email=smoke-$(date +%s)@example.com&requested_sport=world_cup'` returns 303 with Location header pointing to `/pending?email=...` (localhost allowlist still works)
  </acceptance_criteria>

  <done>
A POST to `/api/signup` with no `Origin` header is rejected with a 303 to `/?error=bad-origin`. The existing localhost allowlist and same-host check continue to work. The form on the live site (which sends Origin) continues to work end-to-end. No other behavior changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → POST /api/signup | Untrusted form body crosses here. Origin header is the principal cross-origin signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | cross-origin POST with no Origin header | mitigate | Flip default-allow → default-deny in `originOk` (line 22). After fix, only requests with a same-host Origin OR a localhost/127.0.0.1 Origin are accepted. |
| T-02-02 | Spoofing | crafted Origin header from non-browser client | accept | A non-browser client can set any Origin string. Defense-in-depth via rate limiter (`rate-limit.ts`, 5/hr per IP and email), honeypot, and the email-verification step before any user-visible action. CONCERNS.md HIGH section confirms this layered model is acceptable for v1. |
| T-02-03 | Tampering | Origin header tampering on real browser | accept | Browsers attach Origin to cross-origin form POSTs and forbid scripts from overriding it (Fetch spec). Trusting Origin from real browsers is the standard CSRF defense. |
</threat_model>

<verification>
1. `grep "if (!origin) return false" src/pages/api/signup.ts` finds exactly one match
2. `npm run build` succeeds
3. After deploy (or against `npm run dev`):
   - `curl -i -X POST http://localhost:4321/api/signup --data 'email=test@example.com&requested_sport=world_cup' -H 'Content-Type: application/x-www-form-urlencoded'` (no Origin) → 303 to `/?error=bad-origin`
   - `curl -i -X POST http://localhost:4321/api/signup -H 'Origin: http://localhost:4321' --data 'email=smoke-test@example.com&requested_sport=world_cup' -H 'Content-Type: application/x-www-form-urlencoded'` → 303 to `/pending?email=...`
   - Real browser submit from `http://localhost:4321/` works unchanged (browser attaches Origin automatically)
</verification>

<success_criteria>
A POST to `/api/signup` with no `Origin` header is rejected with the appropriate error response (303 to `/?error=bad-origin`). All existing accepted paths (real browser submits with Origin, localhost dev submits) continue to work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-launch-hardening/01-02-SUMMARY.md` documenting:
- The single-line fix applied
- Confirmation that the rate limiter, honeypot, and email-verification layers are unchanged (defense-in-depth still intact)
- The `bad-origin` COPY entry at index.astro:66 surfaces the user-facing message
</output>
