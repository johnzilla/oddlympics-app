---
phase: 11-end-to-end-launch-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch-gate.mjs
  - scripts/cleanup-gate-rows.mjs
  - package.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "D-06: a single consolidated `scripts/launch-gate.mjs` runner (default target https://oddlympics.app, SMOKE_BASE_URL override) automates the scriptable ACs (AC1, AC2, AC6, AC7, AC9, AC12), drives AC3 via puppeteer-core, shells AC8 lighthouse to references/lighthouse-final.html, prompts+captures operator evidence for AC4/AC10/AC11 + the opengraph.xyz preview, emits one per-AC PASS/FAIL table, and is re-runnable as ONE command"
    - "D-03: AC3 uses puppeteer-core + the gitignored chrome-headless-shell binary (NOT Playwright); locales America/Detroit, Europe/London, Africa/Lagos; the documented Playwright-wording deviation is recorded in the runner header and 11-SUMMARY"
    - "D-03 open question RESOLVED: AC3's persisted-IANA-tz assertion path is pinned to the rendered #timezone hidden input value (set by the page's Intl JS) plus the #tz-label text — no prod-DB read off the droplet (AC9/AC12 'no row' likewise asserted via the 303 redirect contract alone)"
    - "D-06: AC7 prohibited-terms grep extends beyond today's check:land-02 (index.html only) to all four surfaces / , /privacy , /terms , /manage on the live target"
    - "D-05: a committed `scripts/cleanup-gate-rows.mjs` deletes the D-04 +tag gate rows from vip_signups — dry-run by default, --confirm to execute, idempotent re-run no-op, DATABASE_PATH override, writable better-sqlite3, DELETE ... WHERE email LIKE the +tag pattern"
    - "D-04: the gate runner's AC9/AC12 POSTs and the AC3/AC4 prompts use John's-Gmail +tag local-parts so every gate row matches the cleanup LIKE pattern"
    - "AC8 artifact path is EXACTLY references/lighthouse-final.html (SC2 / done-def #5 — NOT under .planning/) and all four categories are asserted >= 90"
    - "npm aliases `smoke:gate` and `cleanup:gate` are added following the existing smoke:* / colon-kebab convention"
  artifacts:
    - path: "scripts/launch-gate.mjs"
      provides: "Consolidated AC1-AC12 prod gate runner with per-AC PASS/FAIL table + evidence capture"
      min_lines: 200
    - path: "scripts/cleanup-gate-rows.mjs"
      provides: "Dry-run-by-default destructive delete of D-04 +tag gate rows"
      min_lines: 40
    - path: "package.json"
      provides: "smoke:gate + cleanup:gate npm aliases"
      contains: "smoke:gate"
  key_links:
    - from: "scripts/launch-gate.mjs"
      to: "https://oddlympics.app (prod) + chrome-headless-shell"
      via: "fetch (SMOKE_BASE_URL default prod) + puppeteer-core executablePath / CHROME_PATH"
      pattern: "SMOKE_BASE_URL.*oddlympics\\.app"
    - from: "scripts/cleanup-gate-rows.mjs"
      to: "vip_signups (prod DB via DATABASE_PATH)"
      via: "better-sqlite3 prepared DELETE ... WHERE email LIKE '%+ac%@gmail.com'"
      pattern: "DELETE FROM vip_signups WHERE email LIKE"
---

<objective>
Author the two Phase-11 scripts and their npm aliases: `scripts/launch-gate.mjs`
(the consolidated AC1-AC12 prod gate runner, D-06) and
`scripts/cleanup-gate-rows.mjs` (the post-tag destructive cleanup of the D-04
test rows, D-05).

Purpose: D-06's single re-runnable command is what makes the D-01
fix-and-reverify loop cheap before the 2026-05-19 deadline. D-05's
dry-run-by-default cleanup is the control that prevents an unintended prod
delete. Both follow exact in-repo analogs (the four `smoke-*.mjs` scripts;
`launch-blast.mjs` / `backup-pre-05.mjs`).

Output: `scripts/launch-gate.mjs`, `scripts/cleanup-gate-rows.mjs`, and two new
`package.json` script aliases. No new npm dependencies (Phase-6
chrome-headless-shell + npx-only invocation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md
@.planning/phases/11-end-to-end-launch-gate/11-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Established analogs the executor mirrors (do NOT reimplement from scratch). -->
<!-- 11-PATTERNS.md has the exact line refs + code excerpts for every block below. -->

scripts/smoke-landing.mjs   — runCase harness, server-reachability probe (exit 2),
                              AC1 grep pattern, LAND-02 bracket-char-class grep,
                              RFC 5737 X-Forwarded-For (192.0.2.43 used here)
scripts/smoke-signup.mjs    — env init, redirect:manual POST helper, AC2 48-team
                              JSON assertion, AC9/AC12 POST+303 assertions,
                              result summary + exit 0/1 (192.0.2.42 used here)
scripts/smoke-manage.mjs    — get(path) with redirect:'manual' returning {status,location,body}
scripts/launch-blast.mjs    — dry-run-by-default: args.includes('--confirm'),
                              SELECT-before-delete, idempotent no-op, exit codes
scripts/backup-pre-05.mjs   — better-sqlite3 open + DATABASE_PATH resolve()
package.json:13-17          — "smoke:confirm" / "smoke:landing" / "smoke:manage" /
                              "check:land-02" / "og:render" (colon-kebab convention)
references/teams.json       — canonical 48-team list (slug field) AC2 asserts against
.gitignore:34               — chrome-headless-shell/ already ignored (Phase-6)

Phase-6 chrome-headless-shell invocation (06-03 established; PATTERNS lines 428-461):
  One-time:  npx @puppeteer/browsers install chrome-headless-shell@stable  -> ./chrome-headless-shell/
  Lighthouse: CHROME_PATH=<binary> npx lighthouse <url> --preset=mobile --output=html
              --output-path=references/lighthouse-final.html
              --chrome-flags="--headless=new --no-sandbox --disable-dev-shm-usage" --quiet
  Puppeteer:  import puppeteer from 'puppeteer-core' (npx/tmp path, NOT a project dep);
              launch({ executablePath: process.env.CHROME_PATH, args:['--no-sandbox','--disable-dev-shm-usage'] });
              page.emulateTimezone('America/Detroit'); page.goto(`${BASE}/`);
              read #tz-label textContent + #timezone value

DOM contract on the live landing page (Phase-6 locked):
  <span id="tz-label">  — JS sets to "Detroit time" / "London time" / "Lagos time"
  <input id="timezone"> — JS sets value to the IANA string ("America/Detroit" etc.)

AC3 persistence-assertion path (PINNED here — resolves the CONTEXT open question):
  Assert the page-rendered #timezone hidden-input VALUE (the exact string that
  POSTs to /api/signup and is persisted) === the spoofed IANA tz, AND #tz-label
  === expected city label. This proves the persisted value WITHOUT an
  off-droplet prod-DB read (the persisted column is byte-identical to the
  posted #timezone value — Phase-5 /api/signup persists the field verbatim
  after VALID_TZ validation; all three locales are real IANA zones so no
  FALLBACK_TZ substitution). AC4's operator full-loop is the independent
  end-to-end persistence proof. AC9/AC12 "no row" asserted by the 303 redirect
  contract alone (CONTEXT — no DB read needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/launch-gate.mjs — consolidated AC1-AC12 prod gate runner (D-06, D-03)</name>
  <files>scripts/launch-gate.mjs</files>
  <read_first>
    - scripts/smoke-landing.mjs (runCase harness, reachability probe with exit 2, AC1 grep, LAND-02 bracket grep, X-Forwarded-For)
    - scripts/smoke-signup.mjs (env init, redirect:manual POST helper, AC2 teams.json assertion, AC9/AC12 POST+303, result summary + exit 0/1)
    - scripts/smoke-manage.mjs (get(path) redirect:'manual' helper returning {status, location, body})
    - .planning/phases/11-end-to-end-launch-gate/11-PATTERNS.md (`scripts/launch-gate.mjs` section — every block has exact source line refs + the AC6 PNG-header check + the operator-prompt shape; Shared Patterns: GATE_IP=192.0.2.44, chrome-headless-shell invocation, evidence dir)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-06 full AC list + operator-gated ACs; D-03 tooling + locales + deviation; D-04 +tag contract; Claude's Discretion AC3 persistence open question — resolve per the pinned path in this plan's interfaces)
    - MILESTONE-consumer-landing.md §"Acceptance criteria (UAT)" AC1-AC12 (exact verification commands: AC1 `grep -c "Your team's matches"` >= 1; AC6 1200x630 / image/png; AC8 `lighthouse --preset=mobile` all >= 90; AC9 `team=fake_team` -> 303 `/?error=bad-form`; AC12 honeypot `website` set -> no row)
    - .planning/REQUIREMENTS.md §"Acceptance criteria (milestone-level)" AC1-AC12 table + LAND-02 prohibited-terms list
    - references/teams.json (48-team canonical slug list AC2 asserts against)
  </read_first>
  <behavior>
    - AC1: GET `${BASE}/` body includes the literal `Your team's matches. In your time zone. One ping before kickoff.` (count >= 1) -> PASS
    - AC2: GET `${BASE}/` rendered `<select>` parsed -> exactly 48 non-placeholder `<option>` values, set-equal to the `slug` set in references/teams.json -> PASS
    - AC3 (3 locales Detroit/London/Lagos via puppeteer-core emulateTimezone): for each, page #timezone input value === the IANA string AND #tz-label text === "<City> time" -> PASS; on chrome-headless-shell binary absent -> exit 2 with the install hint
    - AC6: GET `${BASE}/og-image.png` -> 200, content-type includes `image/png`, byte length < 300*1024, PNG header width 1200 + height 630 -> PASS
    - AC7: for each of `/`, `/privacy`, `/terms`, `/manage` the fetched HTML does NOT match the LAND-02 bracket-char-class regex (case-insensitive) -> PASS only if all four clean
    - AC8: shells lighthouse mobile via CHROME_PATH to references/lighthouse-final.html; parses the four category scores; PASS iff Performance, Accessibility, Best Practices, SEO are ALL >= 90 (>= 0.90)
    - AC9: POST `/api/signup` with team=`fake_team` (email `johnturner+ac9@gmail.com`, tz America/New_York, requested_sport world_cup, website empty) -> status 303 AND location `/?error=bad-form` -> PASS (no DB read)
    - AC12: POST `/api/signup` with honeypot `website` set (email `johnturner+ac12@gmail.com`, team england) -> status 303 AND location `/pending` -> PASS (no DB read)
    - AC4/AC10/AC11 + opengraph.xyz: emit a clear OPERATOR ACTION prompt (target +tag email for AC4 = `johnturner+ac4@gmail.com`, < 60s timing, unsubscribe; AC10 backfilled-row banner+save; AC11 Plausible dashboard `Signup Submit` w/ team prop; opengraph.xyz preview for done-def #4), capture the pasted evidence path / dashboard URL into the evidence dir, and mark the AC OPERATOR-CONFIRMED in the table
    - Runner emits a single per-AC PASS/FAIL/OPERATOR table, writes per-AC artifacts to .planning/phases/11-end-to-end-launch-gate/evidence/, and exits 0 only if every automated AC PASSed (1 on any FAIL, 2 on setup error / server unreachable / chrome-headless-shell missing)
  </behavior>
  <action>
    Create `scripts/launch-gate.mjs` mirroring the four `smoke-*.mjs` scripts (11-PATTERNS.md gives the exact source line refs + excerpts for every block — reuse them, do not invent a new harness). Shebang `#!/usr/bin/env node`; ESM; `node:` prefix on built-ins; why-only comments; strict (no `any`). Header comment block: name it the Phase-11 AC1-AC12 gate runner, document `node scripts/launch-gate.mjs` / `npm run smoke:gate`, the `SMOKE_BASE_URL` override (default `https://oddlympics.app` — inverted from the smoke-* localhost default), exit codes 0/1/2, and RECORD the D-03 documented deviation verbatim (AC3/MILESTONE literally say "Playwright"; this satisfies the AC intent with the established puppeteer-core + chrome-headless-shell repo pattern, zero new devDependency). `const BASE = process.env.SMOKE_BASE_URL ?? 'https://oddlympics.app'`. Reuse the `runCase(name, fn)` harness + `pass`/`fail` counters + exit-0/1 summary verbatim from smoke-signup.mjs; reuse the server-reachability probe (exit 2) from smoke-landing.mjs; reuse the `redirect:'manual'` POST/get helpers from smoke-signup.mjs/smoke-manage.mjs. Use `GATE_IP = '192.0.2.44'` (RFC 5737 TEST-NET-1, distinct from smoke-signup 192.0.2.42 / smoke-landing 192.0.2.43) as the `X-Forwarded-For` on AC9/AC12 POSTs. Implement each AC per the `<behavior>` block: AC1 literal-includes grep; AC2 parse the live `<select>` AND set-compare to `references/teams.json` slugs (the static-JSON check alone is insufficient — D-06); AC6 raw fetch + PNG-header `readUInt32BE(16)`/`(20)` width/height + `< 300*1024` + `image/png`; AC7 LAND-02 bracket-char-class regex `/([b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics)/i` (brackets keep the literal terms out of this file so it self-passes) iterated over `/`, `/privacy`, `/terms`, `/manage`; AC9 `team=fake_team` -> assert `303` + `/?error=bad-form`; AC12 honeypot `website` set -> assert `303` + `/pending`. AC3: install hint = `npx @puppeteer/browsers install chrome-headless-shell@stable`; load `puppeteer-core` via an npx/tmp path (NOT a project dep — keep package.json deps unchanged); `launch({ executablePath: process.env.CHROME_PATH, args:['--no-sandbox','--disable-dev-shm-usage'] })`; for each of `America/Detroit`/`Europe/London`/`Africa/Lagos` call `page.emulateTimezone(tz)`, `page.goto(\`${BASE}/\`)`, read `#timezone` value + `#tz-label` text, assert value === tz AND label === `\`${city} time\`` (Detroit/London/Lagos) — this is the PINNED AC3 persistence-assertion path (no off-droplet DB read; rationale in this plan's interfaces). AC8: shell `lighthouse ${BASE} --preset=mobile --output=html --output-path=references/lighthouse-final.html --chrome-flags="--headless=new --no-sandbox --disable-dev-shm-usage" --quiet` with `CHROME_PATH` set to the chrome-headless-shell binary, then parse the four category scores from the lighthouse JSON/HTML and assert all four >= 0.90; the output path MUST be exactly `references/lighthouse-final.html` (SC2 / done-def #5 — NOT under `.planning/`). For AC4/AC10/AC11 + opengraph.xyz emit the operator-prompt shape from 11-PATTERNS.md (readline-captured evidence path / dashboard URL), pin AC4's target email to `johnturner+ac4@gmail.com` (D-04 +tag), and write the captured evidence into the evidence dir. Write per-AC artifacts (e.g. `AC1-pass.txt`, `AC7-prohibited-terms.txt`, `AC8-lighthouse-score.txt`) to `.planning/phases/11-end-to-end-launch-gate/evidence/` (mkdir -p) and emit one per-AC PASS/FAIL/OPERATOR table at the end (mirrors the Phase-10 evidence/+SUMMARY-table pattern). Final summary line `[gate] result: pass=${pass} fail=${fail}`; exit 1 if any FAIL, 0 otherwise; exit 2 for unreachable server / missing chrome-headless-shell. Ensure THIS file passes its own AC7 grep (bracket-char-class trick — no literal prohibited substring anywhere in the source).
  </action>
  <verify>
    <automated>node --check scripts/launch-gate.mjs && SMOKE_BASE_URL=http://localhost:4321 node scripts/launch-gate.mjs --help 2>&1 | head -1 || true; ! grep -nE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' scripts/launch-gate.mjs && grep -c 'references/lighthouse-final.html' scripts/launch-gate.mjs && grep -c '192.0.2.44' scripts/launch-gate.mjs && grep -Ec 'America/Detroit|Europe/London|Africa/Lagos' scripts/launch-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/launch-gate.mjs` exits 0 (valid ESM, parses clean)
    - `grep -c 'references/lighthouse-final.html' scripts/launch-gate.mjs` returns >= 1 (AC8 artifact path is EXACTLY this — not under .planning/)
    - `grep -c '192.0.2.44' scripts/launch-gate.mjs` returns >= 1 (distinct RFC 5737 X-Forwarded-For for AC9/AC12)
    - `grep -E 'America/Detroit|Europe/London|Africa/Lagos' scripts/launch-gate.mjs` shows all three D-03 locales present
    - LAND-02 bracket-grep on `scripts/launch-gate.mjs` returns NOTHING (the runner self-passes AC7)
    - Source contains `SMOKE_BASE_URL` defaulting to `https://oddlympics.app` (prod default, override documented)
    - Source contains the D-03 deviation note (Playwright-wording / puppeteer-core substitution recorded in the header)
    - Source contains an operator-prompt + readline evidence capture for AC4 (`johnturner+ac4@gmail.com`), AC10, AC11, opengraph.xyz
    - Source writes per-AC artifacts under `.planning/phases/11-end-to-end-launch-gate/evidence/` and emits one per-AC PASS/FAIL table
    - Source exits 0 only when all automated ACs PASS; 1 on FAIL; 2 on server-unreachable / chrome-headless-shell-missing
  </acceptance_criteria>
  <done>
    `scripts/launch-gate.mjs` exists, parses clean, mirrors the smoke-* harness,
    automates AC1/AC2/AC6/AC7/AC9/AC12, drives AC3 (3 locales via puppeteer-core
    against the pinned #timezone/#tz-label assertion), shells AC8 to exactly
    `references/lighthouse-final.html` with a four-category >= 90 assertion,
    prompts+captures operator evidence for AC4/AC10/AC11 + opengraph.xyz, writes
    per-AC artifacts to the phase evidence/ dir, emits one PASS/FAIL table, uses
    exit codes 0/1/2, and self-passes its own LAND-02 grep.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/cleanup-gate-rows.mjs + package.json aliases (D-05, D-04)</name>
  <files>scripts/cleanup-gate-rows.mjs, package.json</files>
  <read_first>
    - scripts/launch-blast.mjs (dry-run-by-default: `args.includes('--confirm')` flag, SELECT-before-delete, idempotent no-op exit, exit codes — the exact shape to mirror)
    - scripts/backup-pre-05.mjs (better-sqlite3 open + `resolve(process.env.DATABASE_PATH ?? './data/oddlympics.db')` operator pattern)
    - .planning/phases/11-end-to-end-launch-gate/11-PATTERNS.md (`scripts/cleanup-gate-rows.mjs` section — header, args, better-sqlite3 open, idempotent SELECT-before-delete, dry-run output vs execute, prepared DELETE; `package.json` section — exact alias names)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-05 dry-run-by-default + --confirm + idempotent + DATABASE_PATH + operator-on-droplet + runs AFTER the tag; D-04 +tag contract)
    - package.json (scripts block lines 6-17 — colon-kebab convention; smoke:confirm/landing/manage, check:land-02, og:render)
    - CLAUDE.md §"Dry-run-by-default safety pattern for outbound side effects" §"Production" (operator/SSH/oddlympics-writable-DB mechanism)
  </read_first>
  <action>
    Create `scripts/cleanup-gate-rows.mjs` mirroring `scripts/launch-blast.mjs` + `scripts/backup-pre-05.mjs` (11-PATTERNS.md gives the exact excerpts). Shebang `#!/usr/bin/env node`; ESM; `node:` prefix; why-only comments. Header comment block: name it the Phase-11 gate-row cleanup, document `node scripts/cleanup-gate-rows.mjs` (dry-run, prints rows it WOULD delete) and `node scripts/cleanup-gate-rows.mjs --confirm` (really delete), state it runs AFTER the `v1.0-consumer-landing` tag is pushed (D-05/D-07) and is an operator action on the droplet (`ssh oddlympics 'cd /opt/oddlympics && node scripts/cleanup-gate-rows.mjs --confirm'`), and document `DATABASE_PATH` (default `./data/oddlympics.db`). `const CONFIRM = process.argv.slice(2).includes('--confirm')`. Open the DB WRITABLE via `import Database from 'better-sqlite3'` + `const DB_PATH = resolve(process.env.DATABASE_PATH ?? './data/oddlympics.db')` + `new Database(DB_PATH)` (NOT readonly — it deletes). Use the D-04 LIKE pattern `const PATTERN = '%+ac%@gmail.com'` (matches every `johnturner+acN@gmail.com` gate row — AC3/AC4/AC9/AC12 all use the `+ac` infix; this is the locked +tag contract from D-04). Idempotent: `SELECT email, confirmed_at, created_at FROM vip_signups WHERE email LIKE ?` first; if zero rows print `nothing to do (idempotent — re-run is a no-op)` and exit 0. Dry-run (no `--confirm`): print `would delete:` then each `  - <email>`, then `dry-run complete. Re-run with --confirm to actually delete.`, close db, exit 0. With `--confirm`: run a prepared `DELETE FROM vip_signups WHERE email LIKE ?` with the same `PATTERN`, print `done. deleted=${result.changes}`, close db. The LIKE pattern MUST be a single bound parameter (no string interpolation) and MUST contain the `+ac` infix (NOT a bare `%@gmail.com` — that would over-match real John's-Gmail subscribers; the `+ac` infix scopes the delete to gate rows only — threat mitigation). Then edit `package.json` scripts block: add `"smoke:gate": "node scripts/launch-gate.mjs"` and `"cleanup:gate": "node scripts/cleanup-gate-rows.mjs"`, placed to keep the colon-kebab `smoke:*` family grouped (alphabetical within the block, consistent with the existing ordering). Do not add any dependency or devDependency.
  </action>
  <verify>
    <automated>node --check scripts/cleanup-gate-rows.mjs && grep -c "'%+ac%@gmail.com'" scripts/cleanup-gate-rows.mjs && grep -c -- '--confirm' scripts/cleanup-gate-rows.mjs && ! grep -c "'%@gmail.com'" scripts/cleanup-gate-rows.mjs; node -e "const p=require('./package.json'); if(!p.scripts['smoke:gate']||!p.scripts['cleanup:gate']) process.exit(1); console.log('aliases ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/cleanup-gate-rows.mjs` exits 0
    - `grep "'%+ac%@gmail.com'" scripts/cleanup-gate-rows.mjs` matches (the scoped +tag LIKE pattern with the `+ac` infix is present)
    - `grep "'%@gmail.com'" scripts/cleanup-gate-rows.mjs` returns NOTHING (no over-broad bare-domain pattern — the delete is scoped to gate rows, not all John's-Gmail subscribers)
    - Source contains `--confirm` gate and a dry-run path that prints rows WITHOUT deleting and exits 0
    - Source contains an idempotent zero-rows `nothing to do` no-op path (re-run safe)
    - Source opens better-sqlite3 WRITABLE with `DATABASE_PATH` override (default `./data/oddlympics.db`)
    - Source uses a single bound parameter for the LIKE (no string interpolation)
    - `package.json` has `scripts["smoke:gate"] === "node scripts/launch-gate.mjs"` and `scripts["cleanup:gate"] === "node scripts/cleanup-gate-rows.mjs"`
    - No new entry in `dependencies` / `devDependencies`
  </acceptance_criteria>
  <done>
    `scripts/cleanup-gate-rows.mjs` exists, parses clean, is dry-run-by-default
    with a `--confirm` execute path and an idempotent no-op, opens the DB
    writable via `DATABASE_PATH`, scopes the DELETE to the `%+ac%@gmail.com`
    bound LIKE pattern (never the bare domain), and `package.json` exposes
    `smoke:gate` + `cleanup:gate` with no new deps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → prod API | `launch-gate.mjs` POSTs (AC9/AC12) to the live `/api/signup` — untrusted-input path against the production DB |
| script → prod DB | `cleanup-gate-rows.mjs` issues a destructive DELETE against the production `vip_signups` table |
| committed source → repo | test-email PII (`johnturner+...@gmail.com`) and the prod DB path appear in committed script source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering / Denial | `cleanup-gate-rows.mjs` over-broad LIKE | mitigate | Pattern hard-pinned to `%+ac%@gmail.com` (the `+ac` infix scopes to gate rows only); `<verify>` asserts the bare `%@gmail.com` pattern is ABSENT; SELECT-before-delete prints the exact row set; dry-run-by-default + `--confirm` gate is the control preventing an unintended prod delete (D-05) |
| T-11-02-02 | Tampering | `launch-gate.mjs` mutating prod beyond test rows | mitigate | Runner only POSTs AC9 (bad-team -> no row by design) + AC12 (honeypot -> no row by design); all writing ACs use D-04 `+ac` +tag emails so every row it could create is reachable by the scoped cleanup; AC1/2/6/7 are read-only GETs |
| T-11-02-03 | Information disclosure | committed test-email PII / prod DB path in script source | accept | Emails are John's own Gmail +tag addresses (operator's own inbox, not third-party PII); `DATABASE_PATH` default is a relative repo path, the real prod path is supplied via env at operator runtime — not committed |
| T-11-02-04 | Spoofing | runner targets wrong host | mitigate | `SMOKE_BASE_URL` defaults to the literal `https://oddlympics.app`; server-reachability probe exits 2 before any AC runs if the target is unreachable |
| T-11-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new package.json deps (verified by `<verify>`); `puppeteer-core` + `lighthouse` + `@puppeteer/browsers` are npx one-shot / Phase-6 chrome-headless-shell binary only — no install task in this plan; chrome-headless-shell was vetted in Phase 6 |
</threat_model>

<verification>
- `node --check` passes for both scripts.
- `launch-gate.mjs`: self-passes LAND-02 grep; references exactly
  `references/lighthouse-final.html`; uses `192.0.2.44`; all three D-03 locales
  present; prod-default `SMOKE_BASE_URL`; D-03 deviation note present.
- `cleanup-gate-rows.mjs`: `%+ac%@gmail.com` present, bare `%@gmail.com` absent;
  `--confirm` gate + idempotent no-op + writable DATABASE_PATH open present.
- `package.json` exposes `smoke:gate` + `cleanup:gate`; no new deps.
</verification>

<success_criteria>
Both scripts exist, parse clean, and mirror their in-repo analogs. The gate
runner is a single re-runnable command that automates the scriptable ACs,
drives AC3 via puppeteer-core, shells AC8 to the exact mandated path, and
captures operator evidence for the rest with one PASS/FAIL table. The cleanup
script is dry-run-by-default, scoped to the `+ac` +tag rows only, and
idempotent. npm aliases follow the existing convention. No new dependencies.
</success_criteria>

<output>
Create `.planning/phases/11-end-to-end-launch-gate/11-02-SUMMARY.md` when done
</output>
