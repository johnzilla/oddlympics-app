---
phase: 11-end-to-end-launch-gate
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch-gate.mjs
  - .planning/phases/11-end-to-end-launch-gate/evidence/
  - references/lighthouse-final.html
autonomous: false

requirements: []
user_setup: []

must_haves:
  truths:
    - "D-09: the Phase 11 launch gate is RE-RUN against post-Phase-12 production — Phase 12 (multi-team restore) shipped + verified PASSED 11/11 on 2026-05-16, so the original single-team blocker is RESOLVED and the stale 11-03 single-team gate evidence (deploy 8092814, pre-Phase-12) is superseded by a fresh post-Phase-12 prod run"
    - "D-09: scripts/launch-gate.mjs gains ONE new multi-team /manage acceptance check (label AC-MT, runCase name AC-MT-multi-team-manage, matching the existing AC<id>-<name> convention) that drives prod /manage for a D-04 +tag test row, selects 1-5 confederation-grouped team checkboxes (Phase-12 restored model), saves via POST /api/save-selection, reads /manage back, and asserts the multi-team selection persisted (the saved slugs render as checked checkboxes — the smoke-manage M11 read-back pattern); kickoff fan-out via user_teams is asserted by citing the prod-verified 12-VERIFICATION.md (truth #7/#8, M14) because an off-box prod-DB read is not available, mirroring the 11-CONTEXT AC3 persistence-assertion reasoning"
    - "D-09: the full gate re-runs as the single `npm run smoke:gate` command against https://oddlympics.app post-Phase-12 and AC1-AC12 PLUS the new AC-MT are all green on the CURRENT deployed build (the gate runs against the green-deploy commit recorded by Task 2)"
    - "D-06: a single re-run command, one per-AC PASS/FAIL/OPERATOR table, and all artifacts written under .planning/phases/11-end-to-end-launch-gate/evidence/ (the new check writes evidence/AC-MT-multi-team.txt; references/lighthouse-final.html is freshly regenerated all four >= 90)"
    - "D-01: the launch-gate.mjs AC-MT extension is itself ONE small task (a new AC function mirroring the existing AC3 puppeteer-core path + the smoke-manage M10/M11 save+read-back pattern); any AC that fails large enough to need its own investigation escalates to a separate /gsd-quick or fix phase and is reported RED with evidence — it does NOT bloat this phase (bounded fix-and-reverify boundary held)"
    - "AC1-AC12 + AC-MT all green on post-Phase-12 prod: AC1 landing renders; AC2 48-option select == teams.json; AC3 3-locale tz-spoof; AC4 real John's-Gmail full confirm->manage->unsubscribe loop < 60s (operator); AC5 /privacy + /terms 200 + canonical copy; AC6 og-image 1200x630 <300KB + opengraph.xyz preview (done-def #4, operator); AC7 zero prohibited terms across / /privacy /terms /manage; AC8 fresh Lighthouse mobile to references/lighthouse-final.html all four >= 90; AC9 bad-team -> 303 /?error=bad-form no row; AC10 backfilled-row banner + save (operator); AC11 Plausible Signup Submit event with team prop (operator); AC12 honeypot -> 303 /pending no row; AC-MT multi-team /manage select+save+read-back persists"
    - "Reconciliation: 11-SUMMARY.md (currently STALE — says 'Phase 11 stays open/blocked until multi-team in') is rewritten to reflect that multi-team shipped + verified (Phase 12), the re-gate ran on post-Phase-12 prod, and the phase is unblocked; the recorded final green-deploy commit SHA supersedes the stale 11-03 SHA 8092814 for 11-04's tag"
  artifacts:
    - path: "scripts/launch-gate.mjs"
      provides: "AC1-AC12 + the new AC-MT multi-team /manage check; single npm run smoke:gate command; per-AC PASS/FAIL/OPERATOR table"
      contains: "AC-MT-multi-team-manage"
    - path: "references/lighthouse-final.html"
      provides: "Fresh post-Phase-12 prod Lighthouse mobile report, all four categories >= 90 (SC2 / done-def #5)"
    - path: ".planning/phases/11-end-to-end-launch-gate/evidence/"
      provides: "Fresh post-Phase-12 per-AC evidence including evidence/AC-MT-multi-team.txt — supersedes the stale pre-Phase-12 11-03 evidence"
  key_links:
    - from: "scripts/launch-gate.mjs (AC-MT)"
      to: "https://oddlympics.app/manage + POST /api/save-selection"
      via: "select 1-5 confederation checkboxes, save, GET /manage read-back asserts checked persisted"
      pattern: "save-selection"
    - from: "AC-MT kickoff-fan-out assertion"
      to: ".planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md"
      via: "cite prod-verified truth #7/#8 + M14 (cron user_teams JOIN) — off-box prod-DB read unavailable"
      pattern: "12-VERIFICATION"
    - from: "operator + npm run smoke:gate"
      to: "https://oddlympics.app (post-Phase-12 green deploy)"
      via: "single re-runnable D-06 command against the recorded green-deploy commit"
      pattern: "smoke:gate"
---

<objective>
The D-09 multi-team re-gate. Phase 12 (restore-multi-team-selection) shipped and
verified PASSED 11/11 on 2026-05-16 — the original single-team founder blocker
that halted Phase 11 before tagging is RESOLVED. Per locked decision D-09 the
Phase 11 launch gate MUST re-run after Phase 12: re-verify AC1-AC12 PLUS the new
multi-team behavior on production, so a fresh post-Phase-12 evidence set
supersedes the now-stale single-team 11-03 evidence (deploy `8092814`,
pre-Phase-12) and the now-stale 11-SUMMARY.md is reconciled.

This plan: (1) extends `scripts/launch-gate.mjs` with ONE new multi-team
`/manage` acceptance check (AC-MT) mirroring the existing AC3 puppeteer-core
path + the `smoke-manage.mjs` M10/M11 save+read-back pattern; (2) deploys the
post-Phase-12 build and records the green-deploy commit; (3) runs the full gate
once via `npm run smoke:gate` against post-Phase-12 prod, captures the
still-pending operator-gated evidence (AC4 full Gmail loop, AC10 backfilled-row,
AC11 Plausible, opengraph.xyz preview) plus the new AC-MT, drives the D-01
bounded fix-and-reverify loop, and reconciles 11-SUMMARY.md.

Purpose: ROADMAP Phase-11 SC1/SC2/SC3 re-certified on the CURRENT deployed
multi-team build (D-09). Output of this plan is the precondition for 11-04 (cut
the deliberately-withheld `v1.0-consumer-landing` tag) which then precedes 11-05
(operator cleanup).

Output: The extended `launch-gate.mjs` (AC-MT added), a green per-AC table
(AC1-AC12 + AC-MT all PASS/OPERATOR-CONFIRMED on post-Phase-12 prod), a fresh
`references/lighthouse-final.html` (all four >= 90), fresh per-AC evidence under
the phase `evidence/` dir, a recorded post-Phase-12 green-deploy commit SHA for
11-04's tag, and a reconciled (un-stale) 11-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md
@.planning/phases/11-end-to-end-launch-gate/11-SUMMARY.md
@.planning/phases/11-end-to-end-launch-gate/11-03-SUMMARY.md
@.planning/phases/12-restore-multi-team-selection/12-CONTEXT.md
@.planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md

<interfaces>
<!-- The exact contracts the AC-MT extension is built against. No exploration needed. -->

scripts/launch-gate.mjs — the existing AC1-AC12 prod runner (built 11-02,
  D-01-patched in 11-03). Key existing shape AC-MT must match:
  - BASE = process.env.SMOKE_BASE_URL ?? 'https://oddlympics.app'
  - EVIDENCE_DIR = .planning/phases/11-end-to-end-launch-gate/evidence (mkdir'd up front)
  - runCase(name, async fn) — fn returns boolean; PASS/FAIL counted into pass/fail
  - resolveChromePath() — auto-discovers ./chrome-headless-shell/ binary (AC3/AC8 use it)
  - puppeteer-core self-bootstrap block (npm install --no-save before first import,
    Node ESM negative-cache reason) — AC3 already does this; AC-MT reuses the
    SAME already-imported `puppeteer` handle / browser if it drives a browser
  - prompt(question) — readline operator-evidence capture (AC4/AC10/AC11/OG use it)
  - Final per-AC summary console block + `[gate] result: pass=N fail=N` + exit 0/1
  - AC numbering present: AC1 AC2 AC3 AC4 AC6 AC7 AC8 AC9 AC10 AC11 AC12 + OG block
    (AC5 is asserted inside AC7's /privacy + /terms fetch). The new check's label
    is AC-MT (NOT AC13 — keeps the AC1-AC12 milestone sequence intact; AC-MT
    reads as "multi-team", a Phase-12-restored behavior re-gated per D-09).

scripts/smoke-manage.mjs — the multi-team /manage analog. AC-MT mirrors:
  - postMultiTeam(slugs, tz, cookieHeader) (lines 213-231): URLSearchParams with
    REPEATED `team=` appends (NOT object form — that serializes one value/key),
    POST /api/save-selection, redirect:'manual', returns {status, location}
  - M10-multi-save-n (lines 653-684): POST 3 valid slugs -> 303 status=saved
  - M11-pre-check (lines 689-721): GET /manage with saved slugs -> assert each
    slug renders as a CHECKED checkbox via
    /value="<slug>"[^>]*checked|checked[^>]*value="<slug>"/i (the read-back proof)
  - manage.astro post-12-02 uses confederation-grouped `type="checkbox"
    name="team" value="<slug>"` (NO `<select name="team">`) — AC-MT asserts the
    select is gone and the saved slugs are checked

D-04 +tag contract: prod test rows use John's-Gmail +tag addresses with the
  `+ac` infix so 11-05's cleanup LIKE `%+ac%@gmail.com` reaches them. AC-MT's
  test email: `johnturner+acmt@gmail.com` (pin; `+ac` infix satisfies D-04).

Phase 12 prod-verified facts AC-MT cites (off-box prod-DB read unavailable):
  12-VERIFICATION.md truth #7 (cron joins vip_signups->user_teams->teams.slug,
  filters preserved), truth #8 + M14 (one email per match per followed team,
  NOTIFY-04). These were verified on the Phase-12 build now deployed to prod.

Session for the prod /manage save: the gate runs OFF-BOX with no
  MAGIC_LINK_SECRET, so it cannot mint a prod session cookie itself (unlike
  smoke-manage which runs against a dev-secret local server). AC-MT is therefore
  OPERATOR-GATED for obtaining the authenticated /manage session (mirrors the
  AC4/AC10 operator pattern): the operator performs the real magic-link sign-in
  for johnturner+acmt@gmail.com, pastes back the resulting session Cookie
  header (or drives the save in-browser), and the script does the automated
  multi-select POST + /manage read-back assertion with that cookie. This keeps
  AC-MT a single small task (D-01) and honors "no secret off-box".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend launch-gate.mjs with the AC-MT multi-team /manage acceptance check</name>
  <files>scripts/launch-gate.mjs</files>
  <read_first>
    - scripts/launch-gate.mjs (THE FILE BEING MODIFIED — the existing AC1-AC12 structure: BASE, EVIDENCE_DIR, runCase, resolveChromePath, the puppeteer-core self-bootstrap block AC3 uses, prompt(), the final per-AC summary block + result line + exit codes; the AC numbering AC1 AC2 AC3 AC4 AC6 AC7 AC8 AC9 AC10 AC11 AC12 + OG)
    - scripts/smoke-manage.mjs (the multi-team analog: postMultiTeam lines 213-231 — repeated `team=` appends; M10-multi-save-n lines 653-684 — POST N slugs -> 303 status=saved; M11-pre-check lines 689-721 — GET /manage read-back asserting each saved slug renders as a CHECKED checkbox via the value=…checked regex; the "no <select name=\"team\">" assertion)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (what multi-team restoration delivered: user_teams join table, /manage confederation-grouped checkboxes 1-5, signup still single-team D-03, cron fan-out via user_teams D-06)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (truth #3 checkbox editor, truth #4 atomic save, truth #7 cron user_teams JOIN, truth #8/M14 one-email-per-match — the prod-verified facts AC-MT cites for the kickoff-fan-out leg since an off-box prod-DB read is unavailable)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-01 bounded single-task fix scope; D-04 +tag email contract — `+ac` infix; D-06 single command + per-AC table + evidence dir; the Claude's-Discretion "open implementation question" AC3 persistence-assertion reasoning that AC-MT's read-back mirrors)
  </read_first>
  <behavior>
    - AC-MT runCase is named exactly `AC-MT-multi-team-manage` and is counted into the same pass/fail tally as AC1-AC12 (so `[gate] result: pass=N fail=N` and exit code include it).
    - AC-MT uses the D-04 test email `johnturner+acmt@gmail.com` (the `+ac` infix makes it reachable by 11-05's `%+ac%@gmail.com` cleanup LIKE).
    - AC-MT is operator-gated for the authenticated /manage session ONLY (the gate runs off-box with no MAGIC_LINK_SECRET): it prints a clear OPERATOR ACTION block instructing the operator to sign in via magic-link for johnturner+acmt@gmail.com and paste back the session Cookie header (`prompt()`), then performs the automated assertion with that cookie. If the operator presses Enter to skip, AC-MT writes a SKIPPED evidence file and is reported OPERATOR-GATED-SKIPPED (NOT silently PASS).
    - With a pasted session cookie: AC-MT POSTs a 3-slug multi-team selection (e.g. `england,france,germany`, each a valid teams.json slug, count within the Phase-12 1-5 bound) to `${BASE}/api/save-selection` using the repeated-`team=` URLSearchParams shape from smoke-manage's postMultiTeam, asserts a 303 with `status=saved` in Location, then GET `${BASE}/manage` with the same cookie and asserts (a) NO `<select name="team">` substring (confederation checkboxes replaced it post-12-02) and (b) each of the 3 saved slugs renders as a CHECKED checkbox via the smoke-manage M11 regex `value="<slug>"[^>]*checked|checked[^>]*value="<slug>"` (case-insensitive). All three must be checked for PASS.
    - AC-MT writes `evidence/AC-MT-multi-team.txt` recording: timestamp, target, the test email, the posted slugs, the save status/Location, the per-slug checked read-back result, PASS/FAIL/SKIPPED, and a one-line citation that kickoff fan-out via `user_teams` is prod-verified by 12-VERIFICATION.md truth #7/#8 + M14 (off-box prod-DB read unavailable — the /manage read-back is the persistence proof; this mirrors the 11-CONTEXT AC3 persistence-assertion reasoning).
    - The final per-AC summary console block gains an `AC-MT  Multi-team /manage select+save+read-back  → OPERATOR-GATED (AC-MT-multi-team.txt)` line; the existing AC1-AC12 + OG lines are unchanged.
    - `node --check scripts/launch-gate.mjs` exits 0. AC1-AC12 logic is otherwise UNCHANGED (no regression to the existing checks); no new package.json dependency (reuse the existing puppeteer-core self-bootstrap only if a browser is actually needed — the cookie+fetch read-back path needs only `fetch`, so no browser is required for AC-MT and none should be launched for it).
  </behavior>
  <action>
    Add ONE new acceptance check, AC-MT, to scripts/launch-gate.mjs, placed after the AC12 honeypot block and before the opengraph.xyz operator block (so it sits with the operator-gated checks and the OG block stays the last operator prompt). Mirror the existing file idiom exactly: reuse BASE, EVIDENCE_DIR, runCase, prompt(), the writeFileSync evidence pattern, and the smoke-manage postMultiTeam repeated-`team=` URLSearchParams shape (object-form URLSearchParams serializes one value per key — it MUST append `team` per slug). Name the runCase exactly `AC-MT-multi-team-manage`. Use test email `johnturner+acmt@gmail.com` (the `+ac` infix is the D-04 contract that lets 11-05's `%+ac%@gmail.com` cleanup reach it — do NOT invent a non-`+ac` local-part). Operator-gate ONLY the authenticated session: print an OPERATOR ACTION block (same style as the AC4/AC10 blocks) telling the operator to magic-link sign in for that email on prod and paste back the `oddlympics_session` Cookie header; capture via prompt(). On a pasted cookie: POST 3 valid teams.json slugs (within Phase-12's 1-5 bound) to `${BASE}/api/save-selection` with the repeated-`team=` body + the cookie, assert 303 + `status=saved` in Location; then GET `${BASE}/manage` with the cookie and assert there is NO `<select name="team">` and that EACH posted slug matches the smoke-manage M11 checked-checkbox regex `value="<slug>"[^>]*checked|checked[^>]*value="<slug>"` (case-insensitive) — all three checked => the multi-team selection persisted. Write evidence/AC-MT-multi-team.txt with the timestamp/target/email/slugs/save-status/per-slug-checked results/verdict plus the one-line "kickoff fan-out via user_teams is prod-verified by 12-VERIFICATION.md truth #7/#8 + M14; off-box prod-DB read unavailable so the /manage read-back is the persistence proof (mirrors 11-CONTEXT AC3 persistence-assertion reasoning)" citation. On operator skip, write a SKIPPED evidence file and report OPERATOR-GATED-SKIPPED — never silently PASS. Add the `AC-MT` line to the final per-AC summary console block. Do NOT alter any AC1-AC12 logic, do NOT add a package.json dependency, do NOT launch a browser for AC-MT (fetch + cookie is sufficient — the AC3 puppeteer path is the structural analog for "operator-gated prod check that writes evidence", not a requirement to drive Chrome here). Honor D-01: this is one small bounded task — a single new AC function in one file; if writing it surfaces a need for anything larger, STOP and escalate rather than expanding scope.
  </action>
  <verify>
    <automated>node --check scripts/launch-gate.mjs && grep -c 'AC-MT-multi-team-manage' scripts/launch-gate.mjs && grep -F 'johnturner+acmt@gmail.com' scripts/launch-gate.mjs && grep -F 'api/save-selection' scripts/launch-gate.mjs && grep -F '<select name="team"' scripts/launch-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/launch-gate.mjs` exits 0 (syntactically valid ESM)
    - `grep -c 'AC-MT-multi-team-manage' scripts/launch-gate.mjs` returns >= 1 (the named runCase exists)
    - `scripts/launch-gate.mjs` contains the literal D-04 test email `johnturner+acmt@gmail.com` (the `+ac` infix is reachable by 11-05's `%+ac%@gmail.com` cleanup)
    - `scripts/launch-gate.mjs` POSTs to `api/save-selection` with a repeated-`team=` URLSearchParams body (the smoke-manage postMultiTeam shape, NOT object form) and asserts a 303 with `status=saved`
    - `scripts/launch-gate.mjs` GETs `/manage` and asserts BOTH the absence of `<select name="team"` AND each posted slug matching the M11 checked-checkbox regex `value="…"[^>]*checked|checked[^>]*value="…"`
    - AC-MT writes `evidence/AC-MT-multi-team.txt` and the file content cites 12-VERIFICATION.md truth #7/#8 + M14 for the kickoff-fan-out leg
    - The final per-AC summary console block contains an `AC-MT` line; the AC1-AC12 + OG lines are unchanged (no AC1-AC12 logic touched)
    - No new entry added under package.json `dependencies`/`devDependencies`; `node --check` confirms no syntax change broke the existing checks
  </acceptance_criteria>
  <done>
    scripts/launch-gate.mjs has a single new operator-gated AC-MT check (named
    AC-MT-multi-team-manage) that, with an operator-supplied prod /manage
    session, posts a 1-5-team multi-select to /api/save-selection and asserts
    the read-back /manage HTML shows the saved slugs as checked checkboxes (no
    <select name="team">), writes evidence/AC-MT-multi-team.txt citing the
    Phase-12 prod-verified cron facts, adds the AC-MT summary line, leaves
    AC1-AC12 untouched, adds no dependency, and node --check passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Deploy post-Phase-12 build, re-run the full gate (AC1-AC12 + AC-MT) on prod, capture operator evidence, D-01 loop, reconcile 11-SUMMARY.md</name>
  <files>scripts/launch-gate.mjs, references/lighthouse-final.html, .planning/phases/11-end-to-end-launch-gate/evidence/, .planning/phases/11-end-to-end-launch-gate/11-SUMMARY.md</files>
  <read_first>
    - scripts/launch-gate.mjs (the now-extended gate — AC1-AC12 + AC-MT; the single `npm run smoke:gate` command, default target https://oddlympics.app, the operator-gated AC4/AC10/AC11/OG/AC-MT prompts, the per-AC table + exit codes)
    - .github/workflows/deploy.yml (push-to-main -> rsync + `npm rebuild better-sqlite3` + `systemctl restart oddlympics` + 5x curl-200 ~47s — the GREEN run of this defines the post-Phase-12 green-deploy commit 11-04 will tag)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-01 bounded fix-and-reverify — single atomic commit -> redeploy -> single re-run, anything larger escalates RED; D-04 +tag emails incl. johnturner+acmt@gmail.com; D-06 single command + per-AC table + evidence dir; the AC3 persistence-assertion reasoning; the Deferred note that AC1 visual-diff is best-effort/N-A on a missing reference asset — do NOT block on it)
    - .planning/phases/11-end-to-end-launch-gate/11-03-SUMMARY.md (the STALE pre-Phase-12 single-team gate evidence — deploy 8092814, pass=10 — that this re-run SUPERSEDES; the operator-gated AC4/AC10/AC11/opengraph were SKIPPED there and must be captured now)
    - .planning/phases/11-end-to-end-launch-gate/11-SUMMARY.md (the STALE phase summary — "Phase 11 stays open/blocked until multi-team in"; "NOT done (deliberate)" is the gap list — multi-team is NOW in + verified, so this file MUST be reconciled by this task)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (Phase 12 PASSED 11/11 on prod-equivalent build — the multi-team certification this re-gate confirms is live on prod; truth #7/#8 + M14 are the cron-fan-out facts AC-MT cites)
    - MILESTONE-consumer-landing.md §"Acceptance criteria (UAT)" AC1-AC12 + §"Done definition" (the 5 done conditions: all AC pass on prod; tag; real John's-Gmail signup < 60s; opengraph.xyz preview renders; references/lighthouse-final.html all >= 90)
    - .planning/phases/10-confirmation-email-update/10-SUMMARY.md (AC4 deliverability baseline — 10/10 Mail-Tester, Gmail+Proton render from prod sender hello@oddlympics.app; Outlook out of scope for v2.0)
  </read_first>
  <what-built>
    Task 1 added the AC-MT multi-team /manage check to scripts/launch-gate.mjs.
    Phase 12 (restore-multi-team-selection) shipped + verified PASSED 11/11 on
    2026-05-16 (user_teams join table, /manage confederation checkboxes 1-5,
    cron fan-out via user_teams, signup still single-team per D-03, consent
    regressions CR-01/CR-02 closed, smoke M1-M16 green pass=17 fail=0). The
    original single-team founder blocker that halted Phase 11 before tagging is
    RESOLVED. The stale 11-03 single-team gate (deploy 8092814, pre-Phase-12,
    operator ACs SKIPPED) must be superseded by a fresh post-Phase-12 prod run
    of the full extended gate (D-09).
  </what-built>
  <action>
    Operator-gated re-gate (blocking-human): deploy the post-Phase-12 build + Task 1's launch-gate.mjs extension, confirm the GitHub Actions Deploy run is GREEN, record the post-Phase-12 green-deploy commit SHA (it SUPERSEDES the stale 11-03 SHA 8092814 — the only SHA 11-04 may tag), then run the single `npm run smoke:gate` command against post-Phase-12 https://oddlympics.app. Read the per-AC PASS/FAIL/OPERATOR table; perform the operator-gated steps (AC4 real John's-Gmail full confirm->manage->unsubscribe loop < 60s; AC10 backfilled-row banner+save; AC11 Plausible Signup Submit event with team prop; AC-MT magic-link sign-in for johnturner+acmt@gmail.com then paste the oddlympics_session cookie so the script posts the 3-team multi-select + reads /manage back; opengraph.xyz preview) and capture each as committed evidence under the phase evidence/ dir. Drive the D-01 bounded fix-and-reverify loop (single atomic commit -> redeploy -> single re-run for any SMALL failing AC; escalate RED + STOP for anything larger). Then reconcile the now-STALE 11-SUMMARY.md so it states multi-team shipped+verified (Phase 12), the gate re-ran on post-Phase-12 prod per D-09, the AC1-AC12 + AC-MT results with the new green-deploy SHA superseding 8092814, and that the phase is unblocked / 11-04 actionable. Follow the numbered how-to-verify steps exactly.
  </action>
  <how-to-verify>
    1. DEPLOY: ensure the Phase-12 multi-team code AND Task 1's launch-gate.mjs extension are committed + pushed to `main`. Confirm the GitHub Actions Deploy run for that commit completed GREEN (`gh run list --workflow=deploy.yml`, `gh run view <id>` — rsync + `npm rebuild better-sqlite3` + `systemctl restart oddlympics` + the 5x curl-200 CI smoke all pass). Record the EXACT `main` commit SHA whose Deploy run is green — this is the post-Phase-12 green-deploy commit (it SUPERSEDES the stale 11-03 SHA 8092814; it is the only SHA 11-04 may tag). If the Deploy run is red, that is a deploy-infra failure (not an AC) — resolve it before the gate run; do NOT run the gate against a stale or failed deploy.
    2. ONE-TIME (if not present from Phase 6/11-03): `npx @puppeteer/browsers install chrome-headless-shell@stable` (gitignored ./chrome-headless-shell/); the gate auto-discovers it (or set CHROME_PATH).
    3. RUN THE FULL GATE ONCE: `npm run smoke:gate` (default target https://oddlympics.app — the post-Phase-12 green deploy). The runner emits the per-AC PASS/FAIL/OPERATOR table and writes per-AC artifacts under `.planning/phases/11-end-to-end-launch-gate/evidence/`.
    4. AUTOMATED ACs (AC1, AC2, AC6, AC7, AC9, AC12) + AC3 (3-locale puppeteer) + AC8 (Lighthouse -> references/lighthouse-final.html, four categories >= 90): read the per-AC table. AC1 hard gate = the "Your team's matches" headline render PASS; the references/landing_preview.png visual diff is best-effort — record N/A with a one-line note if the asset is absent (NEVER a launch blocker, per the 11-CONTEXT Deferred note). Confirm references/lighthouse-final.html is freshly regenerated and shows Performance / Accessibility / Best Practices / SEO ALL >= 90 on the post-Phase-12 build.
    5. AC5: confirm `/privacy` and `/terms` return 200 with canonical copy and a last-updated date matching the deploy date (the gate's AC7 fetch pulls both bodies — read the captured evidence).
    6. AC4 (OPERATOR — ROADMAP SC3 / done-def #3): from a FRESH browser profile, submit the prod form team=England email `johnturner+ac4@gmail.com`; time it — the confirmation email must arrive in John's Gmail within 60s, name England + the timezone, render cleanly (Phase-10 Gmail baseline). Click the email's unsubscribe link — confirm it lands on /unsubscribed and removes the row from active sending. Screenshot the inbox (timestamp visible) + the /unsubscribed page into the evidence dir; paste the path when the runner prompts.
    7. AC10 (OPERATOR): sign in to /manage as a pre-milestone backfilled subscriber (team=NULL, timezone='America/New_York'); confirm the one-time "Pick a team" banner shows, save a team, confirm no error and the banner dismisses. Screenshot -> evidence dir; paste path.
    8. AC11 (OPERATOR): submit a form (the AC4 submission counts) and confirm a `Signup Submit` Plausible event with the `team` prop populated appears in the Plausible dashboard. Screenshot -> evidence dir; paste path/URL. If the dashboard shows an UNACCEPTABLE drop rate, that is a D-01 small in-phase fix: apply the Phase-6-noted `preventDefault + setTimeout(form.submit, 0)` swap to the Plausible submit listener (one atomic commit -> GitHub Actions redeploy -> re-run `npm run smoke:gate` + re-verify AC11).
    9. AC-MT (OPERATOR — the D-09 multi-team re-gate): when the runner reaches the AC-MT operator block, magic-link sign in for `johnturner+acmt@gmail.com` on prod and paste back the `oddlympics_session` Cookie header. The script then POSTs the 3-team multi-select to /api/save-selection and reads /manage back, asserting the 3 saved slugs render as checked confederation checkboxes (no `<select name="team">`). Confirm AC-MT PASS in the table and evidence/AC-MT-multi-team.txt records the per-slug checked read-back + the 12-VERIFICATION.md truth #7/#8 + M14 citation for the cron-fan-out leg (off-box prod-DB read unavailable — read-back is the persistence proof, mirroring AC3 reasoning).
    10. opengraph.xyz (done-def #4): open opengraph.xyz for https://oddlympics.app; confirm the card shows the headline + banner + URL + og-image cleanly. Screenshot -> evidence dir; paste path.
    11. D-01 BOUNDED FIX-AND-REVERIFY: for any FAIL that is SMALL (a CSS value, a one-line preventDefault, a copy/header nit — itself a single trivial change): ONE atomic commit -> push to main -> GitHub Actions Deploy green -> re-run `npm run smoke:gate` (and re-do the affected operator step). Re-record the new green-deploy SHA (it supersedes step 1's for 11-04's tag). For any FAIL large enough to need its own plan/investigation: STOP, report that AC RED with its captured evidence, escalate to a separate /gsd-quick or fix phase, do NOT silently patch — the phase boundary stays bounded (D-01).
    12. RECONCILE 11-SUMMARY.md: rewrite the now-STALE 11-SUMMARY.md so it no longer says "Phase 11 stays open/blocked until multi-team in" / "Will not tag a launch the founder rejected". It must state: multi-team shipped + verified PASSED 11/11 (Phase 12, 2026-05-16); the Phase 11 gate RE-RAN on post-Phase-12 prod per D-09; AC1-AC12 + AC-MT results (with the recorded post-Phase-12 green-deploy SHA superseding the stale 8092814); the operator-gated AC4/AC10/AC11/opengraph evidence is now captured; the phase is UNBLOCKED and 11-04 (tag) is now actionable. Keep it factual — record any AC that escalated RED.
    13. Final state required to pass this checkpoint: the per-AC table shows AC1-AC12 + AC-MT all GREEN (PASS for automated, OPERATOR-CONFIRMED for AC4/AC10/AC11/AC-MT/opengraph) on the post-Phase-12 deploy, references/lighthouse-final.html is freshly regenerated all four >= 90, every AC + AC-MT has captured evidence under the phase evidence/ dir (superseding the stale 11-03 set), the post-Phase-12 green-deploy commit SHA is recorded for 11-04, and 11-SUMMARY.md is reconciled (un-stale).
  </how-to-verify>
  <verify>
    <automated>npm run smoke:gate</automated>
  </verify>
  <acceptance_criteria>
    - The GitHub Actions Deploy run for the post-Phase-12 + AC-MT commit is GREEN (confirmed via `gh run view`) and the exact green-deploy `main` commit SHA is recorded (it SUPERSEDES the stale 11-03 SHA 8092814 — it is the only SHA 11-04 may tag)
    - `npm run smoke:gate` (single D-06 command, target https://oddlympics.app post-Phase-12) emits a per-AC table with AC1-AC12 ALL GREEN (PASS automated / OPERATOR-CONFIRMED for AC4/AC10/AC11/opengraph) and exits 0 on the automated set
    - AC-MT shows OPERATOR-CONFIRMED PASS in the table; evidence/AC-MT-multi-team.txt records the 3 posted slugs, the 303 status=saved, the per-slug checked read-back from /manage, and the 12-VERIFICATION.md truth #7/#8 + M14 citation for the cron-fan-out leg
    - references/lighthouse-final.html is freshly regenerated (post-Phase-12 run) and shows Performance, Accessibility, Best Practices, SEO ALL >= 90
    - AC1 `curl -sL https://oddlympics.app | grep -c "Your team's matches"` returns >= 1 (hard gate); visual-diff recorded PASS or N/A-missing-asset (never a launch blocker)
    - AC4: a `johnturner+ac4@gmail.com` signup from a fresh profile delivered a correctly-rendered England+tz email within 60s and its unsubscribe link reached /unsubscribed + removed the row — screenshot evidence committed
    - AC5: /privacy + /terms both 200 with canonical copy + matching last-updated date — evidence captured
    - AC10 + AC11 operator screenshots are in the evidence dir; opengraph.xyz preview screenshot captured (done-def #4); if AC11 needed the preventDefault+setTimeout swap it was a single atomic commit + redeploy + re-run (D-01)
    - Every AC1-AC12 + AC-MT has a corresponding fresh post-Phase-12 artifact under `.planning/phases/11-end-to-end-launch-gate/evidence/` (the stale pre-Phase-12 11-03 set is superseded)
    - 11-SUMMARY.md is reconciled: it no longer claims the phase is blocked on missing multi-team; it states multi-team shipped+verified (Phase 12), the re-gate ran on post-Phase-12 prod, records the new green-deploy SHA, and marks the phase unblocked / 11-04 actionable
    - Any AC that could NOT be made green with a small in-phase fix is reported RED with evidence and escalated (not silently patched) — D-01 boundary honored
  </acceptance_criteria>
  <done>
    AC1-AC12 + AC-MT all green on post-Phase-12 prod via the single `npm run
    smoke:gate`, references/lighthouse-final.html freshly regenerated all four
    >= 90, every AC + AC-MT has fresh evidence superseding the stale 11-03 set,
    the post-Phase-12 green-deploy SHA is recorded (superseding 8092814) for
    11-04's tag, and 11-SUMMARY.md is reconciled to mark the phase unblocked.
  </done>
  <resume-signal>Type "approved" once AC1-AC12 + AC-MT are all green with fresh post-Phase-12 evidence captured, references/lighthouse-final.html all four >= 90, the post-Phase-12 green-deploy commit SHA is recorded, and 11-SUMMARY.md is reconciled — OR describe which AC is red and the escalation taken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator runner → prod API | The gate POSTs (AC9/AC12) and the operator's AC3/AC4/AC-MT submissions hit the live `/api/signup` and `/api/save-selection` against the production DB |
| operator → committed evidence | Screenshots + the per-AC table (containing John's `+ac`/`+acmt` Gmail addresses + a pasted prod session cookie) flow into the repo / are pasted at the prompt |
| GitHub Actions → droplet | The post-Phase-12 deploy commit + any D-01 fix commit triggers an outward redeploy to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | gate run mutating prod beyond the intended test rows | mitigate | Only AC3 (3 rows), AC4 (1 row), AC-MT (1 row: `johnturner+acmt@gmail.com`) write — all D-04 `+ac` +tag emails reachable by 11-05's scoped `%+ac%@gmail.com` cleanup; AC9/AC12 write no row by design; AC-MT's /api/save-selection write is one user_teams row-set under that same `+ac` email; AC1/2/5/6/7 are read-only GETs; no DB mutation off the droplet |
| T-11-06-02 | Information disclosure | a prod session cookie + John's emails pasted into the gate prompt / evidence | mitigate | The pasted session cookie is captured via readline into a transient process variable used only for the AC-MT fetch and is NOT written to the evidence file (evidence records the email/slugs/verdict only — not the cookie); the only emails are John's own Gmail `+tag` addresses (operator's own inbox, not third-party PII); screenshots are the deliberate audit trail (Phase-10 evidence precedent); the AC-MT test row is deleted by 11-05's cleanup |
| T-11-06-03 | Spoofing / Elevation | AC-MT save with an operator-pasted session cookie | accept | The cookie is the operator's own legitimately-obtained magic-link session for their own `+acmt` test email on prod; it grants only that email's /manage scope; HMAC `purpose=session` claim prevents cross-purpose replay (CLAUDE.md token model); no privilege boundary is crossed beyond a test row the cleanup removes |
| T-11-06-04 | Repudiation | unclear which commit was verified (stale 8092814 vs post-Phase-12) | mitigate | The exact post-Phase-12 green-deploy commit SHA is recorded in 11-06-SUMMARY and explicitly noted to SUPERSEDE the stale 11-03 SHA 8092814; it is the only SHA 11-04 may tag (D-07 binding); 11-SUMMARY.md reconciliation records the supersession |
| T-11-06-05 | Denial of service | a D-01 redeploy bricking prod mid-gate | mitigate | GitHub Actions deploy includes a 5x curl-200 CI smoke + `RestartSec=2`; a red Deploy run blocks the gate (Task 2 step 1 gate); D-01 caps in-phase fixes to single trivial commits — anything larger escalates rather than risking prod |
| T-11-06-SC | Tampering | npm/pip/cargo installs | mitigate | Task 1 adds NO package.json dependency. Only `npx @puppeteer/browsers install chrome-headless-shell@stable` (Phase-6-vetted binary into the gitignored dir) for AC3/AC8 and the existing `npm install --no-save puppeteer-core` AC3 self-bootstrap (already vetted in 11-03, package.json untouched); AC-MT itself uses only `fetch` — no new install in its path |
</threat_model>

<verification>
- GitHub Actions Deploy run for the post-Phase-12 + AC-MT commit is GREEN; the
  new green-deploy SHA is recorded and noted to supersede the stale 8092814.
- `npm run smoke:gate` per-AC table: AC1-AC12 + AC-MT all GREEN on post-Phase-12 prod.
- references/lighthouse-final.html freshly regenerated; all four categories >= 90.
- evidence/AC-MT-multi-team.txt records the save+read-back + the 12-VERIFICATION
  cron-fan-out citation; every AC1-AC12 has a fresh artifact (stale 11-03 set superseded).
- 11-SUMMARY.md reconciled (no longer claims blocked-on-missing-multi-team).
- Any unfixable-small AC reported RED + escalated (D-01 boundary honored).
- `node --check scripts/launch-gate.mjs` exits 0; AC1-AC12 logic unchanged; no new dep.
</verification>

<success_criteria>
AC1-AC12 PLUS the new AC-MT multi-team /manage check are all verified green on
post-Phase-12 `https://oddlympics.app` via the single `npm run smoke:gate`
command, references/lighthouse-final.html is freshly regenerated with all four
categories >= 90, the operator-gated ACs (AC4 < 60s Gmail loop + unsubscribe,
AC10, AC11, AC-MT multi-team save+read-back, opengraph.xyz) are confirmed with
captured evidence that supersedes the stale pre-Phase-12 11-03 set, the
post-Phase-12 green-deploy commit SHA is recorded (superseding the stale
8092814) for 11-04's tag, 11-SUMMARY.md is reconciled to reflect that multi-team
shipped+verified and the phase is unblocked, any small failing AC was closed via
one atomic commit + redeploy + single re-run (D-01) or escalated RED, and the
launch-gate.mjs AC-MT extension stayed a single bounded task with no new
dependency.
</success_criteria>

<output>
Create `.planning/phases/11-end-to-end-launch-gate/11-06-SUMMARY.md` when done.
MUST record: the post-Phase-12 green-deploy commit SHA (explicitly noted to
SUPERSEDE the stale 11-03 SHA 8092814 — the only SHA 11-04 may tag), the per-AC
PASS/FAIL/OPERATOR table including AC-MT, any D-01 fix taken, the AC-MT
save+read-back evidence + the 12-VERIFICATION cron-fan-out citation, the D-03
documented Playwright-wording deviation (inherited), and that 11-SUMMARY.md was
reconciled. This plan's completion is the precondition for 11-04 (release tag).
</output>
