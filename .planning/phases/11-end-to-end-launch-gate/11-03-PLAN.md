---
phase: 11-end-to-end-launch-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01-contrast-fix
  - 11-02-gate-runner-and-cleanup-script
files_modified:
  - .planning/phases/11-end-to-end-launch-gate/evidence/
  - references/lighthouse-final.html
autonomous: false

requirements: []
user_setup: []

must_haves:
  truths:
    - "AC1: GET https://oddlympics.app renders and contains \"Your team's matches\" (>= 1) — the hard AC1 gate; the references/landing_preview.png visual diff is best-effort/N-A if the asset is absent (do NOT block launch on a missing reference image)"
    - "AC2: the live rendered <select> on prod exposes exactly 48 <option> values set-equal to references/teams.json slugs"
    - "AC3: puppeteer-core in America/Detroit, Europe/London, Africa/Lagos against prod asserts the rendered #timezone value === the IANA string and #tz-label === the expected city label (D-03; documented Playwright-wording deviation recorded)"
    - "AC4: a real John's-Gmail (johnturner+ac4@gmail.com) signup from a fresh browser profile delivers a correctly-rendered confirmation email naming team + timezone within 60s, and that email's unsubscribe link returns the user to /unsubscribed and removes them from active sending (ROADMAP SC3; builds on the Phase-10 deliverability baseline)"
    - "AC5: GET /privacy and GET /terms return 200 with canonical copy and a last-updated date matching the deploy date (verified on prod as part of the gate sweep)"
    - "AC6: https://oddlympics.app/og-image.png returns 200, image/png, exactly 1200x630, < 300KB; AND the opengraph.xyz preview for https://oddlympics.app shows headline/banner/URL (done-def #4 operator evidence captured)"
    - "AC7: zero prohibited terms (bitcoin|lightning|crypto|world domination|personal olympics, case-insensitive) across / , /privacy , /terms , /manage on prod"
    - "AC8: a fresh prod Lighthouse mobile run is saved to EXACTLY references/lighthouse-final.html and shows Performance, Accessibility, Best Practices, SEO ALL >= 90 (SC2 / done-def #5; the D-02 contrast fix from 11-01 must be deployed first)"
    - "AC9: POST /api/signup team=fake_team -> 303 /?error=bad-form, no row (asserted via the redirect contract alone)"
    - "AC10: an existing pre-milestone backfilled subscriber row loads on /manage, sees the one-time banner, and can save a team without errors (operator-verified)"
    - "AC11: a form submission fires a Signup Submit Plausible event with the team prop populated, visible in the Plausible dashboard (operator-verified; if drop rate is unacceptable apply the Phase-6-noted preventDefault+setTimeout swap as a D-01 small in-phase fix)"
    - "AC12: programmatic POST /api/signup with the website honeypot set -> 303 /pending, no row (redirect contract alone)"
    - "D-01: bounded fix-and-reverify is the gate model — a small failing AC (a CSS value, a one-line preventDefault, a copy/header nit) is fixed in one atomic commit -> GitHub Actions redeploy -> single re-run of npm run smoke:gate; any failure needing its own plan/investigation escalates to a separate /gsd-quick or fix phase and the AC is reported RED with its evidence (the phase boundary stays bounded)"
    - "D-04: AC3 + AC4 prod submissions use John's-Gmail +tag addresses (johnturner+ac3-detroit / +ac3-london / +ac3-lagos / +ac4 @gmail.com) so every gate row is reachable by the D-05 cleanup; AC9/AC12 +ac emails write no row by design"
    - "D-06: the entire sweep runs via the single `npm run smoke:gate` command emitting one per-AC PASS/FAIL/OPERATOR table; evidence captured under the phase evidence/ dir + references/lighthouse-final.html"
  artifacts:
    - path: "references/lighthouse-final.html"
      provides: "Fresh prod Lighthouse mobile report, all four categories >= 90 (SC2 / done-def #5)"
    - path: ".planning/phases/11-end-to-end-launch-gate/evidence/"
      provides: "Per-AC PASS evidence (gate table, AC3 puppeteer logs, AC4/AC10/AC11 screenshots, opengraph.xyz preview)"
  key_links:
    - from: "operator + npm run smoke:gate"
      to: "https://oddlympics.app (prod)"
      via: "scripts/launch-gate.mjs against the green-deploy commit"
      pattern: "smoke:gate"
    - from: "AC8"
      to: "references/lighthouse-final.html"
      via: "lighthouse --preset=mobile via chrome-headless-shell"
      pattern: "lighthouse-final\\.html"
---

<objective>
The launch gate itself. Deploy 11-01's contrast fix + 11-02's gate runner to
production via GitHub Actions, then have the operator run `npm run smoke:gate`
against `https://oddlympics.app`, perform the operator-gated ACs (AC4 real
Gmail loop, AC10 backfilled-row, AC11 Plausible, opengraph.xyz preview),
capture per-AC evidence, and drive the D-01 bounded fix-and-reverify loop until
all twelve ACs are green.

Purpose: This is ROADMAP Phase-11 SC1 (per-AC evidence on prod), SC2
(`references/lighthouse-final.html` all >= 90), and SC3 (John's-Gmail loop < 60s
+ unsubscribe). It proves Phases 5-10 landed. Per D-01 the phase MAY carry
SMALL AC-closing fixes in-phase; anything larger escalates and the AC is
reported red.

Output: A green per-AC PASS table, `references/lighthouse-final.html` (all four
>= 90), per-AC evidence under the phase `evidence/` dir, and a recorded
green-deploy commit SHA for the 11-04 tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md
@.planning/phases/11-end-to-end-launch-gate/11-PATTERNS.md
@.planning/phases/10-confirmation-email-update/10-SUMMARY.md
@.planning/phases/11-end-to-end-launch-gate/11-01-SUMMARY.md
@.planning/phases/11-end-to-end-launch-gate/11-02-SUMMARY.md

<interfaces>
<!-- The deploy + gate mechanism (Phase-10 Wave-3 operator precedent). -->
.github/workflows/deploy.yml — push to main -> rsync + `npm rebuild better-sqlite3`
  + `systemctl restart oddlympics` + 5x curl-200 (~47s). The GREEN run of this
  defines the D-07 tag commit (used by 11-04).
npm run smoke:gate            — the single re-runnable D-06 command (from 11-02)
references/lighthouse-final.html — AC8 artifact path (SC2 / done-def #5)
.planning/phases/11-end-to-end-launch-gate/evidence/ — per-AC evidence dir
Phase-10 baseline (10-SUMMARY.md): 10/10 Mail-Tester, Gmail+Proton render proof
  from prod sender hello@oddlympics.app — AC4's deliverability baseline; AC4
  here is the independent full confirm->manage->unsubscribe loop (ROADMAP SC3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy 11-01 + 11-02 to production and record the green-deploy commit</name>
  <files>(no source files — deploy + verification only)</files>
  <read_first>
    - .planning/phases/11-end-to-end-launch-gate/11-01-SUMMARY.md (confirm the D-02 contrast fix landed)
    - .planning/phases/11-end-to-end-launch-gate/11-02-SUMMARY.md (confirm launch-gate.mjs + cleanup-gate-rows.mjs + aliases landed)
    - .github/workflows/deploy.yml (the push-to-main -> rsync + rebuild + restart + 5x curl-200 mechanism)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-01 redeploy via GitHub Actions; D-07 "green-deploy commit" definition)
  </read_first>
  <action>
    Ensure 11-01 (contrast fix) and 11-02 (gate runner + cleanup script + npm aliases) are committed and pushed to `main`. Confirm the GitHub Actions **Deploy** workflow run for that commit completed green (rsync + `npm rebuild better-sqlite3` + `systemctl restart oddlympics` + the 5x curl-200 CI smoke all pass) — use `gh run list --workflow=deploy.yml` / `gh run view <id>` to confirm. Record the exact `main` commit SHA whose Deploy run is green (this is the D-07 "green-deploy commit" 11-04 will tag, and the state the gate verifies). Spot-check prod is live: `curl -sI https://oddlympics.app/` returns HTTP 200 and the consumer landing is served (the D-02 deepened accent is in the served `dist/client/index.html` — `grep #b8350d`). If the Deploy run is red, this is a deploy-infra failure (not an AC) — resolve the deploy before proceeding to the gate run; do NOT run the gate against a stale or failed deploy.
  </action>
  <verify>
    <automated>curl -sI https://oddlympics.app/ | grep -E 'HTTP/.* 200' && curl -sL https://oddlympics.app/ | grep -c '#b8350d'</automated>
  </verify>
  <acceptance_criteria>
    - The GitHub Actions Deploy run for the pushed commit is green (rsync + rebuild + restart + 5x curl-200 all passed) — confirmed via `gh run view`
    - The exact green-deploy `main` commit SHA is recorded (carried into 11-03-SUMMARY for 11-04's tag)
    - `curl -sI https://oddlympics.app/` returns `HTTP/.. 200`
    - `curl -sL https://oddlympics.app/` body contains `#b8350d` (the 11-01 D-02 contrast fix is live on prod — confirms AC8 runs against the fixed surface)
  </acceptance_criteria>
  <done>
    11-01 + 11-02 are deployed; the Deploy workflow ran green; the green-deploy
    commit SHA is recorded for 11-04; prod returns 200 and serves the deepened
    `#b8350d` accent.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator gate run + per-AC evidence + bounded fix-and-reverify (AC1-AC12, D-01, D-04, D-06)</name>
  <read_first>
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-01 bounded fix-and-reverify; D-04 +tag emails; D-06 single command + per-AC table + evidence dir; the Deferred note that AC1 visual-diff is best-effort/N-A on a missing reference asset)
    - MILESTONE-consumer-landing.md §"Acceptance criteria (UAT)" + §"Done definition" (AC1-AC12 exact verification commands; done-def: all AC pass, real Gmail < 60s, opengraph.xyz preview, references/lighthouse-final.html all >= 90)
    - .planning/REQUIREMENTS.md §"Acceptance criteria (milestone-level)" AC1-AC12 table
    - .planning/phases/10-confirmation-email-update/10-SUMMARY.md §"Hand-off to Phase 11" (AC4 deliverability baseline; Outlook out of scope — Gmail+Proton is the v2.0 standard)
    - .planning/STATE.md (Accumulated Context > Decisions — the Phase-6 fire-and-forget Plausible-listener note: if AC11 drop rate is unacceptable, swap to preventDefault + setTimeout(form.submit,0) — this is exactly a D-01 small in-phase fix)
  </read_first>
  <what-built>
    11-01's contrast fix and 11-02's `scripts/launch-gate.mjs` +
    `scripts/cleanup-gate-rows.mjs` + `smoke:gate`/`cleanup:gate` aliases are
    deployed to production (Task 1). The gate runner automates AC1/AC2/AC6/AC7/
    AC9/AC12, drives AC3 (3 locales via puppeteer-core, pinned #timezone/#tz-label
    assertion), shells AC8 to `references/lighthouse-final.html` (four-category
    >= 90 assertion), and prompts+captures operator evidence for AC4/AC10/AC11 +
    opengraph.xyz, emitting one per-AC PASS/FAIL/OPERATOR table.
  </what-built>
  <how-to-verify>
    1. One-time (if not already present from Phase 6): `npx @puppeteer/browsers install chrome-headless-shell@stable` (lands in the gitignored `./chrome-headless-shell/`). Set `CHROME_PATH` to that binary.
    2. Run the gate against prod: `npm run smoke:gate` (default target `https://oddlympics.app`; the runner emits the per-AC PASS/FAIL/OPERATOR table and writes per-AC artifacts to `.planning/phases/11-end-to-end-launch-gate/evidence/`).
    3. AUTOMATED ACs (AC1, AC2, AC6, AC7, AC9, AC12) + AC3 (3-locale puppeteer) + AC8 (lighthouse -> `references/lighthouse-final.html`, four categories >= 90): read the per-AC table. AC1 hard gate = the `Your team's matches` grep + render PASS (the `references/landing_preview.png` visual diff is best-effort — if the reference asset is absent, record AC1 visual-diff N/A with a one-line note; do NOT block on it). Confirm `references/lighthouse-final.html` exists and shows Performance / Accessibility / Best Practices / SEO all >= 90.
    4. AC5: confirm `curl -s -o /dev/null -w '%{http_code}' https://oddlympics.app/privacy` and `.../terms` both return `200` and the served copy's last-updated date matches the deploy date (the gate runner's AC7 fetch already pulls both bodies — read the captured `/privacy` + `/terms` evidence).
    5. AC4 (OPERATOR — ROADMAP SC3): from a FRESH browser profile, submit the prod form with team=England, email `johnturner+ac4@gmail.com`. Time it: the confirmation email must arrive in John's Gmail within 60s, name England + the timezone, render cleanly (Phase-10 Gmail baseline). Click the unsubscribe link in that email — confirm it lands on `/unsubscribed` and the row is removed from active sending. Screenshot the inbox (timestamp visible) + the `/unsubscribed` page into the evidence dir; paste the path when the runner prompts.
    6. AC10 (OPERATOR): sign in to `/manage` as an existing pre-milestone backfilled subscriber (team=NULL, timezone='America/New_York'); confirm the one-time "Pick a team" banner shows, save a team, confirm no error and the banner dismisses. Screenshot -> evidence dir; paste path.
    7. AC11 (OPERATOR): submit a form (the AC4 submission counts) and confirm a `Signup Submit` event with the `team` prop populated appears in the Plausible dashboard. Screenshot the dashboard event -> evidence dir; paste path/URL. If the dashboard shows an UNACCEPTABLE drop rate, that is a D-01 small in-phase fix: apply the Phase-6-noted `preventDefault + setTimeout(form.submit, 0)` swap to the Plausible submit listener (one atomic commit), let GitHub Actions redeploy, and re-run `npm run smoke:gate` + re-verify AC11.
    8. opengraph.xyz (done-def #4): open opengraph.xyz for `https://oddlympics.app`, confirm the card shows the headline + banner + URL cleanly. Screenshot -> evidence dir; paste path.
    9. D-01 BOUNDED FIX-AND-REVERIFY: for any FAIL that is SMALL (a CSS value, a one-line `preventDefault`, a copy/header nit — itself a single trivial change): make ONE atomic commit, push to `main`, let GitHub Actions Deploy go green, re-run `npm run smoke:gate` (and re-do the affected operator step). Re-record the new green-deploy commit SHA (it supersedes Task 1's for 11-04's tag). For any FAIL large enough to need its own plan or investigation: STOP, report that AC **RED** with its captured evidence, escalate to a separate `/gsd-quick` / fix phase, and do NOT silently patch — the phase boundary stays bounded (D-01).
    10. Final state required to pass this checkpoint: the per-AC table shows all twelve AC1-AC12 GREEN (PASS or OPERATOR-CONFIRMED), `references/lighthouse-final.html` shows all four >= 90, every AC has captured evidence in the evidence dir, and the final green-deploy commit SHA is recorded for 11-04.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run smoke:gate` emits a per-AC table with AC1-AC12 all GREEN (PASS for automated, OPERATOR-CONFIRMED for AC4/AC10/AC11 + opengraph.xyz) and exits 0 on the automated set
    - `references/lighthouse-final.html` exists and shows Performance, Accessibility, Best Practices, SEO ALL >= 90
    - AC1 `curl -sL https://oddlympics.app | grep -c "Your team's matches"` returns >= 1 (hard gate); visual-diff recorded PASS or N/A-missing-asset (never a launch blocker)
    - AC4: a `johnturner+ac4@gmail.com` signup from a fresh profile delivered a correctly-rendered England+tz email within 60s and its unsubscribe link reached `/unsubscribed` and removed the row — screenshot evidence committed
    - AC5: `/privacy` + `/terms` both return 200 with canonical copy + matching last-updated date — evidence captured
    - AC6: og-image 200/image-png/1200x630/<300KB PASS in the table AND an opengraph.xyz preview screenshot is captured
    - AC10 + AC11 operator screenshots are in the evidence dir; if AC11 needed the preventDefault+setTimeout swap it was a single atomic commit + redeploy + re-run (D-01)
    - Every AC1-AC12 has a corresponding artifact under `.planning/phases/11-end-to-end-launch-gate/evidence/`
    - The final green-deploy commit SHA (post any D-01 fix) is recorded in 11-03-SUMMARY for 11-04's tag
    - Any AC that could NOT be made green with a small in-phase fix is reported RED with evidence and escalated (not silently patched) — D-01 boundary honored
  </acceptance_criteria>
  <resume-signal>Type "approved" once all twelve ACs are green with captured evidence and the final green-deploy commit SHA is recorded, OR describe which AC is red and the escalation taken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator runner → prod API | The gate POSTs (AC9/AC12) and the operator's AC3/AC4 submissions hit the live `/api/signup` against the production DB |
| operator → committed evidence | Screenshots + the per-AC table (containing John's +tag email addresses) are committed to the repo |
| GitHub Actions → droplet | A D-01 fix commit triggers an outward redeploy to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | gate run mutating prod beyond the intended test rows | mitigate | Only AC3 (3 rows) + AC4 (1 row) write, all via D-04 `+ac` +tag emails reachable by the 11-04/11-05 scoped cleanup; AC9/AC12 write no row by design; AC1/2/5/6/7 are read-only GETs; no DB mutation off the droplet |
| T-11-03-02 | Information disclosure | real email addresses / prod state in committed evidence | accept | The only emails are John's own Gmail `+tag` addresses (operator's own inbox, not third-party PII); screenshots are the deliberate audit trail (Phase-10 evidence precedent); no secrets/tokens captured |
| T-11-03-03 | Denial of service | a D-01 redeploy bricking prod mid-gate | mitigate | GitHub Actions deploy includes a 5x curl-200 CI smoke + `RestartSec=2`; a red Deploy run blocks the gate (Task 1 gate); D-01 caps in-phase fixes to single trivial commits — anything larger escalates rather than risking prod |
| T-11-03-04 | Repudiation | unclear which commit was verified | mitigate | The exact green-deploy commit SHA is recorded in 11-03-SUMMARY and is the only SHA 11-04 may tag (D-07 binding) |
| T-11-03-SC | Tampering | npm/pip/cargo installs | mitigate | Only `npx @puppeteer/browsers install chrome-headless-shell@stable` (Phase-6-vetted binary into the gitignored dir); no package.json dependency change; no third-party install in the gate path |
</threat_model>

<verification>
- GitHub Actions Deploy run for the gate commit is green; green-deploy SHA recorded.
- `npm run smoke:gate` per-AC table: AC1-AC12 all GREEN.
- `references/lighthouse-final.html` exists; all four categories >= 90.
- Every AC has an artifact in `.planning/phases/11-end-to-end-launch-gate/evidence/`.
- Any unfixable-small AC reported RED + escalated (D-01 boundary honored).
</verification>

<success_criteria>
All twelve AC1-AC12 are verified green on `https://oddlympics.app` with per-AC
evidence captured, `references/lighthouse-final.html` shows all four categories
>= 90, the operator-gated ACs (AC4 < 60s Gmail loop + unsubscribe, AC10, AC11,
opengraph.xyz) are confirmed with screenshots, any small failing AC was closed
via one atomic commit + redeploy + single re-run (D-01) or escalated red, and
the final green-deploy commit SHA is recorded for the 11-04 tag.
</success_criteria>

<output>
Create `.planning/phases/11-end-to-end-launch-gate/11-03-SUMMARY.md` when done.
MUST record: the green-deploy commit SHA, the per-AC PASS/FAIL table, any D-01
fix taken, and the D-03 documented Playwright-wording deviation.
</output>
