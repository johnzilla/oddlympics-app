---
phase: 11-end-to-end-launch-gate
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04-release-tag
files_modified: []
autonomous: false

requirements: []
user_setup: []

must_haves:
  truths:
    - "D-05: the D-04 gate-test rows (johnturner+ac*@gmail.com) are removed from the production vip_signups table via `scripts/cleanup-gate-rows.mjs --confirm`, run by the operator on the droplet against DATABASE_PATH"
    - "D-05: the cleanup is run AFTER the v1.0-consumer-landing tag is pushed (11-04) so the verified gate rows existed on the tagged state and through any re-verify"
    - "D-05: a dry-run (no --confirm) is performed FIRST and the printed row set is reviewed before the --confirm execute, per the dry-run-by-default destructive-script convention; the script is idempotent so a confirming re-run is a safe no-op"
    - "D-04: only the +ac +tag gate rows match the scoped LIKE pattern (%+ac%@gmail.com) — real John's-Gmail subscriber rows are NOT in scope and are not deleted"
  artifacts: []
  key_links:
    - from: "operator on droplet"
      to: "prod vip_signups (DATABASE_PATH)"
      via: "node scripts/cleanup-gate-rows.mjs --confirm"
      pattern: "cleanup-gate-rows\\.mjs --confirm"
---

<objective>
The final operator step (D-05). After the `v1.0-consumer-landing` tag is pushed
(11-04), the operator runs `scripts/cleanup-gate-rows.mjs` on the droplet —
dry-run first to review the row set, then `--confirm` to delete the D-04
gate-test rows from the production `vip_signups` table.

Purpose: D-05 — keep the production subscriber table clean of the gate-test
`+tag` rows after the launch gate, without disturbing real subscribers.
Ordering is load-bearing: this runs AFTER the tag (D-05/D-07) so the verified
rows survive on the tagged state and through any re-verify.

Output: The `%+ac%@gmail.com` gate rows deleted from prod; the cleanup recorded
as the final operator step in the phase summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md
@.planning/phases/11-end-to-end-launch-gate/11-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The script + operator mechanism. -->
scripts/cleanup-gate-rows.mjs — built in 11-02: dry-run-by-default, `--confirm`
  to execute, idempotent no-op on re-run, opens better-sqlite3 WRITABLE via
  `DATABASE_PATH` (default ./data/oddlympics.db), `DELETE FROM vip_signups
  WHERE email LIKE '%+ac%@gmail.com'` (scoped to the D-04 +ac +tag rows only —
  never the bare domain).
Operator mechanism (CLAUDE.md §Production / DEPLOY.md Day-2 DB-inspection):
  the cleanup runs on the droplet as the `oddlympics` user against the live DB
  path — same mechanism as backup-pre-05.mjs. The deploy user's sudoers is
  systemctl-only; DB scripts run directly as the app user.
11-04-SUMMARY.md — confirms the v1.0-consumer-landing tag is pushed (the
  precondition: cleanup runs AFTER the tag).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator post-tag cleanup of the D-04 gate rows (D-05)</name>
  <read_first>
    - .planning/phases/11-end-to-end-launch-gate/11-04-SUMMARY.md (CONFIRM the v1.0-consumer-landing tag is pushed — the precondition; cleanup runs AFTER the tag)
    - scripts/cleanup-gate-rows.mjs (the exact dry-run / --confirm / idempotent behavior + the %+ac%@gmail.com scoped LIKE pattern + DATABASE_PATH override — built in 11-02)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-05 — dry-run-by-default, --confirm, idempotent, operator-on-droplet, runs after the tag; D-04 +tag contract)
    - CLAUDE.md §"Production" §"Dry-run-by-default safety pattern for outbound side effects" (operator/app-user/writable-DB mechanism; DEPLOY.md Day-2 DB-inspection precedent)
  </read_first>
  <what-built>
    `scripts/cleanup-gate-rows.mjs` (built + deployed in 11-02): dry-run by
    default (prints the rows it WOULD delete), `--confirm` to execute,
    idempotent (re-run = no-op), opens the prod DB writable via `DATABASE_PATH`,
    deletes only `vip_signups` rows matching the scoped `%+ac%@gmail.com` LIKE
    pattern (the D-04 `+ac` +tag gate rows — never the bare domain). The
    `v1.0-consumer-landing` tag is pushed (11-04).
  </what-built>
  <how-to-verify>
    1. PRECONDITION: confirm 11-04-SUMMARY.md shows the `v1.0-consumer-landing` tag is pushed to origin. If the tag is NOT pushed, STOP — the cleanup must run AFTER the tag so the verified rows survive on the tagged state (D-05/D-07). Do not proceed.
    2. On the droplet, as the app user, in the app directory, against the production DB path: run the DRY-RUN first — `node scripts/cleanup-gate-rows.mjs` (no flag). It prints `mode=dry-run rows=<n>` and `would delete:` followed by each gate-row email. REVIEW the printed list: every line MUST be a `johnturner+ac...@gmail.com` gate address (AC3 detroit/london/lagos, AC4, plus any AC9/AC12 rows if the contract created them) — confirm NO real subscriber address is in the list. If anything unexpected appears, STOP and escalate (do NOT `--confirm`).
    3. Once the dry-run list is confirmed to contain only the D-04 gate rows, run the EXECUTE: `node scripts/cleanup-gate-rows.mjs --confirm`. It prints `mode=DELETE rows=<n>` then `done. deleted=<n>` where `<n>` equals the dry-run count.
    4. Verify idempotency: re-run `node scripts/cleanup-gate-rows.mjs --confirm`. It MUST print `nothing to do (idempotent — re-run is a no-op).` and exit 0 (zero rows now match the pattern).
    5. Spot-check prod is unaffected: `curl -sI https://oddlympics.app/` still returns 200 (the delete touched only gate rows; the app is unchanged).
    6. Record in 11-05-SUMMARY: the dry-run count, the deleted count, the idempotent-no-op confirmation, and that this ran AFTER the pushed tag (D-05/D-07 ordering honored).
  </how-to-verify>
  <acceptance_criteria>
    - 11-04-SUMMARY.md confirms the `v1.0-consumer-landing` tag is pushed (precondition met — cleanup runs strictly after the tag)
    - The dry-run was performed first and its printed `would delete:` list contained ONLY `johnturner+ac...@gmail.com` gate rows (no real subscriber address) — reviewed before `--confirm`
    - `node scripts/cleanup-gate-rows.mjs --confirm` printed `done. deleted=<n>` with `<n>` equal to the reviewed dry-run count
    - An immediate confirming re-run printed `nothing to do (idempotent — re-run is a no-op).` and exited 0 (idempotency proven)
    - `curl -sI https://oddlympics.app/` still returns HTTP 200 (prod app unaffected — only the scoped gate rows removed)
    - 11-05-SUMMARY records the dry-run count, deleted count, idempotent confirmation, and the tag-before-cleanup ordering
  </acceptance_criteria>
  <resume-signal>Type "approved" once the dry-run list was reviewed (gate rows only), `--confirm` deleted exactly that set, the idempotent re-run is a confirmed no-op, and prod still returns 200 — OR describe the unexpected row that halted the cleanup.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod DB | A destructive `DELETE` against the production `vip_signups` table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering / Denial | accidental mass-delete of real subscribers | mitigate | Dry-run-by-default is the primary control: the operator REVIEWS the exact printed row set before `--confirm`; the LIKE pattern is hard-scoped to `%+ac%@gmail.com` (the `+ac` infix excludes real subscribers — verified absent-of-bare-domain in 11-02); SELECT-before-delete prints every affected row; explicit "STOP if any non-gate address appears" instruction |
| T-11-05-02 | Repudiation | cleanup run before the tag (verified rows lost from tagged state) | mitigate | Hard precondition gate: 11-04-SUMMARY must confirm the pushed tag before any cleanup command; wave ordering (11-05 is wave 4, after 11-04 wave 3) enforces it |
| T-11-05-03 | Denial of service | DB corruption / app disruption from the write | mitigate | better-sqlite3 single parameterized `DELETE` (no interpolation); idempotent re-run is a safe no-op; post-cleanup `curl` 200 spot-check confirms the app is unaffected; DigitalOcean Backups enabled (v1 HARDEN-05 replacement) is the recovery backstop |
| T-11-05-04 | Information disclosure | gate-row emails surfaced during cleanup | accept | The printed rows are John's own Gmail `+tag` addresses (operator's own inbox); recorded counts in the summary, not the raw address list |
| T-11-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — runs the already-committed `scripts/cleanup-gate-rows.mjs` (built + reviewed in 11-02) with no new deps |
</threat_model>

<verification>
- 11-04 tag-pushed precondition confirmed.
- Dry-run reviewed: only `johnturner+ac...@gmail.com` rows listed.
- `--confirm` deleted exactly the reviewed count; confirming re-run is a no-op.
- Prod still returns HTTP 200.
- Summary records counts + the tag-before-cleanup ordering.
</verification>

<success_criteria>
The D-04 gate-test rows are removed from the production `vip_signups` table via
a reviewed dry-run then `--confirm`, the delete was scoped to the
`%+ac%@gmail.com` rows only (no real subscriber touched), an immediate re-run
is a confirmed idempotent no-op, prod is unaffected, and the cleanup ran
strictly after the pushed `v1.0-consumer-landing` tag.
</success_criteria>

<output>
Create `.planning/phases/11-end-to-end-launch-gate/11-05-SUMMARY.md` when done.
This is the FINAL operator step — record dry-run/deleted counts + idempotent
no-op + tag-before-cleanup ordering, and that the phase is complete.
</output>
