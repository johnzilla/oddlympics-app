---
phase: 11-end-to-end-launch-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "D-02: index.astro banner-pill accent is deepened from #d94a1f to #b8350d and the submit button to #c43d15, both clearing WCAG-AA >= 4.5:1 (closes the Phase-6 A11y-94 contrast hits ahead of the AC8 prod gate)"
    - "D-02: the change is color-value-only — markup, OG meta tags, inline JS, and the deferred Layout.astro refactor are untouched (one focused commit)"
    - "AC7/LAND-02: index.astro still contains zero prohibited terms after the edit (color value only — it does)"
    - "AC8: the deepened contrast removes the two serious WCAG-AA Lighthouse hits (banner 3.6:1, button 4.24:1) that held Phase-6 A11y at 94, de-risking the prod Lighthouse run against run-to-run variance"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Deepened accent token (#b8350d) + submit-button override (#c43d15) + tracked focus-ring colors"
      contains: "#b8350d"
  key_links:
    - from: "src/pages/index.astro :root --accent"
      to: ".banner pill + every --accent consumer"
      via: "CSS custom property at line ~260"
      pattern: "--accent:\\s*#b8350d"
    - from: "src/pages/index.astro button[type=\"submit\"]"
      to: "submit button background + its focus ring"
      via: "direct #c43d15 override + rgba(196,61,21,...) focus ring"
      pattern: "#c43d15"
---

<objective>
Land the diagnosed Phase-6 accessibility contrast fix (D-02) as the FIRST
Phase-11 change, before the gate run. Deepen the two accent values in
`src/pages/index.astro`'s inline `<style is:global>` so the banner pill and the
submit button both clear WCAG-AA >= 4.5:1.

Purpose: Phase-6 Lighthouse scored A11y 94 with exactly two serious
color-contrast hits — the banner pill (3.6:1) and the submit button (4.24:1).
AC8 (Lighthouse mobile >= 90 on prod) is a hard Phase-11 gate. Landing this
proactively as its own commit removes a known redeploy+rerun round-trip and
de-risks AC8 against prod Lighthouse run-to-run variance (D-02 rationale).

Output: One focused, color-only commit to `src/pages/index.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md
@.planning/phases/11-end-to-end-launch-gate/11-PATTERNS.md

<interfaces>
<!-- Exact CSS lines the executor edits (grep-verified from src/pages/index.astro). -->
<!-- Do NOT explore the file beyond these — the edit is color-value-only. -->

src/pages/index.astro:260   --accent: #d94a1f;          (the :root accent token)
src/pages/index.astro:261   --accent-ink: #ffffff;       (leave unchanged)
src/pages/index.astro:262   --accent-soft: #fbe9e0;      (leave unchanged)
src/pages/index.astro:306-308  .banner uses color/border var(--accent) + background var(--accent-soft)
src/pages/index.astro:384   box-shadow: 0 0 0 3px rgba(217, 74, 31, 0.15);   (email input focus ring)
src/pages/index.astro:391   button[type="submit"] {                          (submit button rule opens)
src/pages/index.astro:398   background: var(--accent);                       (submit button background)
src/pages/index.astro:406-408  button[type="submit"]:focus-visible { ... box-shadow: 0 0 0 3px rgba(217, 74, 31, 0.35); }
src/pages/index.astro:559   box-shadow: 0 0 0 3px rgba(217, 74, 31, 0.15);   (FAQ/details focus ring)
src/pages/index.astro:586   box-shadow: 0 0 0 3px rgba(217, 74, 31, 0.15);   (mobile focus ring)

RGB equivalences (for the focus-ring updates):
  #d94a1f = rgb(217, 74, 31)   (old accent — being replaced)
  #b8350d = rgb(184, 53, 13)   (new banner/global accent)
  #c43d15 = rgb(196, 61, 21)   (new submit-button accent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen the two accent values + track the focus rings (D-02)</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (the inline `<style is:global>` block — read lines 255-410 and 555-590 to confirm the exact tokens/rules below; do NOT read or touch markup, the `<head>` meta block, or any `<script>` block)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-02 — the locked decision; the Deferred Ideas note that the Layout.astro refactor must NOT be bundled)
    - .planning/phases/11-end-to-end-launch-gate/11-PATTERNS.md (`src/pages/index.astro` section — the two-distinct-values requirement, the ASCII-apostrophe / no-U+2019 constraint, the "color edit ONLY" structure constraint)
    - .planning/STATE.md (Accumulated Context > Decisions — the Phase-6 Lighthouse note: banner 3.6:1 + button 4.24:1 serious WCAG-AA hits; cheapest fix is #d94a1f->#b8350d / button #c43d15)
  </read_first>
  <action>
    Make exactly four color-value edits to the inline `<style is:global>` block of `src/pages/index.astro`, nothing else:
    1. Line ~260: change `--accent: #d94a1f;` to `--accent: #b8350d;`. This deepens the banner pill (and every other `--accent` consumer — color/border at lines 306-308, link accents, etc.) to clear WCAG-AA >= 4.5:1. Leave `--accent-ink` (#ffffff) and `--accent-soft` (#fbe9e0) unchanged — the banner pill background stays `--accent-soft` and only its text/border use `--accent`, which is what the contrast audit measured.
    2. On the `button[type="submit"]` rule (the `background: var(--accent);` declaration at line ~398): add a direct override so the submit button uses `#c43d15` independently of the global token. Per D-02 two distinct darkened values are intended (banner `#b8350d`, button `#c43d15`); use a direct `background: #c43d15;` on the submit-button rule (the existing `var(--accent)` line becomes/yields `#c43d15`). Do NOT introduce a project-wide token rename — keep the change local to this rule (Phase-6 structure-lock: minimal surface).
    3. Line ~408: the submit-button `:focus-visible` ring `box-shadow: 0 0 0 3px rgba(217, 74, 31, 0.35);` must track the new BUTTON color — change it to `rgba(196, 61, 21, 0.35)` (the RGB of `#c43d15`, alpha unchanged).
    4. The three non-button focus rings that hardcode the old accent RGB — line ~384 (email input focus), line ~559, line ~586 — must track the new GLOBAL accent: change each `rgba(217, 74, 31, 0.15)` to `rgba(184, 53, 13, 0.15)` (the RGB of `#b8350d`, alpha unchanged) so the focus styling stays visually consistent with the deepened `--accent` and no stale `217, 74, 31` value remains in the file.
    Do NOT touch: any markup/structure, the `<head>` OG/Twitter/meta tags (hardcoded `https://oddlympics.app`, Phase-6 D-08 lock), any `<script>` block (Plausible listener, tz-label swap), `--accent-ink`, `--accent-soft`, or anything outside the inline `<style>`. Do NOT introduce U+2019; this edit adds zero text. Do NOT bundle the deferred Layout.astro refactor (standing v1.1 deferral — one focused color-only commit per D-02).
  </action>
  <verify>
    <automated>npm run build && grep -c '#b8350d' src/pages/index.astro && grep -c '#c43d15' src/pages/index.astro && ! grep -nE '217,\s*74,\s*31|#d94a1f' src/pages/index.astro && ! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' dist/client/index.html && ! grep -nP '\xe2\x80\x99' src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#b8350d' src/pages/index.astro` returns >= 1 (new global accent present)
    - `grep -c '#c43d15' src/pages/index.astro` returns >= 1 (new submit-button accent present)
    - `grep -nE '217,\s*74,\s*31|#d94a1f' src/pages/index.astro` returns NOTHING (zero stale old-accent values — old hex and old RGB both gone)
    - `npm run build` succeeds and produces `dist/server/entry.mjs` + `dist/client/index.html`
    - LAND-02 grep on `dist/client/index.html` returns zero hits (AC7 still green after the edit — color value only)
    - U+2019 negative grep on `src/pages/index.astro` returns nothing (ASCII-apostrophe convention preserved; no curly quote introduced)
    - `git diff --stat` shows ONLY `src/pages/index.astro` changed (one focused commit — no Layout.astro extraction, no markup churn)
  </acceptance_criteria>
  <done>
    `src/pages/index.astro` has `--accent: #b8350d`, a `#c43d15` submit-button
    background, the submit-button focus ring at `rgba(196, 61, 21, 0.35)`, the
    three other focus rings at `rgba(184, 53, 13, 0.15)`, zero remaining
    `#d94a1f` / `217, 74, 31` occurrences, build passes, LAND-02 clean, and the
    diff is limited to this one file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → public surface | Edited inline CSS is served to every visitor; a stray text change could violate LAND-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | `src/pages/index.astro` inline style | mitigate | Color-value-only edit; `git diff --stat` asserts only this file changed; LAND-02 grep on built `dist/client/index.html` in `<verify>` blocks any prohibited-term regression |
| T-11-01-02 | Information disclosure | served HTML | accept | No PII or secrets in CSS color values; deepening hex is inert |
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new deps — color edit + existing `astro build` only) |
</threat_model>

<verification>
- `npm run build` succeeds; `dist/client/index.html` produced.
- LAND-02 grep on `dist/client/index.html`: zero hits (AC7 unaffected).
- Stale-accent grep (`#d94a1f` / `217, 74, 31`) on the source: zero hits.
- New values (`#b8350d`, `#c43d15`) present in source.
- Diff limited to `src/pages/index.astro` (one focused commit; no bundled refactor).
</verification>

<success_criteria>
The two accent values are deepened to `#b8350d` (banner/global) and `#c43d15`
(submit button), all focus rings track the correct new color, no stale accent
value remains, the build passes, LAND-02 stays clean, and the change is a
single color-only commit to `src/pages/index.astro`.
</success_criteria>

<output>
Create `.planning/phases/11-end-to-end-launch-gate/11-01-SUMMARY.md` when done
</output>
