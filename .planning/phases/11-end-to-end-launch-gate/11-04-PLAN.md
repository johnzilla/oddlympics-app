---
phase: 11-end-to-end-launch-gate
plan: 04
type: execute
wave: 2
depends_on:
  - 11-06-multi-team-re-gate
files_modified: []
autonomous: false

requirements: []
user_setup: []

must_haves:
  truths:
    - "D-07: an annotated tag named EXACTLY `v1.0-consumer-landing` is created on the green-deploy commit recorded by 11-03 — the annotation names the v2.0 Consumer Landing & Signup Flow milestone and states \"AC1-AC12 verified passing on production\""
    - "D-07: the tag string is deliberately `v1.0-consumer-landing` (NOT the v2.0 milestone label) per ROADMAP/MILESTONE/PROJECT done-definition — it MUST NOT be \"fixed\" to v2.0"
    - "D-07: `git push origin v1.0-consumer-landing` succeeds — the tag is the only outward-facing publish step and is the LAST code step before the D-05 cleanup delete (the tag is cut BEFORE cleanup so verified rows survive any re-verify)"
    - "AC/done-def: the tagged commit is the exact `main` commit whose GitHub Actions Deploy run is green and which 11-03's gate verified all 12 ACs against (no tagging of an unverified or stale commit)"
  artifacts: []
  key_links:
    - from: "git tag -a v1.0-consumer-landing"
      to: "the 11-03 green-deploy commit SHA"
      via: "annotated tag on the verified deploy commit"
      pattern: "v1\\.0-consumer-landing"
    - from: "git push origin v1.0-consumer-landing"
      to: "origin (GitHub)"
      via: "outward-facing tag publish — last code step before cleanup"
      pattern: "push origin v1\\.0-consumer-landing"
---

<objective>
Cut and push the release tag (D-07). Only after 11-03 has all twelve ACs green
with captured evidence: create the annotated `v1.0-consumer-landing` tag on the
exact green-deploy commit 11-03 recorded, and push it to origin.

Purpose: ROADMAP Phase-11 SC4 + MILESTONE done-definition #2 — the release is
tagged on the verified deploy commit. This is the last code step before the
D-05 cleanup; the tag is cut BEFORE cleanup so the verified gate rows still
exist on the tagged state and through any re-verify.

Output: An annotated, pushed `v1.0-consumer-landing` git tag on the verified
green-deploy commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md
@.planning/phases/11-end-to-end-launch-gate/11-03-SUMMARY.md

<interfaces>
<!-- Inputs from 11-03. -->
11-03-SUMMARY.md — records the GREEN-DEPLOY COMMIT SHA (the only SHA this plan
  may tag) + the all-green AC1-AC12 table. Read it for the exact SHA.
Tag string: EXACTLY `v1.0-consumer-landing` (lowercase, hyphens; D-07 / PROJECT
  Key Decisions — deliberately different from the "v2.0" milestone label;
  do NOT rename to v2.0). No tags currently exist in the repo.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Annotated v1.0-consumer-landing tag on the green-deploy commit, pushed (D-07)</name>
  <read_first>
    - .planning/phases/11-end-to-end-launch-gate/11-03-SUMMARY.md (the recorded green-deploy commit SHA + the all-green AC1-AC12 table — the precondition and the exact commit to tag)
    - .planning/phases/11-end-to-end-launch-gate/11-CONTEXT.md (D-07 — exact tag string, annotated message content, tag-before-cleanup ordering, "do NOT fix to v2.0")
    - .planning/PROJECT.md §"Key Decisions" (the deliberate v2.0-milestone-label vs v1.0-consumer-landing-tag-name split — binds D-07)
  </read_first>
  <what-built>
    11-03 verified all twelve AC1-AC12 green on production with captured
    evidence and recorded the exact `main` commit SHA whose GitHub Actions
    Deploy run is green (the green-deploy commit). No git tags exist in the repo
    yet.
  </what-built>
  <how-to-verify>
    1. PRECONDITION: confirm 11-03-SUMMARY.md shows all twelve AC1-AC12 GREEN and records a green-deploy commit SHA. If any AC is RED/escalated, STOP — do not tag (D-07: only after all 12 are green).
    2. Read the exact green-deploy commit SHA from 11-03-SUMMARY.md. Confirm it is reachable on `main` and is the commit whose GitHub Actions Deploy run is green (`gh run list --workflow=deploy.yml` — cross-check the SHA).
    3. Create the annotated tag with the EXACT string `v1.0-consumer-landing` on that SHA: `git tag -a v1.0-consumer-landing <green-deploy-sha> -m "<message>"` where `<message>` names the milestone "v2.0 Consumer Landing & Signup Flow" and states "AC1-AC12 verified passing on production". The tag string is EXACTLY `v1.0-consumer-landing` — do NOT rename it to v2.0 (deliberate per ROADMAP/MILESTONE/PROJECT done-definition; this is a known intentional naming split, not a bug).
    4. Verify locally: `git tag --list` shows `v1.0-consumer-landing`; `git rev-list -n 1 v1.0-consumer-landing` equals the recorded green-deploy SHA; `git cat-file -t v1.0-consumer-landing` returns `tag` (annotated, not lightweight).
    5. Push it: `git push origin v1.0-consumer-landing`. Confirm with `git ls-remote --tags origin v1.0-consumer-landing` (the ref exists on origin).
    6. Do NOT run the D-05 cleanup yet — that is 11-05, which runs AFTER this tag is pushed so the verified gate rows survive on the tagged state.
  </how-to-verify>
  <acceptance_criteria>
    - 11-03-SUMMARY.md shows all twelve AC1-AC12 GREEN (precondition met) — tagging an unverified commit is prohibited
    - `git tag --list` shows exactly `v1.0-consumer-landing` (exact string — not `v2.0`, not a variant)
    - `git cat-file -t v1.0-consumer-landing` returns `tag` (annotated, not lightweight)
    - `git rev-list -n 1 v1.0-consumer-landing` equals the green-deploy commit SHA recorded in 11-03-SUMMARY.md
    - The tag annotation message names the v2.0 Consumer Landing & Signup Flow milestone AND states "AC1-AC12 verified passing on production"
    - `git ls-remote --tags origin v1.0-consumer-landing` shows the tag on origin (push succeeded)
    - The D-05 cleanup (11-05) has NOT been run (tag-before-cleanup ordering preserved)
  </acceptance_criteria>
  <resume-signal>Type "approved" once `git ls-remote --tags origin v1.0-consumer-landing` confirms the annotated tag is on origin at the verified green-deploy commit, OR describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin | `git push origin v1.0-consumer-landing` publishes an outward-facing release marker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | tag points at an unverified/stale commit | mitigate | The SHA is read verbatim from 11-03-SUMMARY's recorded green-deploy commit; `git rev-list -n 1` cross-checked against it; precondition gate requires all 12 ACs green before tagging |
| T-11-04-02 | Repudiation | wrong/ambiguous tag string | mitigate | Tag string hard-pinned to the exact literal `v1.0-consumer-landing`; `git tag --list` asserts the exact name; "do NOT fix to v2.0" called out explicitly (deliberate naming split) |
| T-11-04-03 | Tampering | cleanup runs before the tag (verified rows lost from tagged state) | mitigate | Plan ordering: 11-04 (tag) is wave 3, 11-05 (cleanup) is wave 4; acceptance criteria explicitly assert the cleanup has NOT run |
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan is git-only (tag + push) |
</threat_model>

<verification>
- 11-03 all-green precondition confirmed.
- `git tag --list` shows exactly `v1.0-consumer-landing`; annotated; on the
  recorded green-deploy SHA.
- `git ls-remote --tags origin v1.0-consumer-landing` confirms the push.
- D-05 cleanup not yet run (ordering preserved).
</verification>

<success_criteria>
The annotated `v1.0-consumer-landing` tag exists on the exact verified
green-deploy commit, carries the milestone + "AC1-AC12 verified passing on
production" message, is pushed to origin, the tag string was not altered to
v2.0, and the D-05 cleanup has not yet run.
</success_criteria>

<output>
Create `.planning/phases/11-end-to-end-launch-gate/11-04-SUMMARY.md` when done.
Record the tagged SHA + the pushed tag ref.
</output>
