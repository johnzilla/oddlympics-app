---
phase: 05-schema-signup-payload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - MILESTONE-consumer-landing.md
autonomous: true
requirements:
  - COMPAT-01
tags:
  - docs
  - sweep
  - prep
must_haves:
  truths:
    - "Every docs reference to the v2.0 fallback default timezone reads 'America/New_York' (not 'America/Detroit') after this commit"
    - "ROADMAP.md Phase 5 SC #3 + SC #4, REQUIREMENTS.md SIGNUP-02 + COMPAT-01, and MILESTONE-consumer-landing.md Phase 1 bullet are consistent with CONTEXT.md D-03"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 SC #3 + #4 with fallback default 'America/New_York'"
      contains: "America/New_York"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SIGNUP-02 + COMPAT-01 with fallback default 'America/New_York'"
      contains: "America/New_York"
    - path: "MILESTONE-consumer-landing.md"
      provides: "Phase 1 — Schema + payload bullet with 'America/New_York'"
      contains: "America/New_York"
  key_links:
    - from: ".planning/ROADMAP.md Phase 5 SC #3-#4"
      to: "CONTEXT.md D-03"
      via: "shared fallback default literal 'America/New_York'"
      pattern: "America/New_York"
    - from: ".planning/REQUIREMENTS.md SIGNUP-02"
      to: "CONTEXT.md D-03"
      via: "shared fallback default literal"
      pattern: "America/New_York"
---

<objective>
Pre-plan docs sweep mandated by CONTEXT.md D-03 ("Pre-plan docs sweep is REQUIRED"). Replace every `America/Detroit` mention in the three planning/milestone docs with `America/New_York` so ROADMAP/REQUIREMENTS/MILESTONE are consistent with the locked Phase 5 decision before any code lands.

Purpose: every downstream PLAN reads these three docs as canonical refs. If they disagree with CONTEXT.md, executors will either ship the wrong default or stall mid-task asking which value to use.

Output: three small edits in a single commit. Zero code changes. Lands before any schema/signup/migration work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-schema-signup-payload/05-CONTEXT.md
@MILESTONE-consumer-landing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fallback-tz default in ROADMAP.md Phase 5 SC #3 + #4</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the whole Phase 5 section, currently at lines 34-54)
    - .planning/phases/05-schema-signup-payload/05-CONTEXT.md §"D-03"
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, find Phase 5's Success Criteria #3 and #4 (currently lines ~41-42). Both reference `America/Detroit` as the fallback default. Replace both occurrences with `America/New_York`. Leave SC #1, #2, #5 untouched. Leave every other phase untouched. Leave the R-2 risk note and the "Data files in this phase" paragraph untouched (they don't mention the fallback default).
    Do NOT introduce any other edits in this file — this is a verbatim find-and-replace scoped to the Phase 5 SC block, per D-03.
  </action>
  <verify>
    <automated>grep -n 'America/Detroit' .planning/ROADMAP.md | grep -vi 'previous milestones\|v1 MVP' ; test $(grep -c 'America/New_York' .planning/ROADMAP.md) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'America/Detroit' .planning/ROADMAP.md` returns zero matches inside the Phase 5 SC block (lines 38-44).
    - `grep -c 'America/New_York' .planning/ROADMAP.md` returns ≥ 2 (one for SC #3, one for SC #4).
    - `git diff .planning/ROADMAP.md` shows ONLY substitutions of `America/Detroit` → `America/New_York` and NO other edits.
  </acceptance_criteria>
  <done>ROADMAP Phase 5 SC #3 and SC #4 both name `America/New_York` as the fallback default; no other lines in the file are modified.</done>
</task>

<task type="auto">
  <name>Task 2: Replace fallback-tz default in REQUIREMENTS.md SIGNUP-02 + COMPAT-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §"`/api/signup` payload" (SIGNUP-02) and §"Backward compatibility" (COMPAT-01)
    - .planning/phases/05-schema-signup-payload/05-CONTEXT.md §"D-03"
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, find SIGNUP-02 (currently line ~30) — it reads "On invalid or empty: fall back to `America/Detroit`". Replace with `America/New_York`. Find COMPAT-01 (currently line ~58) — it reads "set `team = NULL`, `timezone = 'America/Detroit'`". Replace with `'America/New_York'`. Leave the rest of the file untouched, including the v1 MVP historical section and the Out-of-Scope table — nothing else mentions the v2.0 fallback default.
  </action>
  <verify>
    <automated>! grep -n 'America/Detroit' .planning/REQUIREMENTS.md | grep -E 'SIGNUP-02|COMPAT-01' && test $(grep -c 'America/New_York' .planning/REQUIREMENTS.md) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - SIGNUP-02 line names `America/New_York` as the fallback.
    - COMPAT-01 line names `'America/New_York'` as the backfill value (note the single quotes around the literal — match the file's existing quoting style).
    - `git diff .planning/REQUIREMENTS.md` shows exactly two substitutions and zero other edits.
  </acceptance_criteria>
  <done>SIGNUP-02 and COMPAT-01 both name `America/New_York`; the rest of REQUIREMENTS.md is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Replace fallback-tz default in MILESTONE-consumer-landing.md Phase 1 bullet</name>
  <files>MILESTONE-consumer-landing.md</files>
  <read_first>
    - MILESTONE-consumer-landing.md §"Phase 1 — Schema + payload" (currently lines 147-153)
    - MILESTONE-consumer-landing.md §"R3 — `/api/signup` payload handling" (R3.2)
    - MILESTONE-consumer-landing.md §"R10 — Backward compatibility" (R10.1)
    - .planning/phases/05-schema-signup-payload/05-CONTEXT.md §"D-03"
  </read_first>
  <action>
    In `MILESTONE-consumer-landing.md`, replace every reference to `America/Detroit` as the fallback/backfill default with `America/New_York`. Specifically:
    - The Phase 1 bullet "Migrate subscribers table to add `team TEXT`, `timezone TEXT` (default `'America/Detroit'`)." — change the parenthetical default to `'America/New_York'`.
    - R3.2 — change "fall back to `America/Detroit`" to `America/New_York`.
    - R10.1 — change "`timezone = 'America/Detroit'`" to `'America/New_York'`.
    DO NOT touch the AC3 line (line ~130) — it lists `America/Detroit`, `Europe/London`, `Africa/Lagos` as Playwright test locales (user-supplied locale strings, not the fallback default). Per D-03 the docs sweep targets only the fallback default. Leave AC3 verbatim.
    Also leave R1.3's "Detroit time" sub-headline example untouched — that's a display label, not the fallback default.
  </action>
  <verify>
    <automated>grep -n 'America/Detroit' MILESTONE-consumer-landing.md | grep -v 'AC3\|locale' ; test $(grep -c 'America/New_York' MILESTONE-consumer-landing.md) -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - The three fallback-default occurrences (Phase 1 bullet, R3.2, R10.1) all now read `America/New_York`.
    - AC3's `America/Detroit`/`Europe/London`/`Africa/Lagos` locale-list line is verbatim unchanged.
    - R1.3's "Detroit time" sub-headline label example is verbatim unchanged.
    - `git diff MILESTONE-consumer-landing.md` shows exactly three substitutions.
  </acceptance_criteria>
  <done>The three fallback-default references in MILESTONE-consumer-landing.md are updated to `America/New_York`; AC3's locale list and the R1.3 "Detroit time" label are preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure docs edit; no input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T (Tampering) | docs sweep accidentally edits an AC3 locale or an R1.3 display label | mitigate | Each task names the exact lines to edit and the exact lines to leave verbatim. Verification greps explicitly exclude AC3/locale matches so an accidental over-broad replace fails the gate. |
</threat_model>

<verification>
After all three tasks complete:
1. `grep -rn 'America/Detroit' .planning/ROADMAP.md .planning/REQUIREMENTS.md MILESTONE-consumer-landing.md` returns ONLY matches inside historical/locale contexts (v1 MVP previous-milestones text in ROADMAP, AC3 Playwright locale list in MILESTONE, R1.3 "Detroit time" label).
2. `grep -c 'America/New_York' .planning/ROADMAP.md .planning/REQUIREMENTS.md MILESTONE-consumer-landing.md` shows the three files together gained at least 7 new occurrences of `America/New_York`.
3. `git diff --stat` shows three files changed, total ≤ 15 lines.
</verification>

<success_criteria>
SC1: All fallback-default references to `America/Detroit` in the three docs are replaced with `America/New_York`.
SC2: AC3 Playwright-locale list and R1.3 display-label example are preserved verbatim.
SC3: A single git commit lands the sweep with no incidental edits.
</success_criteria>

<output>
After completion, create `.planning/phases/05-schema-signup-payload/05-01-SUMMARY.md` capturing:
- Which lines changed in each of the three files (line numbers before/after).
- The grep evidence (counts) confirming AC3 and R1.3 were preserved.
- The commit SHA of the docs sweep.
</output>
