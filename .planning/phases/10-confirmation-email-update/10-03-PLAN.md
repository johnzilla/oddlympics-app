---
phase: 10-confirmation-email-update
plan: 03
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - .planning/phases/10-confirmation-email-update/evidence/mailtester-score.png
  - .planning/phases/10-confirmation-email-update/evidence/mail-gmail.png
  - .planning/phases/10-confirmation-email-update/evidence/mail-proton.png
  - .planning/phases/10-confirmation-email-update/evidence/mail-outlook.png
  - .planning/phases/10-confirmation-email-update/10-SUMMARY.md
autonomous: false
requirements:
  - SIGNUP-04
tags:
  - email
  - deliverability
  - mail-tester
  - cross-client
  - operator-action
user_setup:
  - service: mail-tester.com
    why: "Manual one-shot deliverability scoring (no API exists); operator visits the site, copies a throwaway address, signs up with it on prod, captures the score + 7 sub-checks"
    env_vars: []
    dashboard_config:
      - task: "Open https://www.mail-tester.com/ in a fresh browser; copy the throwaway address from the displayed input"
        location: "https://www.mail-tester.com/ — main page; the address appears as test-xxxxxxxxx@srv1.mail-tester.com"
      - task: "After sending the test email, click 'Then check your score'; screenshot the full report; capture the 7 sub-check items verbatim"
        location: "Same browser tab; the score page opens automatically"
  - service: oddlympics.app (production)
    why: "Operator must run 4 real signups on production (1 to mail-tester, 1 each to operator-controlled Gmail / Proton / Outlook) — pre-deploy step is firing the existing GitHub Actions deploy on push to main"
    env_vars: []
    dashboard_config:
      - task: "Confirm Phase 10 commit landed on main and GitHub Actions deploy succeeded — check the green check on the latest commit at https://github.com/<user>/oddlympics-app/commits/main"
        location: "GitHub Actions tab"
      - task: "From a fresh browser profile (clear cookies on oddlympics.app), submit a real signup with each of: the mail-tester throwaway address; an operator @gmail.com; an operator @proton.me (or @protonmail.com); an operator @outlook.com (or @hotmail.com). Each signup uses a real team (e.g., england) so the SIGNUP-04 value-prop line renders meaningfully."
        location: "https://oddlympics.app — the landing form"
      - task: "Open each delivered confirmation email in its native web client; screenshot the rendered body (sender + subject + body + footer; do NOT screenshot the mailbox list)"
        location: "Gmail web, Proton webmail, Outlook.com webmail"

must_haves:
  truths:
    - "Confirmation email body matches SIGNUP-04 spec verbatim — value-prop line copy is locked, team + tz interpolation uses Phase 5 row values"
    - "ASCII apostrophe (0x27) in all body copy — no U+2019"
    - "Zero LAND-02 prohibited terms (bitcoin, lightning, crypto, world domination, personal olympics) anywhere in subject, text body, html body, or headers"
    - "Resend sandbox sender (onboarding@resend.dev) is the production sender — custom-domain DKIM/DMARC for oddlympics.app is locked to v1.1"
    - "Existing dev-fallback console.log path must continue to work — never throw on missing RESEND_API_KEY when NODE_ENV !== 'production'"
    - "Mail-Tester score ≥ 8/10 from the production sender — SC3, gated by operator-captured screenshot + 7-sub-check breakdown in 10-SUMMARY.md"
    - "Three real confirmation emails delivered to Gmail / Proton / Outlook from the prod sender render cleanly — SC2, gated by 3 committed screenshots under evidence/ with pass/fail bullets per client in 10-SUMMARY.md"
    - "Each delivered email's rendered body shows: layout intact (no broken card, no inline-style stripping, accent button visible), magic-link resolves to /confirmed?status=ok, unsubscribe path visible (List-Unsubscribe header rendered as the client's 'Unsubscribe' control), zero LAND-02 prohibited terms"
    - "10-SUMMARY.md exists with §Deliverability Evidence (numeric score + 7 sub-checks verbatim) AND §Cross-Client Evidence (3 rows, 1 per client, with pass/fail bullets + screenshot references)"
  artifacts:
    - path: ".planning/phases/10-confirmation-email-update/evidence/mailtester-score.png"
      provides: "Full Mail-Tester report screenshot (numeric score + 7 sub-check breakdown visible)"
    - path: ".planning/phases/10-confirmation-email-update/evidence/mail-gmail.png"
      provides: "Gmail web rendering of the confirmation email (sender + subject + body + footer)"
    - path: ".planning/phases/10-confirmation-email-update/evidence/mail-proton.png"
      provides: "Proton webmail rendering of the confirmation email"
    - path: ".planning/phases/10-confirmation-email-update/evidence/mail-outlook.png"
      provides: "Outlook.com webmail rendering of the confirmation email"
    - path: ".planning/phases/10-confirmation-email-update/10-SUMMARY.md"
      provides: "Phase 10 deliverability + cross-client evidence record; §Deliverability Evidence has numeric score + 7 sub-checks; §Cross-Client Evidence has 3 client rows + pass/fail bullets"
      contains: "Mail-Tester score"
  key_links:
    - from: "10-SUMMARY.md §Deliverability Evidence"
      to: "evidence/mailtester-score.png"
      via: "relative markdown image reference"
      pattern: "evidence/mailtester-score\\.png"
    - from: "10-SUMMARY.md §Cross-Client Evidence"
      to: "evidence/mail-gmail.png + mail-proton.png + mail-outlook.png"
      via: "relative markdown image references"
      pattern: "evidence/mail-(gmail|proton|outlook)\\.png"
    - from: "Phase 11 AC4 (downstream)"
      to: "10-SUMMARY.md + evidence/"
      via: "AC4 references the evidence captured in this plan"
      pattern: "AC4|Phase 11"
---

<objective>
Deploy Plan 10-01's code to production (the GitHub Actions pipeline on push to
main handles this automatically — see `.github/workflows/deploy.yml`), then
execute the two operator-action gates SIGNUP-04 SC2 and SC3 demand:

1. **Mail-Tester ≥ 8/10 from prod sender (D-08 + SC3).** One real signup on
   `https://oddlympics.app` to a Mail-Tester throwaway address; capture the
   numeric score + 7 sub-checks; commit `evidence/mailtester-score.png`;
   paste the score + sub-check breakdown verbatim into `10-SUMMARY.md`
   §Deliverability Evidence. If < 8: iterate the copy / headers per the
   RESEARCH.md §1 "Rollback decision tree" recommendations; re-deploy;
   re-run. Goal-backward: SC3 PASS = numeric score ≥ 8.

2. **Gmail / Proton / Outlook clean render (D-09 + SC2).** Three real signups
   from operator-controlled inboxes (one each on `@gmail.com`, `@proton.me`,
   `@outlook.com`); open each delivered email in its native web client;
   screenshot the rendered body; commit three PNGs to
   `.planning/phases/10-confirmation-email-update/evidence/` as
   `mail-gmail.png`, `mail-proton.png`, `mail-outlook.png`; reference all
   three in `10-SUMMARY.md` §Cross-Client Evidence with a pass/fail bullet
   list per client (layout intact, magic-link resolves, unsubscribe visible,
   LAND-02 absent). Goal-backward: SC2 PASS = all three clients render
   cleanly per the four-bullet checklist.

These are operator actions because the verification paths have no API surface
— Mail-Tester does not expose programmatic submission (D-08 §Rationale);
Litmus / Email-on-Acid was rejected in favor of three throwaway-inbox
screenshots for cost + audit-trail reasons (D-09 §Rationale).

Plan 10-01's code edit and Plan 10-02's offline smoke are the prerequisites
— they prove the body composes correctly in source. This plan proves the
body renders correctly in the wild.

Purpose: SIGNUP-04 SC2 + SC3 are the launch-gate metrics for Phase 10. They
cannot be automated. They MUST be captured before Phase 11 AC4 can reference
them as evidence for the end-to-end launch-gate run.

Output:
- 4 PNG screenshots committed under `.planning/phases/10-confirmation-email-update/evidence/`
- `10-SUMMARY.md` with §Deliverability Evidence + §Cross-Client Evidence sections
- A clear pass/fail call on each of the four signups recorded in the SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-confirmation-email-update/10-CONTEXT.md
@.planning/phases/10-confirmation-email-update/10-RESEARCH.md
@.planning/phases/10-confirmation-email-update/10-PATTERNS.md
@.planning/phases/10-confirmation-email-update/10-VALIDATION.md
@.planning/phases/10-confirmation-email-update/10-01-SUMMARY.md
@.planning/phases/10-confirmation-email-update/10-02-SUMMARY.md
@.planning/phases/09-manage-editor-unsubscribe/09-05-SUMMARY.md
@DEPLOY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy Plan 10-01/10-02 to production</name>
  <files>(none — operator deploy + verification only)</files>
  <read_first>
    - DEPLOY.md (the operator playbook for deploy + Day 2 ops; the deploy is automated via GitHub Actions on push to main, but the operator confirms the green check)
    - .github/workflows/deploy.yml (the deploy pipeline definition — what runs on push to main; ~40s end-to-end per CLAUDE.md)
    - .planning/phases/10-confirmation-email-update/10-01-SUMMARY.md (the artifact summary from Plan 10-01)
    - .planning/phases/10-confirmation-email-update/10-02-SUMMARY.md (the artifact summary from Plan 10-02)
  </read_first>
  <what-built>
    Plan 10-01 (widened sendMagicLink + new helpers + Resend headers) and Plan 10-02 (smoke + npm alias) are committed on `main`. The GitHub Actions deploy on push to main is now landing on the DigitalOcean droplet via rsync + better-sqlite3 rebuild + `systemctl restart oddlympics`.

    No CLI/API command Claude can run replaces operator verification of "did production actually serve the new bytes" — the systemd restart, the service status check, and the Caddy reverse-proxy health check are operator-visible signals.
  </what-built>
  <action>
    OPERATOR ACTION REQUIRED. Verify the GitHub Actions deploy of Plans 10-01 + 10-02 succeeded and that the new bytes are serving from oddlympics.app. Execute the 6 verification steps in `<how-to-verify>` below; do NOT run a real signup test yet (Task 2 owns the first prod-sender signup).
  </action>
  <how-to-verify>
    1. Confirm the green check on the most recent commit on `main` at the GitHub Actions tab — the workflow run for the Plan 10-01 + 10-02 commits must show "All checks have passed".
    2. From a local terminal: `ssh deploy@<droplet> 'sudo systemctl status oddlympics'` — the service must be `active (running)`, no recent restart loops, and the recent journal lines must not show a startup throw.
    3. From any browser: `curl -sI https://oddlympics.app | head -5` — must return `HTTP/2 200` and the Server header reflects Caddy + the latest deploy.
    4. From any browser: `curl -s https://oddlympics.app | head -20` — the served HTML is the v2.0 landing page (consumer copy, "Your team's matches. In your time zone. One ping before kickoff." headline visible). Confirms Caddy is serving the new build.
    5. **Spot-check the new code is live:** run `curl -s https://oddlympics.app/api/manage -X POST -d 'email=___not_real___@example.com' -H 'Origin: https://oddlympics.app' -i | head -5` — must respond with a 303 redirect (or 4xx for bad-email), NOT a 500 or a stack trace. Any 500 means the deploy broke startup; abort and rollback before continuing.
    6. **DO NOT** run a real signup test yet — the Mail-Tester run in Task 2 is the first real signup, and we want to send it from a clean rate-limit bucket on the freshly-restarted server. Verify and then wait.
  </how-to-verify>
  <verify>
    Operator-driven; no automated assertion. Pass condition: steps 1-5 of `<how-to-verify>` all confirm green; step 5's curl returns 303 (or 4xx) NOT 500.
  </verify>
  <resume-signal>Type "deployed" when steps 1-5 pass, or describe the issue (Actions red, systemd inactive, 500 response, etc.) and abort.</resume-signal>
  <done>
    GitHub Actions on `main` shows green for the Plan 10-01 + 10-02 commits; `systemctl status oddlympics` reports `active (running)` on the droplet; `curl -sI https://oddlympics.app` returns 200; the spot-check POST returns a 3xx or 4xx (not 500). Operator confirms "deployed" via resume-signal.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run Mail-Tester ≥ 8/10 gate (D-08 + SC3)</name>
  <files>.planning/phases/10-confirmation-email-update/evidence/mailtester-score.png</files>
  <read_first>
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-08 (Procedure)
    - .planning/phases/10-confirmation-email-update/10-RESEARCH.md §1 (Mail-Tester scoring mechanics + realistic ceiling 8.5-9.5 from onboarding@resend.dev) and §6 (Operator playbook for 10-SUMMARY.md evidence — the 7 sub-check enumeration)
    - .planning/phases/10-confirmation-email-update/10-VALIDATION.md §"Manual-Only Verifications" — task 10-03-01 procedure
  </read_first>
  <what-built>
    The Phase 10 confirmation email body (D-04 value-prop line + D-05 subject + replyTo + List-Unsubscribe headers) is live in production via Task 1. The Resend sandbox sender `onboarding@resend.dev` is signing with Resend's own DKIM on `resend.dev` (D-07 + RESEARCH §1). The realistic ceiling per RESEARCH is 8.5-9.5/10; the gate is ≥ 8/10.
  </what-built>
  <action>
    OPERATOR ACTION REQUIRED. Run a single Mail-Tester deliverability scan against the production sender by submitting one real signup on https://oddlympics.app to a Mail-Tester throwaway address. Capture the numeric score + 7 sub-check breakdown verbatim. If < 8/10, iterate the body / headers per the rollback decision tree in RESEARCH.md §1, re-deploy via Task 1, and re-run this task with a FRESH throwaway address. Commit the screenshot at evidence/mailtester-score.png. Carry the captured 7-sub-check text forward to Task 4 (the SUMMARY paste).
  </action>
  <how-to-verify>
    1. Open https://www.mail-tester.com/ in a fresh browser tab. Wait for the page to display a throwaway address in the form `test-xxxxxxxxx@srv1.mail-tester.com`. Copy that address.
    2. Open https://oddlympics.app in a different fresh browser profile (no oddlympics.app cookies — incognito or a separate browser).
    3. On the landing form: paste the Mail-Tester throwaway address in the email field; select a real team (recommend `England` — it produces the literal SIGNUP-04 example string in the email body for direct visual confirmation); let the JS-captured timezone fire naturally (do NOT override). Submit.
    4. Wait for the `/pending?email=...` page to confirm the signup landed.
    5. Switch back to the Mail-Tester tab; click "Then check your score". Mail-Tester will scan within 10-30 seconds and produce a numeric score + 7 sub-checks (SPF / DKIM / DMARC authentication, content / SpamAssassin rules, blacklists, body/subject sanity, server / IP reputation, broken-link detection, unsubscribe / List-Unsubscribe headers).
    6. **Screenshot the full Mail-Tester report.** Save as `.planning/phases/10-confirmation-email-update/evidence/mailtester-score.png`. The screenshot must show the numeric score (e.g., `9.1/10`) + the 7 sub-checks expanded.
    7. **Capture the 7 sub-checks verbatim.** For each: the sub-check name, its pass/warn/fail status, and any SpamAssassin rules that fired with their score contribution (the report lists these inline). This text is what gets pasted into `10-SUMMARY.md` §Deliverability Evidence in Task 4.
    8. **Gate decision:**
       - If score ≥ 8: proceed to Task 3.
       - If score < 8: STOP. Apply the RESEARCH.md §1 rollback decision tree (first knob: missing Reply-To — but Plan 10-01 already added it; second knob: missing List-Unsubscribe — Plan 10-01 already added them; third knob: text/html parity — verify both bodies carry the same value-prop line; fourth knob: HTTP vs HTTPS link in body — verify SITE_URL is https on prod). Make a code edit, push to main, re-deploy via Task 1, then re-run this task with a FRESH Mail-Tester throwaway address (the old one is one-shot per scan window). If three iterations cannot lift to ≥ 8, escalate to the operator for a manual call on whether to ship at 7.x and re-test post-launch.
    9. Commit the screenshot — `git add .planning/phases/10-confirmation-email-update/evidence/mailtester-score.png && git commit -m "docs(10-03): mail-tester score evidence"`.
  </how-to-verify>
  <verify>
    File assertion: `test -f .planning/phases/10-confirmation-email-update/evidence/mailtester-score.png` exits 0. The numeric score is recorded ≥ 8.0 (captured in operator scratch notes, pasted into 10-SUMMARY.md by Task 4). The 7 sub-checks are captured verbatim (operator scratch notes).
  </verify>
  <resume-signal>Type "mailtester=X.Y/10 PASS" with the numeric score, or "mailtester=X.Y/10 FAIL" + the issue, or "mailtester escalate" if three iterations still ship < 8.</resume-signal>
  <done>
    evidence/mailtester-score.png is committed. Operator has recorded the numeric score (≥ 8.0) and the 7 sub-check breakdown for Task 4 to consume. If iteration was needed, the iteration log is captured (number of attempts, code changes per attempt, final score).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Cross-client send to Gmail / Proton / Outlook + screenshots (D-09 + SC2)</name>
  <files>
    - .planning/phases/10-confirmation-email-update/evidence/mail-gmail.png
    - .planning/phases/10-confirmation-email-update/evidence/mail-proton.png
    - .planning/phases/10-confirmation-email-update/evidence/mail-outlook.png
  </files>
  <read_first>
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-09 (Procedure)
    - .planning/phases/10-confirmation-email-update/10-RESEARCH.md §4 (HTML email cross-client compatibility — per-client findings table) and §6 (Operator playbook)
    - .planning/phases/10-confirmation-email-update/10-VALIDATION.md §"Manual-Only Verifications" — task 10-03-02 procedure
  </read_first>
  <what-built>
    Same prod email as Task 2 — the only inputs that change per send are the recipient address + the team selected. Each send produces a real Resend transactional email to a real inbox; the screenshots capture what the user will actually see.

    RESEARCH.md §4 documents the per-client risks: Gmail Promotions-tab classification (probably benign for a confirmation), Proton dark-mode aggressive background inversion (the accent-orange button must remain readable), Outlook.com webmail Blink engine (modern; no MSO concerns since we ship zero `mso-*` attributes), iOS Mail WebKit (HIGH confidence).
  </what-built>
  <action>
    OPERATOR ACTION REQUIRED. Submit three real signups on https://oddlympics.app — one each to operator-controlled Gmail, Proton, and Outlook inboxes — rotating teams (England / France / Brazil suggested) so each delivered email proves a different team-name interpolation. Open each delivered email in its native web client; screenshot the rendered body (subject + sender + body + footer, NOT the surrounding mailbox list); commit three PNGs to evidence/. Run the four-bullet pass/fail checklist per client and carry the result forward to Task 4 (the SUMMARY paste). If any client fails any bullet beyond minor dark-mode contrast caveats, roll back and re-iterate per the Task 2 rollback decision tree (re-run Task 2 first because the body changed).
  </action>
  <how-to-verify>
    1. From a fresh browser profile (incognito or separate browser; no oddlympics.app cookies), submit a real signup on https://oddlympics.app to an operator-controlled `@gmail.com` address. Use a real team (recommend rotating teams — England for Gmail, France for Proton, Brazil for Outlook — so each screenshot proves a different team-name interpolation, not just the canonical England case).
    2. Open the delivered email in Gmail web (https://mail.google.com). Verify visually:
       - **Layout intact:** the styled card displays as a single bordered max-520px box; the mono font renders; the accent-orange CTA button (`hsl(18 70% 56%)`) is visible and clickable.
       - **Magic link resolves:** click the CTA button; the destination is `/confirmed?status=ok` (or `/confirmed?status=already` if you re-clicked); no 4xx/5xx.
       - **Unsubscribe visible:** in the Gmail web UI, there should be an "Unsubscribe" link in the message header (Gmail surfaces this from the List-Unsubscribe header — confirms Plan 10-01's header wire works).
       - **No LAND-02 terms:** scan the rendered body — no occurrences of bitcoin / lightning / crypto / world domination / personal olympics.
    3. **Screenshot the rendered email body.** Crop to subject + sender + body + footer. Do NOT screenshot the mailbox list around it (that's PII). Save as `.planning/phases/10-confirmation-email-update/evidence/mail-gmail.png`.
    4. Repeat steps 1-3 for Proton (operator-controlled `@proton.me` or `@protonmail.com`; team=France recommended; save screenshot as `mail-proton.png`). **Extra check for Proton:** dark-mode rendering. RESEARCH §4 flags that Proton's Carbon / Monokai / Ebony themes can invert backgrounds aggressively; if the operator uses dark mode by default, take the screenshot in dark mode and ALSO in light mode (save the dark one as `mail-proton.png`, the light one as `mail-proton-light.png` if both are captured). The accent-orange button must remain readable in both modes.
    5. Repeat steps 1-3 for Outlook.com (operator-controlled `@outlook.com` or `@hotmail.com`; team=Brazil recommended; save screenshot as `mail-outlook.png`). Outlook.com uses the Blink engine — no MSO concerns; the screenshot should look near-identical to the Gmail one.
    6. **Pass/fail call per client:** For each of the three clients, record (in your scratch notes, to be pasted into the SUMMARY in Task 4) a four-bullet checklist:
       - [ ] Layout intact (styled card, mono font, button visible)
       - [ ] Magic link resolves to /confirmed?status=ok
       - [ ] Unsubscribe visible (header-level or footer-level)
       - [ ] Zero LAND-02 prohibited terms
    7. If any client fails any bullet: STOP and decide — minor render quirk (Proton dark-mode contrast on the button): note in SUMMARY as "pass with caveat, follow-up Phase 11 AC8". Real layout break (broken card, missing link, stripped styles): roll back, fix in code, re-deploy via Task 1, re-run Task 2 (re-test deliverability since the body changed), re-run Task 3.
    8. Commit the screenshots — `git add .planning/phases/10-confirmation-email-update/evidence/mail-{gmail,proton,outlook}.png && git commit -m "docs(10-03): cross-client render evidence"`.
  </how-to-verify>
  <verify>
    File assertion: `ls .planning/phases/10-confirmation-email-update/evidence/mail-gmail.png .planning/phases/10-confirmation-email-update/evidence/mail-proton.png .planning/phases/10-confirmation-email-update/evidence/mail-outlook.png` exits 0. Operator records four-bullet pass/fail per client (3 clients × 4 bullets = 12 checkboxes) for Task 4's SUMMARY paste.
  </verify>
  <resume-signal>Type "cross-client PASS" with a one-line note per client, or "cross-client FAIL @<client>: <issue>" + the failure mode.</resume-signal>
  <done>
    Three PNG screenshots committed to evidence/ (mail-gmail.png, mail-proton.png, mail-outlook.png). Operator has recorded the 12-bullet pass/fail matrix for Task 4 to consume. If iteration was required (real layout break), the iteration log is captured alongside Task 2's iteration log in operator scratch notes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write 10-SUMMARY.md with Deliverability + Cross-Client Evidence sections</name>
  <files>.planning/phases/10-confirmation-email-update/10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-manage-editor-unsubscribe/09-05-SUMMARY.md (the most recent shipped SUMMARY — the template shape Phase 10 extends; PATTERNS.md §"10-SUMMARY.md" pins this analog)
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-08 + D-09 (the evidence-record contract)
    - .planning/phases/10-confirmation-email-update/10-RESEARCH.md §6 ("Operator playbook for `10-SUMMARY.md` evidence") for the exact 7-sub-check enumeration to use as section headings
    - .planning/phases/10-confirmation-email-update/10-01-SUMMARY.md (the code-edit artifact summary — referenced from §Files Modified)
    - .planning/phases/10-confirmation-email-update/10-02-SUMMARY.md (the smoke artifact summary — referenced from §Verification)
    - The operator's scratch notes from Tasks 2 + 3 (the captured Mail-Tester score + 7 sub-checks; the four-bullet per-client pass/fail calls)
  </read_first>
  <action>
    Create `.planning/phases/10-confirmation-email-update/10-SUMMARY.md` from scratch. The file is a fresh markdown document — not an edit to an existing SUMMARY. Mirror the shape of 09-05-SUMMARY.md (the most recent shipped sibling) plus the two Phase-10-specific sections.

    Required structure (top to bottom):

    ## Header
    Top of file: `# Phase 10 Confirmation Email Update — Summary`
    Frontmatter line: `**Phase:** 10-confirmation-email-update`
    Frontmatter line: `**Plans:** 10-01, 10-02, 10-03 (this summary closes the phase)`
    Frontmatter line: `**Status:** Complete`
    Frontmatter line: `**Shipped:** YYYY-MM-DD` (the actual completion date)

    ## §Files Modified
    Bullet list aggregating all files touched across the three plans:
    - src/lib/email.ts (Plan 10-01)
    - src/lib/teams.ts (Plan 10-01)
    - src/lib/timezones.ts (Plan 10-01)
    - src/pages/api/signup.ts (Plan 10-01)
    - scripts/smoke-confirm-email.mjs (Plan 10-02, created)
    - package.json (Plan 10-02, scripts block edit)
    - .planning/phases/10-confirmation-email-update/evidence/ (Plan 10-03, 4 PNGs)
    - This SUMMARY (Plan 10-03)

    ## §Decisions Honored
    Single table mapping D-01 through D-10 to "Implemented in Plan NN, see ..." — the executor reads CONTEXT.md to get the D-XX list and the plan-level implementation evidence (Plan 10-01 SUMMARY for D-01/D-02/D-03/D-04/D-05/D-06/D-07; Plan 10-02 SUMMARY for D-10; this plan for D-08/D-09).

    ## §Verification (automated)
    - smoke:confirm result: `pass=10 fail=0` (pasted verbatim from `npm run smoke:confirm` output in 10-02-SUMMARY.md)
    - astro check exit status: 0 (from 10-01-SUMMARY.md)
    - LAND-02 grep on src/lib/email.ts: 0 hits (from 10-01-SUMMARY.md)

    ## §Deliverability Evidence (D-08 / SC3)
    First line: `**Score:** X.Y / 10 — PASS (gate: ≥ 8/10)` with the actual numeric score captured in Task 2 step 6.
    Image embed: `![Mail-Tester report](evidence/mailtester-score.png)` — relative path to the committed screenshot.
    Paste verbatim the 7 sub-checks captured in Task 2 step 7, each as a sub-section heading or bullet:
    1. **Authentication (SPF / DKIM / DMARC):** <status + any notes from the report>
    2. **Content / SpamAssassin rules:** <status + any rules that fired with their score contribution>
    3. **Blacklists (Spamhaus / SORBS / Barracuda):** <status>
    4. **Body / subject sanity:** <status>
    5. **Server / sending IP reputation:** <status>
    6. **Broken-link / bad-URL detection:** <status>
    7. **Unsubscribe / List-Unsubscribe headers:** <status + confirmation of one-click compliance>

    If the score required iteration (re-deploy + re-test), include a brief sub-section `### Iteration log` with one bullet per iteration (date, code change, new score). If the score was ≥ 8 on first try, omit this sub-section.

    ## §Cross-Client Evidence (D-09 / SC2)
    Sub-section per client, each with a four-bullet pass/fail checklist + a screenshot embed:

    ### Gmail web
    - [ ] Layout intact (styled card, mono font, accent button visible)
    - [ ] Magic link resolves to /confirmed?status=ok
    - [ ] Unsubscribe visible (Gmail header "Unsubscribe" link)
    - [ ] Zero LAND-02 prohibited terms
    Embed: `![Gmail render](evidence/mail-gmail.png)`

    ### Proton webmail
    - [ ] Layout intact
    - [ ] Magic link resolves
    - [ ] Unsubscribe visible
    - [ ] Zero LAND-02 prohibited terms
    Note: dark-mode tested = (yes / no); if yes, accent-orange button readability = (good / acceptable / poor)
    Embed: `![Proton render](evidence/mail-proton.png)`

    ### Outlook.com webmail
    - [ ] Layout intact
    - [ ] Magic link resolves
    - [ ] Unsubscribe visible
    - [ ] Zero LAND-02 prohibited terms
    Embed: `![Outlook render](evidence/mail-outlook.png)`

    Each checkbox MUST be filled (`[x]` for PASS, `[ ]` ONLY if a follow-up Phase 11 AC8 ticket is filed for the remaining gap — and a `### Follow-ups` sub-section under §Cross-Client Evidence lists the ticket).

    ## §Hand-off to Phase 11
    One-paragraph note that Phase 11 AC4 (real signup from John's personal Gmail in a fresh browser profile, full confirm → manage → unsubscribe loop with delivery in < 60s) references this SUMMARY's evidence as the deliverability + cross-client baseline. The AC4 run is a separate end-to-end exercise; Phase 10 produces the asset, Phase 11 verifies the full loop on the production sender.

    ## §Open Items
    - Custom Resend domain DKIM/DMARC (deferred to v1.1 per PROJECT.md Key Decisions; will lift the Mail-Tester score asymptote toward 10/10 but is not required to ship Phase 10)
    - Layout.astro refactor (deferred to v1.1 per CLAUDE.md and Phase 7)
    - Per-team imagery / crest in email header (deferred to v2)

    Conventions:
    - ASCII apostrophe (0x27) throughout — no U+2019
    - Markdown image embeds use relative paths (`evidence/...`) not absolute paths
    - Checkboxes use `[x]` / `[ ]` (not Unicode check marks) for grep-ability
    - All four PNG paths under evidence/ must be referenced at least once

    Implements D-08 + D-09 evidence-record contract. Closes the phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-confirmation-email-update/10-SUMMARY.md &amp;&amp; grep -nF "Mail-Tester" .planning/phases/10-confirmation-email-update/10-SUMMARY.md &amp;&amp; grep -nF "evidence/mailtester-score.png" .planning/phases/10-confirmation-email-update/10-SUMMARY.md &amp;&amp; grep -nFc "evidence/mail-" .planning/phases/10-confirmation-email-update/10-SUMMARY.md ; ls .planning/phases/10-confirmation-email-update/evidence/ ; ! grep -F "$(printf '’')" .planning/phases/10-confirmation-email-update/10-SUMMARY.md ; ! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' .planning/phases/10-confirmation-email-update/10-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/10-confirmation-email-update/10-SUMMARY.md`
    - All 4 PNG screenshots exist: `ls .planning/phases/10-confirmation-email-update/evidence/mailtester-score.png .planning/phases/10-confirmation-email-update/evidence/mail-gmail.png .planning/phases/10-confirmation-email-update/evidence/mail-proton.png .planning/phases/10-confirmation-email-update/evidence/mail-outlook.png` exits 0
    - Source assertion: `grep -nF "Mail-Tester" .planning/phases/10-confirmation-email-update/10-SUMMARY.md` matches at least one line
    - Source assertion: `grep -nF "evidence/mailtester-score.png" .planning/phases/10-confirmation-email-update/10-SUMMARY.md` matches at least one line (image embed present)
    - Source assertion: `grep -nFc "evidence/mail-" .planning/phases/10-confirmation-email-update/10-SUMMARY.md` returns ≥ 3 (all three client screenshots referenced)
    - Source assertion: the numeric Mail-Tester score is present in §Deliverability Evidence (format: `X.Y / 10` or `X.Y/10`; capture matches `grep -nE '[0-9]+\.[0-9]+\s*/\s*10' 10-SUMMARY.md`)
    - Source assertion: §Cross-Client Evidence contains exactly 3 client sub-sections — `grep -nFc "### Gmail" 10-SUMMARY.md == 1`, `grep -nFc "### Proton" 10-SUMMARY.md == 1`, `grep -nFc "### Outlook" 10-SUMMARY.md == 1`
    - Source assertion: each client sub-section has 4 checklist bullets — `[ $(($(grep -Fc "[x]" .planning/phases/10-confirmation-email-update/10-SUMMARY.md) + $(grep -Fc "[ ]" .planning/phases/10-confirmation-email-update/10-SUMMARY.md))) -ge 12 ]` exits 0 (3 clients × 4 bullets, plus the Deliverability gate line if checklist-shaped there too)
    - LAND-02 grep on the SUMMARY: `! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' 10-SUMMARY.md` exits 0
    - U+2019 negative check on the SUMMARY: `! grep -F "$(printf '’')" 10-SUMMARY.md` exits 0
    - Phase 11 hand-off section exists: `grep -nF "Phase 11" 10-SUMMARY.md` matches at least one line
    - The Mail-Tester score recorded is ≥ 8.0 (SC3 gate) — operator-confirmed in Task 2
    - All four cross-client checkboxes per client are `[x]` OR the §Follow-ups sub-section names the specific Phase 11 AC8 ticket covering each `[ ]` deferral
  </acceptance_criteria>
  <done>
    `10-SUMMARY.md` exists with both §Deliverability Evidence and §Cross-Client Evidence sections fully populated; the numeric Mail-Tester score ≥ 8/10 is captured; all 4 PNG screenshots are committed under evidence/ and referenced in the SUMMARY; the §Hand-off to Phase 11 section names AC4 explicitly; LAND-02 grep + U+2019 grep both pass on the SUMMARY itself.

    The phase is now closeable: ROADMAP Phase 10 can be marked `[x]` after this plan's commit lands; REQUIREMENTS.md SIGNUP-04 row can flip to Complete; STATE.md current-phase moves to 11.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → production deploy (via GitHub Actions on push to main) | The push-to-main side is operator-controlled; the runner is GitHub-hosted; the rsync target is the DigitalOcean droplet. No new untrusted bytes; the deploy pipeline shipped under Phase 1 hardening. |
| Production sendMagicLink → Resend HTTP API → Mail-Tester throwaway address | One real signup crosses prod → Resend → Mail-Tester. The "untrusted recipient" path goes nowhere — Mail-Tester is a third-party deliverability service, and the throwaway address is one-shot. |
| Production sendMagicLink → operator-controlled Gmail / Proton / Outlook inboxes | Three real signups cross prod → Resend → three real recipient inboxes. The operator controls all three; no end-user data leaks. |
| Committed PNG screenshots → repo (public on GitHub) | Operator must crop screenshots so the surrounding mailbox UI (PII, other emails) is not captured. Standard repo-public threat model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information disclosure | Cross-client screenshots committed to public repo | mitigate | Operator-controlled inboxes only; screenshots must be cropped to subject + sender + body + footer (NOT the surrounding mailbox list with other emails). Plan instructions in Task 3 step 3 + step 5 explicitly call this out. LOW severity. |
| T-10-03-02 | Repudiation | Mail-Tester score not reproducible | accept | Mail-Tester is a per-deploy one-shot; the committed screenshot is the audit trail. Future re-tests use a new throwaway address. The numeric score captured in §Deliverability Evidence is the canonical record; a future regression run can re-test from the same prod sender for comparison. LOW severity. |
| T-10-03-03 | Tampering | Deploy ships broken bytes; Phase 10 evidence captured before bug surfaces | mitigate | Task 1 step 5 includes a curl spot-check to confirm the deploy did not break the API. Task 2 + Task 3 produce real-recipient evidence — if the deploy is broken, the magic-link click in Task 3 step 2 surfaces it. LOW severity; the smoke (Plan 10-02) + the operator manual verification create defense in depth. |

All three threats are LOW; security_block_on=high does not gate this plan.
</threat_model>

<verification>
After all four tasks complete:

1. `test -f .planning/phases/10-confirmation-email-update/10-SUMMARY.md` (SUMMARY exists)
2. `ls .planning/phases/10-confirmation-email-update/evidence/` shows exactly 4 PNGs (mailtester-score, mail-gmail, mail-proton, mail-outlook) — operator may have committed additional PNGs (e.g., a `mail-proton-light.png` for dark/light comparison); ≥ 4 is acceptable; < 4 is a failure
3. `grep -nE '[0-9]+\.[0-9]+\s*/\s*10' .planning/phases/10-confirmation-email-update/10-SUMMARY.md` — the Mail-Tester numeric score is captured (the captured score must be ≥ 8.0; SC3 gate)
4. `grep -nF "Phase 11" .planning/phases/10-confirmation-email-update/10-SUMMARY.md` — the hand-off section names Phase 11 explicitly
5. `! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' .planning/phases/10-confirmation-email-update/10-SUMMARY.md` — LAND-02 holds on the SUMMARY too
6. `git log --oneline -5 .planning/phases/10-confirmation-email-update/` — at least 2 commits show up (one for the screenshots, one for the SUMMARY); the messages reference the docs(10-03) scope

Per-task acceptance criteria provide the per-task gate; this section is the plan-wide cross-check.
</verification>

<success_criteria>
- Phase 10 code (Plans 10-01 + 10-02) is deployed to production and verified live
- Mail-Tester score ≥ 8/10 captured from production sender (`onboarding@resend.dev`); screenshot committed; 7 sub-checks pasted verbatim into 10-SUMMARY.md §Deliverability Evidence
- Three real cross-client sends (Gmail / Proton / Outlook) delivered cleanly; three screenshots committed under evidence/; each client has a four-bullet pass checklist in 10-SUMMARY.md §Cross-Client Evidence
- 10-SUMMARY.md exists with all required sections (Files Modified, Decisions Honored, Verification automated, Deliverability Evidence, Cross-Client Evidence, Hand-off to Phase 11, Open Items)
- The LAND-02 source grep + the U+2019 negative grep both pass on the SUMMARY itself
- The 4 PNG screenshots are committed to git (visible in `git log --stat`)
- ROADMAP.md Phase 10 entry is ready to be flipped to `[x]` (the gsd-verify-work step that runs after this plan handles the actual ROADMAP flip)
- REQUIREMENTS.md SIGNUP-04 row is ready to be flipped to Complete
</success_criteria>

<output>
After completion, this plan's artifact IS the phase SUMMARY (10-SUMMARY.md). There is no separate per-plan SUMMARY for Plan 10-03 — the phase-level SUMMARY subsumes it. The plan's §Verification (automated) section pulls the relevant numbers from 10-01-SUMMARY.md and 10-02-SUMMARY.md so the reader has a single document closing the phase.

A follow-up verifier (gsd-verify-work or operator) should:
1. Flip ROADMAP.md Phase 10 from `[ ]` to `[x]` with the completion date
2. Flip REQUIREMENTS.md SIGNUP-04 row from `Pending` to `Complete` in the Traceability table
3. Update STATE.md current position to Phase 11
</output>
