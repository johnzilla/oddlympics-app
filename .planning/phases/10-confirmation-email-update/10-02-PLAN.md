---
phase: 10-confirmation-email-update
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - scripts/smoke-confirm-email.mjs
  - package.json
autonomous: true
requirements:
  - SIGNUP-04
tags:
  - email
  - smoke
  - verification
  - land-02

must_haves:
  truths:
    - "Confirmation email body matches SIGNUP-04 spec verbatim — value-prop line copy is locked, team + tz interpolation uses Phase 5 row values"
    - "ASCII apostrophe (0x27) in all body copy — no U+2019"
    - "Zero LAND-02 prohibited terms (bitcoin, lightning, crypto, world domination, personal olympics) anywhere in subject, text body, html body, or headers"
    - "Resend sandbox sender (onboarding@resend.dev) is the production sender — custom-domain DKIM/DMARC for oddlympics.app is locked to v1.1"
    - "Existing dev-fallback console.log path must continue to work — never throw on missing RESEND_API_KEY when NODE_ENV !== 'production'"
    - "scripts/smoke-confirm-email.mjs exits 0 on the canonical pair + 9 additional cases; exits 1 on any byte-mismatch — drift catcher for future edits"
    - "Smoke re-implements teamLabel + tzLabel + body composer inline (no TS-loader friction) — the byte-exact duplication is the point: a future drift in src/lib/* gets caught"
    - "npm run smoke:confirm exists in package.json scripts, kebab-with-colon naming matching smoke:landing / smoke:manage"
  artifacts:
    - path: "scripts/smoke-confirm-email.mjs"
      provides: "Offline Node ESM smoke; runs 10 table-driven cases against re-implemented helpers + body composer; exits 0 = all PASS, 1 = any FAIL"
      contains: "[smoke] PASS"
      min_lines: 80
    - path: "package.json"
      provides: "smoke:confirm npm-script alias"
      contains: "\"smoke:confirm\""
  key_links:
    - from: "scripts/smoke-confirm-email.mjs"
      to: "references/teams.json"
      via: "node:fs readFileSync + JSON.parse (smoke's inline TEAMS lookup)"
      pattern: "readFileSync.*teams\\.json"
    - from: "scripts/smoke-confirm-email.mjs"
      to: "src/lib/email.ts (semantic — byte-equivalence under test)"
      via: "inline re-implementation of body composer + helpers"
      pattern: "We'll email you 1 hour before every"
    - from: "package.json scripts"
      to: "scripts/smoke-confirm-email.mjs"
      via: "npm-script invocation"
      pattern: "node scripts/smoke-confirm-email\\.mjs"
---

<objective>
Create `scripts/smoke-confirm-email.mjs` — an offline, zero-network, zero-DB
Node ESM smoke script that exercises the SIGNUP-04 value-prop line composition
across 10 table-driven cases (CONTEXT D-10 + PATTERNS.md §"The 10 smoke cases"
table). The smoke RE-IMPLEMENTS `teamLabel`, `tzLabel`, and a body composer
inline rather than importing the .ts sources — the byte-exact duplication is
the point: a future drift in `src/lib/email.ts`, `src/lib/teams.ts`, or
`src/lib/timezones.ts` is caught when the smoke regresses (PATTERNS.md
§"Re-implementing TS helpers inline" — same pattern `scripts/smoke-manage.mjs`
uses for `mintToken`).

Add `"smoke:confirm": "node scripts/smoke-confirm-email.mjs"` to package.json
scripts, placed alphabetically before `smoke:landing` —
the existing `smoke:*` naming convention (PATTERNS.md §"package.json").

Purpose: SIGNUP-04 verification depth lives in this smoke. The Phase 10 code
edit (Plan 10-01) is ~30 lines; the deliverability gate (Plan 10-03) is
operator-driven manual. This smoke is the only automated regression net for
the email body — it must pass before any deploy.

Output:
- `scripts/smoke-confirm-email.mjs` (~80-120 lines): shebang + header banner +
  10 runCase invocations + final tally + `process.exit(fail === 0 ? 0 : 1)`
- `package.json` scripts block gains the new `smoke:confirm` alias
- `npm run smoke:confirm` exits 0 against the Plan 10-01 code; exits 1 against
  any byte-drift in the helpers or body composer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-confirmation-email-update/10-CONTEXT.md
@.planning/phases/10-confirmation-email-update/10-RESEARCH.md
@.planning/phases/10-confirmation-email-update/10-PATTERNS.md
@.planning/phases/10-confirmation-email-update/10-VALIDATION.md
@.planning/phases/10-confirmation-email-update/10-01-SUMMARY.md
@CLAUDE.md
@scripts/smoke-signup.mjs
@scripts/smoke-manage.mjs
@scripts/smoke-landing.mjs
@src/lib/email.ts
@src/lib/teams.ts
@src/lib/timezones.ts
@package.json
@references/teams.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From package.json (current scripts block — exactly what is present today; the smoke:confirm alias goes in alphabetic order before smoke:landing):
```json
"scripts": {
  "dev": "astro dev",
  "start": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "serve": "node --env-file=.env ./dist/server/entry.mjs",
  "astro": "astro",
  "smoke:landing": "node scripts/smoke-landing.mjs",
  "smoke:manage": "node scripts/smoke-manage.mjs",
  "check:land-02": "! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' dist/client/index.html",
  "og:render": "node scripts/render-og-image.mjs"
},
```

Resulting scripts block after this plan (with new entry in correct position):
```json
"scripts": {
  "dev": "astro dev",
  "start": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "serve": "node --env-file=.env ./dist/server/entry.mjs",
  "astro": "astro",
  "smoke:confirm": "node scripts/smoke-confirm-email.mjs",
  "smoke:landing": "node scripts/smoke-landing.mjs",
  "smoke:manage": "node scripts/smoke-manage.mjs",
  "check:land-02": "! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' dist/client/index.html",
  "og:render": "node scripts/render-og-image.mjs"
},
```

From scripts/smoke-signup.mjs (header banner + runCase + final tally — the template to mirror; lines 1-32, 52-69, 311-320 documented in PATTERNS.md):
- Shebang: `#!/usr/bin/env node`
- Header comment: `// Phase 10 — Plan 02.` followed by a 2-3 line purpose line
- How-to-run block (no server boot needed since smoke is offline — DROP that line)
- Exit codes block (0 PASS, 1 FAIL, 2 unreachable since no setup dep — keep table for consistency)
- `let pass = 0; let fail = 0;` counters
- `async function runCase(name, fn) { try { const ok = await fn(); ... } catch (err) { ... } }` harness emitting `[smoke] PASS <name>` / `[smoke] FAIL <name>`
- Final: `console.log(\`[smoke] result: pass=${pass} fail=${fail}\`); process.exit(fail === 0 ? 0 : 1);`

From scripts/smoke-manage.mjs (the inline-re-implementation pattern — lines 58-74 documented in PATTERNS.md):
- ASCII-art divider `// ---...---`
- Block comment explaining "Re-implemented via node:crypto rather than importing the .ts source to avoid TypeScript loader friction"
- Smoke uses `node:` prefix on built-ins (`node:fs`, `node:path`, `node:crypto`)

The 10 smoke cases (from PATTERNS.md §"The 10 smoke cases" table — locked):

| # | Case (input) | Assertion (against composed body) |
|---|--------------|-----------------------------------|
| 1 | team=england, tz=America/Detroit | body contains `every England match in Detroit time.` |
| 2 | team=united_states, tz=Europe/London | body contains `every United States match in London time.` |
| 3 | team=france, tz=America/New_York (FALLBACK) | body contains `every France match in New York time.` |
| 4 | team=brazil, tz=Asia/Ho_Chi_Minh | body contains `every Brazil match in Ho Chi Minh time.` |
| 5 | team=germany, tz=Etc/UTC | body contains `every Germany match in your local time.` |
| 6 | team=curacao, tz=America/Curacao | body contains `every Curaçao match in Curacao time.` (diacritic in teamLabel from teams.json; raw last-segment in tzLabel) |
| 7 | subject literal | equals `Confirm your World Cup alerts — oddlympics` (em-dash U+2014) |
| 8 | LAND-02 grep over subject + text + html | regex `/bitcoin|lightning|crypto|world domination|personal olympics/i` matches nothing |
| 9 | team=zzz_unknown (raw slug fallback) | body contains `every zzz_unknown match in` (teamLabel falls back to raw slug) |
| 10 | team=spain, tz='' (empty tz) | body contains `every Spain match in your local time.` |

Case 6 note: `curacao` IS in references/teams.json with the diacritic label "Curaçao" — verified at plan-time (`{ "slug": "curacao", "label": "Curaçao", "confederation": "CONCACAF" }`). The diacritic-preservation path is the canonical one; assert exactly `every Curaçao match in Curacao time.` with no runtime branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/smoke-confirm-email.mjs with 10 cases</name>
  <files>scripts/smoke-confirm-email.mjs</files>
  <read_first>
    - scripts/smoke-signup.mjs lines 1-80 + 270-320 (header banner template, runCase harness, final tally — PATTERNS.md §"scripts/smoke-confirm-email.mjs" pins these line ranges)
    - scripts/smoke-manage.mjs lines 1-100 (inline-re-implementation pattern; node: prefix on imports; ASCII-art comment dividers — PATTERNS.md §"Re-implementing TS helpers inline" pins this)
    - scripts/smoke-landing.mjs lines 1-60 (the closest sibling smoke — same-wave phase, same `[smoke] PASS|FAIL` tag convention)
    - src/lib/teams.ts (the teamLabel implementation to mirror byte-for-byte in the smoke — see Plan 10-01 Task 1)
    - src/lib/timezones.ts (the tzLabel implementation to mirror byte-for-byte — see Plan 10-01 Task 1)
    - src/lib/email.ts (the sendMagicLink body composer to mirror — see Plan 10-01 Task 2; the literal subject + value-prop strings are the byte-equivalence target)
    - references/teams.json (to confirm whether `curacao` is in the 48-team set — drives Case 6's expected string; see Interfaces note above)
    - .planning/phases/10-confirmation-email-update/10-PATTERNS.md §"The 10 smoke cases" table for the locked case enumeration
    - CLAUDE.md §"TypeScript" for the `node:` prefix convention and the ASCII apostrophe rule (smoke must `grep` byte-exact strings using 0x27)
  </read_first>
  <action>
    Create scripts/smoke-confirm-email.mjs from scratch. Mirror smoke-signup.mjs's banner shape and smoke-manage.mjs's inline-re-implementation pattern. Target length: ~80-120 lines.

    File layout (top to bottom):

    1. Shebang and header banner
    Line 1: `#!/usr/bin/env node`
    Lines 2-25 (block comment): `// Phase 10 — Plan 02.` followed by a 2-3 sentence purpose paragraph stating this is an offline smoke that exercises teamLabel + tzLabel + the sendMagicLink body composer over the 10 cases from PATTERNS.md §"The 10 smoke cases", asserts byte-exact strings, and re-implements the helpers inline so a future drift in src/lib/email.ts (or teams.ts / timezones.ts) is caught. Include a "How to run" block: simply `node scripts/smoke-confirm-email.mjs` or `npm run smoke:confirm` — NO server boot prereq, NO DB prereq, NO env vars consumed. Include the "Exit codes" block: 0 = all PASS, 1 = any FAIL. (Exit code 2 from sibling smokes is unreachable here — no setup dependencies — but keep the table-of-contents comment block for consistency.)

    2. Imports
    `import { readFileSync } from 'node:fs';`
    `import { resolve } from 'node:path';`
    (`node:` prefix required per CLAUDE.md TypeScript section.) No third-party imports. No imports from src/lib/* (the smoke deliberately re-implements — see PATTERNS.md §"Re-implementing TS helpers inline to dodge TS-loader friction").

    3. Inline TEAMS lookup
    Read references/teams.json via `readFileSync(resolve(import.meta.dirname, '../references/teams.json'), 'utf8')` (or equivalent ESM path resolution); parse with JSON.parse; assign to a const `TEAMS`. The smoke does NOT need TeamEntry's full type — it only needs the `slug → label` mapping. Build a small lookup helper inline:
    `function teamLabel(slug) { const hit = TEAMS.find((t) => t.slug === slug); return hit ? hit.label : slug; }`

    This is a byte-equivalent re-implementation of src/lib/teams.ts:teamLabel. Add a 3-4 line block comment immediately above the function noting it mirrors src/lib/teams.ts:teamLabel — same wording pattern as scripts/smoke-manage.mjs:58-62.

    4. Inline tzLabel
    Add `function tzLabel(tz)` mirroring src/lib/timezones.ts:tzLabel byte-equivalently:
    - If `!tz || tz.indexOf('/') === -1 || tz.indexOf('Etc/') === 0` → return `'your local time'`
    - `const last = tz.split('/').pop() ?? '';`
    - `const human = last.replace(/_/g, ' ');`
    - `return human ? \`${human} time\` : 'your local time';`

    Add a 3-line block comment above noting it mirrors src/lib/timezones.ts:tzLabel + src/pages/index.astro:204-210.

    5. Inline body composer
    Add `function composeBody({ url, team, timezone })` that produces an object `{ subject, text, html }` byte-equivalent to what Plan 10-01 Task 2 emits inside sendMagicLink. The composer:
    - Computes `const teamHuman = teamLabel(team);` and `const tzHuman = tzLabel(timezone);`
    - Returns the subject literal `'Confirm your World Cup alerts — oddlympics'` (D-05; em-dash U+2014)
    - Returns the text body as the same 12-element array `.join('\n')` shape as Plan 10-01 Task 2's plain-text body (the SIGNUP-04 value-prop line on entry 5)
    - Returns the html body as the same `<!doctype html>...` template-literal shape as Plan 10-01 Task 2's HTML body
    - Uses ASCII apostrophe (0x27) in `We'll` and `didn't` — verify byte-exact with `grep -F` outside the file before committing

    Add a 2-line block comment above the composer noting it mirrors the body composition inside src/lib/email.ts:sendMagicLink (Plan 10-01 Task 2).

    6. Test harness
    Drop in the runCase wrapper from scripts/smoke-signup.mjs:52-69 verbatim:
    ```js
    let pass = 0;
    let fail = 0;

    async function runCase(name, fn) {
      try {
        const ok = await fn();
        if (ok) { console.log(`[smoke] PASS ${name}`); pass++; }
        else { console.error(`[smoke] FAIL ${name}`); fail++; }
      } catch (err) {
        console.error(`[smoke] FAIL ${name} (exception) ${err.message}`);
        fail++;
      }
    }
    ```

    7. The 10 cases
    Each case is a single `await runCase('NAME', () => { ... return boolean })` invocation. Per the PATTERNS.md table:

    - Case 1 `confirm-canonical-england-detroit`: `composeBody({url: 'https://oddlympics.app/api/confirm?token=tok1', team: 'england', timezone: 'America/Detroit'})` — assert `text.includes('every England match in Detroit time.')` AND `html.includes('every <strong>England</strong> match in Detroit time.')`
    - Case 2 `confirm-multi-word-united-states-london`: team=united_states, tz=Europe/London — assert text + html contain `every United States match in London time.` / `every <strong>United States</strong> match in London time.`
    - Case 3 `confirm-fallback-tz-france-new-york`: team=france, tz=America/New_York — assert text + html contain `every France match in New York time.` / `every <strong>France</strong> match in New York time.` (the FALLBACK_TZ underscore→space path)
    - Case 4 `confirm-underscore-tz-brazil-ho-chi-minh`: team=brazil, tz=Asia/Ho_Chi_Minh — assert text + html contain `every Brazil match in Ho Chi Minh time.` (multi-underscore → multiple spaces)
    - Case 5 `confirm-etc-utc-fallthrough-germany`: team=germany, tz=Etc/UTC — assert text + html contain `every Germany match in your local time.` (Etc/* branch falls through to the literal)
    - Case 6 `confirm-diacritic-curacao`: team=curacao, tz=America/Curacao — assert text contains `every Curaçao match in Curacao time.` and html contains `every <strong>Curaçao</strong> match in Curacao time.` (`curacao` IS in references/teams.json with diacritic label "Curaçao", verified at plan-time — no runtime branch).
    - Case 7 `confirm-subject-literal`: subject equals exactly `'Confirm your World Cup alerts — oddlympics'` (em-dash U+2014) — `composeBody({...any inputs}).subject === 'Confirm your World Cup alerts — oddlympics'`
    - Case 8 `confirm-land-02-grep-zero-hits`: assert `/bitcoin|lightning|crypto|world domination|personal olympics/i.test(subject + '\n' + text + '\n' + html) === false` — the LAND-02 grep over the composed body for the canonical england/detroit case (use the same case-insensitive regex as `check:land-02` in package.json)
    - Case 9 `confirm-unknown-slug-fallback-zzz`: team=zzz_unknown, tz=America/Detroit — assert text contains `every zzz_unknown match in Detroit time.` and html contains `every <strong>zzz_unknown</strong> match in Detroit time.` (teamLabel returns the raw slug for unknown teams; protects future retired-team rows)
    - Case 10 `confirm-empty-tz-fallthrough-spain`: team=spain, tz='' — assert text + html contain `every Spain match in your local time.` (tzLabel's falsy-tz branch)

    All assertions use `String.prototype.includes` for substring checks (avoids regex escaping for the em-dash + diacritic). Case 7 uses strict equality. Case 8 uses `RegExp.test` with the `/i` flag.

    8. Final tally
    Mirror smoke-signup.mjs:311-320 (minus the DB cleanup hint and `db.close()` — both are HTTP/DB artifacts the offline smoke does not need):
    ```js
    console.log(`[smoke] result: pass=${pass} fail=${fail}`);
    process.exit(fail === 0 ? 0 : 1);
    ```

    Conventions to follow:
    - `node:` prefix on every built-in import (PATTERNS.md §"`node:` prefix on built-ins in `.mjs` smoke scripts")
    - `[smoke] PASS|FAIL` tag prefix on every case line (matches smoke-signup / smoke-landing / smoke-manage)
    - ASCII apostrophe (0x27) on `We'll`, `didn't`, etc. — never U+2019. The grep gate runs over the smoke source AS WELL as the email source.
    - No JSDoc (CLAUDE.md §"What you won't see in this codebase"). Why-comments where the choice is non-obvious (the byte-equivalence-as-drift-detection rationale earns a comment).
    - No external dependencies — the smoke uses only Node built-ins and references/teams.json read via node:fs

    Implements D-10 (Tests / smoke verification).
  </action>
  <verify>
    <automated>node scripts/smoke-confirm-email.mjs ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/smoke-confirm-email.mjs`
    - First line is `#!/usr/bin/env node`
    - File header contains `Phase 10 — Plan 02`
    - File imports `node:fs` and `node:path` (no other imports)
    - Source assertion: `grep -nF "function teamLabel(slug)" scripts/smoke-confirm-email.mjs` matches exactly one line (inline re-implementation present)
    - Source assertion: `grep -nF "function tzLabel(tz)" scripts/smoke-confirm-email.mjs` matches exactly one line (inline re-implementation present)
    - Source assertion: `grep -nF "function composeBody" scripts/smoke-confirm-email.mjs` matches exactly one line (body composer present)
    - Source assertion: `grep -nFc "runCase(" scripts/smoke-confirm-email.mjs` returns 10 (one invocation per case)
    - Source assertion: `grep -nF "Confirm your World Cup alerts — oddlympics" scripts/smoke-confirm-email.mjs` matches at least one line (case 7 literal present)
    - Source assertion: `grep -nF "every England match in Detroit time." scripts/smoke-confirm-email.mjs` matches at least one line (case 1 expected substring present)
    - Source assertion: `grep -nF "every France match in New York time." scripts/smoke-confirm-email.mjs` matches at least one line (case 3 expected substring present)
    - Source assertion: `grep -nF "every Germany match in your local time." scripts/smoke-confirm-email.mjs` matches at least one line (case 5 expected substring present)
    - LAND-02 self-grep on the smoke source (defense in depth — Case 8 checks the composed body at runtime; this checks the source file): `! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' scripts/smoke-confirm-email.mjs` exits 0
    - U+2019 negative check: `! grep -F "$(printf '’')" scripts/smoke-confirm-email.mjs` exits 0 (no curly apostrophe anywhere)
    - Behavior assertion: `node scripts/smoke-confirm-email.mjs` exits 0 against Plan 10-01's code
    - Behavior assertion: the smoke prints exactly 10 `[smoke] PASS <name>` lines and one `[smoke] result: pass=10 fail=0` line (per the harness convention)
    - Drift-detection assertion (manual sanity check, not gated): temporarily edit the value-prop line in src/lib/email.ts to drop an `'` or swap a word — re-run the smoke — at least one case fails. Revert. This proves the byte-equivalence net works as intended.
  </acceptance_criteria>
  <done>
    scripts/smoke-confirm-email.mjs exists, runs offline with no env / no server / no DB, exits 0 against Plan 10-01's code, and contains all 10 cases with exact substring assertions. The inline re-implementations of teamLabel + tzLabel + body composer are present (byte-equivalence drift net). The LAND-02 grep + ASCII-apostrophe grep both pass on the smoke source itself.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire smoke:confirm npm-script alias in package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (full — 29 lines; the scripts block at lines 6-17 is the only edit point)
    - .planning/phases/10-confirmation-email-update/10-PATTERNS.md §"package.json (MODIFIED — script-alias config)" for the exact placement + value
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-10 for the alias name + value contract
    - .planning/phases/10-confirmation-email-update/10-PATTERNS.md §"Note for the planner" — the watchout about smoke:signup NOT being an existing alias (only smoke:landing + smoke:manage are; do not try to "follow smoke:signup's pattern" because there is no such pattern in package.json)
  </read_first>
  <action>
    Edit package.json. Insert exactly ONE new line in the `scripts` block, in alphabetic order (between `smoke:landing` and `smoke:manage` is wrong — `smoke:confirm` is alphabetically first). The correct insertion point puts `smoke:confirm` BEFORE `smoke:landing`:

    Before (current lines 12-14):
    ```
    "astro": "astro",
    "smoke:landing": "node scripts/smoke-landing.mjs",
    "smoke:manage": "node scripts/smoke-manage.mjs",
    ```

    After:
    ```
    "astro": "astro",
    "smoke:confirm": "node scripts/smoke-confirm-email.mjs",
    "smoke:landing": "node scripts/smoke-landing.mjs",
    "smoke:manage": "node scripts/smoke-manage.mjs",
    ```

    The value `"node scripts/smoke-confirm-email.mjs"` mirrors the bare-node invocation pattern of `smoke:landing` and `smoke:manage` — no `--env-file=.env` (the smoke is offline; no env reads). No prepended `cd` or path manipulation.

    DO NOT TOUCH:
    - Any other scripts entry (dev, start, build, preview, serve, astro, check:land-02, og:render)
    - The `dependencies` block (no new deps for Phase 10)
    - The `devDependencies` block (no new dev-deps)
    - Top-level fields (name, type, version, private)

    Validate the resulting JSON parses correctly before committing.

    Implements D-10 (npm-script alias).
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); if (pkg.scripts['smoke:confirm'] !== 'node scripts/smoke-confirm-email.mjs') { console.error('FAIL: smoke:confirm alias missing or wrong'); process.exit(1); } console.log('OK: smoke:confirm=' + pkg.scripts['smoke:confirm']);" &amp;&amp; npm run smoke:confirm &amp;&amp; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - JSON parses: `node -e "JSON.parse(require('node:fs').readFileSync('./package.json', 'utf8'))"` exits 0
    - Source assertion: `node -e "process.exit(require('./package.json').scripts['smoke:confirm'] === 'node scripts/smoke-confirm-email.mjs' ? 0 : 1)"` exits 0
    - Source assertion: `grep -nF '"smoke:confirm": "node scripts/smoke-confirm-email.mjs"' package.json` matches exactly one line
    - Source assertion: `grep -nFc '"smoke:' package.json` returns 3 (smoke:confirm + smoke:landing + smoke:manage)
    - Behavior assertion: `npm run smoke:confirm` exits 0 and prints `[smoke] result: pass=10 fail=0`
    - The three other `smoke:*` aliases (`smoke:landing`, `smoke:manage`) are byte-identical to before — no accidental edits
    - The `dependencies` and `devDependencies` blocks are byte-identical to before — no new deps
    - Resulting file is valid JSON (no trailing comma after the new entry, no missing comma before)
  </acceptance_criteria>
  <done>
    package.json scripts contains the new `smoke:confirm` alias in alphabetic order; `npm run smoke:confirm` exits 0 against Plan 10-01's code; no other lines in package.json are modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke script → references/teams.json read | Reads a checked-in JSON data file via node:fs. The file is repo-controlled; no untrusted bytes cross. |
| smoke script → process stdout/stderr | Logs to journald-equivalent via console.log/console.error. No network, no DB, no external IPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | scripts/smoke-confirm-email.mjs (inline body composer drift) | mitigate | The smoke RE-IMPLEMENTS the body composer rather than importing src/lib/email.ts — this is intentional (PATTERNS.md §"Re-implementing TS helpers inline to dodge TS-loader friction"). A future edit to src/lib/email.ts that drops the SIGNUP-04 value-prop line, swaps the subject, removes the ASCII apostrophe, or introduces a LAND-02 term causes the smoke to fail. Mitigation: the byte-exact duplication IS the mitigation. Plan 10-03's operator-driven Mail-Tester run is the second-tier net for any byte-equivalence escape (e.g., Resend SDK change). LOW severity; smoke is the regression net for normal-path edits. |
| T-10-02-02 | Tampering | package.json scripts injection | accept | The new alias `smoke:confirm` invokes a checked-in script via bare `node` — no shell metacharacters, no env interpolation, no PATH ambiguity (npm resolves `node` from the standard PATH). Same pattern as smoke:landing and smoke:manage which have shipped under three phases of code review. LOW severity. |

Both threats are LOW; security_block_on=high does not gate this plan.
</threat_model>

<verification>
After both tasks complete:

1. `node scripts/smoke-confirm-email.mjs` — exits 0, prints `[smoke] result: pass=10 fail=0`
2. `npm run smoke:confirm` — same result (proves the alias wires correctly)
3. `node -e "const p=require('./package.json'); console.log(Object.keys(p.scripts).sort().join(' '))"` — shows `astro build check:land-02 dev og:render preview serve smoke:confirm smoke:landing smoke:manage start` (alphabetic ordering preserved within smoke:* group)
4. LAND-02 grep on the smoke source: `! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' scripts/smoke-confirm-email.mjs` exits 0
5. U+2019 negative grep on the smoke source: `! grep -F "$(printf '’')" scripts/smoke-confirm-email.mjs` exits 0
6. Manual drift sanity check (operator may run; not gated): temporarily edit src/lib/email.ts to drop a single character from the value-prop line, re-run `npm run smoke:confirm`, observe at least one FAIL line, revert the edit, re-run, observe `pass=10 fail=0` again. This proves the drift net works.

Per-task acceptance criteria provide the per-task gate; this section is the plan-wide cross-check.
</verification>

<success_criteria>
- `scripts/smoke-confirm-email.mjs` exists and runs offline (no env, no server, no DB) producing 10 PASS lines + final tally
- `npm run smoke:confirm` is wired in package.json and invokes the script with bare node
- The 10 cases cover the exact assertions enumerated in PATTERNS.md §"The 10 smoke cases" — canonical pair, multi-word team, FALLBACK_TZ row, underscore tz, Etc/UTC fallthrough, diacritic-or-slug-fallback, subject literal, LAND-02 grep, unknown-slug fallback, empty-tz fallthrough
- The smoke uses `node:` prefix on built-in imports per CLAUDE.md
- ASCII apostrophe (0x27) used throughout the smoke source; no U+2019
- No new dependencies introduced (package.json `dependencies` and `devDependencies` are byte-identical)
- The smoke is the only automated regression net for the email body — and it passes against Plan 10-01's code
</success_criteria>

<output>
After completion, create `.planning/phases/10-confirmation-email-update/10-02-SUMMARY.md` capturing:
- The smoke script line count and the 10 case names (one bullet each)
- The `node scripts/smoke-confirm-email.mjs` output pasted verbatim (the 10 PASS lines + final tally)
- Confirmation that Case 6 (`curacao`) resolved to the diacritic-preserving label `Curaçao` (asserted string: `every Curaçao match in Curacao time.`)
- The new package.json scripts entry as a one-line confirmation
- A pointer to Plan 10-03 as the next plan (deploy + operator-action verification: Mail-Tester + cross-client + 10-SUMMARY.md evidence record)
</output>
