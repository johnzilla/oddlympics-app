---
phase: 10-confirmation-email-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email.ts
  - src/lib/teams.ts
  - src/lib/timezones.ts
  - src/pages/api/signup.ts
autonomous: true
requirements:
  - SIGNUP-04
tags:
  - email
  - resend
  - confirmation
  - signup

must_haves:
  truths:
    - "Confirmation email body matches SIGNUP-04 spec verbatim — value-prop line copy is locked, team + tz interpolation uses Phase 5 row values"
    - "ASCII apostrophe (0x27) in all body copy — no U+2019"
    - "Zero LAND-02 prohibited terms (bitcoin, lightning, crypto, world domination, personal olympics) anywhere in subject, text body, html body, or headers"
    - "Resend sandbox sender (onboarding@resend.dev) is the production sender — custom-domain DKIM/DMARC for oddlympics.app is locked to v1.1"
    - "Existing dev-fallback console.log path must continue to work — never throw on missing RESEND_API_KEY when NODE_ENV !== 'production'"
    - "sendMagicLink signature widens to (email, token, team, timezone); /api/signup call site passes Phase-5-validated rawTeam and tz"
    - "teamLabel('england') === 'England' via TEAMS lookup; falls back to raw slug when unknown"
    - "tzLabel('America/Detroit') === 'Detroit time'; tzLabel('Etc/UTC') === 'your local time'; mirrors src/pages/index.astro:200-210 byte-equivalently"
    - "Resend send call carries Reply-To: hello@oddlympics.app via the SDK replyTo field AND List-Unsubscribe + List-Unsubscribe-Post headers via buildUnsubscribeHeaders(email)"
    - "sendManageLink (Phase 9 D-01) is unchanged — no edits to its signature, subject, body, URL, or send call"
  artifacts:
    - path: "src/lib/teams.ts"
      provides: "teamLabel(slug) exported helper, sits next to isValidTeamSlug"
      contains: "export function teamLabel"
    - path: "src/lib/timezones.ts"
      provides: "tzLabel(tz) exported helper, sits next to isValidTimezone"
      contains: "export function tzLabel"
    - path: "src/lib/email.ts"
      provides: "Widened sendMagicLink signature, new subject + body copy, replyTo + List-Unsubscribe headers on Resend send"
      contains: "sendMagicLink(email: string, token: string, team: string, timezone: string)"
    - path: "src/pages/api/signup.ts"
      provides: "Call site passes (rawEmail, token, rawTeam, tz)"
      contains: "sendMagicLink(rawEmail, token, rawTeam, tz)"
  key_links:
    - from: "src/lib/email.ts:sendMagicLink"
      to: "src/lib/teams.ts:teamLabel"
      via: "ESM named import"
      pattern: "import.*teamLabel.*from.*'./teams'"
    - from: "src/lib/email.ts:sendMagicLink"
      to: "src/lib/timezones.ts:tzLabel"
      via: "ESM named import"
      pattern: "import.*tzLabel.*from.*'./timezones'"
    - from: "src/lib/email.ts:sendMagicLink"
      to: "src/lib/email.ts:buildUnsubscribeHeaders"
      via: "in-file call, spread into Resend headers field"
      pattern: "headers:.*buildUnsubscribeHeaders\\(email\\)"
    - from: "src/pages/api/signup.ts:109"
      to: "src/lib/email.ts:sendMagicLink"
      via: "4-arg call with validated primitives"
      pattern: "sendMagicLink\\(rawEmail, token, rawTeam, tz\\)"
---

<objective>
Widen `sendMagicLink()` to accept `team` (snake_case slug) and `timezone`
(IANA) primitives validated upstream by `/api/signup`. Add two pure-data
helpers — `teamLabel(slug)` in `src/lib/teams.ts` and `tzLabel(tz)` in
`src/lib/timezones.ts` — to render the SIGNUP-04 value-prop line:

> *We'll email you 1 hour before every England match in Detroit time.*

Rewrite the confirmation email subject + plain-text body + HTML body to drop
teaser-era copy ("VIP", "early access", "spot", "launch ping") and ship the
v2.0 consumer copy. Wire two Mail-Tester-lifting headers onto the Resend send
call: `replyTo: 'hello@oddlympics.app'` (Resend SDK camelCase per
`node_modules/resend/dist/index.d.mts:551`) and the existing
`buildUnsubscribeHeaders(email)` (List-Unsubscribe + List-Unsubscribe-Post
with 1y TTL inherited from Phase 9 D-05). Extend the dev-fallback `console.log`
branch to also print the rendered value-prop line so contributors without
`RESEND_API_KEY` can verify team + tz interpolation without firing Resend.

Update the single caller `src/pages/api/signup.ts:109` to pass `rawTeam` and
`tz` — both already in scope after the Phase 5 validation chain.

Purpose: SIGNUP-04 demands the confirmation email body name the user's team
and timezone in human-readable form. This plan delivers that asset; Plans
10-02 and 10-03 verify offline + on production.

Output:
- `src/lib/teams.ts` exports new `teamLabel(slug: string): string` (D-03)
- `src/lib/timezones.ts` exports new `tzLabel(tz: string): string` (D-02)
- `src/lib/email.ts:sendMagicLink` signature widens (D-01); subject (D-05)
  and body (D-04) replaced; Resend send call gains `replyTo` + `headers`
- `src/pages/api/signup.ts:109` call site widens to pass `rawTeam, tz`
- `src/lib/email.ts:sendManageLink` and `buildUnsubscribeHeaders` UNCHANGED
- `astro build` (or `tsc --noEmit` equivalent via `astro check`) passes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-confirmation-email-update/10-CONTEXT.md
@.planning/phases/10-confirmation-email-update/10-RESEARCH.md
@.planning/phases/10-confirmation-email-update/10-PATTERNS.md
@.planning/phases/10-confirmation-email-update/10-VALIDATION.md
@.planning/phases/09-manage-editor-unsubscribe/09-CONTEXT.md
@.planning/phases/05-schema-signup-payload/05-CONTEXT.md
@CLAUDE.md
@src/lib/email.ts
@src/lib/teams.ts
@src/lib/timezones.ts
@src/pages/api/signup.ts
@src/pages/index.astro
@references/teams.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/lib/teams.ts (lines 1-14):
```ts
import teams from '../../references/teams.json' with { type: 'json' };

export type TeamEntry = {
  slug: string;
  label: string;
  confederation: 'UEFA' | 'CONMEBOL' | 'CONCACAF' | 'CAF' | 'AFC' | 'OFC';
};

export const TEAMS: readonly TeamEntry[] = teams as TeamEntry[];
export const VALID_TEAMS: ReadonlySet<string> = new Set(TEAMS.map((t) => t.slug));

export function isValidTeamSlug(slug: string): boolean {
  return VALID_TEAMS.has(slug);
}
```

From src/lib/timezones.ts (lines 1-15):
```ts
export const FALLBACK_TZ = 'America/New_York' as const;

function buildTzSet(): ReadonlySet<string> { ... }

export const VALID_TZ: ReadonlySet<string> = buildTzSet();

export function isValidTimezone(tz: string): boolean {
  return VALID_TZ.has(tz);
}
```

From src/lib/email.ts (current, lines 1-13 — preamble + module-load throw, DO NOT TOUCH):
```ts
import { Resend } from 'resend';
import { mintToken } from './token';

const API_KEY = process.env.RESEND_API_KEY ?? '';
const FROM = process.env.EMAIL_FROM ?? 'oddlympics <onboarding@resend.dev>';
const SITE_URL = process.env.PUBLIC_SITE_URL ?? 'http://localhost:4321';
const isProd = process.env.NODE_ENV === 'production';

if (!API_KEY && isProd) {
  throw new Error('RESEND_API_KEY is required in production');
}

const resend = API_KEY ? new Resend(API_KEY) : null;
```

From src/lib/email.ts (current sendMagicLink signature to widen):
```ts
export async function sendMagicLink(email: string, token: string): Promise<void>
```

Target signature (per D-01):
```ts
export async function sendMagicLink(
  email: string,
  token: string,
  team: string,         // snake_case slug, member of VALID_TEAMS
  timezone: string,     // IANA, member of VALID_TZ or literally FALLBACK_TZ
): Promise<void>
```

From src/lib/email.ts:55-65 (buildUnsubscribeHeaders — DO NOT MODIFY, just consume):
```ts
export function buildUnsubscribeHeaders(email: string): {
  'List-Unsubscribe': string;
  'List-Unsubscribe-Post': string;
} {
  const token = mintToken(email, { purpose: 'unsubscribe' });
  const url = `${SITE_URL}/api/unsubscribe?token=${encodeURIComponent(token)}`;
  return {
    'List-Unsubscribe': `<${url}>`,
    'List-Unsubscribe-Post': 'List-Unsubscribe=One-Click',
  };
}
```

From src/pages/api/signup.ts:75-90 (validation chain producing rawTeam + tz — DO NOT TOUCH, just observe):
```ts
const rawTeam = ((form.get('team') as string | null) ?? '').trim().toLowerCase();
if (!VALID_TEAMS.has(rawTeam)) {
  console.log(`[signup] bad-team rejected email=${rawEmail} input=${JSON.stringify(rawTeam)}`);
  return back('bad-form');
}

const rawTz = ((form.get('timezone') as string | null) ?? '').trim();
let tz: string;
if (rawTz && VALID_TZ.has(rawTz)) {
  tz = rawTz;
} else {
  tz = FALLBACK_TZ;
  console.log(`[signup] tz-fallback email=${rawEmail} input=${JSON.stringify(rawTz)}`);
}
```

From src/pages/api/signup.ts:108-113 (current call site to widen):
```ts
try {
  await sendMagicLink(rawEmail, token);
} catch (err) {
  console.error('[signup] email error', err);
  return back('email');
}
```

Resend SDK CreateEmailBaseOptions (from node_modules/resend/dist/index.d.mts:511-565):
- Required: `from`, `to`, `subject`
- One of (or both): `text`, `html`
- Optional: `headers: Record<string,string>`, `replyTo: string | string[]` (camelCase),
  `tags`, `attachments`, etc.
- Note: BOTH `replyTo` (camelCase, typed SDK field) and `reply_to` (snake_case, HTTP wire)
  are accepted; PATTERNS.md §"Resend `emails.send` call" pins us to `replyTo` because
  it is the typed/idiomatic field. The SDK translates to wire format.

From src/pages/index.astro:200-210 (the inline JS algorithm tzLabel must mirror byte-for-byte):
```js
const tz = Intl.DateTimeFormat().resolvedOptions().timeZone || '';
if (tz && tz.indexOf('/') !== -1 && tz.indexOf('Etc/') !== 0) {
  const label = tz.split('/').pop().replace(/_/g, ' ') + ' time';
  if (label && label !== ' time') { /* set #tz-label */ }
}
// otherwise (no slash, Etc/* prefix, or empty post-replace): label remains the
// HTML fallback 'your local time'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add teamLabel + tzLabel helpers to lib</name>
  <files>src/lib/teams.ts, src/lib/timezones.ts</files>
  <read_first>
    - src/lib/teams.ts (full — 14 lines, see Interfaces above for content)
    - src/lib/timezones.ts (full — 15 lines, see Interfaces above for content)
    - src/pages/index.astro lines 198-211 (the inline JS tzLabel mirrors verbatim — see Interfaces above for excerpt)
    - .planning/phases/10-confirmation-email-update/10-PATTERNS.md §"src/lib/teams.ts" + §"src/lib/timezones.ts" sections for the exact insertion points
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-02 and D-03 for the locked semantics
    - CLAUDE.md §"TypeScript" + §"What you won't see in this codebase" for the no-JSDoc + return-type convention
  </read_first>
  <action>
    Append two new exported helpers to existing lib files, immediately AFTER each file's existing `is...` helper (no JSDoc, no inline comments except the one why-comment on tzLabel noted below).

    File 1: src/lib/teams.ts
    Per D-03, append a new exported function named `teamLabel` after `isValidTeamSlug`. Signature: `teamLabel(slug: string): string`. Implementation uses `TEAMS.find((t) => t.slug === slug)?.label ?? slug` — return the canonical English label from references/teams.json, fall back to the raw slug when the slug is not in TEAMS (future-proofs retired-team rows; never produces an empty span in the email body). No comments needed — function name self-documents.

    File 2: src/lib/timezones.ts
    Per D-02, append a new exported function named `tzLabel` after `isValidTimezone`. Signature: `tzLabel(tz: string): string`. Algorithm mirrors src/pages/index.astro:200-210 byte-for-byte:
    1. If `tz` is falsy OR has no `/` OR starts with `Etc/` → return the literal string 'your local time'
    2. Take the last `/`-separated segment of `tz`, replace `_` with space → `${human} time`
    3. If the post-replace `human` is empty → return 'your local time'

    Examples for the executor to verify mentally before writing:
    - tzLabel('America/Detroit') → 'Detroit time'
    - tzLabel('America/New_York') → 'New York time'  (underscore becomes space)
    - tzLabel('Europe/London') → 'London time'
    - tzLabel('Asia/Ho_Chi_Minh') → 'Ho Chi Minh time'
    - tzLabel('Etc/UTC') → 'your local time'
    - tzLabel('UTC') → 'your local time' (no slash)
    - tzLabel('') → 'your local time'

    Add ONE why-comment immediately above tzLabel per PATTERNS.md §"src/lib/timezones.ts":
    `// Mirrors src/pages/index.astro:204-210 byte-for-byte — the email-side label`
    `// and the landing-side label must agree on every input the user could see.`

    Use the ASCII apostrophe (0x27) — never U+2019 — in the literal 'your local time' (no apostrophe in that literal, but the convention applies to all body copy).

    No `any`, no JSDoc, no `node:` import needed (pure string ops). Both helpers keep return-type annotations per CLAUDE.md TypeScript conventions.

    Implements D-02, D-03.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -20 ; grep -nF "export function teamLabel" src/lib/teams.ts &amp;&amp; grep -nF "export function tzLabel" src/lib/timezones.ts &amp;&amp; node -e "import('./src/lib/teams.ts').catch(()=>{}); import('./src/lib/timezones.ts').catch(()=>{})" 2>&amp;1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nF "export function teamLabel(slug: string): string" src/lib/teams.ts` matches exactly one line
    - Source assertion: `grep -nF "export function tzLabel(tz: string): string" src/lib/timezones.ts` matches exactly one line
    - Source assertion: `grep -nF "TEAMS.find" src/lib/teams.ts` matches exactly one line (within teamLabel body)
    - Source assertion: `grep -nF "Mirrors src/pages/index.astro" src/lib/timezones.ts` matches exactly one line (the why-comment)
    - Behavior assertion (after Plan 10-02 ships): smoke cases 1-6, 9, 10 PASS
    - No new imports added to either file (both helpers are pure on existing exports / inputs)
    - `npx astro check` reports zero TypeScript errors in either modified file
    - `sendManageLink` body and signature in src/lib/email.ts UNCHANGED (Phase 9 D-01 locks it; this task touches teams.ts + timezones.ts only)
  </acceptance_criteria>
  <done>
    Two new exported helpers exist (`teamLabel` in teams.ts, `tzLabel` in timezones.ts) with return-type annotations, no JSDoc, one earned why-comment on tzLabel only. astro check passes. The four file paths in this plan's files_modified are correct: only teams.ts and timezones.ts changed by this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen sendMagicLink + rewrite subject + body + wire headers</name>
  <files>src/lib/email.ts</files>
  <read_first>
    - src/lib/email.ts (full — 100 lines, see Interfaces above for the parts to keep verbatim)
    - .planning/phases/10-confirmation-email-update/10-PATTERNS.md §"src/lib/email.ts (MODIFIED — email-send lib, request-response)" for the exact lines to swap vs keep
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-01 / D-04 / D-05 / D-06 / D-07 for the locked signature, subject, body, multipart, sender constraints
    - .planning/phases/10-confirmation-email-update/10-RESEARCH.md §"Multipart message construction with Resend" for the replyTo vs reply_to clarification (use replyTo — typed SDK field)
    - .planning/phases/10-confirmation-email-update/10-RESEARCH.md §5 for Mail-Tester anti-patterns the body must NOT trip on
    - CLAUDE.md §"Dev email fallback" for the dev-fallback invariant (must NOT throw on missing RESEND_API_KEY in non-prod)
    - CLAUDE.md §"Conventions established" for the ASCII apostrophe rule (0x27 not U+2019)
  </read_first>
  <action>
    Edit src/lib/email.ts. Touch ONLY `sendMagicLink` (lines 15-53) — do NOT modify the preamble (lines 1-13), do NOT modify `buildUnsubscribeHeaders` (lines 55-65), do NOT modify `sendManageLink` (lines 67-100; Phase 9 D-01 locks it).

    Sub-step A — Imports
    At the top of src/lib/email.ts, add two new ESM named imports below the existing `import { mintToken } from './token';`:
    `import { teamLabel } from './teams';`
    `import { tzLabel } from './timezones';`
    No other imports added. Lib must NOT import `db.ts` (D-01: caller passes validated primitives).

    Sub-step B — Widen signature (D-01)
    Change `export async function sendMagicLink(email: string, token: string): Promise<void>` to a 4-arg signature accepting `email: string, token: string, team: string, timezone: string` returning `Promise<void>`. Argument order is fixed: email, token, team, timezone. Keep the `async` keyword and the `: Promise<void>` return-type annotation. No JSDoc.

    Sub-step C — Subject swap (D-05)
    Replace the subject literal `'Confirm your spot — oddlympics'` with the new literal:
    `'Confirm your World Cup alerts — oddlympics'`
    Em-dash is U+2014 (preserved from the existing literal; do NOT replace with hyphen). NO team name in the subject (D-05 explicitly rejects subject personalization on the sandbox sender).

    Sub-step D — Compute human labels
    Inside `sendMagicLink`, after the existing `const url = ...` line, add two new const bindings before the `subject` line:
    `const teamHuman = teamLabel(team);`
    `const tzHuman = tzLabel(timezone);`
    No comments. These are the strings that fill the SIGNUP-04 value-prop line in both text and html.

    Sub-step E — Plain-text body rewrite (D-04 + D-06)
    Replace the entire `const text = [ ... ].join('\n')` array (lines 18-29 of the current file) with new contents in this exact shape:
    - Line 0: `'Confirm your World Cup alerts for oddlympics.'`
    - Line 1: `''` (blank)
    - Line 2: `'Click below to confirm:'`
    - Line 3: `url`
    - Line 4: `''`
    - Line 5: `` `We'll email you 1 hour before every ${teamHuman} match in ${tzHuman}.` ``
    - Line 6: `''`
    - Line 7: `'No spam. No ads. Unsubscribe anytime.'`
    - Line 8: `''`
    - Line 9: `"If you didn't request this, ignore this email."`
    - Line 10: `''`
    - Line 11: `'— oddlympics'`

    ASCII apostrophe (0x27) in `We'll` and `didn't` — not U+2019. Em-dash (U+2014) preserved in the final `— oddlympics` line and in the subject.

    Sub-step F — HTML body rewrite (D-04 + D-06)
    Replace the entire `const html = ...` template-literal block (lines 31-41 of the current file) with the new HTML body. Preserve the structural shell verbatim (doctype, body inline style, max-width card div, accent button color, font sizes). Swap ONLY the inner copy:
    - `<h1>Confirm your spot</h1>` → `<h1>Confirm your alerts</h1>` (keep the existing inline styles)
    - First `<p>` (`Click below to lock in your early access for <strong>oddlympics</strong>.`) → the SIGNUP-04 value-prop line with `<strong>` wrapping the team name: `` `<p style="margin:0 0 20px;line-height:1.55">We'll email you 1 hour before every <strong>${teamHuman}</strong> match in ${tzHuman}.</p>` ``
    - Keep the accent-orange CTA button anchor (the `<a href="${url}" ...>Confirm email</a>` block) byte-identical — label stays `Confirm email`.
    - Keep the "Or paste this URL:" hint and the URL-paste paragraph byte-identical.
    - Replace the final disclaimer paragraph `No spam, no marketing — just the launch ping. If you didn't request this, ignore this email.` with: `No spam. No ads. Unsubscribe anytime. If you didn't request this, ignore this email.` (ASCII apostrophe; existing inline style preserved verbatim).

    No `<style>` block, no `mso-*` attributes, no remote images, no remote fonts (PATTERNS.md / RESEARCH.md §4). Single CTA anchor + paste-URL hint preserved.

    Sub-step G — Dev-fallback log extension
    Preserve the `if (!resend) { ... return; }` short-circuit block VERBATIM in shape (CLAUDE.md §"Dev email fallback" invariant: NEVER throw on missing RESEND_API_KEY in non-prod). Extend the existing two console.log lines with a THIRD log line so contributors can verify team + tz interpolation without firing Resend. Suggested third log:
    `console.log('   body:', \`every ${teamHuman} match in ${tzHuman}\`, '\n');`
    Keep the `[email-dev-fallback]` tag on the first line (Phase 9 may grep for it). The return-after-log must remain — no Resend call happens when `resend` is null.

    Sub-step H — Resend send call: add replyTo + headers (Claude's Discretion, locked to YES by Mail-Tester lift)
    Replace `const { error } = await resend.emails.send({ from: FROM, to: email, subject, text, html });` with the widened shape:
    ```ts
    const { error } = await resend.emails.send({
      from: FROM,
      to: email,
      subject,
      text,
      html,
      replyTo: 'hello@oddlympics.app',
      headers: buildUnsubscribeHeaders(email),
    });
    ```
    Note: the SDK typed field is `replyTo` (camelCase per node_modules/resend/dist/index.d.mts:551), NOT `reply_to`. The HTTP wire takes `reply_to`; the SDK translates. Stay on the typed field.

    `buildUnsubscribeHeaders(email)` is already imported by virtue of being defined in the same file; no import statement needed. Spreading it into `headers` adds the `List-Unsubscribe` + `List-Unsubscribe-Post` headers with the Phase 9 D-05 1-year TTL inherited automatically.

    Keep the `if (error) throw new Error(\`Resend error: ${error.message}\`);` line VERBATIM. The throw-on-error contract is the established three-error-pattern (CONVENTIONS.md §"Error handling"); the caller in `/api/signup.ts` catches and `back('email')` — no new error codes (COMPAT-02 invariant).

    Implements D-01, D-04, D-05, D-06, D-07. The Resend `from` field still resolves to `'oddlympics <onboarding@resend.dev>'` via the `FROM` const (D-07 locks the sandbox sender).
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -20 ; grep -nF "Confirm your World Cup alerts — oddlympics" src/lib/email.ts ; grep -nFc "We'll email you 1 hour before every" src/lib/email.ts ; grep -nF "replyTo: 'hello@oddlympics.app'" src/lib/email.ts ; grep -nF "headers: buildUnsubscribeHeaders(email)" src/lib/email.ts ; grep -nF "team: string," src/lib/email.ts ; grep -nF "timezone: string," src/lib/email.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nFc "Confirm your World Cup alerts — oddlympics" src/lib/email.ts` equals 1 (D-05 subject literal present exactly once)
    - Source assertion: `grep -nF "We'll email you 1 hour before every" src/lib/email.ts | grep -v '^#' | wc -l` ≥ 2 (line appears in both text and html bodies)
    - Source assertion: `grep -nF "replyTo: 'hello@oddlympics.app'" src/lib/email.ts` matches exactly one line
    - Source assertion: `grep -nF "headers: buildUnsubscribeHeaders(email)" src/lib/email.ts` matches exactly one line
    - Source assertion: `grep -nF "team: string," src/lib/email.ts && grep -nF "timezone: string," src/lib/email.ts` — both new sendMagicLink params present
    - Source assertion: `grep -nF "Confirm your spot — oddlympics" src/lib/email.ts` returns nothing (old subject removed)
    - Source assertion: `grep -nF "lock in your early access" src/lib/email.ts` returns nothing (old teaser copy removed)
    - Source assertion: `grep -nF "launch ping" src/lib/email.ts` returns nothing (old teaser copy removed)
    - Source assertion: `grep -nF "Pick your World Cup teams — oddlympics" src/lib/email.ts` matches exactly one line (sendManageLink UNCHANGED — Phase 9 D-01 invariant)
    - LAND-02 grep on source (defense in depth — final body grep runs in smoke as Case 8): `! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' src/lib/email.ts` exits 0
    - U+2019 negative check: `! grep -F "$(printf '’')" src/lib/email.ts` exits 0 (no curly apostrophe anywhere in file)
    - `npx astro check` reports zero TypeScript errors in src/lib/email.ts
    - Behavior assertion (after Plan 10-02 ships): smoke cases 1-10 PASS
    - File line-count sanity: src/lib/email.ts grows by roughly 5-10 lines net (preamble + buildUnsubscribeHeaders + sendManageLink all untouched; only the widened sendMagicLink + 2 imports change)
  </acceptance_criteria>
  <done>
    src/lib/email.ts: sendMagicLink has the 4-arg signature, the new subject + text + html bodies, the new replyTo + headers fields on the Resend send, and an extended 3-line dev-fallback log. buildUnsubscribeHeaders and sendManageLink are byte-identical to before. astro check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Widen /api/signup call site to pass rawTeam + tz</name>
  <files>src/pages/api/signup.ts</files>
  <read_first>
    - src/pages/api/signup.ts lines 70-120 (the Phase 5 validation chain + the call site — see Interfaces above for excerpts)
    - .planning/phases/10-confirmation-email-update/10-PATTERNS.md §"src/pages/api/signup.ts" for the exact line + the DO-NOT-touch list (try/catch shape, console.error tag, back('email') redirect)
    - .planning/phases/10-confirmation-email-update/10-CONTEXT.md §Decisions D-01 for the call-site widening contract
    - .planning/REQUIREMENTS.md §SIGNUP-04 + §COMPAT-02 (no new error codes)
  </read_first>
  <action>
    Edit src/pages/api/signup.ts at line 109 ONLY. Change the single call:
    `await sendMagicLink(rawEmail, token);`
    to:
    `await sendMagicLink(rawEmail, token, rawTeam, tz);`

    Both `rawTeam` and `tz` are already in scope from the Phase 5 validation chain at lines 75-90:
    - `rawTeam` is guaranteed to be a member of `VALID_TEAMS` (the `if (!VALID_TEAMS.has(rawTeam)) return back('bad-form')` guard at line 77 ensures this)
    - `tz` is guaranteed to be either a member of `VALID_TZ` or literally `FALLBACK_TZ = 'America/New_York'` (the `if (rawTz && VALID_TZ.has(rawTz)) { tz = rawTz; } else { tz = FALLBACK_TZ; ... }` branch at lines 85-90 ensures this)

    DO NOT TOUCH (PATTERNS.md call-out list):
    - The surrounding try/catch shape — lib throws, caller catches and redirects (CONVENTIONS.md §"Error handling")
    - The `console.error('[signup] email error', err)` log tag — Phase 9 may grep for it
    - The `back('email')` redirect on catch — Phase 6 D-* binds the error-code surface; introducing `?error=email-bad-team` would be a COMPAT-02 violation
    - Lines 1-69 (imports, request parsing, Origin / honeypot / email-regex / rate-limit / team / tz validation)
    - Lines 92-104 (the `upsertVipSignup.get(...)` DB write block)
    - Lines 115-119 (the 303 redirect to `/pending`)
    - The `mintToken(rawEmail)` call at line 106 (24h confirm-purpose token from Phase 9 D-05)

    No new imports needed — `sendMagicLink` is already imported. No new variable declarations — both new args are local-scope bindings created upstream.

    Implements D-01 (caller-passes-validated-primitives).
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -20 ; grep -nF "sendMagicLink(rawEmail, token, rawTeam, tz)" src/pages/api/signup.ts ; grep -nFc "sendMagicLink(rawEmail, token);" src/pages/api/signup.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nF "sendMagicLink(rawEmail, token, rawTeam, tz)" src/pages/api/signup.ts` matches exactly one line
    - Source assertion: `grep -nFc "sendMagicLink(rawEmail, token);" src/pages/api/signup.ts` returns 0 (old 2-arg form removed)
    - Source assertion: `grep -nF "[signup] email error" src/pages/api/signup.ts` matches exactly one line (log tag preserved)
    - Source assertion: `grep -nF "back('email')" src/pages/api/signup.ts` matches exactly one line (error-code surface unchanged — COMPAT-02 invariant)
    - Source assertion: `grep -nF "if (!VALID_TEAMS.has(rawTeam))" src/pages/api/signup.ts` matches exactly one line (the Phase 5 validation guard producing rawTeam is preserved verbatim)
    - Source assertion: `grep -nF "tz = FALLBACK_TZ" src/pages/api/signup.ts` matches exactly one line (the Phase 5 tz fallback path is preserved verbatim)
    - `npx astro check` reports zero TypeScript errors (the new 4-arg call type-matches the widened sendMagicLink signature from Task 2)
    - Behavior assertion: a manual dev-mode signup at `npm run dev` → POST /api/signup with a valid team + tz produces a `[email-dev-fallback] Magic link for <email>` console line followed by `   body: every <Team> match in <TzLabel>` line (the extended dev-fallback log from Task 2) — proves end-to-end interpolation pre-deploy
  </acceptance_criteria>
  <done>
    The single sendMagicLink call site is widened to pass 4 args. astro check passes. The Phase 5 validation chain producing rawTeam + tz is untouched. The COMPAT-02 error-code surface (back('email') with no new ?error= codes) is preserved. The plan's must_haves.key_links are satisfied — all four ESM links between sendMagicLink and its new collaborators (teamLabel, tzLabel, buildUnsubscribeHeaders, the call site) are present in source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP POST → /api/signup | Untrusted client form input crosses into the server here. By the time Phase 10 code (sendMagicLink) is reached on line 109, the Phase 5 validation chain at lines 75-90 has already constrained `rawTeam` to VALID_TEAMS and `tz` to VALID_TZ ∪ {FALLBACK_TZ}. These two inputs are allow-listed enums, not free-form strings, before they reach Phase 10. |
| email body composition → Resend HTTP API | The composed `subject`, `text`, `html`, `replyTo`, and `headers` cross from our process into Resend's send pipeline. Resend signs with its own DKIM on resend.dev (D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering / Injection | sendMagicLink body composer in src/lib/email.ts | accept | Email body XSS / HTML injection via team or tz interpolation. team comes from VALID_TEAMS allow-list (fixed snake_case slug character set, source: references/teams.json); tz comes from VALID_TZ IANA set (Intl.supportedValuesOf('timeZone')); teamLabel returns the canonical label from TEAMS (also fixed-content, no user-controlled string). No user-controlled bytes reach the body. Validated upstream — no escaping needed because the inputs are allow-listed enums. LOW severity; documented for traceability. |
| T-10-02 | Information disclosure | Magic-link token in confirmation email URL | accept | Token leak via email body URL. The existing magic-link token is HMAC-signed with purpose=confirm (Phase 9 D-05 TTL = 24h); single-use guard via markConfirmed WHERE clause is unchanged. Phase 10 does not alter token mechanics — just widens sendMagicLink's body. LOW severity. |
| T-10-03 | Repudiation / Replay | List-Unsubscribe header newly wired onto confirmation send | accept | Unsubscribe header forgery / replay. 1y HMAC-signed unsubscribe token from buildUnsubscribeHeaders (Phase 9 D-05 TTL); DB-layer idempotency via WHERE unsubscribed_at IS NULL clause in /api/unsubscribe (Phase 9 D-06). Phase 10 only wires the header onto the confirmation send — does not change semantics. Second click on the same token is a no-op. LOW severity. |

All three threats are LOW; security_block_on=high means none of these gates the phase.
</threat_model>

<verification>
After all three tasks complete:

1. `npx astro check 2>&1 | tail -20` — zero TypeScript errors
2. `npm run build` — succeeds, dist/server/entry.mjs produced
3. `! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' src/lib/email.ts` — LAND-02 grep on source (final body grep runs in Plan 10-02 smoke Case 8)
4. `! grep -F "$(printf '’')" src/lib/email.ts` — no U+2019 curly apostrophe anywhere
5. Manual dev-mode smoke: `npm run dev` in one terminal; from another terminal POST a real signup with `team=england`:
   ```
   curl -i -X POST http://127.0.0.1:4321/api/signup \
     -H "Origin: http://127.0.0.1:4321" \
     -H "Content-Type: application/x-www-form-urlencoded" \
     -d 'email=dev@example.com&team=england&timezone=America/Detroit&requested_sport=world_cup&website='
   ```
   Expected: dev server console prints `[email-dev-fallback] Magic link for dev@example.com` followed by the magic-link URL AND a third line `   body: every England match in Detroit time` — proves the widened signature, the dev-fallback extension, and the team + tz interpolation all wire end-to-end before Plan 10-02 even runs.

Per-task acceptance criteria provide the per-task gate; this section is the plan-wide cross-check.
</verification>

<success_criteria>
- `src/lib/teams.ts` exports `teamLabel(slug: string): string` (single grep hit)
- `src/lib/timezones.ts` exports `tzLabel(tz: string): string` (single grep hit)
- `src/lib/email.ts:sendMagicLink` has the 4-arg signature `(email, token, team, timezone): Promise<void>` and the new subject + body + replyTo + headers fields
- `src/lib/email.ts:sendManageLink` and `buildUnsubscribeHeaders` are byte-identical to pre-Phase-10
- `src/pages/api/signup.ts:109` passes `(rawEmail, token, rawTeam, tz)`
- `npx astro check` passes with zero errors
- The U+2019 negative grep, the LAND-02 source grep, and all explicit grep-presence assertions in the per-task acceptance criteria pass
- A manual dev-mode signup POST produces the extended `[email-dev-fallback]` log including the `body: every <Team> match in <TzLabel>` line — visual confirmation of team + tz interpolation without firing Resend
</success_criteria>

<output>
After completion, create `.planning/phases/10-confirmation-email-update/10-01-SUMMARY.md` capturing:
- Files modified (4: teams.ts, timezones.ts, email.ts, api/signup.ts) with line-count delta per file
- The literal subject and value-prop strings shipped, verbatim, for downstream Plan 10-02 byte-equivalence reference
- The astro check exit status and any zero-error confirmation
- The dev-mode manual smoke output (the 3-line dev-fallback log block) pasted verbatim as evidence of working interpolation pre-deploy
- A pointer to Plan 10-02 as the next plan (offline smoke that locks the byte-equivalence)
- A pointer to Plan 10-03 as the deploy + operator-action verification plan
</output>
