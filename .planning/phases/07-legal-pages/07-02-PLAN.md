---
phase: 07-legal-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/terms.astro
autonomous: true
requirements:
  - LEGAL-02
user_setup: []

must_haves:
  truths:
    - "GET /terms returns HTTP 200 with canonical terms copy"
    - "/terms declares: free service through 2026-07-19"
    - "/terms declares: best-effort delivery, no liability for FIFA reschedules or delivery failures"
    - "/terms declares: no FIFA/ESPN/team affiliation"
    - "/terms prohibits submitting fake or others' email addresses"
    - "/terms names Michigan, USA as the governing law"
    - "/terms exposes hello@oddlympics.app for questions; email address renders as `<a href=mailto:><code>` (per D-06)"
    - "/terms shows a Last-updated date matching the deploy date — hardcoded `LAST_UPDATED` constant rendered as long-form US date (per D-03, D-04)"
    - "/terms uses the same site shell (fonts, footer, .wrap, :root tokens) as /, with footer-only navigation and no top header bar (per D-05)"
    - "/terms contains zero LAND-02 prohibited terms (case-insensitive)"
    - "/terms has no inline <script> blocks and no analytics surface"
  artifacts:
    - path: "src/pages/terms.astro"
      provides: "Astro prerendered terms page"
      contains: "export const prerender = true"
      min_lines: 100
  key_links:
    - from: "src/pages/terms.astro"
      to: "references/terms.md"
      via: "hand-translated body copy (per D-01)"
      pattern: "2026-07-19.*Michigan.*hello@oddlympics.app"
    - from: "src/pages/terms.astro footer"
      to: "/manage, /privacy, /terms, mailto:hello@oddlympics.app"
      via: "verbatim copy of src/pages/index.astro:186-196"
      pattern: "Independent project . Not affiliated with FIFA"
    - from: "src/pages/terms.astro <style is:global>"
      to: "src/pages/index.astro :root + .wrap + .site-footer"
      via: "verbatim CSS tokens + container + footer rules (per D-07, D-08)"
      pattern: "--max: 720px"
---

<objective>
Ship `src/pages/terms.astro` — an Astro prerendered static page rendering canonical terms copy from `references/terms.md` on the same v2.0 consumer site shell as `src/pages/index.astro`, satisfying LEGAL-02 and paid-ad reviewer requirements.

Purpose: paid-ad reviewers and basic legal hygiene require a `/terms` route declaring free service through 2026-07-19, best-effort delivery (no liability for FIFA reschedules or delivery failures), no FIFA/ESPN/team affiliation, prohibition on submitting fake or others' emails, Michigan (USA) governing law, and a contact (`hello@oddlympics.app`) — all on a page visually consistent with the landing.

Output: one new file `src/pages/terms.astro` rendering at 200 OK on `npm run dev`, building cleanly via `npm run build`, and passing the LAND-02 grep gate against its own `dist/client` output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-legal-pages/07-CONTEXT.md
@.planning/phases/07-legal-pages/07-PATTERNS.md
@references/terms.md
@src/pages/index.astro

<interfaces>
<!-- Tokens and shell selectors the executor MUST copy verbatim from index.astro. -->
<!-- Extracted from src/pages/index.astro on 2026-05-13. No exploration needed. -->

From src/pages/index.astro frontmatter (line 4):
- `export const prerender = true;`

From src/pages/index.astro <head> (lines 42-48, 62):
- `<!doctype html>` + `<html lang="en">`
- `<meta charset="utf-8" />` + `<meta name="viewport" content="width=device-width, initial-scale=1" />`
- `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`
- Template-interpolated `<title>{TITLE}</title>` and `<meta name="description" content={DESCRIPTION} />`

From src/pages/index.astro footer (lines 186-196) — paste VERBATIM:
- `<footer class="site-footer">` wrapping `<div class="wrap">` wrapping `<nav class="links">` + `<p class="copy">`
- Four nav links: `/manage`, `/privacy`, `/terms`, `mailto:hello@oddlympics.app`
- Copy line: `© 2026 Oddlympics · Independent project · Not affiliated with FIFA` (middle-dot U+00B7 separators)

From src/pages/index.astro <style is:global> (lines 252-289, 564-595) — paste VERBATIM:
- `:root` token block: `--bg`, `--fg`, `--fg-dim`, `--line`, `--line-strong`, `--surface`, `--accent`, `--accent-ink`, `--accent-soft`, `--font-sans`, `--font-mono`, `--max: 720px`, `--pad-x`
- `* { box-sizing: border-box; }`
- `html, body { margin: 0; background: var(--bg); color: var(--fg); font-family: var(--font-sans); font-size: 15px; line-height: 1.55; ... }`
- `body { min-height: 100dvh; }`
- `.wrap { max-width: var(--max); margin: 0 auto; padding-left/right: var(--pad-x); }`
- Full `.site-footer` rule set (border-top, padding, `.links` flex, `a` styling, `:focus-visible` ring, `.copy` mono)

Pattern Map authoritative source: `.planning/phases/07-legal-pages/07-PATTERNS.md` Patterns 1–7 + S1–S5.

Canonical body copy (verbatim hand-translation target): `references/terms.md`.

Coordination note: this plan and plan 01 (privacy.astro) MUST agree on heading-level rendering for `**Bold:**` section labels. Plan 01 specifies `<h2>` per PATTERNS Pattern 3 / 5e recommendation; plan 02 MUST match. If the structure of the terms doc differs (numbered list vs bullet-list sections), still render the section labels as `<h2>` where they appear.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/pages/terms.astro from index.astro shell + references/terms.md body</name>
  <files>src/pages/terms.astro</files>
  <read_first>
    - src/pages/index.astro (canonical site shell — lines 1-69 head, 186-196 footer, 252-289 + 564-595 CSS verbatim sources)
    - references/terms.md (canonical body copy, ~150 words; verbatim hand-translation source)
    - .planning/phases/07-legal-pages/07-CONTEXT.md (D-01 through D-08 locked decisions)
    - .planning/phases/07-legal-pages/07-PATTERNS.md (Patterns 1-7 + S1-S5 — full shell-derivation spec)
    - .planning/phases/07-legal-pages/07-01-PLAN.md (sibling plan; confirms heading-level choice and rendering conventions to match across both pages)
    - CLAUDE.md (prerender = true pattern, inline <style is:global> per page, LAND-02 forbidden words)
  </read_first>
  <action>
    Create `src/pages/terms.astro` as a new Astro prerendered static page.

    Frontmatter (per PATTERNS Pattern 1, CONTEXT D-03/D-04):
    - `export const prerender = true;`
    - `const TITLE = 'Terms — Oddlympics';`
    - `const DESCRIPTION` describing the terms (one short sentence; MUST NOT contain LAND-02 prohibited terms).
    - `const LAST_UPDATED = 'May 13, 2026';` (must match plan 01 — both pages share deploy date; pre-formatted string per CONTEXT discretion bullet 1).
    - Do NOT import any modules; do NOT define OG/Twitter consts.

    `<head>` (per PATTERNS Pattern 2):
    - `<!doctype html>` + `<html lang="en">`.
    - `<meta charset="utf-8" />` + `<meta name="viewport" content="width=device-width, initial-scale=1" />`.
    - `<title>{TITLE}</title>` + `<meta name="description" content={DESCRIPTION} />`.
    - `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`.
    - MUST NOT include any `og:*` or `twitter:*` meta tag.
    - MUST NOT include the Plausible `<script async ...>` tag or the `plausible.init()` shim.

    `<body>` (per PATTERNS Pattern 3 + CONTEXT D-05/D-06 + specifics):
    - `<main>` wrapping `<section class="content">` wrapping `<div class="wrap">`.
    - First child of `.wrap` is `<h1>Terms</h1>` (per CONTEXT §specifics — exact text).
    - Immediately after `<h1>`: `<p class="last-updated">Last updated: {LAST_UPDATED}</p>`.
    - Body copy hand-translated from `references/terms.md`. The source uses an intro sentence (`By using Oddlympics, you agree to the following:`) followed by a numbered list of five items, each with a bold lead-in (`**Free service.**`, `**Best-effort delivery.**`, `**No affiliation.**`, `**Acceptable use.**`, `**Disputes.**`). Render the intro as `<p>By using Oddlympics, you agree to the following:</p>` and the five items as an `<ol>` with each `<li>` opening with `<strong>` for the bold lead-in. Keep the same heading-rendering convention as plan 01 — if plan 01 uses `<h2>` for the privacy section headings, this plan's bold lead-ins inside the numbered list stay as `<strong>` (they are inline labels inside an `<li>`, not section breaks). No `<h2>` is needed inside `<ol>` items.
    - MUST preserve these verbatim lines from CONTEXT §specifics:
      - `Your World Cup subscription will remain free through July 19, 2026.`
      - `Governed by the laws of Michigan, USA.`
    - One mailto link at the end per CONTEXT D-06 and §specifics: `<p>Questions: <a href="mailto:hello@oddlympics.app"><code>hello@oddlympics.app</code></a></p>`.
    - `<footer class="site-footer">` is a sibling of `<main>` (NOT nested inside). Copy the footer block from `src/pages/index.astro` lines 186-196 VERBATIM — every attribute, every link target, every label, the © line with middle-dot U+00B7 separators. MUST be byte-identical to the footer in plan 01.
    - NO inline `<script>` blocks anywhere (PATTERNS Pattern 6).

    `<style is:global>` (per PATTERNS Pattern 5, targeting ~80-120 lines per CONTEXT D-07):
    - Paste 5a (`:root` token block) VERBATIM from `index.astro:253-267`. MUST be byte-identical to the `:root` block in plan 01.
    - Paste 5b (box-sizing + html/body baseline) VERBATIM from `index.astro:269-282`.
    - Paste 5c (`.wrap` container) VERBATIM from `index.astro:284-289`.
    - Add 5d NEW rules: `.content { padding-top: 64px; padding-bottom: 32px; }` and `.last-updated { margin: 0 0 24px; font-size: 13px; color: var(--fg-dim); }`.
    - Add 5e body typography rules: `h1` (clamp(28px,5vw,40px) / 700 / 1.15 / -0.015em), `p` (15px / 1.55 / var(--fg)), `ol` (margin/padding), `li` (15px / 1.55, with reasonable left padding for the numbered counters), `strong { font-weight: 700; }`, `a { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; }`, `a:hover { filter: brightness(0.92); }`, `code { font-family: var(--font-mono); font-size: 0.95em; }`. (`h2` rule is OPTIONAL — terms.astro likely doesn't use `<h2>`; keep the rule only if a `<h2>` is rendered, otherwise drop to keep the block lean.)
    - Paste 5f (`.site-footer` full rule set) VERBATIM from `index.astro:564-595`.
    - Paste 5g (mobile breakpoint) substituting selector: `@media (max-width: 520px) { .content { padding-top: 36px; } }`.
    - Drop 5h `prefers-reduced-motion` block (no animated elements on this page).
    - MUST NOT copy any selector listed in PATTERNS 5i.

    LAND-02 obligation: every byte of the rendered HTML MUST NOT contain (case-insensitive) `bitcoin`, `lightning`, `crypto`, `world domination`, or `personal olympics`. The body, title, description, and constants MUST all stay clear. (Verify: `references/terms.md` is already clean per docs-sweep commit 08c4d4a.)

    TypeScript / Astro conventions:
    - SCREAMING_SNAKE_CASE for module constants.
    - ASCII apostrophe (0x27) throughout body copy — match references/terms.md byte-for-byte (Phase 6 STATE precedent). Note `references/terms.md` line 10 contains the phrase `Don't use the signup form to submit fake or other people's email addresses.` — the apostrophes there are ASCII, preserve them.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('src/pages/terms.astro','utf8');const must=['export const prerender = true','Terms','hello@oddlympics.app','Michigan','2026-07-19','best-effort','site-footer','--max: 720px','Last updated','Independent project','FIFA'];const missing=must.filter(m=>!s.includes(m));if(missing.length){console.error('MISSING:',missing);process.exit(1)}const banned=/(bitcoin|lightning|crypto|world domination|personal olympics)/i;if(banned.test(s)){console.error('LAND-02 violation in source');process.exit(1)}console.log('OK source-level checks pass')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/terms.astro` exists.
    - First non-blank lines are `---` then `export const prerender = true;`.
    - Source contains literal strings (case-sensitive): `Terms`, `hello@oddlympics.app`, `Michigan`, `2026-07-19` (within `July 19, 2026` rendering — see note below) OR the date phrase `July 19, 2026` literally as in references/terms.md, `best-effort`, `Last updated`, `site-footer`, `--max: 720px`, `Independent project`, `FIFA`.
    - (Note on date literal: references/terms.md uses `July 19, 2026`. The `2026-07-19` form is the ROADMAP/REQUIREMENTS phrasing. The page MUST contain the rendered form `July 19, 2026`; the verify command above relaxes to either. If `2026-07-19` literal does not appear, the `July 19, 2026` form MUST.)
    - Source contains zero matches for `bitcoin|lightning|crypto|world domination|personal olympics` (case-insensitive grep).
    - Source contains NO `<script` tag.
    - Source contains NO `og:` or `twitter:` meta property.
    - Source includes the four footer nav links: `href="/manage"`, `href="/privacy"`, `href="/terms"`, `href="mailto:hello@oddlympics.app"`.
    - `<style is:global>` block includes the verbatim `:root` token block (contains `--accent: #d94a1f;` and `--max: 720px;`).
  </acceptance_criteria>
  <done>
    src/pages/terms.astro is committed-ready: prerendered static page with canonical terms copy, verbatim site shell from index.astro, no JS, no OG tags, no LAND-02 violations in source bytes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build + serve verification — /terms returns 200 with all LEGAL-02 declarations and zero LAND-02 hits</name>
  <files>(no source changes — verification only against dist/ output)</files>
  <read_first>
    - src/pages/terms.astro (the file just created in Task 1)
    - references/terms.md (cross-reference for LEGAL-02 declaration coverage)
    - package.json (npm scripts: dev, build, serve)
    - astro.config.mjs (adapter config — confirm output mode and dist layout)
  </read_first>
  <action>
    Run `npm run build`. Astro 5 with `@astrojs/node` standalone adapter emits prerendered pages under `dist/client/<route>/index.html`. For `/terms` the expected path is `dist/client/terms/index.html`; if absent, fall back to `find dist/client -name 'index.html' -path '*terms*'` to locate the actual emitted path.

    Against the emitted HTML file run, in order:
    1. Confirm file exists and is non-empty.
    2. LAND-02 grep on the emitted HTML: `grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' dist/client/terms/index.html` MUST output `0`.
    3. Confirm LEGAL-02 declaration coverage with literal-string grep on the emitted HTML:
       - `July 19, 2026` (free service end date — rendered form from references/terms.md)
       - `best-effort` (best-effort delivery declaration)
       - `FIFA` (no FIFA affiliation declaration — combined with `not affiliated` context)
       - `not affiliated` (combined with FIFA in the no-affiliation clause; case-insensitive ok via `grep -i`)
       - `fake or other` (acceptable use prohibition — the exact phrasing from references/terms.md `fake or other people's email`)
       - `Michigan` (governing law)
       - `hello@oddlympics.app` (Questions contact)
       - `May 13, 2026` (Last-updated date matching the LAST_UPDATED constant)
       Every literal MUST appear at least once. Treat any miss as a fail.
    4. Confirm shell parity grep on the emitted HTML:
       - `--max: 720px` (token block present)
       - `<a href="/manage">` AND `<a href="/privacy">` AND `mailto:hello@oddlympics.app` (footer nav verbatim)
       - `Independent project` AND `Not affiliated with FIFA` (footer © line — note this same exact phrase also satisfies the body's "no FIFA affiliation" requirement via the footer, but the body MUST also have its own affiliation clause from references/terms.md line 9)

    Then verify the page serves 200 OK live (same Path A or Path B choice as plan 01; the executor MAY combine both build verifications into a single server boot):
    - Path A: `node --env-file=.env ./dist/server/entry.mjs` then `curl -sS -o /dev/null -w '%{http_code}' http://127.0.0.1:4321/terms` MUST print `200`.
    - Path B: `npm run dev` then same curl. MUST print `200`.

    Document the verification path used and the build output path in the SUMMARY.

    If any check fails, surface the failure with the offending grep output / curl status code, fix only the specific gap in `src/pages/terms.astro`, do NOT touch unrelated files.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && TFILE=$(find dist/client -name 'index.html' -path '*terms*' | head -1) && test -s "$TFILE" && echo "Built: $TFILE" && BAD=$(grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' "$TFILE") && test "$BAD" = "0" && echo "LAND-02 OK" && for s in 'July 19, 2026' 'best-effort' 'FIFA' 'fake or other' 'Michigan' 'hello@oddlympics.app' 'May 13, 2026' '--max: 720px' 'href="/manage"' 'href="/privacy"' 'mailto:hello@oddlympics.app' 'Independent project' 'Not affiliated with FIFA'; do grep -qF "$s" "$TFILE" || { echo "MISSING in built HTML: $s"; exit 1; }; done && grep -qi 'not affiliated' "$TFILE" || { echo "MISSING: not affiliated (case-insensitive)"; exit 1; } && echo "LEGAL-02 + shell-parity grep OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `dist/client/terms/index.html` (or equivalent under `dist/client/` per find) exists and is non-empty.
    - `grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' dist/client/terms/index.html` returns `0` (LAND-02 PASS, AC7 evidence).
    - Built HTML contains literal strings: `July 19, 2026`, `best-effort`, `FIFA`, `fake or other`, `Michigan`, `hello@oddlympics.app`, `May 13, 2026` (LEGAL-02 declaration coverage).
    - Built HTML contains `not affiliated` (case-insensitive — covers either the body's no-affiliation clause or the footer © line, both should be present).
    - Built HTML contains `--max: 720px`, `href="/manage"`, `href="/privacy"`, `mailto:hello@oddlympics.app`, `Independent project`, `Not affiliated with FIFA` (shell parity with landing).
    - `GET http://127.0.0.1:4321/terms` returns HTTP 200 against either `npm run dev` or the built server.
  </acceptance_criteria>
  <done>
    /terms ships as a prerendered Astro page returning 200 OK locally with full LEGAL-02 declaration coverage, the same site shell as `/`, and zero LAND-02 prohibited-term occurrences in the emitted HTML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author -> repo | Hand-translated copy from `references/terms.md` into `src/pages/terms.astro`. Copy errors can leak prohibited terms or wrong legal declarations (governing law, free-through date). |
| Build pipeline -> served HTML | Astro prerender emits the final static HTML that paid-ad reviewers and end users will GET. |
| User browser -> page | One-way: browser requests `/terms`, receives static HTML. No form, no JS, no cookies, no inputs accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | Hand-translated body copy in `terms.astro` (critical legal declarations: free-through date, governing law, no-affiliation clause, acceptable-use clause) | mitigate | Task 1 read_first pins `references/terms.md` as canonical; Task 2 greps the BUILT HTML for every LEGAL-02 declaration literal (`July 19, 2026`, `best-effort`, `Michigan`, `fake or other`, `FIFA`, `not affiliated`, `hello@oddlympics.app`) — any drift fails the gate. |
| T-07-10 | Information disclosure | Prohibited-term leak in source, head, body, style, or filename | mitigate | Task 1 source-level grep + Task 2 built-HTML grep both run `grep -ciE 'bitcoin\|lightning\|crypto\|world domination\|personal olympics'` and require exit `0`. Defense in depth: same gate at Phase 11 AC7 on prod. |
| T-07-11 | Tampering | Footer drift between `/` and `/terms` or between `/privacy` and `/terms` | mitigate | Task 1 instructs verbatim copy of `index.astro:186-196` and explicitly requires byte-identical footer to plan 01; Task 2 greps for the four nav targets and the © line literals. |
| T-07-12 | Spoofing | Reviewer/user claims governing law or free-through date is wrong | mitigate | Both dates and the governing-law clause are pinned to verbatim literals from references/terms.md (`Governed by the laws of Michigan, USA.` and `Your World Cup subscription will remain free through July 19, 2026.`); Task 2 greps for both. References file is the canonical source-of-truth per CONTEXT D-01. |
| T-07-13 | Spoofing | Reviewer/user claims "last updated" date is wrong/stale | mitigate | Hardcoded LAST_UPDATED per CONTEXT D-03 — same convention as plan 01; Task 2 greps for `May 13, 2026` literally. |
| T-07-14 | XSS via copy injection | mailto link `hello@oddlympics.app` | accept | Hard-coded string in Astro template; no request-time interpolation; no JS on the page. No XSS surface. |
| T-07-15 | Repudiation | Author claims they did not change a legal declaration | accept | Git history is the audit log; `references/terms.md` + `terms.astro` co-evolve in the same PR per CONTEXT D-01. Solo-dev posture; no additional repudiation surface warranted at ASVS L1. |
| T-07-16 | Denial of service | Cached prerendered HTML at `/terms` | accept | Static page, CDN-cacheable, no per-request server state. Inherits Caddy/systemd resilience already documented in DEPLOY.md. |
| T-07-17 | Elevation of privilege | n/a | accept | Page has no auth surface, no privileged operations, no DB writes. Nothing to elevate. |
</threat_model>

<verification>
Phase-level checks for this plan:
1. `npm run build` exits 0 and produces `dist/client/terms/index.html`.
2. LAND-02 grep on built HTML returns 0 matches.
3. LEGAL-02 declaration grep on built HTML hits all 7 required literals plus the case-insensitive `not affiliated` check.
4. Shell-parity grep on built HTML hits all 6 verbatim footer/token strings.
5. `GET http://127.0.0.1:4321/terms` returns HTTP 200 against `npm run dev` or `node dist/server/entry.mjs`.

Cross-phase: Phase 11 AC5 will re-verify `/terms` returns 200 with canonical copy on production. Phase 11 AC7 will re-grep prohibited terms on prod HTML. This plan is the local-mode predecessor for both.

Cross-plan (with 07-01-PLAN.md): both plans MUST end up with byte-identical `:root` token blocks and byte-identical `.site-footer` markup. A post-execution spot-check (manual diff of the two files' `:root` and footer regions) is recommended but not a hard automated gate; if drift is found, fix the regressing file to match the canonical source `src/pages/index.astro`.
</verification>

<success_criteria>
- `src/pages/terms.astro` exists, is committed-ready, and renders as a prerendered Astro static page.
- The page returns HTTP 200 at `/terms` against both `npm run dev` and `node dist/server/entry.mjs`.
- All LEGAL-02 declarations are present in the rendered HTML: free service through `July 19, 2026`, best-effort delivery, no FIFA/ESPN/team affiliation, prohibition on submitting fake/others' emails, Michigan (USA) governing law, `hello@oddlympics.app` contact, Last-updated date matching the deploy date.
- The page matches the v2.0 landing site shell exactly: same `:root` tokens, same `--max: 720px` container, same `.site-footer` rules, same fonts.
- Zero occurrences of `bitcoin`, `lightning`, `crypto`, `world domination`, or `personal olympics` (case-insensitive) in the rendered HTML.
- No inline `<script>` blocks; no OG/Twitter meta tags; no analytics surface on this page.
</success_criteria>

<output>
After completion, create `.planning/phases/07-legal-pages/07-02-SUMMARY.md` summarizing: files created, verification path used (Path A or Path B), built HTML output path, grep evidence (LAND-02 hits = 0, LEGAL-02 literal coverage = 7/7 + `not affiliated` case-insensitive PASS, shell parity = 6/6), and the curl status code captured for `/terms`.
</output>
