---
phase: 12-restore-multi-team-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
autonomous: true
requirements: [IDENT-02, IDENT-03, IDENT-04]
must_haves:
  truths:
    - "The user_teams join table exists and is created purely additively (no ALTER, no DROP) on DB boot"
    - "deleteUserTeams, insertUserTeam, getUserTeams prepared statements are exported with typed generics matching project idiom"
    - "Re-booting against an already-migrated DB is a no-op (CREATE TABLE IF NOT EXISTS / INSERT OR IGNORE)"
    - "No legacy data ceremony added — D-02: no backup-pre, no version assert, no backfill for user_teams"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "user_teams DDL + 3 prepared statements + UserTeam type"
      contains: "CREATE TABLE IF NOT EXISTS user_teams"
  key_links:
    - from: "src/lib/db.ts"
      to: "user_teams"
      via: "db.exec CREATE TABLE IF NOT EXISTS + exported prepared statements"
      pattern: "CREATE TABLE IF NOT EXISTS user_teams"
---

<objective>
Add the `user_teams (email, team_slug, UNIQUE(email, team_slug))` join table and its
prepared statements to `src/lib/db.ts` (D-01). This is the schema foundation every
downstream plan (manage editor, save-selection writer, kickoff cron, smoke) joins
against.

Purpose: Restore multi-team subscription storage — the model the founder approved
("single at signup, multi post-signup") that the Phase 5 schema collapse removed.
This plan is the Wave-1 contract: it defines the table + statement signatures the
Wave-2 plans implement against, so executors receive the interface, not a scavenger hunt.

Output: `src/lib/db.ts` extended with one additive `db.exec` block, three exported
prepared statements, and a `UserTeam` type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-restore-multi-team-selection/12-CONTEXT.md
@.planning/phases/12-restore-multi-team-selection/12-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- The contract this plan ESTABLISHES for Wave 2 + 3. Executors of 12-02 / 12-03 / 12-04
     should consume these signatures directly — no codebase exploration needed. -->

New table:
  user_teams (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT NOT NULL,
    team_slug TEXT NOT NULL,
    UNIQUE(email, team_slug)
  )
  + INDEX idx_user_teams_email ON user_teams(email)

New exports from src/lib/db.ts:
  export type UserTeam = { id: number; email: string; team_slug: string };
  export const deleteUserTeams = db.prepare<[string]>(...)        // DELETE FROM user_teams WHERE email = ?
  export const insertUserTeam  = db.prepare<[string, string]>(...) // INSERT OR IGNORE INTO user_teams (email, team_slug) VALUES (?, ?)
  export const getUserTeams    = db.prepare<[string]>(...)         // SELECT team_slug FROM user_teams WHERE email = ?  → { team_slug: string }[]

Existing prepared-statement idiom to match (do not change these):
  export const upsertVipSignup = db.prepare<[...]>(`...`);  // src/lib/db.ts:81-93 — typed generic
  export const getByEmail = db.prepare<[string]>(`SELECT * FROM vip_signups WHERE email = ?`);  // src/lib/db.ts:110-112
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user_teams DDL + prepared statements + UserTeam type to db.ts</name>
  <files>src/lib/db.ts</files>
  <read_first>
    - src/lib/db.ts (FILE BEING MODIFIED — full file already in plan context; note the additive `db.exec` blocks at lines 122-153 and 172-180, the typed-generic prepared-statement pattern at lines 81-93 / 110-119, and the type-export pattern at lines 63-75 / 182-189)
    - .planning/phases/12-restore-multi-team-selection/12-PATTERNS.md (§1 — exact DDL block, prepared-statement names, and UserTeam type shape; lines 24-106)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (D-01 store contract; D-02 no-legacy-ceremony lock)
    - src/lib/teams.ts (VALID_TEAMS is the slug allow-list — referenced for context; validation happens in 12-02, NOT here)
  </read_first>
  <action>
    Append (do NOT rewrite the file) a new additive block after the `feature_requests`
    `db.exec` block that ends at src/lib/db.ts:180. Per PATTERNS.md §1:

    1. A `db.exec` call creating `CREATE TABLE IF NOT EXISTS user_teams` with columns
       `id INTEGER PRIMARY KEY AUTOINCREMENT`, `email TEXT NOT NULL`,
       `team_slug TEXT NOT NULL`, `UNIQUE(email, team_slug)`, plus
       `CREATE INDEX IF NOT EXISTS idx_user_teams_email ON user_teams(email)`.
       Use the exact `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`
       additive style of the existing `teams`/`match_notifications` block — NO probe,
       NO ALTER, NO DROP, NO sqlite_version assert, NO backup. D-02 explicitly
       waives the Phase-5 destructive-migration safety pattern (purely additive here,
       and no real data to preserve). Add a one-line why-comment tagging Phase 12 / D-01.
    2. Export `type UserTeam = { id: number; email: string; team_slug: string }`
       following the `VipSignup`/`Team` type-export shape (src/lib/db.ts:63-75).
    3. Export three prepared statements with typed generics matching the project idiom
       (named exports only, no default export, no barrel):
       - `deleteUserTeams = db.prepare<[string]>` → `DELETE FROM user_teams WHERE email = ?`
       - `insertUserTeam = db.prepare<[string, string]>` → `INSERT OR IGNORE INTO user_teams (email, team_slug) VALUES (?, ?)`
       - `getUserTeams = db.prepare<[string]>` → `SELECT team_slug FROM user_teams WHERE email = ?`
    Discretion call (state in SUMMARY): `vip_signups.team` is LEFT AS-IS — harmless,
    no seed into user_teams, no drop. D-02 grants this freedom; simplest correct option,
    zero migration risk, and signup (D-03) continues writing `vip_signups.team` untouched.
    Do NOT modify any existing statement, type, or `db.exec` block. ESM, `node:` prefix
    already present; strict TS; why-only comment.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5; grep -c 'CREATE TABLE IF NOT EXISTS user_teams' src/lib/db.ts; grep -c 'export const deleteUserTeams\|export const insertUserTeam\|export const getUserTeams' src/lib/db.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CREATE TABLE IF NOT EXISTS user_teams' src/lib/db.ts` == 1
    - `grep -c 'UNIQUE(email, team_slug)' src/lib/db.ts` == 1
    - `grep -c 'idx_user_teams_email' src/lib/db.ts` >= 1
    - `grep -c 'export const deleteUserTeams\|export const insertUserTeam\|export const getUserTeams' src/lib/db.ts` == 3
    - `grep -c 'export type UserTeam' src/lib/db.ts` == 1
    - `grep -v '^//' src/lib/db.ts | grep -c 'ALTER TABLE user_teams\|DROP COLUMN.*user_teams\|backup-pre\|user_teams.*sqlite_version'` == 0 (D-02: no legacy ceremony on the new table)
    - `npx astro check` reports no new type errors introduced by this change
  </acceptance_criteria>
  <done>user_teams table + idx + 3 typed prepared statements + UserTeam type added additively to src/lib/db.ts; no ALTER/DROP/backup ceremony; astro check clean; existing statements untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Boot-verify the table materializes and re-boot is a no-op</name>
  <files>src/lib/db.ts (verification only — no file written)</files>
  <read_first>
    - src/lib/db.ts (post-Task-1 state)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (D-01 — re-running on a migrated DB is a no-op)
  </read_first>
  <action>
    Drive a build + a throwaway DB boot to prove the additive DDL materializes and is
    idempotent. Run `npm run build`, then boot the schema twice against a scratch DB
    by invoking node with `DATABASE_PATH` pointed at a temp file
    (e.g. `node -e "import('./dist/server/entry.mjs')"` is heavyweight — instead use a
    one-liner that imports the built db module path OR run the existing app boot path
    once; simplest: set `DATABASE_PATH=$(mktemp -u)` and run
    `node --input-type=module -e "await import('better-sqlite3'); const D=(await import('better-sqlite3')).default; const db=new D(process.env.DATABASE_PATH);"`
    is NOT sufficient — the DDL lives in db.ts). Concretely: build, then run
    `DATABASE_PATH=/tmp/ut-smoke-$$.db node ./dist/server/entry.mjs &` for ~2s, curl `/`,
    kill it, then run the same boot a SECOND time against the SAME temp DB and assert
    no error. Finally `sqlite3 /tmp/ut-smoke-$$.db ".schema user_teams"` shows the table
    + the UNIQUE constraint, and `PRAGMA index_list(user_teams)` includes
    `idx_user_teams_email`. Delete the temp DB. NO production DB touched. NO new
    script/file committed — this is a verification task, not a deliverable.
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && T=/tmp/ut-smoke-$$.db && DATABASE_PATH=$T HOST=127.0.0.1 PORT=4399 node ./dist/server/entry.mjs >/dev/null 2>&1 & SP=$!; sleep 2; curl -sf -o /dev/null http://127.0.0.1:4399/ ; kill $SP 2>/dev/null; DATABASE_PATH=$T HOST=127.0.0.1 PORT=4399 node ./dist/server/entry.mjs >/dev/null 2>&1 & SP2=$!; sleep 2; kill $SP2 2>/dev/null; sqlite3 "$T" ".schema user_teams" | grep -c 'UNIQUE(email, team_slug)'; sqlite3 "$T" "SELECT name FROM pragma_index_list('user_teams')" | grep -c idx_user_teams_email; rm -f "$T" "$T"-*</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - First boot serves `/` (curl exits 0)
    - Second boot against the same DB file exits without throwing (idempotent — CREATE TABLE IF NOT EXISTS)
    - `sqlite3 <db> ".schema user_teams"` output contains `UNIQUE(email, team_slug)` (count == 1)
    - `pragma_index_list('user_teams')` includes `idx_user_teams_email` (count >= 1)
    - Temp DB removed; no production DB (`./data/oddlympics.db`) modified; no new file added to git
  </acceptance_criteria>
  <done>Built server boots clean against a fresh DB, materializes user_teams + UNIQUE + index, and a second boot against the same DB is a no-op. Temp artifacts cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → SQLite file | DDL executed at module load; no external input crosses here in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `user_teams` DDL on boot | accept | Schema is static literal SQL, no interpolation, no external input. `CREATE TABLE IF NOT EXISTS` is idempotent; cannot corrupt existing tables (additive-only, no ALTER/DROP on existing objects). |
| T-12-02 | Denial of Service | repeated boot DDL | accept | `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS` are no-ops after first run; negligible cost, single-process single-host. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (better-sqlite3 already vendored). N/A — no install task. |
</threat_model>

<verification>
- `npx astro check` clean (no new type errors)
- `grep` gates in Task 1 acceptance criteria all pass
- Boot-twice idempotency proven in Task 2 against a scratch DB
- D-03 fence: this plan touches ONLY `src/lib/db.ts` — `src/pages/index.astro` and
  `src/pages/api/signup.ts` are NOT in `files_modified` (verify: they are absent)
</verification>

<success_criteria>
- `user_teams` table + `idx_user_teams_email` index created additively in `src/lib/db.ts`
- `deleteUserTeams`, `insertUserTeam`, `getUserTeams` exported with typed generics
- `UserTeam` type exported
- No ALTER / DROP / backup / version-assert ceremony added (D-02)
- Built server boots clean; second boot against same DB is a no-op
- Existing statements/types/blocks untouched; signup path (D-03) unaffected
</success_criteria>

<output>
Create `.planning/phases/12-restore-multi-team-selection/12-01-SUMMARY.md` when done.
Record the discretion call: `vip_signups.team` left as-is (no seed, no drop) per D-02.
</output>
