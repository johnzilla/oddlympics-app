---
phase: 12-restore-multi-team-selection
plan: 06
type: execute
wave: 5
depends_on: ["12-05", "12-04"]
files_modified:
  - scripts/smoke-manage.mjs
autonomous: true
gap_closure: true
requirements: [IDENT-02, IDENT-03, IDENT-04, NOTIFY-04, SIGNUP-04, LAND-02]
must_haves:
  truths:
    - "WR-04 closed: scripts/smoke-manage.mjs gains the negative-path cases that would have caught CR-01 and CR-02 — proving the unsubscribe/consent contract is now actually enforced, not just present in source"
    - "CR-01 behavioral proof: a smoke case seeds a CONFIRMED-but-UNSUBSCRIBED user holding a valid 30-day session, POSTs otherwise-valid slugs + a different tz to /api/save-selection, and asserts the 303 Location carries status=unknown AND dbUserTeamSlugs(email) is UNCHANGED from its pre-seeded value (zero writes) AND vip_signups.timezone is NOT clobbered (still the seeded value)"
    - "CR-02 behavioral proof: a smoke case follows N teams → GET /api/unsubscribe (the real route) → asserts user_teams rows are now GONE → dbMarkConfirmed (clears unsubscribed_at) → asserts the kickoff cron usersQuery-equivalent JOIN through user_teams yields ZERO rows for the user (no stale fan-out after re-subscription)"
    - "No regression: every existing M1–M14 case still passes; the new cases use the same runCase harness + existing helpers (dbInsertSmokeRow, dbMarkConfirmed, dbInsertUserTeams, dbUserTeamSlugs, postMultiTeam, dbRowFor, mintToken, get) and the same pass/fail/exit aggregation"
    - "node scripts/smoke-manage.mjs exits 0 against the freshly built server running the 12-05 fixes on a scratch DB (pass=N fail=0); the new M15/M16 cases are real regression detectors (they FAIL against pre-12-05 code), not tautologies"
  artifacts:
    - path: "scripts/smoke-manage.mjs"
      provides: "Two new negative-path cases after M14 — M15 (unsubscribed-POST rejected, zero writes, tz preserved) and M16 (unsubscribe clears user_teams → reconfirm → cron sees zero teams) — plus the header evidence-tag block updated"
      contains: "M15-unsub-save-rejected"
  key_links:
    - from: "scripts/smoke-manage.mjs M15"
      to: "/api/save-selection"
      via: "postMultiTeam against an unsubscribed-seeded session user; asserts status=unknown + unchanged user_teams + preserved tz"
      pattern: "M15-unsub-save-rejected"
    - from: "scripts/smoke-manage.mjs M16"
      to: "user_teams + usersQuery-equivalent JOIN"
      via: "follow N → GET /api/unsubscribe → assert rows gone → dbMarkConfirmed → assert cron JOIN yields zero rows for the user"
      pattern: "M16-unsub-reconfirm-no-stale"
    - from: "scripts/smoke-manage.mjs"
      to: "node scripts/smoke-manage.mjs exit 0"
      via: "runCase aggregation; all M1–M16 PASS against the 12-05-fixed built server"
      pattern: "result: pass="
---

<objective>
Close WR-04: extend scripts/smoke-manage.mjs with the two negative-path cases
that would have caught the CR-01 and CR-02 BLOCKERs, so re-verification has
goal-backward behavioral proof that the consent/unsubscribe contract is now
actually enforced (not merely present in source). The existing M10–M14 cases
exercise only confirmed, active users and therefore could not have caught these
regressions (per the verifier's "Smoke coverage note" and Gaps Summary).

Purpose: This is the verification spine for the gap closure. It depends on
12-05 (the fixes must be live for the new cases to pass) and 12-04 (the harness
+ helpers it extends). The new cases are designed to FAIL against pre-12-05
code — they are regression detectors, not tautologies — which is exactly what
re-verification needs to confirm FAILED truth #10 is now satisfied.

Output: scripts/smoke-manage.mjs (M1–M14 retained + M15/M16 added + header
evidence-tag block updated); the green PASS line captured as gap-closure
evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-restore-multi-team-selection/12-CONTEXT.md
@.planning/phases/12-restore-multi-team-selection/12-PATTERNS.md
@.planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md
@./CLAUDE.md

<interfaces>
<!-- Existing smoke-manage.mjs infra to REUSE verbatim — do NOT rewrite M1–M14. -->

scripts/smoke-manage.mjs (extend; FILE BEING MODIFIED):
  - runCase(name, fn) harness — pass/fail counters + exit code already aggregate every case
  - dbInsertSmokeRow(email, opts) lines 138-159 — opts.team, opts.timezone,
    opts.confirmedAt, opts.unsubscribedAt (pass `unsubscribedAt: <unix ts>` to seed an unsubscribed row)
  - dbMarkUnsubscribed(email) lines 162-170 — sets unsubscribed_at (mirrors db.ts markUnsubscribed)
  - dbMarkConfirmed(email) lines 172-183 — the D-07 RETURNING * shape (clears unsubscribed_at = NULL); returns the row
  - dbInsertUserTeams(email, slugs=[]) lines 187-193 — delete-all-then-insert seed
  - dbUserTeamSlugs(email) lines 196-201 — sorted team_slug[] for assertions
  - postMultiTeam(slugs, tz, cookieHeader) lines 206-219 — repeated team= POST, redirect:'manual', returns { status, location }
  - mintToken(email, { purpose, ttlSeconds }) — `session` (30d) / `manage` / `unsubscribe` tokens the server verifies
  - get(path) — GET helper with redirect:'manual' (M8 uses `get('/api/unsubscribe?token=...')`)
  - dbRowFor(email) lines 132-136 — full vip_signups row (read timezone / unsubscribed_at / confirmed_at for assertions)
  - COOKIE_NAME — the session cookie name M10–M13 use to build the Cookie header
  - dbRead / dbWrite handles; BASE; the result/cleanup footer lines 842-855:
    `console.log('[smoke] result: pass=${pass} fail=${fail}')`, cleanup-hint DELETE block
    (DELETE FROM vip_signups / user_teams WHERE email LIKE 'smoke-%@example.com'),
    `process.exit(fail === 0 ? 0 : 1)`
  - M8-unsub-1y-token lines ~551-595 — the `get('/api/unsubscribe?token=...')` + status=ok idiom to MIRROR
  - M9-resubscribe-path lines 597-643 — the dbMarkUnsubscribed → dbMarkConfirmed idiom to MIRROR
  - M14-cron-visibility lines 788-840 — the usersQuery-equivalent JOIN + seed-two-teams-if-absent
    (INSERT OR IGNORE INTO teams ... id 9900001/9900002) pattern to MIRROR for M16's cron-sees-zero assertion
  - header evidence-tag comment block (~lines 26-44, the "M14-cron-visibility — …" list)

Behavior under test (delivered by 12-05 — these cases pass ONLY against the fixed code):
  - /api/save-selection now state-gates inside its db.transaction: an unsubscribed/unconfirmed
    user → res.changes === 0 → throws 'not-active' → catch → 303 /manage?status=unknown, zero
    user_teams writes, timezone unchanged
  - /api/unsubscribe now calls deleteUserTeams.run(result.email) after markUnsubscribed, so a
    later markConfirmed (unsubscribed_at = NULL) finds zero user_teams rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add M15 (unsubscribed-POST rejected, zero writes, tz preserved) and M16 (unsubscribe clears user_teams → reconfirm → cron sees zero teams) to scripts/smoke-manage.mjs</name>
  <files>scripts/smoke-manage.mjs</files>
  <read_first>
    - scripts/smoke-manage.mjs (FILE BEING MODIFIED — full file in plan context; the helpers dbInsertSmokeRow 138-159, dbMarkUnsubscribed 162-170, dbMarkConfirmed 172-183, dbInsertUserTeams 187-193, dbUserTeamSlugs 196-201, postMultiTeam 206-219, dbRowFor 132-136, get(); M8-unsub-1y-token ~551-595 for the get('/api/unsubscribe?token=…') idiom; M9-resubscribe-path 597-643 for the unsubscribe→markConfirmed idiom; M14-cron-visibility 788-840 for the usersQuery-equivalent + seed-teams idiom; the result/cleanup footer 842-855; the header evidence-tag block ~26-44)
    - .planning/phases/12-restore-multi-team-selection/12-05-PLAN.md (the EXACT behavior 12-05 delivers — save-selection state-gate → status=unknown + zero writes + tz preserved; unsubscribe → deleteUserTeams.run → reconfirm finds zero rows. The smoke must assert precisely this contract.)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (FAILED truth #10 + CR-01/CR-02 + "Smoke coverage note": M10–M14 cover only active users; the gap-closing plan must add the unsubscribed-POST-rejection and unsubscribe→reconfirm→cron-excludes cases)
    - src/lib/db.ts (markConfirmed / markUnsubscribed / deleteUserTeams shapes the helpers mirror; updateTimezoneActive predicate, so the M15 tz-preserved assertion targets the right column)
  </read_first>
  <action>
    Extend the existing smoke. Do NOT rewrite or reorder M1–M14, the helpers,
    runCase, or the footer. Add the two cases AFTER M14-cron-visibility (line
    840) and BEFORE the result/cleanup footer (line 842), using the existing
    runCase harness so pass/fail/exit aggregate automatically. Use unique
    `smoke-m15-<ts>@example.com` / `smoke-m16-<ts>@example.com` emails so the
    existing footer cleanup hint (LIKE 'smoke-%@example.com') already covers them.

    M15-unsub-save-rejected (CR-01 behavioral proof):
    1. Seed a CONFIRMED-but-UNSUBSCRIBED row: dbInsertSmokeRow(email, { team:
       'argentina', timezone: 'America/New_York', unsubscribedAt:
       Math.floor(Date.now()/1000) }) so confirmed_at is set AND
       unsubscribed_at is non-null (an unsubscribed user who still holds a
       valid credential — the exact CR-01 scenario).
    2. Pre-seed a known user_teams state to prove zero writes on reject:
       dbInsertUserTeams(email, ['argentina', 'brazil']) (2 rows) and capture
       the seeded timezone from dbRowFor(email).timezone.
    3. Mint a still-valid 30-day session token (mintToken(email, { purpose:
       'session', ttlSeconds: 60*60*24*30 })) and build the Cookie header the
       same way M10–M13 do (use COOKIE_NAME).
    4. POST 3 different valid slugs (e.g. ['england','france','germany']) + a
       DIFFERENT valid tz (e.g. 'Europe/London') via postMultiTeam.
    5. Assert ALL of (with a descriptive console.error on each failed
       sub-assertion, mirroring M12/M13's assertion+log style; return
       true/false per the runCase contract):
       - the response status is 303 AND location contains `status=unknown`
       - dbUserTeamSlugs(email) deep-equals the pre-seeded sorted
         ['argentina','brazil'] — UNCHANGED: the POSTed england/france/germany
         were NOT inserted and the originals were NOT deleted (the transaction
         rolled back on the not-active throw before deleteUserTeams ran)
       - dbRowFor(email).timezone still equals the seeded 'America/New_York'
         (NOT overwritten to 'Europe/London' — the gated UPDATE matched zero rows)

    M16-unsub-reconfirm-no-stale (CR-02 behavioral proof):
    1. Seed a CONFIRMED, ACTIVE row: dbInsertSmokeRow(email, { team:
       'argentina', timezone: 'Europe/London' }) (confirmed_at set,
       unsubscribed_at NULL).
    2. Seed two teams rows if absent (mirror M14's INSERT OR IGNORE INTO teams
       idiom with unique smoke ids/slugs, e.g. ids 9900003/9900004, slugs
       'smoke-team-gamma'/'smoke-team-delta') and follow BOTH:
       dbInsertUserTeams(email, [gammaSlug, deltaSlug]). Assert
       dbUserTeamSlugs(email).length === 2 (pre-state confirmed).
    3. Unsubscribe via the REAL route (the unit under test for the CR-02 fix):
       mint an unsubscribe token (mintToken(email, { purpose: 'unsubscribe',
       ttlSeconds: 60*60*24*365 }) — match M8's TTL) and
       `get('/api/unsubscribe?token=' + encodeURIComponent(token))` using the
       existing get() helper; assert the 303 location starts with
       `/unsubscribed?status=ok`. Then assert dbUserTeamSlugs(email).length
       === 0 — the CR-02 fix (deleteUserTeams.run inside the unsubscribe
       route) cleared the join rows.
    4. Re-confirm: dbMarkConfirmed(email) (the D-07 RETURNING * shape that
       clears unsubscribed_at = NULL — simulates the user clicking a later
       confirmation magic link). Assert dbRowFor(email).unsubscribed_at is
       now null (the cron re-activation predicate is satisfied) and
       confirmed_at is set.
    5. Run the usersQuery-equivalent JOIN, mirroring M14 lines 822-830
       verbatim (SELECT DISTINCT v.email FROM vip_signups v JOIN user_teams ut
       ON ut.email=v.email JOIN teams t ON t.slug=ut.team_slug WHERE
       v.confirmed_at IS NOT NULL AND v.unsubscribed_at IS NULL — the
       `t.id IN (...)` clause may be omitted, since the JOIN through the now-
       empty user_teams short-circuits to zero rows regardless). Filter the
       result to this email (mirror M14's `users.filter(u => u.email ===
       email)`) and assert that count is ZERO — the re-confirmed user has NO
       followed teams, so the cron produces no stale fan-out. (The user only
       reappears in the cron once they re-pick on /manage — that positive
       path is already M10; do NOT exercise it here.)

    Update the header evidence-tag comment block (~lines 26-44) to add an
    M15-unsub-save-rejected and an M16-unsub-reconfirm-no-stale line in the
    same style as the existing M-list. The footer cleanup hint already covers
    smoke-%@example.com emails; the M16 seeded teams use the same id>=9900000
    namespace as M14 (no new cleanup category — note this in the SUMMARY).

    Plain ESM, `node:` convention unaffected (no new imports needed — reuse
    existing helpers), no new deps, no framework JS. Keep the exact runCase /
    footer / exit semantics so `node scripts/smoke-manage.mjs` exits 0 only
    when ALL M1–M16 pass.
  </action>
  <verify>
    <automated>node --check scripts/smoke-manage.mjs && echo "M15=$(grep -c 'M15-unsub-save-rejected' scripts/smoke-manage.mjs) M16=$(grep -c 'M16-unsub-reconfirm-no-stale' scripts/smoke-manage.mjs) UNKNOWN=$(grep -c 'status=unknown' scripts/smoke-manage.mjs) MC=$(grep -c 'dbMarkConfirmed' scripts/smoke-manage.mjs) M14STILL=$(grep -c 'M14-cron-visibility' scripts/smoke-manage.mjs) M1STILL=$(grep -c 'M1-signed-out-form' scripts/smoke-manage.mjs)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/smoke-manage.mjs` exits 0
    - `grep -c 'M15-unsub-save-rejected' scripts/smoke-manage.mjs` == 1 AND `grep -c 'M16-unsub-reconfirm-no-stale' scripts/smoke-manage.mjs` == 1
    - M15 asserts ALL THREE CR-01 facts: a `status=unknown` location check, an unchanged-`dbUserTeamSlugs` (zero-write) check, and an unchanged-`dbRowFor(...).timezone` (tz-preserved) check — manual structural read confirms all three sub-assertions exist and each fails the case on violation
    - M16 calls the real `get('/api/unsubscribe?token=` route, asserts `dbUserTeamSlugs(...).length === 0` after it, then `dbMarkConfirmed`, then a usersQuery-equivalent JOIN filtered to the email asserting zero rows — manual structural read confirms the chain
    - `grep -c 'dbMarkConfirmed' scripts/smoke-manage.mjs` >= 2 (existing M9 use + new M16 use; M9 untouched)
    - All M1–M14 case names still present and unchanged: `grep -c 'M1-signed-out-form' scripts/smoke-manage.mjs` == 1, `grep -c 'M14-cron-visibility' scripts/smoke-manage.mjs` == 1 (and the M2–M13 names each still == 1)
    - The header evidence-tag block lists M15 and M16 (`grep -c 'M15-unsub-save-rejected' scripts/smoke-manage.mjs` includes the comment line — at least 2 total occurrences: header tag + runCase name)
    - No new npm dependency added; no `import` of a non-existing helper (reuses dbInsertSmokeRow / dbMarkConfirmed / dbInsertUserTeams / dbUserTeamSlugs / postMultiTeam / dbRowFor / mintToken / get already in the file)
    - End-to-end: with the 12-05-fixed code built (`npm run build`) and the server booted against a scratch DATABASE_PATH, `node scripts/smoke-manage.mjs` prints `[smoke] result: pass=N fail=0` and exits 0 (all M1–M16 PASS)
  </acceptance_criteria>
  <done>smoke-manage.mjs has M15 (unsubscribed-POST → status=unknown + zero user_teams writes + tz preserved) and M16 (follow N → real /api/unsubscribe clears user_teams → dbMarkConfirmed → cron JOIN yields zero rows) appended after M14, M1–M14 intact, header tag updated, reusing existing helpers; the full M1–M16 suite exits 0 against the 12-05-fixed built server on a scratch DB.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full M1–M16 smoke against the 12-05-fixed built server and capture the green PASS evidence</name>
  <files>scripts/smoke-manage.mjs (verification run only — no file written)</files>
  <read_first>
    - scripts/smoke-manage.mjs (post-Task-1 state; the result line `[smoke] result: pass=N fail=N` ~line 845 and exit code ~line 855; env vars SMOKE_BASE_URL / DATABASE_PATH / MAGIC_LINK_SECRET)
    - .planning/phases/12-restore-multi-team-selection/12-04-PLAN.md (Task 3 — the exact build-boot-scratch-DB-run-capture recipe + the dev MAGIC_LINK_SECRET literal to mirror; this plan repeats that proven recipe for the full M1–M16 suite)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (this is the goal-backward proof re-verification will consume — FAILED truth #10 must now pass behaviorally)
    - CLAUDE.md (smoke-via-built-server convention: build, boot, run smoke in another shell; scratch DB only, no production DB, no real email)
  </read_first>
  <action>
    Mirror 12-04 Task 3's proven recipe exactly, against the now-12-05-fixed
    code. Build the server (`npm run build`), boot it against a scratch
    DATABASE_PATH so production data is never touched (`DATABASE_PATH=$T
    MAGIC_LINK_SECRET=dev-only-insecure-secret-do-not-use-in-prod
    HOST=127.0.0.1 PORT=4321 node ./dist/server/entry.mjs` in the background),
    wait until `/` responds, then run `SMOKE_BASE_URL=http://127.0.0.1:4321
    DATABASE_PATH=$T
    MAGIC_LINK_SECRET=dev-only-insecure-secret-do-not-use-in-prod node
    scripts/smoke-manage.mjs`. Assert exit 0 and capture the `[smoke] result:
    pass=N fail=0` line into the SUMMARY as the gap-closure goal-backward
    evidence (specifically that M15 + M16 now PASS, proving CR-01/CR-02 closed
    and FAILED truth #10 satisfied). The MAGIC_LINK_SECRET passed to the
    server and the smoke MUST match (the smoke mints session/manage/unsubscribe
    tokens the server verifies). Kill the server, delete the scratch DB. No
    production DB, no real email (the cron is never enabled; /api/save-selection
    and /api/unsubscribe send no email). Do not modify smoke-manage.mjs in
    this task — verification run only.
  </action>
  <verify>
    <automated>bash -c 'set -e; T=/tmp/smoke-gap-$$.db; S=dev-only-insecure-secret-do-not-use-in-prod; npm run build >/dev/null 2>&1; DATABASE_PATH=$T MAGIC_LINK_SECRET=$S HOST=127.0.0.1 PORT=4321 node ./dist/server/entry.mjs >/dev/null 2>&1 & SP=$!; for i in $(seq 1 20); do curl -sf -o /dev/null http://127.0.0.1:4321/ && break; sleep 1; done; SMOKE_BASE_URL=http://127.0.0.1:4321 DATABASE_PATH=$T MAGIC_LINK_SECRET=$S node scripts/smoke-manage.mjs | tee /tmp/smoke-gap-out-$$.txt; RC=${PIPESTATUS[1]}; kill $SP 2>/dev/null || true; grep -E "result: pass=[0-9]+ fail=0" /tmp/smoke-gap-out-$$.txt; grep -E "M15-unsub-save-rejected" /tmp/smoke-gap-out-$$.txt; grep -E "M16-unsub-reconfirm-no-stale" /tmp/smoke-gap-out-$$.txt; rm -f "$T" "$T"-* /tmp/smoke-gap-out-$$.txt; exit $RC'</automated>
  </verify>
  <acceptance_criteria>
    - The smoke prints `[smoke] result: pass=N fail=0` (fail=0; N now covers M1–M16)
    - The captured output shows M15-unsub-save-rejected and M16-unsub-reconfirm-no-stale both PASS (no FAIL line for either)
    - `node scripts/smoke-manage.mjs` exits 0
    - No FAIL lines anywhere in the captured output (no M1–M14 regression)
    - Scratch DB used (DATABASE_PATH temp); ./data/oddlympics.db not modified; server killed; no real email sent
    - The PASS line + the M15/M16 PASS lines are recorded in 12-06-SUMMARY.md as the gap-closure goal-backward evidence (CR-01/CR-02 closed; FAILED truth #10 now satisfied behaviorally)
  </acceptance_criteria>
  <done>The full M1–M16 smoke runs green (pass=N fail=0, exit 0) against the freshly built 12-05-fixed server on a scratch DB; M15 and M16 PASS, proving the consent/unsubscribe contract is enforced end-to-end. The PASS evidence is captured in the SUMMARY and all temp artifacts are cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke harness → built server HTTP | smoke mints HMAC tokens (session / unsubscribe) with a known dev secret; only valid against a server booted with the same secret + a scratch DB |
| smoke → scratch SQLite | seed/assert via the existing parameterized helpers (dbInsertSmokeRow / dbInsertUserTeams / dbMarkConfirmed); never the production DB |
| smoke → /api/unsubscribe (real route) | M16 exercises the actual CR-02-fixed route over HTTP, not a DB shortcut, so the test proves the route itself performs the delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering | smoke seeding/mutating production data | mitigate | Both tasks run against a temp DATABASE_PATH; Task 2 acceptance asserts ./data/oddlympics.db is not modified; smoke emails use the smoke-%@example.com namespace + the existing footer cleanup hint; seeded teams use the id>=9900000 / smoke-team-% namespace. |
| T-12-17 | Information Disclosure | dev secret in smoke | accept | The dev-fallback secret is the documented test literal (src/lib/token.ts), already used by smoke-manage.mjs M1–M14; not a production secret; the server is booted ephemerally on localhost only. |
| T-12-18 | Tampering | accidental real email during verification | mitigate | /api/save-selection and /api/unsubscribe send no email; the kickoff cron is never invoked or enabled by this plan. No outbound path is exercised. |
| T-12-19 | Repudiation | a tautological smoke that passes regardless of the fix | mitigate | M15/M16 are designed to FAIL against pre-12-05 code (an unguarded save-selection would write the new slugs + overwrite tz → status=saved not unknown; an unguarded unsubscribe would leave user_teams rows → M16's post-unsubscribe length-0 assertion fails). Acceptance requires the new cases to be real regression detectors, recorded in the SUMMARY. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. N/A — no install task; no RESEARCH Package Legitimacy table required. |

Residual risk: none beyond the accepted dev-secret-on-localhost item; the
smoke is non-destructive to production and exercises the real fixed routes.
</threat_model>

<verification>
- `node --check scripts/smoke-manage.mjs` passes (Task 1)
- M15 + M16 appended after M14; M1–M14 case names + helpers + runCase + footer unchanged (no regression)
- M15 asserts the three CR-01 facts (status=unknown, unchanged user_teams, preserved tz); M16 asserts the CR-02 chain (real /api/unsubscribe → user_teams empty → dbMarkConfirmed → cron JOIN zero rows)
- The new cases reuse existing helpers only — no new deps, no framework JS, ESM preserved
- Full M1–M16 suite exits 0 (pass=N fail=0) against the 12-05-fixed built server on a scratch DB; ./data/oddlympics.db untouched; no real email
- Goal-backward: FAILED truth #10 in 12-VERIFICATION.md ("unsubscribed user cannot have team subscriptions written, and unsubscribing clears user_teams so re-subscription cannot silently re-activate a stale team list") is now behaviorally proven by M15 (write rejection) + M16 (clear-on-unsubscribe + no stale re-activation)
- D-03 fence: files_modified = ONLY scripts/smoke-manage.mjs; index.astro / api/signup.ts absent
</verification>

<success_criteria>
- WR-04 closed: M15 + M16 added, mirroring the M10–M14 idiom and reusing existing helpers; M1–M14 intact
- M15 proves CR-01 closed behaviorally: unsubscribed-user POST → status=unknown, zero user_teams writes, tz preserved
- M16 proves CR-02 closed behaviorally: real /api/unsubscribe clears user_teams; post-reconfirm cron JOIN yields zero rows (no stale fan-out)
- Full M1–M16 suite green (pass=N fail=0, exit 0) on a scratch DB against the 12-05-fixed built server; no production DB, no real email
- FAILED truth #10 from 12-VERIFICATION.md now satisfied — re-verification has goal-backward proof
</success_criteria>

<output>
Create `.planning/phases/12-restore-multi-team-selection/12-06-SUMMARY.md` when done.
Record: the M15 + M16 case bodies' assertion lists; the final `[smoke] result:
pass=N fail=0` line plus the M15/M16 PASS lines (gap-closure goal-backward
evidence); confirmation that M1–M14 are unchanged and the run used a scratch DB
with no real email sent.
</output>
