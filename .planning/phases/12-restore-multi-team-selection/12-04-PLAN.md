---
phase: 12-restore-multi-team-selection
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - scripts/smoke-manage.mjs
autonomous: true
requirements: [IDENT-02, IDENT-03, IDENT-04, NOTIFY-04, SIGNUP-04, LAND-02]
must_haves:
  truths:
    - "D-08: The phase MVP fence is exactly D-01 store + D-04/D-05 /manage editor + D-06 cron join + D-07 copy check; NO deferred item is implemented in this phase ('primary team' concept, per-team reorder, /schedule page revival, multi-team analytics, subject-line personalization all remain deferred future work)"
    - "D-09: Sequencing of record — Phase 12 depends on Phases 5-10 (current code); the Phase 11 launch gate RE-RUNS AFTER Phase 12 and only then cuts the deliberately-withheld v1.0-consumer-landing tag. The auto-generated ROADMAP Phase-12 stub 'Depends on: Phase 11' was inverted and is corrected in ROADMAP.md; no tag-cutting or Phase-11 work is a task in this phase (traceability record only)"
    - "src/lib/email.ts sendMagicLink value-prop copy is verified to still read correctly for the still-single-team signup confirmation email - no SIGNUP-04 regression, no LAND-02 prohibited terms (D-07)"
    - "scripts/smoke-manage.mjs gains M10-M14 covering: save N (1-5) -> user_teams rows match; reload pre-checks each saved slug; >5 -> too-many + zero writes; 0 -> bad-team + zero writes; cron-visibility through user_teams"
    - "node scripts/smoke-manage.mjs exits 0 against the built server (all M1-M14 PASS) - the existing M1-M9 still pass (no regression on the single->multi change)"
    - "Multi-team is restored end-to-end: store -> /manage 1-5 -> cron fans out via user_teams -> one email per match -> signup still single-team"
  artifacts:
    - path: "scripts/smoke-manage.mjs"
      provides: "M10-M14 multi-team end-to-end cases + a dbInsertUserTeams helper + multi-value POST helper"
      contains: "M10-multi-save-n"
  key_links:
    - from: "scripts/smoke-manage.mjs"
      to: "/api/save-selection"
      via: "multi-value team= POST asserting user_teams rows + status redirects"
      pattern: "M1[0-4]-"
    - from: "scripts/smoke-manage.mjs"
      to: "user_teams"
      via: "dbInsertUserTeams seed + DB read assertions"
      pattern: "user_teams"
---

<objective>
Two deliverables that together prove the phase goal end-to-end:
(1) D-07 verify-only check of the Phase-10 sendMagicLink value-prop copy in
src/lib/email.ts (confirmation email is still single-team because signup is
unchanged per D-03 - pin exact phrasing, assert no SIGNUP-04 regression, no
LAND-02 terms - expect ZERO code edits there);
(2) extend scripts/smoke-manage.mjs with M10-M14 covering the full multi-team
journey (save 1-5, reload pre-check, >5 too-many, 0 bad-team, cron-visibility
through user_teams), so verify-phase has goal-backward proof.

Purpose: This is the verification spine of Phase 12. It runs after 12-02 and
12-03 so the smoke exercises the real new behavior, and it pins the email copy
that D-07 puts in scope.

Output: scripts/smoke-manage.mjs (M1-M9 retained + M10-M14 added); src/lib/email.ts
verified unchanged; the D-07 finding recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-restore-multi-team-selection/12-CONTEXT.md
@.planning/phases/12-restore-multi-team-selection/12-PATTERNS.md
@./CLAUDE.md

<interfaces>
Contracts from prior waves (consume directly):
- 12-01: user_teams (email, team_slug, UNIQUE(email,team_slug)); getUserTeams returns { team_slug }[]
- 12-02: POST /api/save-selection accepts repeated team= form values; >=1/<=5 server-enforced;
  status=saved | too-many | bad-team (bad-tz still applies); writes via delete-all-then-insert txn.
  /manage signed-in branch renders <input type="checkbox" name="team" value=slug checked=...>
- 12-03: cron usersQuery joins vip_signups -> user_teams -> teams.slug

src/lib/email.ts (D-07 verify target - DO NOT EDIT unless a real regression is found):
  sendMagicLink(email, token, team: string, timezone: string)  // ONE team slug (signup single-team, D-03)
  text line 33 / html line 46: "We'll email you 1 hour before every ${teamHuman} match in ${tzHuman}."
  teamHuman = teamLabel(team) (src/lib/teams.ts); tzHuman = tzLabel(timezone) (src/lib/timezones.ts)

scripts/smoke-manage.mjs infra to REUSE (already in the file):
  - runCase(name, fn) harness (lines 218-232)
  - get(path, headers) / postForm(path, form, headers) (lines 179-210) - postForm uses
    URLSearchParams(form) which serializes ONE value per key; for multi-value team=
    build the body manually with body.append('team', slug) per PATTERNS.md SECTION 6
  - dbInsertSmokeRow(email, opts) (lines 131-151) - vip_signups seed
  - mintToken(email, {purpose}) (lines 84-91); COOKIE_NAME (line 53)
  - dbRead / dbWrite handles (lines 107-121); pass/fail counters; exit code (line 588)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 verify-only - pin sendMagicLink copy, assert no regression / no LAND-02</name>
  <files>src/lib/email.ts (verify-only — zero edits expected)</files>
  <read_first>
    - src/lib/email.ts (VERIFY TARGET - full file in plan context; sendMagicLink lines 17-73, value-prop text line 33, html line 46, the teamLabel/tzLabel imports lines 3-4, signature line 17-22)
    - .planning/phases/12-restore-multi-team-selection/12-PATTERNS.md (SECTION 5 - assessment: signup stays single-team, sendMagicLink receives one slug, NO code change expected; if a new multi-team email were introduced the pattern is teamLabel(slug) joined by ', ' - but no new email surface is in scope)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (D-07 - verify N-team copy handling; confirmation email still single-team since signup unchanged; SIGNUP-04 likely unaffected - VERIFY and pin phrasing; no LAND-02 terms; do NOT over-scope new emails)
    - src/lib/teams.ts (teamLabel resolves one slug -> label)
  </read_first>
  <action>
    Verification, not modification. Confirm the reasoning chain and pin the result:
    1. Signup is single-team (D-03, files_modified across the phase never include
       index.astro or api/signup.ts) -> /api/signup still calls
       sendMagicLink(email, token, team, timezone) with exactly ONE slug -> the
       confirmation email value-prop line "We'll email you 1 hour before every
       <teamHuman> match in <tzHuman>." remains grammatically correct for a single
       team. No multi-team confirmation email surface exists in D-08's MVP fence.
    2. Therefore NO code change to src/lib/email.ts is expected. Read the file and
       confirm: (a) sendMagicLink still takes a single `team: string`; (b) the
       value-prop line is still single-team-correct; (c) there are zero LAND-02
       prohibited terms (bitcoin/lightning/crypto/world domination/personal
       olympics) in the file.
    3. Make NO edits unless verification reveals an actual regression (it should
       not). If - and only if - a real defect is found, the minimal fix is the
       teamLabel-join pattern from PATTERNS.md SECTION 5; record the deviation in
       the SUMMARY. Otherwise close the task with the file unchanged and record the
       exact pinned phrasing (the value-prop string verbatim) in the SUMMARY as the
       D-07 / SIGNUP-04 evidence.
    Do NOT introduce any new email surface (D-08 fence - subject-line
    personalization, multi-team copy are deferred).
  </action>
  <verify>
    <automated>echo "SIG=$(grep -c 'team: string,' src/lib/email.ts) VP=$(grep -c 'match in ' src/lib/email.ts) LAND02=$(grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' src/lib/email.ts)"; git diff --stat -- src/lib/email.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'team: string,' src/lib/email.ts >= 1 (sendMagicLink still takes a single team slug - signup single-team confirmed)
    - The value-prop line "every ${teamHuman} match in ${tzHuman}" is present and unchanged (single-team-grammatical)
    - grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' src/lib/email.ts == 0 (LAND-02 clean)
    - git diff --stat -- src/lib/email.ts shows NO changes (verify-only; zero edits expected). If non-empty, the SUMMARY MUST justify the regression found + fix applied per PATTERNS SECTION 5.
    - SUMMARY records the verbatim pinned value-prop string as D-07 / SIGNUP-04 evidence
  </acceptance_criteria>
  <done>src/lib/email.ts confirmed unchanged: sendMagicLink still single-team, value-prop copy still correct, zero LAND-02 terms. The pinned phrasing is recorded as D-07 evidence (or, if a real regression was found, the minimal teamLabel-join fix is applied and justified).</done>
</task>

<task type="auto">
  <name>Task 2: Add M10-M14 multi-team end-to-end cases to scripts/smoke-manage.mjs</name>
  <files>scripts/smoke-manage.mjs</files>
  <read_first>
    - scripts/smoke-manage.mjs (FILE BEING MODIFIED - full file in plan context; note runCase lines 218-232, get/postForm lines 179-210, dbInsertSmokeRow lines 131-151, mintToken lines 84-91, M2 select-assertion lines 273-307, M3 DB-read-assertion lines 333-349, M4 bad-team lines 355-383, the result/exit footer lines 576-588, the cleanup hint lines 38-40)
    - .planning/phases/12-restore-multi-team-selection/12-PATTERNS.md (SECTION 6 - the M10-M14 table, the dbInsertUserTeams helper, the multi-value POST body pattern with body.append, the cleanup-comment extension; lines 342-428)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (the goal: store -> /manage 1-5 -> cron fans out via user_teams; D-05 bounds; D-06 cron visibility)
    - src/lib/db.ts (post-12-01 - user_teams shape for the seed/assert helper)
    - src/pages/api/save-selection.ts (post-12-02 - status codes saved/too-many/bad-team to assert against)
  </read_first>
  <action>
    Extend the existing smoke (do NOT rewrite M1-M9; do NOT change runCase /
    get / dbInsertSmokeRow / mintToken). Per PATTERNS.md SECTION 6:
    1. Add a helper dbInsertUserTeams(email, slugs=[]) mirroring dbInsertSmokeRow's
       style: DELETE FROM user_teams WHERE email = ? then INSERT OR IGNORE per slug
       (parameterized via dbWrite.prepare). Add a helper dbUserTeamSlugs(email)
       returning the sorted team_slug list from dbRead for assertions.
    2. Add a multi-value POST helper (or inline per case) that builds
       `const body = new URLSearchParams(); for (const s of slugs) body.append('team', s); body.append('timezone', tz);`
       then fetch POST /api/save-selection with Content-Type
       application/x-www-form-urlencoded, Origin BASE, the session Cookie, and
       redirect: 'manual' - returning { status, location } like postForm.
    3. Add five runCase blocks AFTER M9 and BEFORE the result/exit footer:
       - M10-multi-save-n: seed a confirmed vip_signups row; session cookie; POST
         3 valid slugs (e.g. england, france, germany) + a valid tz -> assert 303
         Location contains status=saved AND dbUserTeamSlugs(email) deep-equals the
         sorted 3 slugs (exactly those, no more).
       - M11-pre-check: with M10's saved set + session cookie, GET /manage ->
         assert 200 and the HTML contains a checked checkbox input for EACH saved
         slug (e.g. body includes `value="england"` within a `type="checkbox"`
         input that also carries `checked`) and contains NO `<select name="team"`.
       - M12-too-many: seed + session; POST 6 valid slugs -> assert 303 Location
         contains too-many AND dbUserTeamSlugs(email) is UNCHANGED from a known
         pre-state (seed 2 via dbInsertUserTeams first, assert still those 2 -
         zero writes on reject).
       - M13-empty-save: seed + session; POST zero team values (only timezone) ->
         assert 303 Location contains bad-team AND dbUserTeamSlugs(email) unchanged
         from a seeded 2-slug pre-state (zero writes on empty).
       - M14-cron-visibility: seed a confirmed, non-unsubscribed vip_signups row;
         dbInsertUserTeams(email, [slugA, slugB]) where slugA/slugB exist in
         teams.slug; assert a usersQuery-equivalent SQL run via dbRead (the same
         join shape as 12-03's usersQuery: vip_signups v JOIN user_teams ut ON
         ut.email=v.email JOIN teams t ON t.slug=ut.team_slug WHERE
         v.confirmed_at IS NOT NULL AND v.unsubscribed_at IS NULL AND t.id IN
         (idA, idB)) returns exactly ONE distinct email row for this user (proves
         D-06 join visibility + the NOTIFY-04 one-row collapse at the query level).
         If teams.slug rows are absent in the smoke DB, the case must seed two
         teams rows (parameterized) like 12-03 Task 2 does, then assert.
       Each case uses unique smoke-mNN-<ts>@example.com emails and the runCase
       harness so pass/fail counters + exit code already aggregate them.
    4. Update the header evidence-tag comment block (lines 26-37) to list
       M10-M14, and extend the cleanup hint (lines 38-40 and 583-585) to also
       DELETE FROM user_teams WHERE email LIKE 'smoke-%@example.com'.
    Plain ESM, node: prefix, no new deps, no framework JS. Keep the exact
    runCase/exit semantics so `node scripts/smoke-manage.mjs` exits 0 only when
    ALL M1-M14 pass.
  </action>
  <verify>
    <automated>node --check scripts/smoke-manage.mjs && echo "M10=$(grep -c 'M10-multi-save-n' scripts/smoke-manage.mjs) M14=$(grep -c 'M14-cron-visibility' scripts/smoke-manage.mjs) UT=$(grep -c 'user_teams' scripts/smoke-manage.mjs) APPEND=$(grep -c "body.append('team'" scripts/smoke-manage.mjs)"</automated>
  </verify>
  <acceptance_criteria>
    - node --check scripts/smoke-manage.mjs exits 0
    - grep -c 'M10-multi-save-n' scripts/smoke-manage.mjs == 1 AND each of M11-pre-check / M12-too-many / M13-empty-save / M14-cron-visibility appears exactly once
    - grep -c 'dbInsertUserTeams' scripts/smoke-manage.mjs >= 1
    - grep -c "body.append('team'" scripts/smoke-manage.mjs >= 1 (multi-value POST built correctly, not URLSearchParams(object))
    - grep -c 'DELETE FROM user_teams' scripts/smoke-manage.mjs >= 1 (cleanup hint + helper)
    - M1-M9 case names still present and unchanged (grep each of M1-signed-out-form .. M9-resubscribe-path == 1)
    - End-to-end: with the built server running (npm run build && node ./dist/server/entry.mjs) and DATABASE_PATH pointed at a scratch DB, `node scripts/smoke-manage.mjs` prints "result: pass=N fail=0" and exits 0 (all M1-M14 PASS)
  </acceptance_criteria>
  <done>smoke-manage.mjs has M10-M14 proving save-1-to-5 / reload-pre-check / too-many / empty-bad-team / cron-visibility through user_teams, with a dbInsertUserTeams helper + multi-value POST builder, M1-M9 intact, cleanup hint extended; the full suite exits 0 against the built server on a scratch DB.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full M1-M14 smoke against the built server and capture the PASS evidence</name>
  <files>scripts/smoke-manage.mjs (verification run only — no file written)</files>
  <read_first>
    - scripts/smoke-manage.mjs (post-Task-2 state; the result line "[smoke] result: pass=N fail=N" line 578 and exit code line 588; env vars SMOKE_BASE_URL / DATABASE_PATH / MAGIC_LINK_SECRET lines 46-52)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (goal-backward: the whole journey must hold together)
    - CLAUDE.md (smoke-via-built-server testing convention: build, boot, run smoke in another shell)
  </read_first>
  <action>
    Build the server, boot it against a scratch DATABASE_PATH (so production data
    is never touched), run the extended smoke, and capture the PASS line as the
    phase's goal-backward evidence. Concretely: npm run build; boot
    `DATABASE_PATH=$T MAGIC_LINK_SECRET=dev-only-insecure-secret-do-not-use-in-prod
    HOST=127.0.0.1 PORT=4321 node ./dist/server/entry.mjs` in the background; wait
    until / responds; run
    `SMOKE_BASE_URL=http://127.0.0.1:4321 DATABASE_PATH=$T
    MAGIC_LINK_SECRET=dev-only-insecure-secret-do-not-use-in-prod
    node scripts/smoke-manage.mjs`; assert exit 0 and capture the
    "[smoke] result: pass=N fail=0" line into the SUMMARY. Kill the server,
    delete the scratch DB. The MAGIC_LINK_SECRET passed to the server and the
    smoke MUST match (the smoke mints session/manage tokens the server verifies).
    No production DB, no real email (smoke never enables the cron; /api/save-selection
    sends no email).
  </action>
  <verify>
    <automated>bash -c 'set -e; T=/tmp/smoke-m-$$.db; S=dev-only-insecure-secret-do-not-use-in-prod; npm run build >/dev/null 2>&1; DATABASE_PATH=$T MAGIC_LINK_SECRET=$S HOST=127.0.0.1 PORT=4321 node ./dist/server/entry.mjs >/dev/null 2>&1 & SP=$!; for i in $(seq 1 20); do curl -sf -o /dev/null http://127.0.0.1:4321/ && break; sleep 1; done; SMOKE_BASE_URL=http://127.0.0.1:4321 DATABASE_PATH=$T MAGIC_LINK_SECRET=$S node scripts/smoke-manage.mjs | tee /tmp/smoke-m-out-$$.txt; RC=${PIPESTATUS[1]}; kill $SP 2>/dev/null || true; grep -E "result: pass=[0-9]+ fail=0" /tmp/smoke-m-out-$$.txt; rm -f "$T" "$T"-* /tmp/smoke-m-out-$$.txt; exit $RC'</automated>
  </verify>
  <acceptance_criteria>
    - The smoke prints "[smoke] result: pass=N fail=0" (fail=0; N covers M1-M14)
    - `node scripts/smoke-manage.mjs` exits 0
    - No FAIL lines in the captured output
    - Scratch DB used (DATABASE_PATH temp); ./data/oddlympics.db not modified; server killed; no real email sent
    - The PASS line is recorded in 12-04-SUMMARY.md as the phase goal-backward evidence
  </acceptance_criteria>
  <done>The full M1-M14 smoke runs green (pass=N fail=0, exit 0) against the freshly built server on a scratch DB, proving multi-team restored end-to-end; the PASS line is captured as evidence and all temp artifacts are cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke harness -> built server HTTP | smoke mints HMAC tokens with a known dev secret; only valid against a server booted with the same secret + a scratch DB |
| smoke -> scratch SQLite | seed/assert via parameterized prepared statements; never the production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | smoke seeding production data | mitigate | Every task runs against a temp DATABASE_PATH; acceptance criteria assert ./data/oddlympics.db is not modified; smoke emails use the smoke-%@example.com namespace + cleanup hint. |
| T-12-14 | Information Disclosure | dev secret in smoke | accept | The dev-fallback secret is the documented test literal (src/lib/token.ts), already used by the existing smoke-manage.mjs; not a production secret; server booted ephemerally on localhost. |
| T-12-15 | Tampering | accidental real email during verification | mitigate | /api/save-selection sends no email; the cron is never invoked/enabled by this plan; sendMagicLink is verify-only (Task 1, no execution). No outbound path exercised. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. N/A - no install task. |
</threat_model>

<verification>
- node --check passes on the modified smoke
- src/lib/email.ts verified unchanged (D-07); git diff --stat empty
- Full M1-M14 smoke exits 0 (pass=N fail=0) against the built server on a scratch DB
- D-03 fence: files_modified = ONLY scripts/smoke-manage.mjs; email.ts is verify-only (not in files_modified because no edit is expected); index.astro / api/signup.ts absent
- LAND-02 clean in email.ts; SIGNUP-04 phrasing pinned
- Goal-backward: store (12-01) -> /manage 1-5 (12-02) -> cron fan-out via user_teams (12-03) -> one email per match (NOTIFY-04 inherited) -> signup still single-team (D-03) - all proven by M10-M14 + Task 1
</verification>

<success_criteria>
- sendMagicLink copy verified single-team-correct, no SIGNUP-04 regression, no LAND-02 terms (zero edits expected)
- smoke-manage.mjs M10-M14 added; M1-M9 intact; helpers + multi-value POST + cleanup hint added
- Full suite green (pass=N fail=0) on a scratch DB against the built server
- Multi-team restored end-to-end and proven goal-backward
</success_criteria>

<output>
Create .planning/phases/12-restore-multi-team-selection/12-04-SUMMARY.md when done.
Record: the verbatim pinned sendMagicLink value-prop string (D-07/SIGNUP-04 evidence);
the M10-M14 case list; the final "[smoke] result: pass=N fail=0" line (phase
goal-backward evidence); confirmation that email.ts was unchanged.
</output>
