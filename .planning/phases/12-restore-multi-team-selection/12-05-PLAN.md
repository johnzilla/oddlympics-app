---
phase: 12-restore-multi-team-selection
plan: 05
type: execute
wave: 4
depends_on: ["12-02"]
files_modified:
  - src/lib/db.ts
  - src/pages/api/save-selection.ts
  - src/pages/manage.astro
  - src/pages/api/unsubscribe.ts
autonomous: true
gap_closure: true
requirements: [IDENT-02, IDENT-03, IDENT-04, NOTIFY-04, SIGNUP-04, LAND-02]
must_haves:
  truths:
    - "CR-01 closed: an unsubscribed OR unconfirmed user who POSTs to /api/save-selection with an otherwise-valid manage token or 30-day session and valid slugs is REJECTED — zero user_teams rows are written and vip_signups.timezone is NOT overwritten — and the route 303-redirects to /manage?status=unknown (the SQL-level confirmed_at IS NOT NULL AND unsubscribed_at IS NULL state gate the replaced setSelection carried is restored inside the existing db.transaction)"
    - "CR-01 UI closed: STATUS_COPY in manage.astro again contains an 'unknown' key, so the status=unknown 303 renders a visible not-active message instead of nothing (the key was dropped when the guard was removed in 12-02; it is restored with copy consistent with the other STATUS_COPY entries and the original Phase-9 not-active wording)"
    - "CR-02 closed: GET /api/unsubscribe deletes the user's user_teams rows (deleteUserTeams.run) after markUnsubscribed, so a later /api/confirm → markConfirmed (which clears unsubscribed_at back to NULL) CANNOT silently re-activate a stale pre-unsubscribe team list for the kickoff cron — re-subscription requires the user to re-pick teams on /manage"
    - "The unsubscribe → reconfirm → cron path yields ZERO followed teams: after unsubscribe (user_teams empty) then markConfirmed (unsubscribed_at NULL again), the cron usersQuery JOIN through user_teams returns no rows for that user — no stale fan-out"
    - "Changes are surgical (1–3 lines of production code per file) and additive-behavior only: no schema/migration change (D-01 store is correct, D-02 waives migration ceremony), signup untouched (D-03 — index.astro / api/signup.ts not in files_modified), no new error codes beyond restoring the pre-existing 'unknown'"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "A new module-level prepared statement updateTimezoneActive scoped to the active-user predicate (UPDATE vip_signups SET timezone = ? WHERE email = ? AND confirmed_at IS NOT NULL AND unsubscribed_at IS NULL), mirroring setSelection's guard and updateTimezone's idiom/typed generics"
      contains: "confirmed_at IS NOT NULL AND unsubscribed_at IS NULL"
    - path: "src/pages/api/save-selection.ts"
      provides: "State-gated transaction: updateTimezoneActive.run inside the existing db.transaction, throwing a discriminable not-active error on res.changes === 0 so the transaction aborts (no user_teams writes) and the catch maps it to redirectTo(formToken, 'unknown')"
      contains: "updateTimezoneActive"
    - path: "src/pages/manage.astro"
      provides: "Restored 'unknown' entry in STATUS_COPY so the not-active redirect renders a message"
      contains: "'unknown':"
    - path: "src/pages/api/unsubscribe.ts"
      provides: "deleteUserTeams.run(result.email) after markUnsubscribed so unsubscribe is authoritative over the join table"
      contains: "deleteUserTeams"
  key_links:
    - from: "src/pages/api/save-selection.ts"
      to: "src/lib/db.ts updateTimezoneActive"
      via: "imported module-level statement called inside the db.transaction callback; res.changes === 0 throws not-active"
      pattern: "updateTimezoneActive"
    - from: "src/pages/api/save-selection.ts"
      to: "/manage?status=unknown"
      via: "catch arm maps the not-active throw to redirectTo(formToken, 'unknown')"
      pattern: "'unknown'"
    - from: "src/pages/api/unsubscribe.ts"
      to: "user_teams"
      via: "deleteUserTeams.run(result.email) after markUnsubscribed.get(result.email)"
      pattern: "deleteUserTeams\\.run"
    - from: "src/pages/manage.astro STATUS_COPY"
      to: "/manage?status=unknown render"
      via: "'unknown' key present so statusMsg resolves non-null"
      pattern: "'unknown':"
---

<objective>
Close the two BLOCKER consent/privacy regressions verified TRUE in
12-VERIFICATION.md (FAILED truth #10), with surgical, minimal production changes
and zero schema/migration work:

- CR-01: /api/save-selection writes user_teams rows + overwrites timezone for
  ANY authenticated email regardless of confirmed_at/unsubscribed_at. Restore
  the SQL-level state gate (the predicate the replaced setSelection carried)
  inside the existing db.transaction so an unsubscribed/unconfirmed user is
  rejected with zero writes and redirected to /manage?status=unknown. Restore
  the 'unknown' key to STATUS_COPY in manage.astro (dropped in 12-02) so that
  redirect renders a message.
- CR-02: /api/unsubscribe does not clear user_teams, so /api/confirm →
  markConfirmed (which clears unsubscribed_at) silently re-activates the stale
  pre-unsubscribe team list for the cron. Add deleteUserTeams.run after
  markUnsubscribed so unsubscribe is authoritative over the join table.

Purpose: These are the consent-bypass (CR-01) and silent-re-subscription
(CR-02) security regressions that block Phase 12 verification and, downstream,
the Phase 11 launch gate (D-09). The fixes share one concern — the
authorization/lifecycle boundary around the user_teams store — so they ship
together to avoid a half-fixed consent contract.

Output: src/lib/db.ts (one new scoped statement), src/pages/api/save-selection.ts
(state-gated transaction + discriminated catch arm), src/pages/manage.astro
(restored 'unknown' STATUS_COPY entry), src/pages/api/unsubscribe.ts
(deleteUserTeams call). The end-to-end behavioral proof (negative-path smoke
cases) is delivered by 12-06, which depends on this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-restore-multi-team-selection/12-CONTEXT.md
@.planning/phases/12-restore-multi-team-selection/12-PATTERNS.md
@.planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md
@./CLAUDE.md

<interfaces>
<!-- Source-of-truth idioms to mirror — consume directly, no exploration needed. -->

src/lib/db.ts — the guard predicate to restore is ALREADY present verbatim in
the replaced setSelection (lines 239-244):
  export const setSelection = db.prepare<[string, string, string]>(`
    UPDATE vip_signups SET team = ?, timezone = ?
    WHERE email = ? AND confirmed_at IS NOT NULL AND unsubscribed_at IS NULL
    RETURNING *`);
The stateless statement to clone-and-scope (lines 290-292):
  export const updateTimezone = db.prepare<[string, string]>(`
    UPDATE vip_signups SET timezone = ? WHERE email = ?`);  // args (timezone, email)
deleteUserTeams already exported (lines 274-276):
  export const deleteUserTeams = db.prepare<[string]>(`DELETE FROM user_teams WHERE email = ?`);
markUnsubscribed (lines 114-119): UPDATE ... SET unsubscribed_at ... RETURNING *
markConfirmed (lines 102-108): SET confirmed_at, unsubscribed_at = NULL — the re-activation path.

.changes idiom precedent (already in the codebase):
  scripts/send-kickoff-notifications.mjs:207  →  if (claim.changes === 0) continue;
  better-sqlite3 .run() returns { changes: number, lastInsertRowid }.

src/pages/api/save-selection.ts current shape (FILE BEING MODIFIED):
  - import line 2 pulls { db, deleteUserTeams, insertUserTeam, updateTimezone, insertFeatureRequest } from '../../lib/db'
  - the transaction (lines 71-79) is the existing db.transaction((email, slugs, timezone) => { deleteUserTeams.run; for…insertUserTeam.run; updateTimezone.run(timezone, email); }); saveSelection(result.email, validSlugs, tz)
  - the catch (lines 95-98) currently maps EVERY error to redirectTo(formToken, 'server') — a discriminated arm is needed so a not-active throw maps to 'unknown' instead
  - redirectTo(token, status, setCookie?) helper lines 23-29

src/pages/manage.astro STATUS_COPY (lines 97-104) currently has keys:
  saved, 'bad-token', 'bad-tz', 'bad-team', 'too-many', server  — NO 'unknown'.
  The comment at lines 108-111 still references the ?status=unknown contract.
  statusMsg (line 106) = status ? STATUS_COPY[status] ?? null : null — so a
  missing key renders nothing.

src/pages/api/unsubscribe.ts current shape (FILE BEING MODIFIED):
  imports { markUnsubscribed, getByEmail } from '../../lib/db' (line 3)
  line 14: const updated = markUnsubscribed.get(result.email);
  No deleteUserTeams call. deleteUserTeams is exported and ready to import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — add updateTimezoneActive scoped statement in db.ts and state-gate the save-selection transaction (zero writes for unsubscribed/unconfirmed users)</name>
  <files>src/lib/db.ts, src/pages/api/save-selection.ts</files>
  <read_first>
    - src/lib/db.ts (SOURCE-OF-TRUTH for the original setSelection guard idiom — lines 237-244 setSelection with the WHERE email = ? AND confirmed_at IS NOT NULL AND unsubscribed_at IS NULL predicate; lines 288-292 updateTimezone, the stateless statement + its typed generic db.prepare<[string, string]> and arg order (timezone, email) to mirror)
    - src/pages/api/save-selection.ts (FILE BEING MODIFIED — full file in plan context; the import line 2, the existing db.transaction lines 71-79, the saveSelection(result.email, validSlugs, tz) call line 79, the catch lines 95-98 that currently funnels everything to 'server', the redirectTo helper lines 23-29)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (CR-01 section + "Mitigation Sufficiency Assessment": the verifier's recommended shape — scoped updateTimezoneActive, res.changes === 0 throw, catch maps to redirectTo(formToken, 'unknown'), no user_teams writes for an inactive user)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (D-01 store correct, D-02 no migration ceremony, D-03 signup untouched — scope fence)
  </read_first>
  <action>
    Surgical change, ~1-3 lines of new production code per file. Do NOT rewrite
    the route or the transaction structure.

    1. src/lib/db.ts — add ONE new module-level exported prepared statement,
       placed adjacent to the existing updateTimezone (after line 292), named
       `updateTimezoneActive`. Same typed generic as updateTimezone
       (`db.prepare<[string, string]>`), same SET timezone = ? clause and same
       positional arg order (timezone, email), but with the active-user
       predicate appended verbatim from setSelection:
       `WHERE email = ? AND confirmed_at IS NOT NULL AND unsubscribed_at IS NULL`.
       Add a one-line why-comment referencing Phase 12 CR-01 and that it
       restores the SQL-level state gate the replaced setSelection carried.
       Do NOT modify, remove, or repurpose the existing updateTimezone or
       setSelection — updateTimezoneActive is purely additive.

    2. src/pages/api/save-selection.ts — import the new `updateTimezoneActive`
       from '../../lib/db' (extend the existing line-2 import; keep
       updateTimezone imported too if still referenced elsewhere — it is the
       only tz writer, so swap the in-transaction call rather than keep both).
       Inside the EXISTING db.transaction callback (lines 74-78), replace the
       `updateTimezone.run(timezone, email)` call with
       `const res = updateTimezoneActive.run(timezone, email); if (res.changes === 0) throw new Error('not-active');`.
       This must be the FIRST statement in the transaction body (before
       deleteUserTeams.run / the insertUserTeam loop) so that an inactive user
       throws BEFORE any user_teams row is deleted or inserted — better-sqlite3
       rolls back the whole transaction on the throw, guaranteeing zero
       user_teams writes and no timezone change for an unsubscribed/unconfirmed
       user. (Ordering matters: if delete ran first then the throw, the rows
       would still be rolled back by the transaction, but putting the gate
       first is the clearest contract and matches the verifier's recommended
       shape.) Use the `.changes` idiom exactly as
       scripts/send-kickoff-notifications.mjs:207 does.

    3. In the catch block (lines 95-98), add a discriminated arm BEFORE the
       existing `return redirectTo(formToken, 'server')`: if the caught error
       is the not-active error (e.g. `err instanceof Error && err.message ===
       'not-active'`), `return redirectTo(formToken, 'unknown')`. Keep the
       existing `console.error('[save-selection] db error', err)` for the
       genuine-server-error path; for the not-active path log a distinct line
       (e.g. `console.error('[save-selection] not-active: <email>')`) so
       journald distinguishes a consent rejection from a real DB fault.
       Preserve the feature_request block, the bad-tz/bad-team/too-many early
       returns, and the success `redirectTo(formToken, 'saved', buildSessionCookie(...))`
       verbatim. No new error codes are introduced — `unknown` is a
       pre-existing code (it was emitted by the original setSelection-guarded
       route and is being restored, not invented).

    Strict TS, no `any`, ESM, `node:` convention unaffected, why-only comments.
    Parameterized prepared statement only — zero string interpolation.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5; echo "DBGATE=$(grep -c 'updateTimezoneActive' src/lib/db.ts) DBPRED=$(grep -c 'confirmed_at IS NOT NULL AND unsubscribed_at IS NULL' src/lib/db.ts) SSACTIVE=$(grep -c 'updateTimezoneActive' src/pages/api/save-selection.ts) SSCHANGES=$(grep -c 'res.changes === 0' src/pages/api/save-selection.ts) SSNOTACTIVE=$(grep -c \"'not-active'\" src/pages/api/save-selection.ts) SSUNKNOWN=$(grep -c \"redirectTo(formToken, 'unknown')\" src/pages/api/save-selection.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'updateTimezoneActive' src/lib/db.ts` >= 1 (new scoped statement exported)
    - `grep -c 'export const updateTimezoneActive = db.prepare<\[string, string\]>' src/lib/db.ts` == 1 (mirrors updateTimezone's typed generic + arg order)
    - The new statement's body contains the exact predicate: `grep -c 'confirmed_at IS NOT NULL AND unsubscribed_at IS NULL' src/lib/db.ts` >= 2 (now present in BOTH setSelection and updateTimezoneActive — setSelection is untouched)
    - `grep -c 'export const updateTimezone = db.prepare' src/lib/db.ts` == 1 (the original stateless updateTimezone is NOT removed or altered)
    - `grep -c 'updateTimezoneActive' src/pages/api/save-selection.ts` >= 2 (imported AND called inside the transaction)
    - `grep -c "res.changes === 0" src/pages/api/save-selection.ts` == 1 AND `grep -c "throw new Error('not-active')" src/pages/api/save-selection.ts` == 1
    - The `updateTimezoneActive.run(...)` + `if (res.changes === 0) throw` appears INSIDE the `db.transaction((...) => { ... })` callback body (textually between the `db.transaction(` open and its closing `})`), and is the first statement before `deleteUserTeams.run` / the `insertUserTeam` loop (manual structural read of the diff confirms ordering)
    - `grep -c "redirectTo(formToken, 'unknown')" src/pages/api/save-selection.ts` == 1 (the not-active catch arm)
    - The catch block still contains the original `redirectTo(formToken, 'server')` for the non-not-active path (`grep -c "redirectTo(formToken, 'server')" src/pages/api/save-selection.ts` == 1)
    - `grep -oE "redirectTo\(formToken, '[a-z-]+'" src/pages/api/save-selection.ts` yields only: too-many, bad-team, bad-tz, saved, server, unknown — no novel codes
    - `npx astro check` reports no new type errors
    - Diff is surgical: `git diff --stat -- src/lib/db.ts src/pages/api/save-selection.ts` shows only these two files, small line counts (one new statement in db.ts; the in-transaction gate + catch arm + import in save-selection.ts)
  </acceptance_criteria>
  <done>db.ts exports a new module-level updateTimezoneActive scoped to confirmed_at IS NOT NULL AND unsubscribed_at IS NULL (setSelection + updateTimezone untouched); save-selection.ts calls it as the first statement inside the existing db.transaction, throws 'not-active' on res.changes === 0 (rolling back any user_teams writes), and a discriminated catch arm maps that throw to redirectTo(formToken, 'unknown') while genuine DB faults still map to 'server'. No schema change, signup untouched, npx astro check clean.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 UI — restore the 'unknown' key to STATUS_COPY in manage.astro</name>
  <files>src/pages/manage.astro</files>
  <read_first>
    - src/pages/manage.astro (FILE BEING MODIFIED — the STATUS_COPY map lines 96-104 with its existing entries saved / 'bad-token' / 'bad-tz' / 'bad-team' / 'too-many' / server and their { kind: 'ok'|'err'; text } shape; the statusMsg derivation line 106 = status ? STATUS_COPY[status] ?? null : null; the still-present comment lines 108-111 referencing the ?status=unknown / setSelection not-active contract — that comment documents the original intent the key must satisfy)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (CR-01: "The status=unknown path is also dead: STATUS_COPY in manage.astro (lines 97-104) no longer contains an 'unknown' key… the user would see nothing" + Anti-Patterns row manage.astro:97-104 — restore alongside the guard)
    - src/lib/db.ts (the setSelection predicate the 'unknown' state corresponds to — confirmed_at IS NOT NULL AND unsubscribed_at IS NULL; informs the not-active user-facing wording)
  </read_first>
  <action>
    One-line restoration, no other change to manage.astro. Add a single
    `'unknown'` entry to the STATUS_COPY map (lines 96-104), kind `'err'`,
    with copy consistent with the surrounding entries' tone and the original
    Phase-9 not-active wording (the path fires when the authenticated row is
    no longer a confirmed, non-unsubscribed subscriber — i.e. the user has
    unsubscribed or is unconfirmed and tried to save). Use clear,
    non-alarming, action-oriented copy in the same register as the existing
    'bad-token'/'server' entries — e.g. that the account isn't an active
    subscription right now and the user should re-confirm / re-subscribe (do
    not over-specify exact words; match the existing voice and keep it short,
    no LAND-02 prohibited terms). Do NOT touch any other STATUS_COPY entry,
    the statusMsg line, the isUnsubscribed branch, branch A/B/C/D rendering,
    the duplicated `<style is:global>` block (deferred Layout tech-debt — out
    of scope), or any `<script is:inline>`.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5; echo "UNKNOWN=$(grep -c \"'unknown':\" src/pages/manage.astro) KEYS=$(grep -oE \"'(unknown|too-many|bad-team|bad-tz|bad-token)':\" src/pages/manage.astro | sort -u | tr '\n' ' ') LAND02=$(grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' src/pages/manage.astro)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'unknown':" src/pages/manage.astro` == 1 (the key is restored)
    - The 'unknown' entry has the `{ kind: 'err', text: ... }` shape consistent with the other STATUS_COPY entries (manual read confirms it parses inside the same Record literal)
    - The other five entries (saved, 'bad-token', 'bad-tz', 'bad-team', 'too-many', server) are unchanged (`grep -c "'too-many':" src/pages/manage.astro` == 1 and `grep -c "'bad-team':" src/pages/manage.astro` == 1 still hold; no edits to their text)
    - `grep -ciE 'bitcoin|lightning|crypto|world domination|personal olympics' src/pages/manage.astro` == 0 (LAND-02 clean)
    - The duplicated `<style is:global>` block is unchanged byte-for-byte (deferred tech-debt, not this phase)
    - `git diff --stat -- src/pages/manage.astro` shows a single small hunk (one added STATUS_COPY line)
    - `npx astro check` reports no new type errors
  </acceptance_criteria>
  <done>STATUS_COPY in manage.astro again contains an 'unknown' { kind: 'err', text } entry consistent with the other entries and the Phase-9 not-active intent, so the status=unknown 303 from Task 1 renders a visible message. No other manage.astro change; style block + branches untouched; LAND-02 clean.</done>
</task>

<task type="auto">
  <name>Task 3: CR-02 — clear user_teams on unsubscribe so re-confirmation cannot silently re-activate stale subscriptions</name>
  <files>src/pages/api/unsubscribe.ts</files>
  <read_first>
    - src/pages/api/unsubscribe.ts (FILE BEING MODIFIED — full file in plan context; the import line 3 `{ markUnsubscribed, getByEmail } from '../../lib/db'`, line 14 `const updated = markUnsubscribed.get(result.email)`, the idempotent already-unsubscribed/unknown branches lines 15-20)
    - src/lib/db.ts (SOURCE-OF-TRUTH — deleteUserTeams already exported lines 274-276 `db.prepare<[string]>(\`DELETE FROM user_teams WHERE email = ?\`)`; markUnsubscribed lines 114-119 RETURNING * → returns undefined when no row matched; markConfirmed lines 102-108 clears unsubscribed_at = NULL — the re-activation path CR-02 closes)
    - .planning/phases/12-restore-multi-team-selection/12-VERIFICATION.md (CR-02 section + "Mitigation Sufficiency Assessment": one-line fix — after markUnsubscribed, `deleteUserTeams.run(result.email)`; idempotent + safe on the already-unsubscribed / no-rows case)
    - .planning/phases/12-restore-multi-team-selection/12-CONTEXT.md (D-01/D-02 — no schema change; D-03 — signup untouched)
  </read_first>
  <action>
    One-line addition. Import `deleteUserTeams` from '../../lib/db' (extend the
    existing line-3 import). After the existing
    `const updated = markUnsubscribed.get(result.email);` (line 14) and BEFORE
    the success redirect, call `deleteUserTeams.run(result.email);`
    unconditionally. Calling it unconditionally (rather than only when
    `updated` is truthy) is intentional and safe: DELETE on zero matching rows
    is a no-op (.changes === 0, no error), so the call is idempotent on the
    already-unsubscribed and no-such-row branches and makes unsubscribe
    authoritative over user_teams in every code path that reaches a verified
    token. This guarantees that a subsequent /api/confirm → markConfirmed
    (which sets unsubscribed_at = NULL, re-satisfying the cron's
    `v.unsubscribed_at IS NULL` predicate) finds NO user_teams rows — the user
    must re-pick teams on /manage to receive notifications again. Do NOT
    change the verifyToken('unsubscribe') auth chain, the markUnsubscribed
    call, the getByEmail idempotency branches, the redirect helper, or any
    status code (ok / already / unknown / bad-token) — only add the import and
    the single deleteUserTeams.run line. No schema change.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5; echo "IMP=$(grep -c 'deleteUserTeams' src/pages/api/unsubscribe.ts) RUN=$(grep -c 'deleteUserTeams.run(result.email)' src/pages/api/unsubscribe.ts) MU=$(grep -c 'markUnsubscribed.get(result.email)' src/pages/api/unsubscribe.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'deleteUserTeams' src/pages/api/unsubscribe.ts` >= 2 (imported AND called)
    - `grep -c 'deleteUserTeams.run(result.email)' src/pages/api/unsubscribe.ts` == 1
    - `grep -c 'markUnsubscribed.get(result.email)' src/pages/api/unsubscribe.ts` == 1 (the existing call is preserved, not replaced)
    - The deleteUserTeams.run call appears AFTER the markUnsubscribed.get line and is reached on the verified-token path (manual structural read confirms it is not behind the early `!token` / `!result` returns)
    - `grep -c "verifyToken(token, 'unsubscribe')" src/pages/api/unsubscribe.ts` == 1 (auth chain untouched)
    - The status branches (ok / already / unknown / bad-token) are unchanged (`grep -oE "status=[a-z-]+" src/pages/api/unsubscribe.ts | sort -u` is unchanged from pre-edit)
    - `git diff --stat -- src/pages/api/unsubscribe.ts` shows a single small hunk (import extension + one added line)
    - `npx astro check` reports no new type errors
  </acceptance_criteria>
  <done>unsubscribe.ts imports deleteUserTeams and calls deleteUserTeams.run(result.email) after markUnsubscribed on the verified-token path, idempotently clearing the join table so a later markConfirmed cannot re-activate a stale team list. Auth chain, idempotency branches, and status codes unchanged; no schema change; npx astro check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/save-selection | attacker-controlled repeated `team` values + `timezone`; auth-gated by a manage token (≤24h) OR a session cookie (≤30d) that may outlive an unsubscribe — the credential being valid does NOT imply the row is still an active subscription |
| email link → GET /api/unsubscribe | HMAC-signed unsubscribe token crosses here; on success the handler is now responsible for cleaning the user_teams join table it did not previously own |
| GET /api/confirm → markConfirmed | re-confirmation clears unsubscribed_at = NULL — the moment any orphaned user_teams rows would otherwise become live for the cron |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Elevation of Privilege / Tampering | unsubscribed-or-unconfirmed user writing user_teams + overwriting timezone via a still-valid session/manage credential (CR-01, the verified BLOCKER) | mitigate | Task 1: restore the SQL-level state gate — `updateTimezoneActive` scoped to `confirmed_at IS NOT NULL AND unsubscribed_at IS NULL`, executed first inside the existing db.transaction; `res.changes === 0` throws `not-active`, rolling back the whole transaction so ZERO user_teams rows are written and timezone is unchanged; the route 303s to /manage?status=unknown. Task 2 restores the STATUS_COPY 'unknown' key so the rejection is visible. Block-worthy; mitigated by Tasks 1+2. |
| T-12-10 | Tampering / Repudiation | silent re-subscription — stale pre-unsubscribe user_teams rows re-activated for the kickoff cron when /api/confirm → markConfirmed clears unsubscribed_at, with no user action on /manage (CR-02, the verified BLOCKER) | mitigate | Task 3: `deleteUserTeams.run(result.email)` on every verified-token unsubscribe makes unsubscribe authoritative over the join table; after re-confirmation the cron's user_teams JOIN yields zero followed teams until the user explicitly re-picks on /manage. Block-worthy; mitigated by Task 3. |
| T-12-11 | Repudiation | inability to distinguish a consent rejection from a genuine DB fault in logs | accept (mitigated by logging) | Task 1 logs a distinct `[save-selection] not-active` line for the consent-rejection path, separate from the existing `[save-selection] db error` line; journald captures stdout/stderr (existing posture, no new logger). |
| T-12-12 | Tampering (SQLi) | email/slug values reaching SQLite in the new scoped statement | mitigate | `updateTimezoneActive` is a `db.prepare`-backed parameterized statement (typed generic, positional `?` binds only); deleteUserTeams is the existing parameterized statement; zero string interpolation introduced. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. N/A — no install task; no RESEARCH Package Legitimacy table required. |

Residual risk: a session/manage credential minted before unsubscribe remains
cryptographically valid until its TTL — but post-fix it can no longer perform
any user_teams write or timezone change while the row is inactive (CR-01
mitigated), and the orphaned rows it could previously leave behind no longer
exist (CR-02 mitigated). The remaining residual is purely a valid-but-inert
credential, which is the intended sliding-session design and is accepted.
</threat_model>

<verification>
- `npx astro check` clean across all four modified files
- All Task 1/2/3 grep gates pass
- db.ts: new updateTimezoneActive exported with the exact active-user predicate; setSelection and updateTimezone unchanged (purely additive)
- save-selection.ts: state gate is the first statement INSIDE the existing db.transaction; res.changes === 0 throws 'not-active'; discriminated catch maps it to 'unknown' while real faults stay 'server'; no new error codes
- manage.astro: 'unknown' STATUS_COPY entry restored; all other entries + style block + branches untouched
- unsubscribe.ts: deleteUserTeams.run on the verified-token path after markUnsubscribed; auth chain + idempotency branches + status codes unchanged
- D-03 fence: files_modified = ONLY src/lib/db.ts, src/pages/api/save-selection.ts, src/pages/manage.astro, src/pages/api/unsubscribe.ts — src/pages/index.astro and src/pages/api/signup.ts are NOT present
- D-01/D-02 fence: zero CREATE/ALTER/DROP/pragma_table_info/version-assert/backup added — no schema or migration change
- LAND-02: zero prohibited terms in any added manage.astro copy
- Behavioral end-to-end proof (unsubscribed-POST rejection + unsubscribe→reconfirm→cron-excludes) is delivered by 12-06 (depends_on this plan)
</verification>

<success_criteria>
- CR-01 closed: an unsubscribed/unconfirmed user POSTing to /api/save-selection with valid credentials + slugs is rejected with status=unknown, zero user_teams writes, timezone unchanged (SQL-level state gate restored inside the existing transaction)
- CR-01 UI closed: STATUS_COPY 'unknown' entry restored so the rejection renders a visible message
- CR-02 closed: unsubscribe clears user_teams; a later markConfirmed cannot silently re-activate a stale team list — re-subscription requires re-picking on /manage
- Surgical (1–3 lines of production code per file), additive-behavior only: no schema/migration change, signup untouched (D-03), no new error codes
- npx astro check clean; the negative-path smoke proof follows in 12-06
</success_criteria>

<output>
Create `.planning/phases/12-restore-multi-team-selection/12-05-SUMMARY.md` when done.
Record: the verbatim updateTimezoneActive statement body added to db.ts; the
exact in-transaction gate + catch-arm wiring in save-selection.ts; the verbatim
restored 'unknown' STATUS_COPY text; the unsubscribe.ts deleteUserTeams line;
confirmation that files_modified is exactly the four listed files and no
schema/migration/signup change was made.
</output>
