---
phase: 14-share-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/copy.ts
autonomous: true
requirements: [SHARE-04]
must_haves:
  truths:
    - "A single shareText(teamLabel, url) helper exists in src/lib/copy.ts (D-08, D-11)"
    - "The helper composes a personalized line naming the team and a newline-separated URL (SC4)"
  artifacts:
    - path: "src/lib/copy.ts"
      provides: "shareText(teamLabel: string, url: string): string — single template consumed by email + 3 pages (D-08, D-11)"
      contains: "shareText"
  key_links:
    - from: "src/lib/copy.ts shareText"
      to: "consumers (email.ts + pending.astro + confirmed.astro + manage.astro)"
      via: "named export imported alongside existing NO_ACCOUNT_TITLE / NO_ACCOUNT_BODY / REENTRY_CTA constants"
      pattern: "export function shareText"
---

<objective>
Add the `shareText(teamLabel, url)` helper to `src/lib/copy.ts` — the single source of truth for the personalized share-message template that Plans 14-03 (email), 14-04 (pages), and the rest of the phase consume. This is the contract Wave-2 plans implement against.

Purpose: SHARE-04 requires the shared message to name the user's team via the same `references/teams.json` label data used elsewhere. D-08 / D-11 lock the wording and the single-template rule. Defining this helper first lets Wave-2 plans import it without circular discovery.

Output: updated `src/lib/copy.ts` exporting `shareText`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-share-experience/14-CONTEXT.md

<interfaces>
<!-- Current src/lib/copy.ts (4 lines, three string constants). The new helper joins this file. -->

From src/lib/copy.ts (current):
- `export const NO_ACCOUNT_TITLE: string` (`:1`)
- `export const NO_ACCOUNT_BODY: string` (`:2-3`)
- `export const REENTRY_CTA: string` (`:4`)

From src/lib/teams.ts (consumed by callers of shareText, not by shareText itself):
- `export function teamLabel(slug: string): string` (`:16-18`) — callers pass the result as the first arg to `shareText`. shareText receives a pre-resolved label, not a slug.

Locked wording (D-08, CONTEXT.md `<specifics>`):
```
I'm following ${teamLabel} — get your team's World Cup kickoff alerts.
${url}
```
(Newline between message and URL so SMS / WhatsApp / tweet composers line-break gracefully.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shareText helper to src/lib/copy.ts</name>
  <files>src/lib/copy.ts</files>
  <behavior>
    - `shareText('USA', 'https://oddlympics.app/?ref=k7m2qx9a')` returns the literal string `"I'm following USA — get your team's World Cup kickoff alerts.\nhttps://oddlympics.app/?ref=k7m2qx9a"`.
    - The dash between the team label and the rest of the sentence is the em-dash character `—` (U+2014), not a hyphen `-` and not an en-dash `–` — matches D-08 verbatim and the editorial-minimalist house style used in `email.ts:51`.
    - The newline between the message and the URL is a single `\n` (LF), not CRLF, not a space.
    - The team label is interpolated literally (no escaping, no HTML — callers handle email/text-context escaping themselves; per D-09 the label comes from the allow-listed `references/teams.json` so it carries no user input).
    - The URL is appended unchanged (no trailing characters, no encoding) — callers build the full `${PUBLIC_SITE_URL}/?ref=${code}` per D-07.
    - The existing three constants (`NO_ACCOUNT_TITLE`, `NO_ACCOUNT_BODY`, `REENTRY_CTA`) remain exported unchanged.
  </behavior>
  <read_first>
    - src/lib/copy.ts (the full current file — 4 lines, 3 string constants)
    - .planning/phases/14-share-experience/14-CONTEXT.md § "Share copy" (D-08, D-09, D-10, D-11) and § "Specific Ideas" (locked wording)
    - .planning/codebase/CONVENTIONS.md § "TypeScript" (`type` over `interface`, return-type annotation on exported functions, no `any`)
  </read_first>
  <action>
    In `src/lib/copy.ts`, add a new exported function `shareText` immediately after the existing `REENTRY_CTA` constant. Signature: `export function shareText(teamLabel: string, url: string): string` — explicit return-type annotation per CONVENTIONS.md. Body: return a single template-literal string that interpolates `teamLabel` and `url` exactly per the D-08 wording in the `<interfaces>` block above (em-dash U+2014 between the label and the rest of the sentence; `\n` newline between the sentence and the URL; no leading/trailing whitespace). Do NOT add a JSDoc block (project does not use JSDoc per CONVENTIONS.md). Do NOT alter the three existing constants. No new imports needed — the helper takes both arguments as plain strings.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | grep -E "copy\.ts" || echo "no type errors in copy.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function shareText" src/lib/copy.ts` returns `1`
    - `grep -c "I'm following" src/lib/copy.ts` returns `1`
    - `grep -c "get your team's World Cup kickoff alerts" src/lib/copy.ts` returns `1`
    - The em-dash character is present: `grep -c "—" src/lib/copy.ts` returns `≥ 1`
    - The three existing exports remain: `grep -E "^export const (NO_ACCOUNT_TITLE|NO_ACCOUNT_BODY|REENTRY_CTA)" src/lib/copy.ts | wc -l` returns `3`
    - `npx astro check` reports no type errors for `copy.ts`
    - Behavior check via one-liner: `node -e "import('./src/lib/copy.ts').then(m => { const s = m.shareText('USA', 'https://oddlympics.app/?ref=k7m2qx9a'); console.log(s); process.exit(s.includes(\"I'm following USA\") && s.includes('https://oddlympics.app/?ref=k7m2qx9a') && s.includes('\\n') ? 0 : 1); })"` exits 0 (run via tsx or after building if astro check is the gate — the grep checks above are the primary gate)
  </acceptance_criteria>
  <done>`src/lib/copy.ts` exports a new `shareText(teamLabel, url): string` function returning the D-08 template verbatim. Three existing exports unchanged. `npx astro check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → shareText | `teamLabel` arrives from `teamLabel(slug)` (allow-listed `references/teams.json` data, static, no user input). `url` is built from `${PUBLIC_SITE_URL}/?ref=${code}` where `code` is the DB-issued 8-char `[a-z0-9]` referral_code. Neither argument is attacker-controlled at the shareText call site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | template-literal interpolation of teamLabel | accept | `teamLabel` is sourced from `references/teams.json` via `teamLabel(slug)` (`teams.ts:16-18`); the JSON is checked into the repo and code-controlled. No user input reaches this argument. Output context (HTML email body, attribute on `<input value=>`, clipboard text) is the caller's responsibility — Plans 14-03 / 14-04 each handle escaping per their own context (D-12 for email plaintext, D-06 for clipboard, D-03 for DOM `.value=`). |
| T-14-02 | Tampering | template-literal interpolation of url | accept | `url` is built from `PUBLIC_SITE_URL` (env-controlled, not user-controlled — `email.ts:9`) plus a DB-issued 8-char `[a-z0-9]` referral_code (validated by the UNIQUE-indexed column, regex `[a-z0-9]{8}` enforced by `generateReferralCode` in `referral.ts`). No injection vector. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this plan — zero install surface. |
</threat_model>

<verification>
- `npx astro check` passes with no new errors.
- Grep checks (see `<acceptance_criteria>`) confirm the exported symbol and the locked wording.
- Wave-2 plans (14-03 email, 14-04 pages) consume this helper; their own smoke / response-body greps (Plan 14-05) provide end-to-end behavior coverage.
</verification>

<success_criteria>
- SHARE-04 (contract layer): the team-naming template exists at a single import site for all four downstream surfaces.
- D-08 wording locked in code with no per-surface variants (D-11).
- D-09 / D-10 honored: the helper takes a pre-resolved `teamLabel` (single source of truth via `references/teams.json`), no flag emoji.
- The 4-line `copy.ts` file gains exactly one exported function; no other reshape.
</success_criteria>

<output>
Create `.planning/phases/14-share-experience/14-01-SUMMARY.md` when done.
</output>
