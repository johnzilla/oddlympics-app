---
phase: 14-share-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/api/signup.ts
  - src/pages/api/confirm.ts
autonomous: true
requirements: [SHARE-01]
must_haves:
  truths:
    - "A successful POST /api/signup 303-redirects to /pending with both ?email= AND &rc=<8-char [a-z0-9]> AND &team=<slug> appended (D-01, D-15 team-aware variant)"
    - "GET /api/confirm with a valid token 303-redirects to /confirmed?status=ok&rc=<8-char [a-z0-9]> (D-02)"
    - "GET /api/confirm for a re-clicked link 303-redirects to /confirmed?status=already&rc=<8-char [a-z0-9]> (D-02 — re-clicker still deserves their share link)"
    - "GET /api/confirm with a bad/missing/unknown token continues to redirect to /confirmed?status=bad-token or status=unknown WITHOUT &rc= (D-02 — nothing meaningful to share)"
    - "The honeypot short-circuit 303 to /pending (no email, no rc) is unchanged"
    - "The bad-team / bad-email / rate-limited / bad-origin error 303s to /?error=... are unchanged (no &rc= leakage)"
  artifacts:
    - path: "src/pages/api/signup.ts"
      provides: "303 redirect Location now includes &rc=${row.referral_code}&team=${rawTeam}"
      contains: "rc=${"
    - path: "src/pages/api/confirm.ts"
      provides: "303 redirects on status=ok and status=already include &rc=${row.referral_code}; bad-token / unknown remain unchanged"
      contains: "rc=${"
  key_links:
    - from: "src/pages/api/signup.ts upsert call"
      to: "src/pages/api/signup.ts success 303 Location"
      via: "the row returned by upsertVipSignup.get() carries referral_code (RETURNING * in db.ts:140-151); the handler uses it directly when building the Location header"
      pattern: "rc=\\$\\{.*referral_code\\}"
    - from: "src/pages/api/confirm.ts markConfirmed/getByEmail result"
      to: "src/pages/api/confirm.ts success 303 Location"
      via: "the row's referral_code field appended to the Location query string"
      pattern: "rc=\\$\\{.*referral_code\\}"
---

<objective>
Append `&rc=<referral_code>` to the success 303 redirects emitted by `/api/signup` (also appending `&team=<slug>` per D-15) and `/api/confirm` (status=ok and status=already only). This transports the user's referral code from the server to the two prerendered pages that Plan 14-04 wires share UI onto. No new auth surface — `rc` is a public short code (PROJECT.md Key Decision).

Purpose: SHARE-01 (the share prompt must appear on `/pending`, `/confirmed`, `/manage`) cannot be satisfied on the two prerendered pages without first getting the user's `referral_code` to them. The prerendered pages cannot read cookies or DB at request time — they only get URL params. D-01 / D-02 / D-15 lock the redirect-and-read pattern (the same idiom `?error=` / `?email=` / `?status=` already use).

Output: updated `src/pages/api/signup.ts` and `src/pages/api/confirm.ts` with extended Location headers on the success paths only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-share-experience/14-CONTEXT.md
@.planning/phases/13-referral-code-attribution/13-CONTEXT.md

<interfaces>
<!-- Contracts available to this plan from prior phases (Phase 13). Use directly. -->

From src/lib/db.ts (Phase 13, already in `main`):
- `upsertVipSignup.get(...)` returns a full `VipSignup` row via `RETURNING *` (`:140-151`). The returned row's `referral_code` field is a non-null `string` post-Phase-13 (the upsert always inserts or COALESCE-preserves a code). Type is declared `string | null` on `VipSignup` (`db.ts:125`) but the runtime guarantee is non-null on the row returned from a successful upsert.
- `getByEmail.get(email)` (`db.ts:168-170`) returns `VipSignup | undefined` — used by `/api/confirm` to resolve the verified email → referral_code (the verified token only carries `email`, not the code).

From src/pages/api/signup.ts (current state — to be modified):
- The upsert result is currently NOT captured into a named variable — line 122-131 calls `upsertVipSignup.get(...)` inside a try/catch with no assignment. The code path needs the returned row to read `.referral_code` for the new Location header.
- The success 303 is at `:163-166`: `return new Response(null, { status: 303, headers: { Location: \`/pending?email=${encodeURIComponent(rawEmail)}\` } });`.
- The honeypot short-circuit at `:59-62` 303s to `/pending` (no params) — MUST stay unchanged (honeypot should reveal no signup signal).
- The `back(message)` helper at `:39-44` 303s to `/?error=...` — MUST stay unchanged (errors carry no referral code).
- `rawTeam` is the validated lowercase slug at `:77-81` (a value from `VALID_TEAMS`).

From src/pages/api/confirm.ts (current state — to be modified):
- The handler is 25 lines (read in full before editing). The `redirect(to)` helper at `:23-25` is the single Response constructor used by all paths.
- `result.email` is the verified email from `verifyToken(token)` at `:11-12`.
- `markConfirmed.get(result.email)` at `:14` returns the updated `VipSignup` row (RETURNING *) when a confirmation flip happens — its `referral_code` is the value to append on `status=ok`.
- `getByEmail.get(result.email)` at `:18` is called only on the `status=already` branch (already-confirmed user) — its `referral_code` is the value to append on `status=already`.
- The `bad-token` (`:9, :12`) and `unknown` (`:20`) paths get no row in scope — these MUST remain `&rc=`-free (D-02: nothing meaningful to share).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append &rc= + &team= to /api/signup success 303</name>
  <files>src/pages/api/signup.ts</files>
  <behavior>
    - A successful signup (`POST /api/signup` with valid email/team/timezone, no honeypot, not rate-limited) 303s to `/pending?email=<urlencoded>&rc=<8-char [a-z0-9]>&team=<slug>`.
    - The `rc` value equals the `referral_code` written to the new `vip_signups` row in the same request (i.e. the value present on the returned row, NOT a freshly regenerated code).
    - The `team` value equals the validated `rawTeam` slug from the form (per D-15: lets `/pending`'s inline script personalize the share-card heading with the team name).
    - The honeypot short-circuit at `:59-62` still 303s to `/pending` (no email, no rc, no team) — unchanged.
    - All `back('...')` error 303s (`/?error=bad-origin`, `/?error=bad-form`, `/?error=bad-email`, `/?error=rate-limited`, `/?error=server`, `/?error=email`) are unchanged (no `&rc=` leakage on error paths).
    - The order of pre-flight checks, the ref-resolution block, the upsert retry loop, and the email send are unchanged.
  </behavior>
  <read_first>
    - src/pages/api/signup.ts (FULL file — pre-flight chain, the `back()` helper at `:39-44`, the honeypot short-circuit at `:59-62`, the upsert retry loop at `:118-152` which currently discards the row, the `mintToken` / `sendMagicLink` call at `:154-161`, the success 303 at `:163-166`)
    - src/lib/db.ts § upsertVipSignup `:137-151` (RETURNING * — the row is available to capture into a variable)
    - .planning/phases/14-share-experience/14-CONTEXT.md § "Referral-code transport" D-01, D-15; § "Specific Ideas" (URL canonical form)
    - .planning/phases/13-referral-code-attribution/13-02-SUMMARY.md (the retry-loop pattern this plan extends — capture row without breaking retry semantics)
  </read_first>
  <action>
    In `src/pages/api/signup.ts`:
    (a) Inside the retry loop at `:120-148`, capture the row returned by `upsertVipSignup.get(...)` into a `let row: VipSignup | undefined;` declared OUTSIDE the loop (alongside `let upserted = false;` at `:119`). Inside the try block on the successful call, assign `row = upsertVipSignup.get(...) as VipSignup | undefined;` (the existing call already invokes `.get(...)` and discards the result — change `upsertVipSignup.get(...)` to `row = upsertVipSignup.get(...) as VipSignup | undefined;`). The cast is required because `db.prepare<...>` does not infer the return type. Add `import type { VipSignup } from '../../lib/db';` to the existing db import line so it reads `import { upsertVipSignup, lookupByReferralCode, type VipSignup } from '../../lib/db';`.
    (b) After the loop, where the current code checks `if (!upserted)` at `:149-152`, also defensively bail if `!row || !row.referral_code` with `console.error('[signup] db error: upsert returned no row')` and `return back('server')` — this should never happen given RETURNING *, but the type is `string | null` (db.ts:125) so we narrow defensively.
    (c) Change the success 303 Location at `:165` from ``\`/pending?email=${encodeURIComponent(rawEmail)}\``` to ``\`/pending?email=${encodeURIComponent(rawEmail)}&rc=${encodeURIComponent(row.referral_code)}&team=${encodeURIComponent(rawTeam)}\```. Use `encodeURIComponent` on `row.referral_code` (defensive — the value is `[a-z0-9]{8}` and URL-safe, but encoding is the project convention for query params per `:165`'s existing `encodeURIComponent(rawEmail)`) and on `rawTeam` (also `[a-z0-9_]` snake_case, but same defensive-encode convention).
    (d) Do NOT touch the honeypot 303 at `:59-62`, the `back()` helper at `:39-44`, the ref-resolution block at `:97-111`, or the retry loop's catch branch. Do NOT change the param ORDER (`email` first, then `rc`, then `team`) — Plan 14-04's inline-script reader parses by name not position, but stable ordering simplifies the smoke regex in Plan 14-05.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | grep -E "signup\.ts" || echo "no type errors in signup.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rc=\${encodeURIComponent(row.referral_code)}" src/pages/api/signup.ts` returns `1`
    - `grep -c "team=\${encodeURIComponent(rawTeam)}" src/pages/api/signup.ts` returns `1`
    - `grep -c "row = upsertVipSignup.get" src/pages/api/signup.ts` returns `1`
    - `grep -c "type VipSignup" src/pages/api/signup.ts` returns `1`
    - The honeypot 303 is unchanged: `grep -c "Location: '/pending' }" src/pages/api/signup.ts` returns `1`
    - The `back()` helper has not been touched: `grep -c "Location: \`/?error=" src/pages/api/signup.ts` returns `1` (single occurrence inside `back()`)
    - `npx astro check` reports no type errors for `signup.ts`
    - Manual curl (operator, while `npm run dev` runs): `curl -s -X POST -H "Origin: http://localhost:4321" -H "X-Forwarded-For: 192.0.2.99" --data "email=share-check@example.com&team=brazil&timezone=America/Sao_Paulo" -i http://localhost:4321/api/signup | grep -i location` returns `Location: /pending?email=share-check%40example.com&rc=<8-char>&team=brazil`
  </acceptance_criteria>
  <done>The signup success 303 carries `&rc=<code>&team=<slug>` in addition to `&email=<urlencoded>`. Honeypot and `back()` paths unchanged. `npx astro check` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append &rc= to /api/confirm success 303s (status=ok and status=already only)</name>
  <files>src/pages/api/confirm.ts</files>
  <behavior>
    - GET `/api/confirm?token=<valid first-click>` 303s to `/confirmed?status=ok&rc=<8-char [a-z0-9]>` where `rc` equals the `referral_code` on the row updated by `markConfirmed`.
    - GET `/api/confirm?token=<valid re-click>` (token valid, row already confirmed) 303s to `/confirmed?status=already&rc=<8-char [a-z0-9]>` where `rc` equals the `referral_code` on the row returned by `getByEmail`.
    - GET `/api/confirm` with a missing or invalid token 303s to `/confirmed?status=bad-token` — NO `&rc=` appended (D-02: nothing meaningful to share for a stranger).
    - GET `/api/confirm` with a valid token but no DB row found (the rare `getByEmail` returns undefined branch) 303s to `/confirmed?status=unknown` — NO `&rc=` appended.
  </behavior>
  <read_first>
    - src/pages/api/confirm.ts (FULL 25-line file — every code path)
    - src/lib/db.ts § markConfirmed `:160-166` (RETURNING * after the UPDATE; row is in scope on the happy path); § getByEmail `:168-170`
    - .planning/phases/14-share-experience/14-CONTEXT.md § "Referral-code transport" D-02 (re-clicker deserves share link; bad-token / unknown skip rc)
  </read_first>
  <action>
    In `src/pages/api/confirm.ts`:
    (a) The current `markConfirmed.get(result.email)` at `:14` discards the row's body — change to `const updated = markConfirmed.get(result.email) as VipSignup | undefined;`. Already declared `const updated = ...`, so just narrow with an explicit cast. Add `import type { VipSignup } from '../../lib/db';` (extend the existing `import { markConfirmed, getByEmail } from '../../lib/db';` line to `import { markConfirmed, getByEmail, type VipSignup } from '../../lib/db';`).
    (b) On the `markConfirmed` success branch at `:15`, change `redirect('/confirmed?status=ok')` to `redirect(\`/confirmed?status=ok&rc=${encodeURIComponent(updated.referral_code ?? '')}\`)` IF and ONLY IF `updated && updated.referral_code`; if `updated.referral_code` is null/missing (should not happen post-Phase-13 backfill but the type allows it), fall through to `/confirmed?status=ok` with no `rc` rather than crashing.
    (c) On the `status=already` branch at `:18-19`, the existing code already calls `getByEmail.get(result.email)` and captures it as `const existing = ...`. Narrow that cast: `const existing = getByEmail.get(result.email) as VipSignup | undefined;`. Then change `redirect('/confirmed?status=already')` to a guarded variant that appends `&rc=${encodeURIComponent(existing.referral_code)}` only when `existing.referral_code` is truthy (same null-guard pattern as the `status=ok` branch).
    (d) Do NOT touch `/confirmed?status=bad-token` (`:9`, `:12`) or `/confirmed?status=unknown` (`:20`). These paths intentionally carry no `rc` (D-02).
    (e) The `redirect(to)` helper at `:23-25` stays exactly as-is — only callers' arguments change.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | grep -E "confirm\.ts" || echo "no type errors in confirm.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status=ok&rc=\${encodeURIComponent" src/pages/api/confirm.ts` returns `1`
    - `grep -c "status=already&rc=\${encodeURIComponent" src/pages/api/confirm.ts` returns `1`
    - `grep -c "type VipSignup" src/pages/api/confirm.ts` returns `1`
    - The `bad-token` and `unknown` redirects are still rc-less: `grep -E "status=(bad-token|unknown)" src/pages/api/confirm.ts | grep -c "rc="` returns `0`
    - `npx astro check` reports no type errors for `confirm.ts`
    - Behavior (verified end-to-end by Plan 14-05's smoke): valid first-click → `status=ok&rc=…`; re-click → `status=already&rc=…`; bad token → `status=bad-token` (no rc).
  </acceptance_criteria>
  <done>`/api/confirm` success branches (`status=ok` and `status=already`) include `&rc=<referral_code>` in their Location headers; error branches (`status=bad-token`, `status=unknown`) do not. `npx astro check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/signup → /pending redirect | The Location header value composed by the server is a public 303 visible in the browser address bar; `rc` carries a non-secret public referral code |
| /api/confirm → /confirmed redirect | Same shape — Location header carries `rc` only on success branches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | rc in URL bar / browser history / referrer header | accept | The `referral_code` is a public short identifier per PROJECT.md Key Decision ("a referral code is a public short identifier, NOT a signed HMAC token"). It carries no auth state, no PII, and grants no resource access. Exposure in browser history / referrer is the intended share mechanism. |
| T-14-04 | Information Disclosure | rc leaked on error 303s (bad-token / unknown / bad-form) | mitigate | Error 303s are gated to skip `&rc=` (D-02: nothing meaningful to share on a stranger lookup). The `back()` helper in signup.ts and the `bad-token` / `unknown` paths in confirm.ts remain unchanged — verified by the `<acceptance_criteria>` grep that `rc=` does not appear on `status=bad-token` or `status=unknown` lines. |
| T-14-05 | Tampering | open redirect via rc / team query value | accept | The Location header is server-composed from a string literal `/pending?...` or `/confirmed?...` — `rc` and `team` are appended as URL-encoded query params on a same-origin path, not as a redirect target. There is no attacker-controlled redirect destination. `encodeURIComponent` on both values prevents stray `&` / `?` / `#` from reshaping the URL. |
| T-14-06 | Information Disclosure | honeypot 303 leaking signup signal via rc presence | mitigate | The honeypot short-circuit at signup.ts:59-62 stays unchanged (303 to `/pending` with NO params). Bots receive the same response shape they did before this plan — no `?email=` and no `&rc=`, so the bot cannot tell whether their fake signup was distinguished. Verified by the `<acceptance_criteria>` grep that the honeypot Location is unchanged. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this plan — zero install surface. |
</threat_model>

<verification>
- `npx astro check` clean for both files.
- Manual curl smoke (operator, while `npm run dev` runs):
  1. Valid signup → `Location: /pending?email=…&rc=<8-char>&team=<slug>`
  2. Honeypot signup (`website=evil-bot`) → `Location: /pending` (no params, unchanged)
  3. Bad team signup → `Location: /?error=bad-form` (no `rc=`, unchanged)
- Full behavioral coverage is the Plan 14-05 smoke extension, which asserts the redirect Location format for both signup and confirm flows.
</verification>

<success_criteria>
- SHARE-01 (transport layer): the two prerendered pages now receive the user's `referral_code` via URL param, satisfying the precondition for Plan 14-04's share UI.
- D-01 honored: signup 303 carries `&rc=` (and `&team=` per D-15) on success only.
- D-02 honored: confirm 303 carries `&rc=` on `status=ok` and `status=already`; `bad-token` and `unknown` skip it.
- Honeypot and error redirects are unchanged — no new failure modes, no new signal for bots.
- The pre-flight chain order, ref-resolution, retry-loop, and email-send paths in `signup.ts` are unchanged.
</success_criteria>

<output>
Create `.planning/phases/14-share-experience/14-02-SUMMARY.md` when done.
</output>
