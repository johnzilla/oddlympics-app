---
phase: 14-share-experience
plan: 05
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - scripts/smoke-signup.mjs
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-04]
must_haves:
  truths:
    - "Automated smoke proves /pending response body contains the share URL and the user's team label after a successful signup (D-18, SC1, SC4)"
    - "Automated smoke proves /confirmed response body contains the share URL after a successful confirm (D-19 page-render side, SC1)"
    - "Automated smoke proves /api/confirm's 303 Location header carries &rc=<row.referral_code> (D-02 wiring, D-19 redirect-side — closes the gap a synthetic-rc page-render-only check leaves open)"
    - "The smoke continues to pass all pre-existing cases (8 original + 6 Phase-13 referral cases — no regression)"
    - "The script exits 0 only when every case passes"
  artifacts:
    - path: "scripts/smoke-signup.mjs"
      provides: "Three new test cases (SHARE-pending-card, SHARE-confirmed-card, SHARE-confirm-redirect-location) appended after the existing Phase-13 REF-* cases and before case-7 rate-limit"
      contains: "SHARE-pending-card"
  key_links:
    - from: "scripts/smoke-signup.mjs SHARE-pending-card"
      to: "src/pages/pending.astro share card markup (Plan 14-04 Task 1)"
      via: "fetch(`${BASE}/pending?email=...&rc=...&team=...`) then text() grep for the shareUrl and team label"
      pattern: "/pending\\?email="
    - from: "scripts/smoke-signup.mjs SHARE-confirmed-card"
      to: "src/pages/confirmed.astro share card markup (Plan 14-04 Task 2)"
      via: "synthetic GET /confirmed?status=ok&rc=<synthetic> grep for share-card markup"
      pattern: "/confirmed\\?status="
    - from: "scripts/smoke-signup.mjs SHARE-confirm-redirect-location"
      to: "src/pages/api/confirm.ts success 303 Location (Plan 14-02 Task 2)"
      via: "POST /api/signup → read row from DB via dbRowFor → mint a confirm token via src/lib/token.ts mintToken → GET /api/confirm?token=... with redirect: 'manual' → assert 303 Location matches /confirmed?status=ok&rc=<row.referral_code>"
      pattern: "/api/confirm\\?token="
---

<objective>
Extend `scripts/smoke-signup.mjs` with three new automated cases that prove the SHARE-01 / SHARE-02 / SHARE-04 wiring end-to-end without a headless browser:

1. **SHARE-pending-card**: GET `/pending?email=...&rc=...&team=...` and grep the response body for the share URL components and the team label.
2. **SHARE-confirmed-card**: GET `/confirmed?status=ok&rc=...` directly with a synthetic rc and grep for the share-card markup (page-render side of D-19).
3. **SHARE-confirm-redirect-location**: end-to-end signup → mint confirm token → GET `/api/confirm?token=...` with `redirect: 'manual'` → assert the 303 Location header matches `/confirmed?status=ok&rc=<real referral_code>`. This case closes the gap that case 2's synthetic-rc check leaves open (a regression that removed `&rc=` from `/api/confirm`'s 303 would not trip case 2 but WILL trip this one).

Purpose: D-18 / D-19 lock the smoke contract. The third case directly implements the redirect-side of D-19 ("mint a confirm token, GET /api/confirm?token=..., follow the 303, grep /confirmed's body for the referral URL") — without it, the only thing protecting `/api/confirm`'s 303 wiring is operator UAT, and a silent regression could ship. Manual `navigator.share` / `navigator.clipboard` browser-API verification remains deferred to operator walk-through per D-20.

Output: updated `scripts/smoke-signup.mjs` with three new cases inserted in the existing structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-share-experience/14-CONTEXT.md
@.planning/phases/14-share-experience/14-01-SUMMARY.md
@.planning/phases/14-share-experience/14-02-SUMMARY.md
@.planning/phases/14-share-experience/14-03-SUMMARY.md
@.planning/phases/14-share-experience/14-04-SUMMARY.md

<interfaces>
<!-- Contracts for the smoke script: existing helpers and patterns to reuse, plus the new artifacts to assert against. -->

From scripts/smoke-signup.mjs (current state):
- BASE = `process.env.SMOKE_BASE_URL ?? 'http://localhost:4321'` (`:37`)
- SMOKE_IP = `'192.0.2.42'` (`:39`), REF_IP = `'192.0.2.43'` (`:40`), SELF_REF_IP = `'192.0.2.44'` (`:44`). For the three new cases use a NEW IP (e.g. `SHARE_IP = '192.0.2.45'`) to avoid consuming SMOKE_IP rate-limit slots that case-7 depends on. RFC 5737 TEST-NET-1 reserves `192.0.2.0/24` so any address in that block is safe.
- `post(form, extraHeaders)` helper at `:76-90` issues `POST /api/signup` with `redirect: 'manual'`.
- `dbRowFor(email)` at `:98-104` SELECTs `email, team, timezone, requested_sport, referral_code, referred_by`. The SHARE-confirm-redirect-location case uses this helper to read the just-signed-up row's `referral_code` before asserting the confirm 303 Location.
- `runCase(name, fn)` at `:60-74` is the test-runner shell. Cases run in declaration order — case ordering matters (case-7 rate-limit pre-condition is "~3 of 5 SMOKE_IP slots used"; the three new cases use a separate IP and must not increment the SMOKE_IP counter).
- Test result accumulation: `pass++` / `fail++` (`:57-58`); final `process.exit(fail === 0 ? 0 : 1);` at `:482`.

From src/lib/token.ts (Node 22, native TS via `--experimental-strip-types`):
- `export function mintToken(claim: { email: string; purpose: 'confirm' | 'manage' | 'unsubscribe' | 'session'; exp?: number }): string` — HMAC-SHA256 signed token using `MAGIC_LINK_SECRET` from env. For the SHARE-confirm-redirect-location case, mint with `purpose: 'confirm'` and the signed-up email; `exp` defaults to 24h which is plenty for a smoke run.
- The token helper uses only `node:crypto` — no Astro / SQLite / network surface. Safe to import standalone into a Node script.

Token-minting approach (CHOSEN: in-script TS import via `--experimental-strip-types`):
- Node 22 ships with `--experimental-strip-types` which lets a `.mjs` script `import` a `.ts` module by name. The flag is on by default in Node 22.6+; for older 22.x patches it requires the explicit flag.
- The smoke script today runs as `node scripts/smoke-signup.mjs` (no flags). To support the TS import, EITHER (a) add the flag inline: `node --experimental-strip-types scripts/smoke-signup.mjs`, OR (b) detect the Node version inside the script and conditionally invoke the new case (skip with a warning on older Nodes).
- RECOMMENDED: option (a) — update the smoke invocation in `package.json` scripts if one exists, OR add a comment at the top of the smoke script noting the required flag. The PROJECT.md "Common commands" section documents `node scripts/smoke-signup.mjs`; update it in this same plan if the flag is required.
- VERIFICATION: `node --version` on the project's environment (CI + local) returns v22.x. The `.github/workflows/deploy.yml` pins Node 22.
- IF the TS import path proves brittle in the executor's hands (e.g. the version of Node 22 on the operator's box does not support `--experimental-strip-types`), the fallback is to add a documented `curl` recipe to `DEPLOY.md`'s Day-2 ops table and downgrade SHARE-confirm-redirect-location to a manual operator gate. The fallback is inferior (a regression could still ship between operator runs) but is acceptable.

Synthetic rc behavior on /confirmed (used by case 2):
- The `?status=` gate in `/confirmed.astro`'s inline script (Plan 14-04 Task 2) only renders the share card on `status === 'ok' || status === 'already'` — it does NOT validate the rc against the DB. A synthetic 8-char [a-z0-9] rc passes the regex gate and renders the card. This proves the page markup but does NOT prove the /api/confirm redirect populates rc correctly — that is precisely the gap case 3 closes.

From src/pages/pending.astro (after Plan 14-04 Task 1):
- The share card markup contains `id="share-card"`, `id="share-url"`, `id="share-btn"`. The `value=""` on the input is empty at HTML-render time (prerendered + populated by inline script) — the smoke CANNOT grep for `value="http://..."` directly because that value is set client-side. Instead, the smoke greps for: (i) the literal class/id markers `share-card` and `share-url` (proves the share card SHIPPED in the prerendered HTML); (ii) the literal team label inside the embedded `TEAM_LABEL_JSON` (proves the team-aware path is wired); (iii) the `define:vars` injection of `TEAM_LABEL_JSON` (proves the slug-label map is embedded for the inline script to read).
- Note: the rc value never appears in the prerendered HTML (it's read from `location.href` at runtime). The smoke proves the card is present and the labels are embedded — not that the runtime URL substitution happened, which is browser-only and D-20 operator-verified.

From src/pages/confirmed.astro (after Plan 14-04 Task 2):
- Same pattern. The share card markup ships in the prerendered HTML; the inline script populates the URL on the client. The smoke greps for `share-card` and `share-url` to prove the markup shipped. NOTE: /confirmed does NOT embed TEAM_LABEL_JSON (D-16 / per Plan 14-04 Task 2 step (a)); the smoke does NOT grep for team labels on /confirmed.

From src/pages/api/confirm.ts (after Plan 14-02 Task 2):
- Success 303 on `status=ok`: `Location: /confirmed?status=ok&rc=<encodeURIComponent(row.referral_code)>`. The smoke case 3 asserts the Location header matches this format with the REAL referral_code from the just-signed-up row (not a synthetic value).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SHARE-pending-card, SHARE-confirmed-card, and SHARE-confirm-redirect-location smoke cases</name>
  <files>scripts/smoke-signup.mjs</files>
  <behavior>
    - **SHARE-pending-card**: GET `/pending?email=x@y.z&rc=abc12345&team=brazil` returns HTTP 200 and the response body contains the literal substrings `share-card`, `share-url`, and the JSON-embedded team label "Brazil" (proves Plan 14-04 Task 1 markup and TEAM_LABEL_JSON injection both shipped).
    - **SHARE-confirmed-card**: GET `/confirmed?status=ok&rc=abc12345` returns HTTP 200 and the response body contains the literal substrings `share-card` and `share-url` (proves Plan 14-04 Task 2 markup shipped).
    - **SHARE-confirm-redirect-location**: POST a fresh signup (using SHARE_IP, a unique email like `share-confirm-redirect@example.com`), read the row's `referral_code` from the DB via `dbRowFor`, mint a confirm-purpose token via `mintToken` (imported from `../src/lib/token.ts`), GET `/api/confirm?token=<token>` with `redirect: 'manual'`, assert HTTP 303 AND assert `res.headers.get('location') === '/confirmed?status=ok&rc=' + encodeURIComponent(row.referral_code)`. A second GET with the same token asserts a `status=already&rc=` Location (re-click path — D-02 / D-19).
    - All three cases are INSERTED before the existing case-7 rate-limit (so case-7's SMOKE_IP slot-count pre-condition is unchanged).
    - All three cases use a separate `SHARE_IP = '192.0.2.45'` constant for any POSTs. Cases 1 and 2 do not POST (they only GET prerendered pages); case 3 POSTs once to /api/signup with SHARE_IP.
    - The final smoke output prints `pass=N fail=0` where N = old_total + 3 when all cases pass.
  </behavior>
  <read_first>
    - scripts/smoke-signup.mjs FULL file (483 lines) — especially `dbRowFor` at `:98-104`, `post` at `:76-90`, the IP constants at `:39-44`, the `runCase` runner at `:60-74`, and the final case-7 block at `:450-471` (the new cases insert before this).
    - src/lib/token.ts FULL file — confirms the `mintToken` signature, the env requirement (`MAGIC_LINK_SECRET`), the token shape (claim with `email` + `purpose`), and that the helper has no side effects beyond `node:crypto` usage. Confirm the function is a named export.
    - src/pages/api/confirm.ts after Plan 14-02 Task 2 (the Location format to assert against)
    - src/pages/pending.astro after Plan 14-04 Task 1 (the share-card markup to grep for; note the inline-script-populated value)
    - src/pages/confirmed.astro after Plan 14-04 Task 2 (the share-card markup to grep for; note: NO TEAM_LABEL_JSON — do not grep for team labels here)
    - references/teams.json (the literal "Brazil" string for the brazil slug — verify it is exactly capitalized "Brazil")
    - package.json scripts section (to see if a smoke npm script exists that needs the `--experimental-strip-types` flag added)
    - .planning/phases/14-share-experience/14-CONTEXT.md § "Verification" D-18, D-19, D-20
  </read_first>
  <action>
    In `scripts/smoke-signup.mjs`:
    
    (a) **IP constant.** Add a new IP constant alongside the existing ones at `:39-44`: `const SHARE_IP = '192.0.2.45'; // RFC 5737 TEST-NET-1; reserved for Phase 14 share-card smoke cases`.
    
    (b) **TS-import preamble.** At the top of the file (after existing imports, before BASE), add: `import { mintToken } from '../src/lib/token.ts';`. This requires Node 22's `--experimental-strip-types` (default on 22.6+). Add a comment immediately above the import: `// NB: requires Node 22.6+ (or --experimental-strip-types flag) for the TS import below.`
    
    (c) **package.json update (if applicable).** If `package.json` has a `smoke` or `test:smoke` script that runs `node scripts/smoke-signup.mjs`, update it to `node --experimental-strip-types scripts/smoke-signup.mjs` for older 22.x compatibility. If no script exists, add a brief note to PROJECT.md's "Common commands" block (or to the smoke script's leading comment) documenting the flag.
    
    (d) **Insert three new `runCase` blocks** BEFORE the existing case-7 at `:450-471` and AFTER the existing `REF-code-uniqueness` case at `:424-438`. Insert in order: SHARE-pending-card, SHARE-confirmed-card, SHARE-confirm-redirect-location. Add a header comment block above the three new cases (4-6 lines, same density as existing per-case comments) explaining: "Phase 14 SHARE-* cases. Cases 1-2 prove prerendered-page MARKUP shipped (D-18/D-19 page-render side). Case 3 proves /api/confirm's 303 Location carries &rc=<real-code> (D-19 redirect side — closes the gap a synthetic-rc check leaves open). Browser-API behavior (navigator.share / navigator.clipboard) is operator-UAT'd per D-20."
    
    **Case SHARE-pending-card (D-18):**
    - Body: `const url = BASE + '/pending?email=share-smoke%40example.com&rc=abc12345&team=brazil'; const res = await fetch(url); if (res.status !== 200) { console.error('  expected 200, got ' + res.status); return false; } const body = await res.text(); const needed = ['share-card', 'share-url', 'Brazil']; for (const s of needed) { if (!body.includes(s)) { console.error('  body missing: ' + s); return false; } } return true;`.
    - The `Brazil` substring proves the TEAM_LABEL_JSON map is embedded with the right labels.
    
    **Case SHARE-confirmed-card (D-19, page-render side):**
    - Body: `const url = BASE + '/confirmed?status=ok&rc=abc12345'; const res = await fetch(url); if (res.status !== 200) { console.error('  expected 200, got ' + res.status); return false; } const body = await res.text(); const needed = ['share-card', 'share-url']; for (const s of needed) { if (!body.includes(s)) { console.error('  body missing: ' + s); return false; } } return true;`.
    - Do NOT grep for team labels on /confirmed (per Plan 14-04 Task 2: no TEAM_LABEL_JSON on this page).
    
    **Case SHARE-confirm-redirect-location (D-19, redirect side — NEW):**
    - Body:
      1. Pick a unique signup email: `const email = 'share-confirm-redirect@example.com';`
      2. POST a fresh signup with SHARE_IP: `const form = new URLSearchParams({ email, team: 'usa', timezone: 'America/New_York' }); const signupRes = await post(form, { 'X-Forwarded-For': SHARE_IP }); if (signupRes.status !== 303) { console.error('  signup expected 303, got ' + signupRes.status); return false; }`
      3. Read the row from the DB to obtain the real referral_code: `const row = dbRowFor(email); if (!row || !row.referral_code) { console.error('  no row / no referral_code for ' + email); return false; }`
      4. Mint a confirm-purpose token: `const token = mintToken({ email, purpose: 'confirm' });`
      5. GET /api/confirm with manual-redirect: `const confirmRes = await fetch(BASE + '/api/confirm?token=' + encodeURIComponent(token), { redirect: 'manual' }); if (confirmRes.status !== 303) { console.error('  confirm expected 303, got ' + confirmRes.status); return false; }`
      6. Assert the Location header: `const loc = confirmRes.headers.get('location'); const expected = '/confirmed?status=ok&rc=' + encodeURIComponent(row.referral_code); if (loc !== expected) { console.error('  confirm Location mismatch:\\n    expected: ' + expected + '\\n    actual:   ' + loc); return false; }`
      7. Re-click path (D-02): GET the same token again, assert 303 + Location matches `/confirmed?status=already&rc=<row.referral_code>`. `const confirmRes2 = await fetch(BASE + '/api/confirm?token=' + encodeURIComponent(token), { redirect: 'manual' }); if (confirmRes2.status !== 303) { console.error('  re-click expected 303, got ' + confirmRes2.status); return false; } const loc2 = confirmRes2.headers.get('location'); const expected2 = '/confirmed?status=already&rc=' + encodeURIComponent(row.referral_code); if (loc2 !== expected2) { console.error('  re-click Location mismatch:\\n    expected: ' + expected2 + '\\n    actual:   ' + loc2); return false; }`
      8. `return true;`
    
    **Fallback if --experimental-strip-types is unavailable on the executor's Node.** If the executor finds the TS import fails (e.g. `ERR_UNKNOWN_FILE_EXTENSION` or `ERR_MODULE_NOT_FOUND`), DO NOT silently skip the case. Two options:
    - **Preferred fallback**: inline-reimplement `mintToken` in the smoke script using `node:crypto` HMAC-SHA256 + base64url (~10 LOC, mirrors src/lib/token.ts). The smoke can then claim the token shape is in sync — but add a TODO comment pointing back to src/lib/token.ts so a future change to the token format breaks loud (not quiet).
    - **Acceptable fallback**: comment out the third case body, leave the case stub with a `console.warn('SHARE-confirm-redirect-location SKIPPED: requires Node 22 + --experimental-strip-types; see DEPLOY.md for manual curl recipe');` and `return true;` so the smoke still exits 0. Then add a documented curl recipe to `DEPLOY.md`'s Day-2 ops table: a four-step procedure (sign up, query DB for referral_code, mint a token via a one-off `node -e "..."` snippet, curl `/api/confirm` and assert Location). Include the exact `node -e` snippet in DEPLOY.md so the operator can copy-paste.
    
    (e) Do NOT modify any existing case. Do NOT change the case-7 rate-limit pre-condition comment block at `:441-449` (it remains accurate — cases 1 and 2 do not POST; case 3 POSTs once to SHARE_IP, not SMOKE_IP).
    
    (f) Do NOT touch the existing evidence-tag comment block at `:24-27` — leave the existing tags in place; the three new cases' names document themselves.
    
    (g) **Cleanup.** Case 3 leaves a `share-confirm-redirect@example.com` row in the dev DB (confirmed_at set). Add to the existing cleanup comment at `:476-478` (or wherever the script documents cleanup) a note that this email + the existing test emails are safe to leave or can be cleared via the operator's standard DB-reset procedure. If the smoke already has a teardown step, extend it to DELETE the new email.
  </action>
  <verify>
    <automated>node --check scripts/smoke-signup.mjs 2>&1 && echo "syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SHARE-pending-card" scripts/smoke-signup.mjs` returns at least `1`
    - `grep -c "SHARE-confirmed-card" scripts/smoke-signup.mjs` returns at least `1`
    - `grep -c "SHARE-confirm-redirect-location" scripts/smoke-signup.mjs` returns at least `1`
    - `grep -c "SHARE_IP" scripts/smoke-signup.mjs` returns at least `1`
    - The mintToken import is present (preferred path): `grep -c "mintToken" scripts/smoke-signup.mjs` returns at least `2` (import + at least one call). If the fallback inline-reimplementation path was chosen, this drops to 1 (the inline function definition) and is acceptable iff DEPLOY.md gained the documented curl recipe.
    - The case asserts the 303 Location format: `grep -c "status=ok&rc=" scripts/smoke-signup.mjs` returns at least `1`
    - The re-click path is asserted: `grep -c "status=already&rc=" scripts/smoke-signup.mjs` returns at least `1`
    - All three new cases appear BEFORE case-7: line numbers `grep -n "SHARE-pending-card\|SHARE-confirmed-card\|SHARE-confirm-redirect-location\|case-7-rate-limit" scripts/smoke-signup.mjs` shows the SHARE cases first in order.
    - The script remains syntactically valid: `node --check scripts/smoke-signup.mjs` exits 0.
    - End-to-end (operator runs after Plans 14-01..14-04 ship + `npm run dev`): `node --experimental-strip-types scripts/smoke-signup.mjs` (or with the fallback, plain `node scripts/smoke-signup.mjs`) reports `pass=17 fail=0` (the existing 14 cases + 3 new SHARE-* cases — count adjusts if Phase 13's smoke ships more cases).
  </acceptance_criteria>
  <done>Three new SHARE-* cases inserted before case-7 in the smoke script: page-render assertions for /pending and /confirmed, plus an end-to-end /api/confirm 303 Location assertion that closes the synthetic-rc gap. The mintToken import path is the preferred implementation; the inline-reimplementation OR documented DEPLOY.md curl recipe is the documented fallback. Existing cases unchanged. `node --check` clean. End-to-end pass (operator-run) reports zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke script → local dev server | The smoke script issues HTTP requests to a developer-controlled server on localhost. No external network surface. No production data. |
| smoke script → src/lib/token.ts | The smoke imports `mintToken` directly from source. The minted token is signed with the same `MAGIC_LINK_SECRET` the server validates against. In dev (`MAGIC_LINK_SECRET` unset), token.ts falls back to a dev secret — the smoke + server agree. Not a security boundary in this scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-19 | Information Disclosure | smoke leaving residual rows in dev DB | accept | Cases 1-2 do NOT POST; case 3 POSTs one signup row (share-confirm-redirect@example.com). The existing cleanup comment at scripts/smoke-signup.mjs:476-478 covers the POST-based cases and is updated to mention the new email. Operator can DELETE the row or wipe the dev DB via the standard reset procedure. |
| T-14-20 | Tampering | synthetic rc value in case 2 being interpretable as a real referral attribution | accept | The synthetic `rc=abc12345` is a valid-shape 8-char [a-z0-9] string but is astronomically unlikely to collide with a real DB-issued code (~3.6e-13 per real code). Even on a collision, the smoke's GET requests are read-only against the prerendered pages and never write to `referred_by` — no attribution side effect. |
| T-14-21 | Tampering | smoke minting confirm tokens for arbitrary emails | accept | The smoke runs against a dev DB; the email used is `share-confirm-redirect@example.com` (a smoke-only address). In production the smoke is never run against production data. The `MAGIC_LINK_SECRET` in dev is a fixed dev string per src/lib/token.ts — the smoke and server share the secret, so the token is valid. If a real production secret leaked into the dev env, the smoke could mint a real-user confirm token — but that disclosure scenario is already handled by the secret-management protocol (not a smoke-specific risk). |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this plan — zero install surface. The TS-import path uses Node 22's built-in `--experimental-strip-types`, not a new dev dep like `tsx`. |
</threat_model>

<verification>
- `node --check scripts/smoke-signup.mjs` clean.
- Operator-run end-to-end (after Plans 14-01..14-04 ship): `npm run dev` in one terminal, `node --experimental-strip-types scripts/smoke-signup.mjs` in another → final line `[smoke] result: pass=17 fail=0` (or whatever total reflects the current case count + 3). Exit code 0.
- If any prior phase regresses, the smoke fails with a specific message and exit code 1:
  - share-card markup missing → `body missing: share-card` (case 1 or 2)
  - /api/confirm 303 dropped &rc= → `confirm Location mismatch: expected /confirmed?status=ok&rc=<code>, actual /confirmed?status=ok` (case 3)
  - /api/confirm flipped status order → `re-click Location mismatch:...` (case 3 re-click path)
- A regression to the redirect-side wiring (the exact bug case 3 exists to catch) now fails the gate — D-19 is fully covered, not partially.
</verification>

<success_criteria>
- D-18 satisfied: SHARE-pending-card asserts the /pending response body contains the share-card markup and the team label.
- D-19 satisfied (page-render side): SHARE-confirmed-card asserts the /confirmed response body contains the share-card markup.
- D-19 satisfied (redirect side): SHARE-confirm-redirect-location asserts /api/confirm's 303 Location header carries `&rc=<row.referral_code>` on both first-click (`status=ok`) and re-click (`status=already`) paths.
- D-02 wiring is now under automated regression coverage — a future change that drops `&rc=` from `/api/confirm`'s Location will fail the smoke at gate time, not at operator UAT.
- D-20 honored: no new Playwright/Puppeteer dependency. The smoke covers what HTML-grepping + HTTP-header assertions can cover; the browser-API behavior stays in the human checklist.
- Case-7 rate-limit pre-condition is unchanged (cases 1 and 2 do not POST; case 3 POSTs once to SHARE_IP, not SMOKE_IP).
- All existing cases continue to pass — the smoke run reports the original count + 3 with zero failures.
- The smoke remains a single-file Node script. The only new import is `mintToken` from `src/lib/token.ts` (via Node 22's `--experimental-strip-types`, not a new npm dep). If the TS-import path proves unworkable, the fallback paths (inline-reimplement OR document a DEPLOY.md curl recipe + downgrade case 3 to manual) preserve correctness without adding deps.
</success_criteria>

<output>
Create `.planning/phases/14-share-experience/14-05-SUMMARY.md` when done.
</output>
</content>
</invoke>