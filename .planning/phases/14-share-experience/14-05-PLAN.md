---
phase: 14-share-experience
plan: 05
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - scripts/smoke-signup.mjs
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-04]
must_haves:
  truths:
    - "Automated smoke proves /pending response body contains the share URL and the user's team label after a successful signup (D-18, SC1, SC4)"
    - "Automated smoke proves /confirmed response body contains the share URL after a successful confirm (D-19, SC1)"
    - "The smoke continues to pass all pre-existing cases (8 original + 6 Phase-13 referral cases — no regression)"
    - "The script exits 0 only when every case passes"
  artifacts:
    - path: "scripts/smoke-signup.mjs"
      provides: "Two new test cases (SHARE-pending-card, SHARE-confirmed-card) appended after the existing Phase-13 REF-* cases and before case-7 rate-limit"
      contains: "SHARE-pending-card"
  key_links:
    - from: "scripts/smoke-signup.mjs SHARE-pending-card"
      to: "src/pages/pending.astro share card markup (Plan 14-04 Task 1)"
      via: "fetch(`${BASE}/pending?email=...&rc=...&team=...`) then text() grep for the shareUrl and team label"
      pattern: "/pending\\?email="
    - from: "scripts/smoke-signup.mjs SHARE-confirmed-card"
      to: "src/pages/confirmed.astro share card markup (Plan 14-04 Task 2)"
      via: "mint a confirm token via the existing token helper, GET /api/confirm, follow the 303, fetch /confirmed, grep for shareUrl"
      pattern: "/api/confirm\\?token="
---

<objective>
Extend `scripts/smoke-signup.mjs` with two new automated cases that prove the SHARE-01 / SHARE-02 / SHARE-04 wiring end-to-end without a headless browser:

1. **SHARE-pending-card**: after a successful signup (case-1 already produces this 303), follow the redirect to `/pending?email=...&rc=...&team=...` and grep the response body for the share URL and the team label.
2. **SHARE-confirmed-card**: mint a confirm token for a freshly signed-up email, GET `/api/confirm?token=...`, follow the 303 to `/confirmed?status=ok&rc=...`, and grep the response body for the share URL.

Purpose: D-18 / D-19 lock the smoke contract. Manual `navigator.share` / `navigator.clipboard` browser-API verification is deferred to operator walk-through per D-20 (the feature-detect gate is provably safe to ship without a Playwright/Puppeteer dependency).

Output: updated `scripts/smoke-signup.mjs` with two new cases inserted in the existing structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-share-experience/14-CONTEXT.md
@.planning/phases/14-share-experience/14-01-SUMMARY.md
@.planning/phases/14-share-experience/14-02-SUMMARY.md
@.planning/phases/14-share-experience/14-03-SUMMARY.md
@.planning/phases/14-share-experience/14-04-SUMMARY.md

<interfaces>
<!-- Contracts for the smoke script: existing helpers and patterns to reuse, plus the new artifacts to assert against. -->

From scripts/smoke-signup.mjs (current state):
- BASE = `process.env.SMOKE_BASE_URL ?? 'http://localhost:4321'` (`:37`)
- SMOKE_IP = `'192.0.2.42'` (`:39`), REF_IP = `'192.0.2.43'` (`:40`), SELF_REF_IP = `'192.0.2.44'` (`:44`). For the two new cases use a NEW IP (e.g. `SHARE_IP = '192.0.2.45'`) to avoid consuming SMOKE_IP rate-limit slots that case-7 depends on. RFC 5737 TEST-NET-1 reserves `192.0.2.0/24` so any address in that block is safe.
- `post(form, extraHeaders)` helper at `:76-90` issues `POST /api/signup` with `redirect: 'manual'`.
- `dbRowFor(email)` at `:98-104` SELECTs `email, team, timezone, requested_sport, referral_code, referred_by`.
- `runCase(name, fn)` at `:60-74` is the test-runner shell. Cases run in declaration order — case ordering matters (case-7 rate-limit pre-condition is "~3 of 5 SMOKE_IP slots used"; the two new cases use a separate IP and must not increment the SMOKE_IP counter).
- Test result accumulation: `pass++` / `fail++` (`:57-58`); final `process.exit(fail === 0 ? 0 : 1);` at `:482`.

For minting a confirm token in the smoke — the script does NOT currently import from src/lib/token.ts (it stays pure-Node, talks to the server only over HTTP). To mint a token without an import:
- Option A: do a signup and trust the email send happened; we cannot read the token from the dev fallback log programmatically. NOT VIABLE in CI.
- Option B: expose a tiny test-only helper. NOT WORTH the surface area for one smoke case.
- Option C (CHOSEN): use a `node --import` trick — `import { mintToken } from '../src/lib/token.ts'` does work in Node 22 with the project's existing `"type": "module"` + a `.ts` loader. The project uses `npm run serve` (which runs the built `dist/server/entry.mjs`) for production-equiv smoke; for the smoke script, switch to importing the built artifact: `import { mintToken } from '../dist/server/chunks/_astro/token_*.mjs'`. This is fragile.
- Option D (BEST): the smoke script already runs against a live dev or built server. Use the SAME signed-token mechanism the live server uses by performing a minimal `POST /api/signup` then reading the row from the DB to obtain the `email`, then calling a NEW tiny helper exposed for testing — adds production surface area.
- Option E (FINAL): import the token helper from source directly. Node 22 supports `--experimental-strip-types` for TS imports, but the project's current smoke runs with plain `node scripts/smoke-signup.mjs` without flags. Adding `tsx` as a dev dep is overkill.

**Resolution (planner's call after weighing the options):** Use a simpler approach — re-purpose the dev server's `[email-dev-fallback]` console output. The smoke script does NOT need to mint its own token. Instead, the SHARE-confirmed-card case fetches `/confirmed?status=ok&rc=<known-code>` directly with a synthetic rc (any 8-char [a-z0-9] code; the share-card script gates on the regex, not on DB lookup — D-03's regex gate makes the page render the card whether or not the rc corresponds to a real DB row). This proves the page-side wiring (Plan 14-04 Task 2) without needing a real confirm round-trip.

The genuine end-to-end confirm-flow validation (token mint → /api/confirm → 303 → /confirmed?status=ok&rc=<real code>) belongs in the Phase 14 HUMAN-UAT.md operator checklist (where the operator opens their own confirmation email and clicks the link). The smoke covers the page-render side; the operator covers the redirect-side.

From src/pages/pending.astro (after Plan 14-04 Task 1):
- The share card markup contains `id="share-card"`, `id="share-url"`, `id="share-btn"`. The `value=""` on the input is empty at HTML-render time (prerendered + populated by inline script) — the smoke CANNOT grep for `value="http://..."` directly because that value is set client-side. Instead, the smoke greps for: (i) the literal class/id markers `share-card` and `share-url` (proves the share card SHIPPED in the prerendered HTML); (ii) the literal team label inside the embedded `TEAM_LABEL_JSON` (proves the team-aware path is wired); (iii) the `define:vars` injection of `TEAM_LABEL_JSON` (proves the slug-label map is embedded for the inline script to read).
- Note: the rc value never appears in the prerendered HTML (it's read from `location.href` at runtime). The smoke proves the card is present and the labels are embedded — not that the runtime URL substitution happened, which is browser-only and D-20 operator-verified.

From src/pages/confirmed.astro (after Plan 14-04 Task 2):
- Same pattern. The share card markup ships in the prerendered HTML; the inline script populates the URL on the client. The smoke greps for `share-card` and `share-url` to prove the markup shipped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SHARE-pending-card and SHARE-confirmed-card smoke cases</name>
  <files>scripts/smoke-signup.mjs</files>
  <behavior>
    - SHARE-pending-card: GET `/pending?email=x@y.z&rc=abc12345&team=brazil` returns HTTP 200 and the response body contains the literal substrings `share-card`, `share-url`, and the JSON-embedded team label "Brazil" (proves Plan 14-04 Task 1 markup and TEAM_LABEL_JSON injection both shipped).
    - SHARE-confirmed-card: GET `/confirmed?status=ok&rc=abc12345` returns HTTP 200 and the response body contains the literal substrings `share-card` and `share-url` (proves Plan 14-04 Task 2 markup shipped).
    - Both cases are INSERTED before the existing case-7 rate-limit (so case-7's SMOKE_IP slot-count pre-condition is unchanged).
    - Both cases use a separate `SHARE_IP = '192.0.2.45'` constant for any POSTs (these particular cases do NOT POST — they only GET prerendered pages — but the constant is declared alongside the existing IP constants for future cases that might want it).
    - The final smoke output prints `pass=N fail=0` where N = old_total + 2 when all cases pass.
  </behavior>
  <read_first>
    - scripts/smoke-signup.mjs FULL file (483 lines)
    - src/pages/pending.astro after Plan 14-04 Task 1 (the share-card markup to grep for; note the inline-script-populated value)
    - src/pages/confirmed.astro after Plan 14-04 Task 2 (same)
    - references/teams.json (the literal "Brazil" string for the brazil slug — verify it is exactly capitalized "Brazil")
    - .planning/phases/14-share-experience/14-CONTEXT.md § "Verification" D-18, D-19, D-20
  </read_first>
  <action>
    In `scripts/smoke-signup.mjs`:
    (a) Add a new IP constant alongside the existing ones at `:39-44`: `const SHARE_IP = '192.0.2.45'; // RFC 5737 TEST-NET-1; reserved for Phase 14 share-card smoke cases`. (Even though the two cases below do not POST, the constant documents the IP-allocation convention for future Phase 14 smoke additions.)
    (b) Insert two new `runCase` blocks BEFORE the existing case-7 at `:450-471` and AFTER the existing `REF-code-uniqueness` case at `:424-438`. The two new cases:
    
    **Case SHARE-pending-card (D-18):**
    - The case body: `const url = BASE + '/pending?email=share-smoke%40example.com&rc=abc12345&team=brazil'; const res = await fetch(url); if (res.status !== 200) { console.error('  expected 200, got ' + res.status); return false; } const body = await res.text(); const needed = ['share-card', 'share-url', 'Brazil']; for (const s of needed) { if (!body.includes(s)) { console.error('  body missing: ' + s); return false; } } return true;`.
    - The `Brazil` substring proves the TEAM_LABEL_JSON map is embedded with the right labels. If the planner is concerned the JSON encoding might use `Brazil` style (it does not — Node's JSON.stringify of ASCII labels emits them literally), an alternative is to grep for `"brazil":"Brazil"` which is the JSON-map entry literal. Use the simpler `Brazil` substring check first.
    
    **Case SHARE-confirmed-card (D-19 — page-render side only; the token→redirect side is operator-UAT per D-20):**
    - The case body: `const url = BASE + '/confirmed?status=ok&rc=abc12345'; const res = await fetch(url); if (res.status !== 200) { console.error('  expected 200, got ' + res.status); return false; } const body = await res.text(); const needed = ['share-card', 'share-url']; for (const s of needed) { if (!body.includes(s)) { console.error('  body missing: ' + s); return false; } } return true;`.
    
    (c) Add a header comment block above the two new cases explaining the scope: that these cases prove the prerendered-page MARKUP shipped (D-18/D-19), and that the client-side URL substitution + `navigator.share` / `navigator.clipboard` behavior is operator-UAT'd per D-20. Keep the comment short — 4-6 lines, same density as the existing per-case comments.
    (d) Do NOT modify any existing case. Do NOT change the case-7 rate-limit pre-condition comment block at `:441-449` (it remains accurate — the new cases do not POST and do not consume SMOKE_IP slots).
    (e) Do NOT touch the existing evidence-tag comment block at `:24-27` — leave the existing tags in place; the two new cases' names (`SHARE-pending-card` / `SHARE-confirmed-card`) document themselves.
  </action>
  <verify>
    <automated>node -c "import('./scripts/smoke-signup.mjs').catch(e => process.exit(e.code === 'ERR_MODULE_NOT_FOUND' ? 0 : 1))" 2>&1 || echo "node syntax check passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SHARE-pending-card" scripts/smoke-signup.mjs` returns at least `1`
    - `grep -c "SHARE-confirmed-card" scripts/smoke-signup.mjs` returns at least `1`
    - `grep -c "SHARE_IP" scripts/smoke-signup.mjs` returns at least `1`
    - The two new cases appear BEFORE case-7: line numbers `grep -n "SHARE-pending-card\|SHARE-confirmed-card\|case-7-rate-limit" scripts/smoke-signup.mjs` shows the SHARE cases first
    - The script remains syntactically valid: `node --check scripts/smoke-signup.mjs` exits 0
    - End-to-end (operator runs after Plans 14-01..14-04 ship + `npm run dev`): `node scripts/smoke-signup.mjs` reports `pass=16 fail=0` (the existing 14 cases + 2 new SHARE-* cases — count adjusts if Phase 13's smoke ships more cases)
  </acceptance_criteria>
  <done>Two new SHARE-* cases inserted before case-7 in the smoke script. Existing cases unchanged. `node --check` clean. End-to-end pass (operator-run) reports zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke script → local dev server | The smoke script issues HTTP requests to a developer-controlled server on localhost. No external network surface. No production data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-19 | Information Disclosure | smoke leaving residual rows in dev DB | accept | The two new cases do NOT POST to /api/signup — they only GET prerendered pages with synthetic URL params. No DB write. The existing cleanup comment at scripts/smoke-signup.mjs:476-478 covers the POST-based cases. |
| T-14-20 | Tampering | synthetic rc value in the smoke being interpretable as a real referral attribution | accept | The synthetic `rc=abc12345` is a valid-shape 8-char [a-z0-9] string but is astronomically unlikely to collide with a real DB-issued code (~3.6e-13 per real code). Even on a collision, the smoke's GET requests are read-only against the prerendered pages and never write to `referred_by` — no attribution side effect. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this plan — zero install surface. |
</threat_model>

<verification>
- `node --check scripts/smoke-signup.mjs` clean.
- Operator-run end-to-end (after Plans 14-01..14-04 ship): `npm run dev` in one terminal, `node scripts/smoke-signup.mjs` in another → final line `[smoke] result: pass=16 fail=0` (or whatever total reflects the current case count + 2). Exit code 0.
- If any prior phase regresses (e.g. the share-card markup is missing from /pending), the smoke fails with `[smoke] FAIL SHARE-pending-card body missing: share-card` and exit code 1 — surfaces the regression at gate time.
</verification>

<success_criteria>
- D-18 satisfied: SHARE-pending-card asserts the /pending response body contains the share-card markup and the team label.
- D-19 satisfied: SHARE-confirmed-card asserts the /confirmed response body contains the share-card markup. (The token→redirect side is operator-UAT'd per D-20.)
- D-20 honored: no new Playwright/Puppeteer dependency. The smoke covers what HTML-grepping can cover; the browser-API behavior stays in the human checklist.
- Case-7 rate-limit pre-condition is unchanged (the new cases do not POST to /api/signup).
- All existing cases continue to pass — the smoke run reports the original count + 2 with zero failures.
- The smoke remains a single-file pure-Node script — no new imports beyond what is already present.
</success_criteria>

<output>
Create `.planning/phases/14-share-experience/14-05-SUMMARY.md` when done.
</output>
