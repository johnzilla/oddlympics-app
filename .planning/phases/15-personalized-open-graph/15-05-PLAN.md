---
phase: 15-personalized-open-graph
plan: 05
type: execute
wave: 3
depends_on: ["15-01", "15-03", "15-04"]
files_modified:
  - scripts/smoke-signup.mjs
autonomous: true
requirements: [OG-02, OG-03]
must_haves:
  truths:
    - "`scripts/smoke-signup.mjs` gains two new SHARE-r-* cases per D-12b: SHARE-r-known and SHARE-r-unknown."
    - "SHARE-r-known: GET /r/<row.referral_code> with redirect: 'manual' → asserts 200, response body contains og:image (per-team OR fallback per D-10) AND og:title carries 'Following <Team> · oddlympics' (D-14)."
    - "SHARE-r-unknown: GET /r/notarealcode with redirect: 'manual' → asserts 200, response body contains the generic og:image (PUBLIC_SITE_URL/og-image.png) and does NOT contain a Following-prefixed og:title."
    - "Both new cases use the existing runCase wrapper (smoke-signup.mjs:68-82) and increment the pass/fail counters; both contribute to the final exit code at line 612."
    - "Cases are appended after the existing SHARE-confirm-redirect-location case (line 504) and BEFORE case-7-rate-limit (line 580) — preserves SMOKE_IP slot accounting."
    - "Cases use SHARE_IP (192.0.2.45, defined at line 52) so they do NOT consume SMOKE_IP rate-limit slots that case-7 depends on (Phase 14 D-18 pattern)."
    - "SHARE-r-known sources a real {referral_code, team} from the DB via the existing better-sqlite3 connection (mirrors REF-valid-ref pattern at lines 308-316). If the DB has no such row, the case fails with a clear actionable error."
    - "Final smoke run against a live dev server exits 0 (all pre-existing cases PASS + 2 new cases PASS), proving SC2 + SC3 end-to-end."
  artifacts:
    - path: "scripts/smoke-signup.mjs"
      provides: "SHARE-r-known and SHARE-r-unknown cases"
      contains: "SHARE-r-known"
  key_links:
    - from: "scripts/smoke-signup.mjs (SHARE-r-known)"
      to: "src/pages/r/[code].astro"
      via: "fetch GET /r/<code> with redirect manual"
      pattern: "fetch.*BASE.*/r/"
    - from: "scripts/smoke-signup.mjs (SHARE-r-known)"
      to: "vip_signups table"
      via: "better-sqlite3 prepared SELECT"
      pattern: "SELECT referral_code, team FROM vip_signups"
---

<objective>
Prove end-to-end that Phase 15 works. Extend `scripts/smoke-signup.mjs` with two new SHARE-r-* cases per D-12b that exercise the resolved-code and unresolved-code branches of `/r/[code].astro` (Plan 15-01), verifying the OG meta + trim-fallback contract (D-10, D-14, SC2, SC3) against a live server.

Purpose: This plan is the verification gate for Phase 15. Without it, SC2 (route emits per-team OG meta) and SC3 (the response would actually unfurl correctly in a social validator) are only proved at the unit level. The smoke is the integration proof that ships with the codebase and runs on every future change.

Output: Two new cases appended to `scripts/smoke-signup.mjs`. One smoke-run against a live dev server, exit code 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-personalized-open-graph/15-CONTEXT.md
@.planning/phases/15-personalized-open-graph/15-PATTERNS.md
@.planning/phases/15-personalized-open-graph/15-01-SUMMARY.md
@.planning/phases/15-personalized-open-graph/15-03-SUMMARY.md
@.planning/phases/15-personalized-open-graph/15-04-SUMMARY.md
@scripts/smoke-signup.mjs

<interfaces>
<!-- Existing smoke-signup.mjs structures the new cases must integrate with. -->

Line 52 — IP reservation for share cases (use this, NOT SMOKE_IP):
  const SHARE_IP = '192.0.2.45'; // RFC 5737 TEST-NET-1; reserved for Phase 14 share-card smoke cases

Lines 68-82 — runCase wrapper (use as-is):
  async function runCase(name, fn) {
    try {
      const ok = await fn();
      if (ok) { console.log(`[smoke] PASS ${name}`); pass++; }
      else    { console.error(`[smoke] FAIL ${name}`); fail++; }
    } catch (err) { ... fail++; }
  }

Lines 308-316 — DB-read pattern from REF-valid-ref (template for SHARE-r-known's DB lookup):
  const row = db.prepare(
    'SELECT referral_code, team FROM vip_signups WHERE ... LIMIT 1'
  ).get();

Line 498-578 — SHARE-confirm-redirect-location case (template for fetch redirect:'manual' pattern).

Line 612 — final exit:
  process.exit(fail === 0 ? 0 : 1);

The two new cases go between line ~578 (end of SHARE-confirm-redirect-location) and line 580 (start of case-7-rate-limit).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append SHARE-r-known and SHARE-r-unknown cases to scripts/smoke-signup.mjs</name>
  <files>scripts/smoke-signup.mjs</files>
  <read_first>
    - scripts/smoke-signup.mjs (lines 1-100 for header/imports/IP constants/runCase wrapper; lines 300-320 for the REF-valid-ref DB-lookup pattern; lines 448-578 for the existing SHARE-* section as the exact template; lines 580-612 for the case-7-rate-limit case the new cases must NOT push past)
    - .planning/phases/15-personalized-open-graph/15-PATTERNS.md (Wave 3 section, lines 519-617 — has the full case skeletons including DB lookup, response-body grep patterns, exact assertions for both cases)
    - .planning/phases/15-personalized-open-graph/15-CONTEXT.md (D-12b — runtime smoke contract; <specifics> section has copy-pasta sketch for both cases)
  </read_first>
  <action>
    Append two new cases to `scripts/smoke-signup.mjs` between the existing SHARE-confirm-redirect-location case (ends around line 578) and the case-7-rate-limit case (starts at line 580). Use the existing runCase wrapper, SHARE_IP constant, and DB connection.

    **Case 1 — SHARE-r-known (proves SC2 + SC3 resolved branch):**

    Insert this block:
    ```
    // Phase 15 SHARE-r-known (D-12b): GET /r/<real-code> proves the route emits
    // personalized og:image (per-team if PNG exists, generic fallback per D-10)
    // and og:title carrying "Following <Team>" (D-14). Status-agnostic per D-03 —
    // any referral_code with a team works, confirmed or not.
    await runCase('SHARE-r-known (D-12b: personalized og:image + og:title)', async () => {
      const row = db.prepare(
        'SELECT referral_code, team FROM vip_signups WHERE referral_code IS NOT NULL AND team IS NOT NULL LIMIT 1'
      ).get();
      if (!row) {
        console.error('  no vip_signups row with referral_code + team in DB; run a valid signup first');
        return false;
      }
      const res = await fetch(`${BASE}/r/${row.referral_code}`, { redirect: 'manual' });
      if (res.status !== 200) {
        console.error(`  expected 200, got ${res.status}`);
        return false;
      }
      const body = await res.text();
      // og:image must be per-team PNG or generic fallback (D-10 trim path)
      const hasOgImage = body.includes('og:image" content="') &&
        (body.includes(`/og/${row.team}.png`) || body.includes('/og-image.png'));
      if (!hasOgImage) {
        console.error('  body missing og:image (per-team or fallback)');
        return false;
      }
      // og:title must carry "Following <Team> · oddlympics" (D-14)
      if (!body.match(/og:title" content="Following [^"]+ · oddlympics"/)) {
        console.error('  body missing personalized og:title');
        return false;
      }
      return true;
    });
    ```

    **Case 2 — SHARE-r-unknown (proves SC2 + SC3 unresolved branch + D-02 "never 404"):**

    Insert this block immediately after SHARE-r-known:
    ```
    // Phase 15 SHARE-r-unknown (D-12b + D-02): GET /r/notarealcode proves the
    // unresolved branch returns 200 (never 404), serves the generic og:image,
    // and does NOT carry a "Following <Team>" title. Stale-link UX preserved.
    await runCase('SHARE-r-unknown (D-12b + D-02: 200 + generic og:image + no team title)', async () => {
      const res = await fetch(`${BASE}/r/notarealcode`, { redirect: 'manual' });
      if (res.status !== 200) {
        console.error(`  expected 200, got ${res.status}`);
        return false;
      }
      const body = await res.text();
      // Generic og:image (absolute URL — accept both prod and localhost forms)
      const hasGenericOg =
        body.includes('og:image" content="https://oddlympics.app/og-image.png"') ||
        body.includes('og:image" content="http://localhost:4321/og-image.png"');
      if (!hasGenericOg) {
        console.error('  body missing generic og:image');
        return false;
      }
      // Must NOT have personalized "Following …" title — that would mean the
      // shape gate or the unresolved-branch logic regressed.
      if (body.includes('og:title" content="Following ')) {
        console.error('  body has team-personalized og:title for unknown code (wrong)');
        return false;
      }
      return true;
    });
    ```

    Placement rules:
    - Insert ABOVE the case-7-rate-limit case (currently starts at line 580). The two new cases must run BEFORE case-7 because case-7 exhausts SMOKE_IP's rate-limit slots; if the SHARE-r-* cases ran after, they would not be affected (they use SHARE_IP and GET /r/, not POST /api/signup), but appending before case-7 is the conventional position per Phase 14 D-18 pattern.
    - Do NOT renumber any existing cases.
    - Do NOT modify the runCase wrapper, the SHARE_IP constant, or the final `process.exit(fail === 0 ? 0 : 1)` line.
    - The DB connection used by the case is the existing `db` import at the top of the script (mirrors REF-valid-ref's `db.prepare(...)` pattern at line ~308).
    - If `db` is not yet imported in scope at the insertion point, verify by reading the top of the file. If `db` is in scope for REF-valid-ref (line 308), it is in scope at line 580.
    - Use the existing `BASE` constant (defined at the top of the script per Phase 5 — `const BASE = process.env.SMOKE_BASE_URL ?? 'http://localhost:4321'`).
    - `fetch` with `redirect: 'manual'` is the same pattern as SHARE-confirm-redirect-location at line 534 — the route emits a meta-refresh in HTML, not an HTTP 30x, so `redirect: 'manual'` doesn't change behavior here. Including it matches the project convention (no implicit redirect following).

    After the edits, run the smoke against a live dev server:
    - Terminal 1: `npm run dev`
    - Terminal 2: `node scripts/smoke-signup.mjs`
    - Confirm: final output includes `[smoke] PASS SHARE-r-known ...` and `[smoke] PASS SHARE-r-unknown ...`, the trailing pass-count is `19/19` (or whatever the prior count +2 is), and the script exits 0.
    - If SHARE-r-known fails with "no vip_signups row with referral_code + team in DB": the DB is empty / has no team-assigned rows. Either run a Phase-13-or-later signup smoke case manually to seed one, or pick an earlier upstream case to ensure a seed row exists (do NOT mutate the SHARE-r-known case to lower the bar — it correctly fails when the precondition is unmet).
    - Stop the dev server after smoke passes.

    Do NOT:
    - Add a new IP constant — `SHARE_IP` is correctly reused.
    - Add a new database import — the file already opens `db` for REF-* cases.
    - Touch any case outside the SHARE section.
    - Add Playwright/Puppeteer to test the meta-refresh fallback rendering — response-body grep covers the bot-scrape surface (which is what SC3 actually tests; a real social validator runs equivalent meta extraction).
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && grep -c "SHARE-r-known" scripts/smoke-signup.mjs && grep -c "SHARE-r-unknown" scripts/smoke-signup.mjs && grep -c "fetch.*BASE.*/r/" scripts/smoke-signup.mjs && grep -c "/r/notarealcode" scripts/smoke-signup.mjs && grep -c "og:title.*content.*Following" scripts/smoke-signup.mjs && node --check scripts/smoke-signup.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "SHARE-r-known" scripts/smoke-signup.mjs` returns at least `1`
    - Source assertion: `grep -c "SHARE-r-unknown" scripts/smoke-signup.mjs` returns at least `1`
    - Source assertion: `grep -c "/r/notarealcode" scripts/smoke-signup.mjs` returns at least `1`
    - Source assertion: `grep -c "SELECT referral_code, team FROM vip_signups" scripts/smoke-signup.mjs` returns at least `1` (DB lookup for known-code case)
    - Source assertion: `grep -c "og:image\" content=\"" scripts/smoke-signup.mjs` returns at least `2` (both new cases check og:image)
    - Behavior assertion: `node --check scripts/smoke-signup.mjs` exits 0 (syntax-valid)
    - Behavior assertion (live smoke): with `npm run dev` running in another terminal AND at least one `vip_signups` row with non-null `referral_code` and non-null `team` in the DB, `node scripts/smoke-signup.mjs` exits 0 and the final summary line shows `pass=N+2 fail=0` (where N is the pre-15-05 pass count, ~17 after Phase 14)
    - Behavior assertion: the smoke log shows `[smoke] PASS SHARE-r-known ...` AND `[smoke] PASS SHARE-r-unknown ...`
    - Source assertion (no regression): the existing `case-7-rate-limit` case is still present (`grep -c "case-7-rate-limit" scripts/smoke-signup.mjs` returns at least `1`) and the final exit line (`process.exit(fail === 0 ? 0 : 1)`) is unchanged
  </acceptance_criteria>
  <done>
    Both new cases exist, the smoke runs green against a live dev server, exit code 0. SC2 + SC3 are proved end-to-end at the response-body level. Phase 15 is verifiable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → dev server (localhost:4321) | Smoke fetches go over HTTP localhost — trusted; not a production attack surface |
| Test harness → SQLite (data/oddlympics.db) | Direct read access via better-sqlite3 — same trust as the app process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-19 | Tampering | A future PR introduces a regression in `/r/[code].astro` (e.g. accidentally adds 404 on unknown codes) — smoke must catch it | mitigate | SHARE-r-unknown explicitly asserts `res.status === 200` and fails on any other status, including 404. Documented as the D-02 gate. |
| T-15-20 | Tampering | A future PR drops the per-team OG image personalization (e.g. accidentally hardcodes generic og:image for all codes) — smoke must catch it | mitigate | SHARE-r-known asserts og:title matches `/og:title" content="Following [^"]+ · oddlympics"/` — this is the personalization signal. If a regression strips personalization, the title check fails. |
| T-15-21 | Repudiation | Smoke false-passes if the DB has no team-assigned rows (SHARE-r-known would skip silently) | mitigate | SHARE-r-known returns `false` (logged as FAIL with actionable message) when the precondition row is missing — does NOT skip silently. Pre-15-05 smoke cases (REF-valid-ref, etc.) typically seed such rows; if running in a fresh-DB context, the operator gets a clear FAIL message and seeds manually. |
| T-15-22 | Information Disclosure | Smoke output leaks user emails to logs | accept | SHARE-r-known reads only `{referral_code, team}` — no email. SHARE-r-unknown uses a hardcoded literal. No PII in smoke output. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only appends to an existing script. No legitimacy gate needed. |
</threat_model>

<verification>
After Task 1 completes:

1. **Syntax:** `node --check scripts/smoke-signup.mjs` exits 0.
2. **Static checks:** all grep assertions in the verify block pass.
3. **Live smoke run:**
   - `npm run build` (so the production-equivalent server is testable; or just use `npm run dev` for faster iteration)
   - In one terminal: `npm run dev` (or `npm run serve` after a build)
   - In another: `node scripts/smoke-signup.mjs`
   - Confirm exit 0 and PASS for both new cases
4. **Diff check:** `git diff scripts/smoke-signup.mjs` shows only the new case blocks added; no existing-case modifications.
5. **No regression on existing smoke:** the pre-15-05 case count is preserved; the new total is `prior_count + 2`.
6. **Stop the dev server** after smoke passes.
</verification>

<success_criteria>
- Two new SHARE-r-* cases live in `scripts/smoke-signup.mjs`.
- Live smoke run against a populated dev DB exits 0, with both new cases PASS.
- The existing 17 (~ Phase 14 count) cases still PASS — no regression.
- Phase 15 SC2 (route emits per-team OG meta) and SC3 (response would unfurl correctly on a social validator at the response-body level) are proved end-to-end.
- T-15-19..T-15-22 mitigations/acceptances are documented.
</success_criteria>

<output>
Create `.planning/phases/15-personalized-open-graph/15-05-SUMMARY.md` when done.
</output>
