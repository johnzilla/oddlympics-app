---
phase: 15-personalized-open-graph
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/og-image-team.svg
  - scripts/render-team-og-images.mjs
  - package.json
autonomous: true
requirements: [OG-02]
must_haves:
  truths:
    - "A new SVG template `references/og-image-team.svg` exists with `{{TEAM_LABEL}}` and `{{HEADLINE_FONT_SIZE}}` substitution tokens — chrome (wordmark, banner pill, sub line, URL, decorative flags, bottom-right tag) preserved verbatim from `references/og-image.svg` (D-05, D-06)."
    - "Headline copy on the team SVG is the locked D-07 three-line block: line 1 `Following {{TEAM_LABEL}}.`, line 2 `Every match in your zone.`, line 3 `One ping before kickoff.` (line 3 retains the `#d94a1f` accent color per D-07)."
    - "Auto-scale font-size by label length per D-08: `labelLen <= 12 → 64`, `labelLen <= 16 → 52`, `labelLen > 16 → 44` — applied to ALL THREE headline lines for consistent block height."
    - "A new script `scripts/render-team-og-images.mjs` reuses the Resvg config from `scripts/render-og-image.mjs` verbatim — same `fontFiles` (JetBrainsMono-Bold, Inter-Regular, Inter-Bold), `loadSystemFonts: false`, `fitTo: { mode: 'width', value: 1200 }` (D-11)."
    - "The script loops `references/teams.json` (48 entries) and for each team writes `public/og/<slug>.png` (D-09)."
    - "Per-team 6-check verification mirrors the analog: file-exists, png-signature, ihdr-width-1200, ihdr-height-630, size-lt-300kb, land-02-clean-on-substituted-svg (D-11)."
    - "LAND-02 grep runs against the substituted SVG written to a tmpfile (then deleted), NOT the template path — catches team-name accidental matches per D-11."
    - "Script aggregates pass/fail across all 48 iterations and exits 1 on any single FAIL; on success prints `[og:render-teams] N/48 PASS` (D-12 / D-12a — render-time half of the two-way verification contract)."
    - "`package.json` gains an `og:render-teams` script entry next to the existing `og:render` (D-09)."
    - "Re-running the script with unchanged SVG + teams.json is a no-op (deterministic via `loadSystemFonts: false`)."
    - "Script creates `public/og/` directory via `mkdirSync(..., { recursive: true })` if absent — first-run safe."
    - "The script does NOT touch `scripts/render-og-image.mjs` (D-11: separate sibling) and does NOT modify `references/og-image.svg` (the generic OG stays untouched)."
  artifacts:
    - path: "references/og-image-team.svg"
      provides: "Parameterized SVG template for per-team OG images"
      contains: "{{TEAM_LABEL}}"
    - path: "references/og-image-team.svg"
      provides: "Headline font-size token"
      contains: "{{HEADLINE_FONT_SIZE}}"
    - path: "scripts/render-team-og-images.mjs"
      provides: "Per-team OG PNG render+verify script"
      min_lines: 80
    - path: "package.json"
      provides: "npm run og:render-teams script entry"
      contains: "og:render-teams"
  key_links:
    - from: "scripts/render-team-og-images.mjs"
      to: "references/og-image-team.svg"
      via: "readFileSync template once, string.replace inside loop"
      pattern: "readFileSync.*og-image-team\\.svg"
    - from: "scripts/render-team-og-images.mjs"
      to: "references/teams.json"
      via: "JSON import with type assertion"
      pattern: "import.*teams.*references/teams\\.json"
    - from: "scripts/render-team-og-images.mjs"
      to: "@resvg/resvg-js"
      via: "Resvg class"
      pattern: "import.*Resvg.*from.*@resvg/resvg-js"
    - from: "package.json scripts"
      to: "scripts/render-team-og-images.mjs"
      via: "npm script entry"
      pattern: "\"og:render-teams\":.*render-team-og-images\\.mjs"
---

<objective>
Build the per-team OG image render toolchain: an SVG template, a render script that produces 48 PNGs, and a `package.json` script entry to invoke it. This plan creates only the toolchain — it does NOT run the render or commit the PNGs (that's Plan 15-03). Splitting render-toolchain from PNG-commit lets the toolchain land + be reviewable in parallel with the route work (Plan 15-01), and lets the PNG commit be a focused, separable change.

Purpose: SC1 (per-team OG image exists for each of 48 teams) is satisfied by the artifacts this plan produces. Plan 15-03 actuates them.

Output: One new SVG (`references/og-image-team.svg`), one new script (`scripts/render-team-og-images.mjs`), one new `package.json` script entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-personalized-open-graph/15-CONTEXT.md
@.planning/phases/15-personalized-open-graph/15-PATTERNS.md
@scripts/render-og-image.mjs
@references/og-image.svg
@references/teams.json
@package.json

<interfaces>
<!-- Key types and contracts the executor will need. -->

From references/teams.json (each entry):
```json
{ "slug": "england", "label": "England", "confederation": "UEFA" }
```
48 entries total. Slugs are lowercase ASCII with underscores for multi-word (e.g. `united_states`, `czech_republic`, `bosnia` — see file for full list). Labels are display labels (e.g. "USA", "Bosnia and Herzegovina", "Côte d'Ivoire" — verify max length when implementing).

From scripts/render-og-image.mjs (existing, lines 40-55) — the Resvg config block to copy verbatim:
```javascript
const resvg = new Resvg(svg, {
  font: {
    fontFiles: [
      resolve(fontsDir, 'JetBrainsMono-Bold.ttf'),
      resolve(fontsDir, 'Inter-Regular.ttf'),
      resolve(fontsDir, 'Inter-Bold.ttf'),
    ],
    loadSystemFonts: false,
  },
  fitTo: { mode: 'width', value: 1200 },
});
```

From scripts/render-og-image.mjs (existing, lines 62-101) — the 6-check verification block to replicate per-team.

From package.json (existing, line 20) — the analog script entry:
```json
"og:render": "node scripts/render-og-image.mjs",
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create references/og-image-team.svg parameterized template</name>
  <files>references/og-image-team.svg</files>
  <read_first>
    - references/og-image.svg (the entire 76-line file — this is the verbatim chrome to preserve, except for the headline block at lines 31-36)
    - .planning/phases/15-personalized-open-graph/15-PATTERNS.md ("references/og-image-team.svg" section, lines 29-105 — has the full SVG with the parameterized headline)
    - .planning/phases/15-personalized-open-graph/15-CONTEXT.md (D-05, D-06, D-07, D-08 — token shape, chrome-preservation rule, headline copy, font-size buckets)
  </read_first>
  <action>
    Create `references/og-image-team.svg` as a near-copy of `references/og-image.svg` with two substitution tokens in the headline block. Replace the existing headline `<g>` block (lines 31-36 in `og-image.svg`) with the parameterized version below. Every other section (background, defs/gradients, top-left wordmark, banner pill, sub line, URL, right-side decorative flags, bottom-right tag, closing tag) is preserved BYTE-FOR-BYTE from `og-image.svg` per D-06.

    Replacement headline block (per D-07 wording + D-08 token):
    ```
    <g transform="translate(72, 252)" font-family="Inter" fill="#14151a">
      <text x="0" y="0" font-size="{{HEADLINE_FONT_SIZE}}" font-weight="700" letter-spacing="-1.2">Following {{TEAM_LABEL}}.</text>
      <text x="0" y="74" font-size="{{HEADLINE_FONT_SIZE}}" font-weight="700" letter-spacing="-1.2">Every match in your zone.</text>
      <text x="0" y="148" font-size="{{HEADLINE_FONT_SIZE}}" font-weight="700" letter-spacing="-1.2" fill="#d94a1f">One ping before kickoff.</text>
    </g>
    ```

    Token rules:
    - `{{TEAM_LABEL}}` appears EXACTLY ONCE (line 1 of the headline). The render script will replace globally, but only one occurrence is intentional.
    - `{{HEADLINE_FONT_SIZE}}` appears EXACTLY THREE TIMES (one per `<text>` line). The script substitutes globally; all three lines get the same value per D-08 (keeps block height balanced).
    - The accent color `#d94a1f` on line 3 is preserved verbatim from `og-image.svg:35` per D-07. Do NOT replace with `--accent: #b8350d` — those are two different palette tokens (`#b8350d` is the in-app accent; `#d94a1f` is the OG-SVG-only accent — CLAUDE.md "Conventions established" + D-07).
    - The y-offsets (0 / 74 / 148) match the existing 64pt grid from `og-image.svg`. At the 44pt bucket (long labels), the spacing will be slightly loose — acceptable for v1 per the 15-PATTERNS.md adaptation note ("planner may verify balance at the 44pt bucket... leave final y-values to implementer"). If during smoke a long-label render looks visually broken, adjust y-offsets in this task (NOT in Plan 15-03).
    - Preserve the XML declaration `<?xml version="1.0" encoding="UTF-8"?>` as line 1 (exact match to `og-image.svg:1`).
    - Preserve the closing `</svg>` tag.

    Do NOT modify `references/og-image.svg` — Phase 15 is purely additive on the SVG side per D-11 ("no refactor of render-og-image.mjs").
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && test -f references/og-image-team.svg && grep -c "{{TEAM_LABEL}}" references/og-image-team.svg | grep -q "^1$" && grep -c "{{HEADLINE_FONT_SIZE}}" references/og-image-team.svg | grep -q "^3$" && grep -c "fill=\"#d94a1f\"" references/og-image-team.svg | grep -q "^[1-9]" && grep -c "Following {{TEAM_LABEL}}" references/og-image-team.svg | grep -q "^1$" && grep -c "Every match in your zone" references/og-image-team.svg | grep -q "^1$" && grep -c "One ping before kickoff" references/og-image-team.svg | grep -q "^1$" && grep -v '^<!--\|^[[:space:]]*<!--' references/og-image-team.svg | grep -cE 'bitcoin|lightning|crypto|world domination|personal olympics' | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f references/og-image-team.svg` exits 0
    - Source assertion: `grep -c '{{TEAM_LABEL}}' references/og-image-team.svg` returns `1`
    - Source assertion: `grep -c '{{HEADLINE_FONT_SIZE}}' references/og-image-team.svg` returns `3`
    - Source assertion: `grep -c 'Following {{TEAM_LABEL}}\.' references/og-image-team.svg` returns `1` (exact headline-line-1 copy from D-07)
    - Source assertion: `grep -c 'Every match in your zone\.' references/og-image-team.svg` returns `1`
    - Source assertion: `grep -c 'One ping before kickoff\.' references/og-image-team.svg` returns `1`
    - Source assertion: `grep -c 'fill="#d94a1f"' references/og-image-team.svg` returns at least `4` (gradient defs at line 13 + banner pill stroke at line 27 + banner text at line 28 + headline line 3 fill — same count as `og-image.svg`)
    - Source assertion: `grep -iE 'bitcoin|lightning|crypto|world domination|personal olympics' references/og-image-team.svg | grep -v '^<!--'` returns nothing (LAND-02 clean on template itself)
    - Behavior assertion: `cat references/og-image-team.svg | wc -l` is within ±2 lines of `cat references/og-image.svg | wc -l` (only the headline block changed, structurally similar)
    - Source assertion: `grep -c "^<?xml version=\"1.0\" encoding=\"UTF-8\"?>" references/og-image-team.svg` returns `1` (preserved XML declaration)
  </acceptance_criteria>
  <done>
    Template exists with the two tokens, the locked D-07 headline copy, the preserved D-06 chrome, and the preserved D-07 accent color. LAND-02 clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scripts/render-team-og-images.mjs render+verify script</name>
  <files>scripts/render-team-og-images.mjs</files>
  <read_first>
    - scripts/render-og-image.mjs (the entire 102-line file — this is the analog; specifically lines 1-36 for header / paths / imports, lines 40-55 for the Resvg config, lines 62-101 for the 6-check verification block)
    - references/teams.json (lines 1-50, the full team list — confirms 48 entries and slug/label shape)
    - .planning/phases/15-personalized-open-graph/15-PATTERNS.md ("scripts/render-team-og-images.mjs" section, lines 108-248 — has the full script structure with imports, loop skeleton, 6-check pattern, LAND-02 tmpfile note)
    - .planning/phases/15-personalized-open-graph/15-CONTEXT.md (D-08 font-size buckets, D-09 commit-to-public/og/<slug>.png, D-11 Resvg config reuse + per-team LAND-02, D-12a render-time exit-code contract)
  </read_first>
  <action>
    Create `scripts/render-team-og-images.mjs` as a sibling of `scripts/render-og-image.mjs`. Reuse its Resvg config + 6-check verification verbatim per D-11; loop over `references/teams.json`.

    File structure:

    1. Shebang + header comment block (mirrors `render-og-image.mjs:1-25` style):
       - Phase 15 — render-team-og-images.mjs
       - Renders `references/og-image-team.svg` × 48 → `public/og/<slug>.png`
       - Same Resvg config as render-og-image.mjs (D-11 deterministic, loadSystemFonts: false)
       - Run: `npm run og:render-teams`
       - Exit codes: 0 = all PASS; 1 = any FAIL

    2. Imports (`node:` prefix per CLAUDE.md conventions):
       ```
       import { readFileSync, writeFileSync, statSync, existsSync, mkdirSync, unlinkSync } from 'node:fs';
       import { resolve } from 'node:path';
       import { tmpdir } from 'node:os';
       import { execSync } from 'node:child_process';
       import { Resvg } from '@resvg/resvg-js';
       import teams from '../references/teams.json' with { type: 'json' };
       ```

    3. Path resolution (mirrors `render-og-image.mjs:31-36`):
       ```
       const root     = new URL('..', import.meta.url).pathname;
       const tmplPath = resolve(root, 'references/og-image-team.svg');
       const outDir   = resolve(root, 'public/og');
       const fontsDir = resolve(root, 'references/fonts');
       mkdirSync(outDir, { recursive: true });
       ```

    4. Load template ONCE before the loop (`const template = readFileSync(tmplPath, 'utf8');`).

    5. Aggregate counters: `let totalPass = 0; let totalFail = 0;`. Define `function check(label, ok) { ... totalPass++ / totalFail++ ... }` matching `render-og-image.mjs:66-69` style. Log with team-prefix: `[${slug}/og:verify] PASS ${label}`.

    6. Loop body — for each `team` in `teams`:
       a. Destructure `{ slug, label }`. Compute `labelLen = label.length`. Apply D-08 buckets:
          ```
          const fontSize = labelLen <= 12 ? 64
                         : labelLen <= 16 ? 52
                         : 44;
          ```
       b. Substitute both tokens (global replace):
          ```
          const svg = template
            .replace(/\{\{TEAM_LABEL\}\}/g, label)
            .replace(/\{\{HEADLINE_FONT_SIZE\}\}/g, String(fontSize));
          ```
       c. Build `outPath = resolve(outDir, slug + '.png')`.
       d. Construct a per-team Resvg instance with the EXACT same config as `render-og-image.mjs:40-55` (just pass `Buffer.from(svg)` instead of the file buffer).
       e. `const png = resvg.render().asPng(); writeFileSync(outPath, png);`
       f. Console.log a single line per team (mirrors the format in 15-CONTEXT.md <specifics>): `[og:render-teams] ${slug.padEnd(20)} ${Math.round(png.length / 1024).toString().padStart(3)} KB  ${fontSize}pt  → ${outPath}`.
       g. Run the 6 checks (replicate `render-og-image.mjs:71-98` per team):
          - `check('file-exists', existsSync(outPath))`
          - `check('png-signature', PNG_SIG.every((b, i) => buf[i] === b))` with `const PNG_SIG = [0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A]` (define once before loop)
          - `check('ihdr-width-1200', buf.readUInt32BE(16) === 1200)`
          - `check('ihdr-height-630', buf.readUInt32BE(20) === 630)`
          - `check('size-lt-300kb', statSync(outPath).size < 300_000)`
          - **LAND-02 per-team:** Write the SUBSTITUTED `svg` string to a tmpfile (`const tmpSvg = resolve(tmpdir(), 'og-team-' + slug + '-' + process.pid + '.svg'); writeFileSync(tmpSvg, svg);`), then `try { execSync("! grep -iE 'bitcoin|lightning|crypto|world domination|personal olympics' " + JSON.stringify(tmpSvg), { stdio: 'inherit' }); check('land-02-clean', true); } catch { check('land-02-clean', false); } finally { try { unlinkSync(tmpSvg); } catch {} }`. This catches a team label that accidentally contains a banned term per D-11. NEVER grep `tmplPath` — would miss substitution-introduced matches.

    7. After the loop:
       ```
       console.log(`[og:render-teams] ${totalPass / 6}/${teams.length} PASS`);
       if (totalFail > 0) {
         console.error(`[og:render-teams] ${totalFail} check failures across ${teams.length} teams`);
         process.exit(1);
       }
       ```
       (Per D-12a, the headline `N/48 PASS` count is teams-fully-passed, not raw checks. Compute as `totalPass / 6` since each team contributes 6 checks; round or floor as appropriate. Alternative: maintain a per-team `teamPass` boolean and increment `teamsPassed`; pick the cleaner one.)

    Coding conventions to honor (CLAUDE.md + .planning/codebase/CONVENTIONS.md):
    - `node:` prefix on all built-ins (no bare `fs`, `path`, etc.).
    - No comments explaining what; only why-comments (e.g., on the `loadSystemFonts: false` non-obvious choice — copy the comment from `render-og-image.mjs:47-49`).
    - No JSDoc.
    - `const` for all bindings except the `total*` counters.

    Do NOT:
    - Modify `scripts/render-og-image.mjs` (D-11 — separate sibling).
    - Run the script in this task — Plan 15-03 runs it and commits PNGs.
    - Add npm deps — `@resvg/resvg-js` is already in devDependencies.
    - Use `fs/promises` / async — `render-og-image.mjs` is synchronous; match the idiom.
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && test -f scripts/render-team-og-images.mjs && grep -q "@resvg/resvg-js" scripts/render-team-og-images.mjs && grep -q "loadSystemFonts: false" scripts/render-team-og-images.mjs && grep -q "fitTo:.*mode:.*width.*value: 1200" scripts/render-team-og-images.mjs && grep -q "labelLen <= 12 ? 64" scripts/render-team-og-images.mjs && grep -q "labelLen <= 16 ? 52" scripts/render-team-og-images.mjs && grep -q ": 44" scripts/render-team-og-images.mjs && grep -q "TEAM_LABEL" scripts/render-team-og-images.mjs && grep -q "HEADLINE_FONT_SIZE" scripts/render-team-og-images.mjs && grep -q "public/og" scripts/render-team-og-images.mjs && grep -q "mkdirSync.*recursive: true" scripts/render-team-og-images.mjs && grep -q "totalFail > 0" scripts/render-team-og-images.mjs && grep -q "process.exit(1)" scripts/render-team-og-images.mjs && grep -q "bitcoin|lightning|crypto" scripts/render-team-og-images.mjs && node --check scripts/render-team-og-images.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f scripts/render-team-og-images.mjs` exits 0
    - Source assertion: `grep -c "import.*Resvg.*from '@resvg/resvg-js'" scripts/render-team-og-images.mjs` returns `1`
    - Source assertion: `grep -c "loadSystemFonts: false" scripts/render-team-og-images.mjs` returns `1` (D-11 — deterministic)
    - Source assertion: `grep -c "fitTo:.*mode:.*'width'.*value: 1200" scripts/render-team-og-images.mjs` returns `1`
    - Source assertion: `grep -c "labelLen <= 12 ? 64" scripts/render-team-og-images.mjs` returns `1` and `grep -c "labelLen <= 16 ? 52" scripts/render-team-og-images.mjs` returns `1` (D-08 buckets)
    - Source assertion: `grep -c "TEAM_LABEL" scripts/render-team-og-images.mjs` returns at least `1` (substitution token)
    - Source assertion: `grep -c "HEADLINE_FONT_SIZE" scripts/render-team-og-images.mjs` returns at least `1`
    - Source assertion: `grep -c "import teams from '../references/teams.json'" scripts/render-team-og-images.mjs` returns `1`
    - Source assertion: `grep -c "mkdirSync.*outDir.*recursive: true" scripts/render-team-og-images.mjs` returns `1` (first-run safe)
    - Source assertion: `grep -c "process.exit(1)" scripts/render-team-og-images.mjs` returns `1` (D-12a)
    - Source assertion: `grep -E "bitcoin\|lightning\|crypto\|world domination\|personal olympics" scripts/render-team-og-images.mjs` matches at least once (per-team LAND-02 check)
    - Source assertion: `grep -c "tmpdir" scripts/render-team-og-images.mjs` returns at least `1` (tmpfile pattern for substituted-SVG LAND-02 grep)
    - Source assertion: `grep -c "scripts/render-og-image.mjs" scripts/render-team-og-images.mjs` returns `0` (separate sibling — does not import or invoke the analog)
    - Behavior assertion: `node --check scripts/render-team-og-images.mjs` exits 0 (syntax-valid ESM)
  </acceptance_criteria>
  <done>
    The script exists, is syntax-valid, has the correct config-reuse pattern, the correct font-size buckets, and the correct exit-code contract. NOT YET INVOKED — Plan 15-03 invokes it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add og:render-teams entry to package.json scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json (the entire file — only 37 lines; the `scripts` block is lines 6-21, and the analog `og:render` is at line 20)
    - .planning/phases/15-personalized-open-graph/15-PATTERNS.md ("package.json" section, lines 500-512)
    - .planning/phases/15-personalized-open-graph/15-CONTEXT.md (D-09 — npm run og:render-teams is the operator entrypoint)
  </read_first>
  <action>
    Edit `package.json` to add a new `og:render-teams` script entry IMMEDIATELY AFTER the existing `og:render` line (currently line 20). The line MUST end with a comma and be followed by the new line.

    Before (line 19-20):
    ```
        "check:land-02": "! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' dist/client/index.html",
        "og:render": "node scripts/render-og-image.mjs"
    ```

    After:
    ```
        "check:land-02": "! grep -iE '[b]itcoin|[l]ightning|[c]rypto|[w]orld domination|[p]ersonal olympics' dist/client/index.html",
        "og:render": "node scripts/render-og-image.mjs",
        "og:render-teams": "node scripts/render-team-og-images.mjs"
    ```

    Notes:
    - Add a trailing comma to the existing `og:render` line (it currently has no comma — it's the last entry in `scripts`).
    - The new line is the last entry in `scripts` — NO trailing comma.
    - Preserve indentation (4 spaces inside `scripts`).
    - Do NOT touch `dependencies`, `devDependencies`, `overrides`, or any other top-level key.
    - Validate JSON is well-formed after the edit (run `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` — exits 0 on valid).
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/oddlympics-app && node -e "const j = JSON.parse(require('fs').readFileSync('package.json','utf8')); if (j.scripts['og:render-teams'] !== 'node scripts/render-team-og-images.mjs') { console.error('og:render-teams missing or wrong'); process.exit(1); } if (j.scripts['og:render'] !== 'node scripts/render-og-image.mjs') { console.error('og:render regressed'); process.exit(1); } console.log('package.json scripts OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON)
    - Behavior assertion: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['og:render-teams'])"` prints `node scripts/render-team-og-images.mjs`
    - Behavior assertion: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['og:render'])"` prints `node scripts/render-og-image.mjs` (the existing entry is preserved)
    - Source assertion: `grep -c "\"og:render-teams\":" package.json` returns `1`
    - Source assertion: `grep -c "\"og:render\":" package.json` returns `1` (no duplicate entries)
  </acceptance_criteria>
  <done>
    `npm run og:render-teams` is a valid invocation; the existing `npm run og:render` is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `references/teams.json` → `scripts/render-team-og-images.mjs` | Team labels (vendored allow-list, controlled by maintainer) flow into SVG string substitution → PNG render |
| `scripts/render-team-og-images.mjs` → `public/og/*.png` | Script writes binary PNG files into a directory served statically by Caddy |
| `scripts/render-team-og-images.mjs` → `child_process.execSync` (grep) | Script shells out to `grep` against a tmpfile path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering / Injection | `references/og-image-team.svg` substitution — team label containing `<`, `>`, `&`, `"`, `'` could break SVG XML parsing or inject SVG nodes | mitigate | Audit `references/teams.json` labels: 48 entries, expected diacritics + spaces + apostrophes (`Côte d'Ivoire`). If any label contains `<`, `>`, `&` → render WILL produce malformed XML and resvg WILL fail (so the script exits 1 — fail-loud). Confirm via the LAND-02 grep wrapper + the 6-check verification (PNG signature check catches a totally-broken render). If labels contain `'` (apostrophe), SVG allows it inside element text without escaping — no action needed. Add a one-line note in the script: "labels with SVG-special chars (`<`, `>`, `&`) would break this render; current `references/teams.json` is clean (verified Phase 15-02)." |
| T-15-08 | Information Disclosure | `public/og/*.png` — accidental EXIF/metadata leak | mitigate | Resvg produces clean PNGs with no EXIF (it writes only IHDR / IDAT / IEND chunks). Plan 15-03 includes a one-shot `file public/og/england.png` assertion to confirm output is bare PNG — no tEXt/zTXt/iTXt chunks with leakable data. Note: Resvg embeds no source SVG paths or font filenames in output. |
| T-15-09 | Tampering | `execSync(grep ...)` — shell-injection via tmpfile path | mitigate | Tmpfile path is built from `tmpdir()` + literal prefix + `slug` (`[a-z0-9_]` only, vendored from `references/teams.json`) + `process.pid` (integer). `JSON.stringify(tmpSvg)` wraps the path in double quotes for safe shell-arg quoting. The grep pattern is a literal string. No user input flows into the command. |
| T-15-10 | Denial of Service | render-team-og-images.mjs runtime — pathological large render | accept | Each PNG is bounded by SVG complexity (single-pass flat-color render) and the `fitTo: { mode: 'width', value: 1200 }` width-lock. Expected ~50-150KB per PNG, ~5-10s total wall-clock for 48 teams on a modern dev machine. The 300KB size-budget check would catch any pathologically large output. Operator runs this manually (`npm run og:render-teams`) — not on every request. |
| T-15-LAND | Tampering / Brand | `references/og-image-team.svg` could be edited to include `bitcoin`, `lightning`, etc. — would ship in 48 PNGs | mitigate | Per-team LAND-02 grep on the SUBSTITUTED SVG (not template) catches both template-side and label-side regressions. Each iteration writes the substituted SVG to a tmpfile and greps it; any match fails that team's check, increments `totalFail`, exits 1. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only uses existing `@resvg/resvg-js` (already vendored in `devDependencies`). No legitimacy gate needed. |
</threat_model>

<verification>
After all three tasks complete:

1. **Static checks (no render yet):**
   - `node --check scripts/render-team-og-images.mjs` exits 0
   - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
   - `grep -c '{{TEAM_LABEL}}' references/og-image-team.svg` returns `1`
   - `grep -c '{{HEADLINE_FONT_SIZE}}' references/og-image-team.svg` returns `3`
2. **Dry-run sanity (optional — does NOT commit PNGs):** Operator MAY run `npm run og:render-teams` to verify the toolchain works end-to-end before Plan 15-03 commits the output. The script will create `public/og/` and write 48 PNGs. If you do this, leave `public/og/` untracked or run `git restore public/og/` after — Plan 15-03 is the canonical commit point. Render is deterministic, so re-running in 15-03 reproduces identical bytes.
3. **No regression on existing `og:render`:** `npm run og:render` still works, still produces `public/og-image.png` identical to the committed file. (Optional: re-run + `git diff --stat public/og-image.png` shows no diff.)
</verification>

<success_criteria>
- `references/og-image-team.svg` exists with the locked D-07 headline copy and exactly the right token counts.
- `scripts/render-team-og-images.mjs` exists, is syntax-valid ESM, reuses the Resvg config verbatim, applies the D-08 font-size buckets, runs the 6 checks per team, and exits 1 on any FAIL.
- `package.json` has `og:render-teams` registered next to `og:render`. JSON is well-formed.
- `npm run og:render` still works (analog untouched per D-11).
- T-15-07..T-15-LAND mitigations are documented and in place.
</success_criteria>

<output>
Create `.planning/phases/15-personalized-open-graph/15-02-SUMMARY.md` when done.
</output>
