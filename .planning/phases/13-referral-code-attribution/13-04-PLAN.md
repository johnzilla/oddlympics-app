---
phase: 13-referral-code-attribution
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - scripts/smoke-signup.mjs
  - DEPLOY.md
autonomous: true
requirements: [REF-01, REF-03]
must_haves:
  truths:
    - "node scripts/smoke-signup.mjs exits 0 with the existing 8 cases still PASS plus the new referral cases PASS (D-14, SC1/SC3/SC4)"
    - "The smoke proves: valid ref -> referred_by set; direct -> NULL; unknown/malformed/self ref -> NULL + signup succeeds; every created row has a non-null unique referral_code"
    - "DEPLOY.md Day-2 ops has a documented referral-counting SQL recipe (D-15)"
  artifacts:
    - path: "scripts/smoke-signup.mjs"
      provides: "referral smoke cases (valid/direct/unknown/malformed/self/uniqueness) on IP 192.0.2.43; dbRowFor extended with referral_code + referred_by"
      contains: "192.0.2.43"
    - path: "DEPLOY.md"
      provides: "Day-2 referral-attribution SQL recipe -- signups grouped by referred_by, referred vs direct, % referred"
      contains: "referred_by"
  key_links:
    - from: "scripts/smoke-signup.mjs referral cases"
      to: "vip_signups.referral_code / referred_by columns"
      via: "extended dbRowFor SELECT + DB assertions"
      pattern: "referral_code"
    - from: "scripts/smoke-signup.mjs referral POST cases"
      to: "/api/signup"
      via: "post() with X-Forwarded-For 192.0.2.43"
      pattern: "192\\.0\\.2\\.43"
---

<objective>
Extend `scripts/smoke-signup.mjs` with referral attribution cases (keeping the existing 8 green) and add a documented referral-counting SQL recipe to `DEPLOY.md`'s Day-2 ops section. This is the verification + measurement layer for Phase 13 — it proves SC1/SC3/SC4 automatically and gives the operator the queryable attribution path REQUIREMENTS.md names.

Purpose: D-14 (smoke extension) is the automated proof for REF-01 (`referral_code` non-null + unique on every row), REF-03 (`referred_by` set for valid refs, NULL otherwise), and SC4 (unknown/malformed/self ref never blocks the signup). D-15 (DEPLOY.md recipe) is the measurement mechanism — REQUIREMENTS.md explicitly says attribution is "queryable via the DEPLOY.md Day-2 ops path", no dashboard.

Output: extended `scripts/smoke-signup.mjs`, updated `DEPLOY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-referral-code-attribution/13-CONTEXT.md
@.planning/phases/13-referral-code-attribution/13-RESEARCH.md
@.planning/phases/13-referral-code-attribution/13-PATTERNS.md
@.planning/phases/13-referral-code-attribution/13-VALIDATION.md
@.planning/phases/13-referral-code-attribution/13-01-SUMMARY.md
@.planning/phases/13-referral-code-attribution/13-02-SUMMARY.md

<interfaces>
<!-- From the live scripts/smoke-signup.mjs and DEPLOY.md. Use directly. -->

scripts/smoke-signup.mjs structure:
- `SMOKE_IP = '192.0.2.42'` at :39 — RFC 5737 TEST-NET-1.
- `runCase(name, fn)` at :55-69 — awaits `fn()`, increments `pass`/`fail`.
- `post(form, extraHeaders = {})` at :71-85 — POSTs to `/api/signup`, sends `X-Forwarded-For: SMOKE_IP` by default; `extraHeaders` overrides it (pass `{ 'X-Forwarded-For': '192.0.2.43' }`). Returns `{ status, location }` only — no response body; referral cases assert via the DB.
- `dbHasEmail(email)` at :87-91, `dbRowFor(email)` at :93-99 — `dbRowFor` currently `SELECT email, team, timezone, requested_sport FROM vip_signups WHERE email = ?`.
- `case-1-valid` at :147-176 is the POST + 303 + DB-assertion template.
- `case-7-rate-limit` at :288-309 fires 4 valid POSTs from `SMOKE_IP`; its comment (:281-287) documents that exactly 3 `SMOKE_IP` slots were consumed by cases 1, 4, 5 before it. Adding valid POSTs from `SMOKE_IP` before case-7 would break its slot math.
- Final tally + `process.exit(fail === 0 ? 0 : 1)` at :311-320.

CRITICAL sequencing (RESEARCH Pitfall 2 / 13-VALIDATION.md): referral POST cases that issue VALID signups MUST use `X-Forwarded-For: 192.0.2.43` (a distinct RFC 5737 IP, NOT `SMOKE_IP` `192.0.2.42`) so they do not consume the rate-limit slots case-7 depends on. The self-referral case is a re-signup of an existing email — if it re-uses `SMOKE_IP` it consumes a slot, so it too uses `192.0.2.43`.

DEPLOY.md Day-2 ops:
- The "Want to... / Command" table is at :103-114.
- The longer "From your laptop (one-shot signal pull)" heredoc block is at :116-126 (`<<SQL` style).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend smoke-signup.mjs with referral cases</name>
  <files>scripts/smoke-signup.mjs</files>
  <read_first>
    - scripts/smoke-signup.mjs (the full file — `post`, `runCase`, `dbRowFor`, the `case-1-valid` template, the `case-7-rate-limit` slot-accounting comment at :281-287)
    - .planning/phases/13-referral-code-attribution/13-RESEARCH.md § "Referral Smoke Cases (D-14 breakdown)" and Pitfall 2 (rate-limit slot consumption)
    - .planning/phases/13-referral-code-attribution/13-VALIDATION.md § "Wave 0 Requirements" (the 192.0.2.43 sequencing constraint)
    - .planning/phases/13-referral-code-attribution/13-PATTERNS.md § scripts/smoke-signup.mjs (the six new cases table)
  </read_first>
  <action>
    In `scripts/smoke-signup.mjs`:
    (a) Extend `dbRowFor` (:93-99): change its SELECT column list to `SELECT email, team, timezone, requested_sport, referral_code, referred_by FROM vip_signups WHERE email = ?`. Widening the SELECT does not break the existing 8 cases (they read only the original 4 fields).
    (b) Add a constant near `SMOKE_IP` (:39): `const REF_IP = '192.0.2.43';` with a comment that it is a distinct RFC 5737 address used so referral POSTs do not consume the `SMOKE_IP` rate-limit slots case-7 depends on.
    (c) Insert SIX referral cases BEFORE the `case-7-rate-limit` case, each using `runCase` and the `case-1-valid` POST+DB-assertion shape; every referral POST passes `{ 'X-Forwarded-For': REF_IP }` as `post()`'s second argument:
        - `REF-valid-ref`: POST signup A (unique email A, team england, tz Europe/London) -> 303; read A's `referral_code` via `dbRowFor` (call it `codeA`); assert `codeA` matches `/^[a-z0-9]{8}$/`. Then POST signup B (distinct unique email B) with `ref: codeA` -> 303; assert `dbRowFor(B).referred_by === codeA`.
        - `REF-direct-no-ref`: POST a signup with NO `ref` field -> 303; assert `dbRowFor(email).referred_by` is `null` (better-sqlite3 returns SQL NULL as JS `null`).
        - `REF-unknown-ref`: POST a signup with `ref: 'zzzzzzzz'` (well-formed 8-char but not in the DB) -> assert status 303 AND `location` starts with `/pending?email=` (signup succeeded, NOT `/?error=`) AND `dbRowFor(email).referred_by` is `null`.
        - `REF-malformed-ref`: POST a signup with `ref: '!!bad!!'` (wrong charset) -> assert 303 + `location` starts with `/pending?email=` + `referred_by` is `null` (bad ref never blocks — SC4).
        - `REF-self-ref`: re-signup email A (re-use the same email from `REF-valid-ref`) passing `ref` = `codeA` (A's own code) -> assert 303 AND `dbRowFor(A).referred_by` is still `null` (self-referral ignored — D-08). Use `REF_IP` for this POST.
        - `REF-code-uniqueness`: run `db.prepare('SELECT referral_code FROM vip_signups').all()`; collect the `referral_code` values; assert every value is non-null AND `new Set(codes).size === codes.length` (all distinct) — proves SC1 across the whole table.
    (d) Update the `case-7-rate-limit` comment block (:281-287) to note the Phase 13 referral cases run on `REF_IP` (`192.0.2.43`) and therefore do NOT change the `SMOKE_IP` slot count — case-7's "3 slots used" precondition is unchanged. Do not alter case-7's logic.
    Keep the final `process.exit(fail === 0 ? 0 : 1)` — all existing 8 cases plus the 6 new ones must PASS for exit 0.
  </action>
  <verify>
    <automated>npm run build && (node ./dist/server/entry.mjs & SRV=$!; sleep 3; node scripts/smoke-signup.mjs; RC=$?; kill $SRV 2>/dev/null; exit $RC)</automated>
  </verify>
  <acceptance_criteria>
    - `dbRowFor`'s SELECT column list contains `referral_code` and `referred_by`
    - smoke-signup.mjs contains `const REF_IP = '192.0.2.43'`
    - smoke-signup.mjs contains six new `runCase(` calls named `REF-valid-ref`, `REF-direct-no-ref`, `REF-unknown-ref`, `REF-malformed-ref`, `REF-self-ref`, `REF-code-uniqueness`
    - All six referral POST cases pass `X-Forwarded-For: 192.0.2.43` (none uses `SMOKE_IP` for a valid POST)
    - Running the smoke against a freshly built + booted server: `node scripts/smoke-signup.mjs` exits 0 (all 8 existing + 6 new cases PASS)
    - `case-7-rate-limit` still PASSes (slot accounting undisturbed)
  </acceptance_criteria>
  <done>`scripts/smoke-signup.mjs` has six referral cases on IP `192.0.2.43`, an extended `dbRowFor`, an updated case-7 comment; `node scripts/smoke-signup.mjs` exits 0 with all 14 cases PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Add the referral-counting SQL recipe to DEPLOY.md Day-2 ops</name>
  <files>DEPLOY.md</files>
  <read_first>
    - DEPLOY.md (the Day-2 ops section — the "Want to... / Command" table at :103-114 and the one-shot heredoc block at :116-126)
    - .planning/phases/13-referral-code-attribution/13-RESEARCH.md § "DEPLOY.md Day-2 ops table row addition" (the recommended SQL)
    - .planning/phases/13-referral-code-attribution/13-PATTERNS.md § DEPLOY.md (table-row + heredoc format)
  </read_first>
  <action>
    In `DEPLOY.md`, add a documented referral-counting recipe to the Day-2 ops section (D-15). Match the existing format exactly — either:
    - add one row to the "Want to... / Command" table at :103-114 in the style of the "See team distribution" row (a single `sqlite3 -column -header` one-liner), OR
    - extend the "From your laptop (one-shot signal pull)" `<<SQL` heredoc block at :116-126 with the referral query (preferred for the multi-statement form).
    The recipe must report: signups grouped by `referred_by` (top referral codes), total referred vs. direct, and % referred. Use the SQL shape from RESEARCH.md § "DEPLOY.md Day-2 ops table row addition": `SELECT COALESCE(referred_by, '(direct)') AS referrer, COUNT(*) AS signups, ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM vip_signups WHERE confirmed_at IS NOT NULL), 1) AS pct FROM vip_signups WHERE confirmed_at IS NOT NULL AND unsubscribed_at IS NULL GROUP BY referred_by ORDER BY signups DESC`. Point at the production DB path `/var/lib/oddlympics/oddlympics.db` (consistent with the other Day-2 commands). Add a one-line description framing it as the Phase 13 referral-attribution measurement path.
  </action>
  <verify>
    <automated>grep -c "referred_by" DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - DEPLOY.md contains a referral-attribution recipe in the Day-2 ops section
    - The recipe SQL references `referred_by` and `COALESCE(referred_by, '(direct)')` (or `"(direct)"`)
    - The recipe groups by `referred_by` and computes a percentage of referred vs. total
    - The recipe points at `/var/lib/oddlympics/oddlympics.db`
    - `grep -c "referred_by" DEPLOY.md` returns >= 1
    - The added block matches the existing Day-2 ops Markdown format (table row or `<<SQL` heredoc)
  </acceptance_criteria>
  <done>`DEPLOY.md` Day-2 ops has a documented referral-counting SQL recipe grouping signups by `referred_by` with referred-vs-direct percentages, matching the existing ops-section format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke test → /api/signup + SQLite | The smoke drives the real signup endpoint and reads the real DB file; a flawed smoke gives false assurance |
| operator → production DB (Day-2 SQL) | The recipe runs read-only SQL against the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Repudiation | smoke gives a false PASS by not actually exercising the bad-ref paths | mitigate | Each referral case asserts the concrete observable DB state (`referred_by === codeA`, `referred_by === null`) AND the HTTP outcome (303 + `/pending?email=` for the never-rejects cases). The `REF-unknown`/`REF-malformed`/`REF-self` cases explicitly assert the signup still 303s — proving SC4, not just that no exception was thrown. |
| T-13-12 | Denial of Service | referral cases consuming `SMOKE_IP` rate-limit slots and breaking case-7 | mitigate | All referral POST cases use `REF_IP` (`192.0.2.43`), a distinct RFC 5737 address, so the `SMOKE_IP` slot count case-7 depends on is unchanged (RESEARCH Pitfall 2). |
| T-13-13 | Information Disclosure | Day-2 SQL recipe leaking PII | accept | The referral recipe selects only `referred_by` (an opaque code) and aggregate counts — no `email`, no PII. It is read-only and run by the operator over SSH, consistent with every other Day-2 ops command. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this plan — zero install surface. |
</threat_model>

<verification>
- `npm run build` succeeds, then with the built server running, `node scripts/smoke-signup.mjs` exits 0 — all 14 cases (8 existing + 6 referral) PASS.
- `case-7-rate-limit` still PASSes, confirming the referral cases did not disturb the `SMOKE_IP` slot accounting.
- `DEPLOY.md` contains the referral recipe; running its SQL against a copy of `data/oddlympics.db` produces a grouped-by-`referred_by` result with a `(direct)` row and percentage column.
- This plan's smoke is the phase gate: full smoke green before `/gsd:verify-work` (per 13-VALIDATION.md § Sampling Rate).
</verification>

<success_criteria>
- D-14 satisfied: `scripts/smoke-signup.mjs` extended with the six referral cases; the existing 8 stay green; exit 0 proves SC1 (unique non-null codes), SC3 (valid ref -> `referred_by`; direct -> NULL), and SC4 (unknown/malformed/self ref never blocks the signup).
- D-15 satisfied: `DEPLOY.md` Day-2 ops has the documented referral-counting SQL recipe — the measurement mechanism REQUIREMENTS.md names.
- REF-01 and REF-03 are verifiable by an automated command (`node scripts/smoke-signup.mjs`).
</success_criteria>

<output>
Create `.planning/phases/13-referral-code-attribution/13-04-SUMMARY.md` when done.
</output>
